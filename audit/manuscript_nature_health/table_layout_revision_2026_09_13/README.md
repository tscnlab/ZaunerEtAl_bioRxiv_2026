# Single Table 2 and table layout revision

This candidate-only package implements the author's source and static-layout
instruction in `single_table2_and_column_layout_order_005.md`. The historical
two-panel proposal, accepted manuscript source, scientific reports, Word files
and website remain unchanged.

The implementation uses R 4.6.1 to copy accepted result strings and verify
their source mappings. It does not load models, calculate new estimates or
execute a research notebook. Table 2 uses only the primary any-valid sample.
Descriptive pooled-minute fractions remain separate from model estimates.

Only source and static checks are performed. No browser, server, capture,
Quarto, office, document assembly or website command is permitted by this order.
Actual table fit and final-document production remain pending. The successful
synthetic localhost test is not visual acceptance of these research tables.

Run instructions and a source-matched page index accompany the completed
candidate. All meaningful stopped checks are retained; a manifest is sealed
only after the full candidate verification passes.
