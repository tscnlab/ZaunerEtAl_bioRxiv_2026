# Bounded HTML visual QA completion

14 September 2026. Writer Order014. Result: **BOUNDED_HTML_VISUAL_QA_PASS**.

The frozen HTML `d7ee926c0910227004581915040f30ae01142290f0759839e897fa2b3f5f10aa` was inspected in the supported in-app browser. No source, display, Word file, HTML file, scientific input or live website was edited or rendered.

## Coverage and observations

All six changed endpoints were inspected at desktop 1440×1000 and phone 390×844. Both tables were also inspected at 708×1000. All 34 screenshots were visually reviewed, with overlapping vertical portions and horizontal left/middle/right views wherever necessary. `visual_review_matrix.csv` records the 14 endpoint/viewport cases, and `screenshot_inventory.csv` binds every capture to its URL, actual viewport, scroll position, pixel dimensions and hash.

- S4: four retained outcome rows and four columns; complete four-test family note and caption. Both horizontal edges and the whole interpretation column remain reachable. Narrow screens use a working internal horizontal scroller, not page-wide overflow.
- S7: one continuous eight-column, 17-metric table. All groups and rows remain in order. Exact sample cells use deliberate participant/participant-day line breaks. Widths remain consistent throughout the table. Every portion of the notes and caption was inspected, including both horizontal edges. No artificial Word-style page split was introduced on the webpage.
- S15 and S16: separate headings and captions. S15 contains adjusted chronotype associations; S16 contains the already accepted observed-timing artwork with its own internal subpanels. The external A/B grouping is absent.
- S17 and S18: renumbering, headings and captions agree; full artwork is present.
- The two changed Results paragraphs visibly distinguish S15/S16 and cite S17/S18. All four local targets resolve and are navigable. One initial centre-point pointer click on the wrapped S15 hyperlink did not activate it; ordinary keyboard Enter activated the intended target. That initial attempt and the successful follow-up are both preserved. The other three pointer activations passed. No href or target defect was found.
- The existing recommendation-adherence, geographic-context and person-level theme Contents entries resolve. The page's existing Contents depth lists these themes, not individual third-level figure headings. No stale figure-level entry was introduced.
- The unchanged S8 image remains one complete appearance with its complete caption.

There were no captured browser errors or warnings, missing images, new page-level horizontal overflow, clipped scientific values, overlapping captions or broken table scrollers. Dense SVG artwork is naturally smaller on the phone screen; the accepted source typography and artwork were not redesigned. The pre-existing Word converter footnote qualification remains disclosed in the separate Word handoff and was not repaired or rerendered here.

## Frozen identities and authority

Before and after browser use, R4.6.1 reproduced the 318-member candidate, the 44-member release, the 49-member independent review, C271, N145 and the 49 unchanged source/profile/history inputs. The exact accepted HTML content checker passed 15/15 with evidence-output routing only. The original candidate manifest and handoff are unchanged, including their historical browser-held wording.

The Word remains `6f0ce7a50b608f91d24c31ae0b9b0edefe15828ed4f966732ea616c6e395a570`. All 19 native files, all image identities, approved prose history, model results and source inputs remain frozen. The already classified external Order013 corpus transition is not a Writer mutation or a broader exemption.

## Server, browser and teardown

The first sandbox launch was refused at localhost socket binding before a listener or browser session existed. The ordinary execution-permission route then authorized the same bounded server and exact one-file root. This was not a browser-policy bypass, different browser or alternate exposure route.

The GET/HEAD-only server bound strictly to `127.0.0.1:55675`, PID 29906, from `/private/tmp/nh_html_qa014_gjv0u4un`. Only `/manuscript.html` served the exact frozen document; its resources were already embedded. Directory listing, upload/write routes and source/evidence paths were not exposed. The mandatory preflight found no symlinks.

The task-created in-app tab 11 was closed and the viewport override reset. The exact server process exited normally. A fresh `lsof` check found no listener on port 55675. After the successful frozen-input postflight, the served copy was rehashed, then the one verified task-owned HTML copy and its empty temporary directory were removed. The original document remains intact. Lifecycle, no-listener, browser teardown and cleanup evidence are retained.

## Next owner action

This completes the isolated Writer candidate's browser inspection. It does not promote the standalone HTML or replace the full website. The coordinator may independently review this new QA seal, then authorize integration of the already prepared minimal delta against completed Order013. No Harmonizer contact or interruption occurred.
