# Order 009 Word layout contract

The unchanged reference is the staged copy of `assets/reference.docx`, SHA-256 `8c0cf634a4958aa05412de3f5f15aa92c40cbb5331a2319f761530472697e15f`. The previously completed template inspection is recorded in `audit/manuscript_nature_health/revision_2026_09_11/artifact.md` and its `reference_pages` evidence. This continuation preserves that established template rather than redesigning it.

The manuscript retains A4 portrait prose, Arial 11 pt, 1.5-line body spacing, 26 pt Title, 18/16/14 pt heading levels, the 28-author and 14-affiliation block, existing header/footer/page-number system, and 13 top-level sections starting on new pages. Display paragraphs and captions are single-spaced. Main-table captions remain above and left-aligned. The reviewed two empty portrait section transitions are removed, without removing their neighboring content or bookmarks.

Main and supplementary table images preserve the accepted physical widths. S2 is A3 landscape at 15.55 inches and its existing secondary text size is author-accepted. S7 continuation parts retain the same 10.55-inch width. There are 30 table image parts and 24 figure appearances from 23 exact SVGs, totaling 54 drawings. S5, S7 and S15 each have separate A/B sources; S8 retains its two exact crop windows. Native SVG content remains authoritative, with no scientific redrawing or manual rasterization.

The 19 separate editable documents each contain one real Word table. Seventeen files and their complete-page visual evidence are reused exactly. Only Table 2 and S2 are exported anew. Their established Arial 10 pt body and 9 pt secondary native-table typography remains unchanged, with single spacing, repeating headers, expandable rows and explicit column grids. S2 retains all 170 complete mean-plus/minus-SD spans, 14 columns and 17 original embedded plot payloads. The old optional one-point reduction is not used.

Only path relocation, accepted display-wrapper integration and formatting are allowed. Accepted source prose, scientific values, citations, labels, captions, authors and declarations are frozen. The earlier template record's Brown and SVG holds are historical and superseded by Order 009; they are not copied into the candidate's reader-facing content.

The document-operation marker completed successfully exactly once before the first DOCX render, using the bundled Node runtime with `--operation-kind create --expected-output-count 20 --output-format docx`. The first raw DOCX render succeeded, with expected missing-rsvg-convert fallback warnings; final native-SVG visibility must be checked in the assembled output, not inferred from that warning or XML validity alone.
