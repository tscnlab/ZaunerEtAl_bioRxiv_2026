# Manual table-source pages

Open `index.html` manually, then read `manual_capture_guide.html`.

The seven final HTML files in this directory are byte-exact copies of the
passing second static attempt. They are the implemented source layouts, not
the old previews. The pages are self-contained, contain no active JavaScript
or fetched resources, and place instructions/provenance outside the complete
table rectangle. No tool opened them or captured an image.

All 411 static checks pass. They establish source content and declared
geometry, not visual appearance. S2 retains its canonical 16 CSS px base,
170 whole mean ± SD units and 17 original embedded PNG payloads. The archived
12px capture override is excluded. No font reduction is applied.

The seven PNGs remain absent. Final Word, editable-table exports, assembly,
native compatibility checks and the complete website remain outside this
source-only package. Follow the guide for manual, lossless artifact provision.

`attempt_01/` preserves the stopped structural-check attempt. Its pages were
already identical to the passing pages; only the verifier's treatment of a
trailing newline outside the final row changed. `attempt_comparison.csv`
documents that identity. `attempt_02/verification/` holds complete R 4.6.1
verification and input records.

`package_manifest.csv` excludes itself and `package_seal.json`; the seal hashes
the manifest and its checked members without including itself. Do not edit
this sealed source package when images are supplied. Record supplied artifacts
and any subsequent authorized correction in a new bounded location.
