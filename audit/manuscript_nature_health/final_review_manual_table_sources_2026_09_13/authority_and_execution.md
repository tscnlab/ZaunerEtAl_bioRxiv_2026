# Authority and execution record

Controlling order:
`audit/report_harmonization/final_documents_2026_09_13/writer_source_acceptance_and_manual_sources_002.md`,
SHA-256 `8f666099974e1fdbadb80d22e12933421409b57cac4d3303ce656b6b813e9acd`.
The new output root was confirmed absent before preparation.

The coordinator subsequently confirmed in this same Writer task that the
canonical S2 `gt_table` base is 16 CSS px and must remain unchanged. The
historical 12px automated-capture override is not incorporated. This clarifies
the existing no-font-reduction order, not a new trial or permission.

The active Quarto-authoring skill was used for table structure, safe local
artifact handling and the distinction between source checks and visual QA.
Its render/visual workflow is deliberately deferred under the controlling
zero-render and browser restrictions. No scientific/language review was
reopened and no other skill or worker was invoked.

## Static operations performed

- Read the sealed seven-artifact map and four exact source fragments. Inspect
  their table structure, inherited CSS, headers, notes and embedded resources.
- Construct the exact seven declared body-row slices, preserving complete
  theads and final-part notes. All slice boundaries start at original group
  headings, so no invented or additional scientific group rows are needed.
- Fix the specified rectangle widths. For S2, add the approved 14-column
  width vector and complete-unit nowrap protection, four-pixel numeric side
  padding, visible numeric overflow and width-bounded image rules using
  `object-fit: contain`. Original image data and inline 72px height declarations are
  unchanged. No resampling, font reduction or source-language edit occurred.
- Keep all instructions/provenance outside the white capture rectangle.
  All page resources are inline. A restrictive content-security policy blocks
  automatic network resources.
- Verify every selected row, column span, note, header and embedded image in
  R 4.6.1; check unique IDs and local header references; preserve source CSS
  and declared base sizes of 14px (Table 2), 16px (S2), and 12px (S7).
- Copy the passing pages unchanged to the package root, and create the index,
  manual guide and exact page identities.

The only stopped check concerned a newline after a closing `tr`, outside
cell content. The full first attempt and its executed verifier remain present.
Attempt 02 removes only terminal CR/LF from the row-serialization comparison;
all page bytes remain identical across attempts. No page was altered to make
the check pass. The completed check count is 411/411.

## Commands and environment

All source construction/reconciliation used R 4.6.1 with the existing R
library, `xml2`, `openssl` and `jsonlite`. Session records are retained. No
participant data, analytical function, fitted model or QMD was loaded.

Each R command used:

```text
RENV_CONFIG_AUTOLOADER_ENABLED=FALSE
R_LIBS_USER=/Users/zauner/Library/R/arm64/4.6/library
Rscript --vanilla
```

Executed scripts, in order: `inspect_sources.R`, `build_static_pages.R
attempt_01`, `verify_static_pages.R attempt_01` (stopped), `build_static_pages.R
attempt_02`, `verify_static_pages.R attempt_02`, and
`prepare_manual_delivery.R`. The final delivery verification and seal are
recorded separately. The scripts do not start subprocesses, servers, browsers,
office software, renders or capture helpers.

## Boundaries retained

Browser openings: 0. Servers: 0. Captures: 0. Quarto/office renders: 0.
Assemblies/embeds: 0. Website promotions: 0. Scientific computations: 0.
No earlier trial budget is consumed or reset by these source checks.

The accepted 84-member Brown manuscript package, its manifest and seal, live
manuscript/SI/configuration/bibliography/CSS, and the f055 Word preview remain
unchanged. Nothing was edited outside the newly authorized Writer root.

Actual pixel heights, line fit, clipping, overlap, intended-size readability
and native Word compatibility are not established by static source inspection.
There is no visual PASS. The manual guide requests unaltered author-supplied
PNGs from these exact pages; it does not ask for old or unimplemented layouts.
