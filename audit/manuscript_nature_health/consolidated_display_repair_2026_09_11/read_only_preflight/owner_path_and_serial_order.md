# Proposed owner allocation and serial integration order

Date: 2026-09-11

Status: proposal only. No implementation or rendering is released by Order72i.

## Gate 0: Coordinator seal

The Coordinator reviews this preflight, resolves the S5 caption authority conflict, and issues a new order that names the candidate roots, owner inputs, trial limits, independent reviewers, and promotion boundary. The accepted Order72h preview and stopped Order72d Word candidate remain immutable.

No user-side trigger is required at this gate. The next action belongs to the Coordinator.

## Phase 1: component SVG work

After the Coordinator releases implementation, the following bounded owner tasks can run independently:

1. H07 exports one genuine native SVG for S7 panel B from the pinned frozen display rows. Candidate path:
   `audit/hypotheses/H07/report018_order72j_split_svg_export/candidate/H07_revised_smooth_derivative_pairs_near_eye.svg`.
2. H09 exports two genuine native SVGs for S15 panels A and B from the pinned frozen display rows. Candidate paths:
   `audit/hypotheses/H09/report018_order72j_split_svg_export/candidate/H09_primary_effects.svg` and
   `audit/hypotheses/H09/report018_order72j_split_svg_export/candidate/H09_observed_timing_patterns.svg`.
3. H11 may create one bounded S17 compatibility candidate at
   `audit/hypotheses/H11/report018_order72j_libreoffice_compatibility/candidate/H11_reader_near_eye_curves_arial_safe_margin.svg`.
   Native Word remains the primary target. The accepted S17 source remains valid if the compatibility candidate does not strictly improve LibreOffice without regression.

H01 does not regenerate S7 panel A. Its current native SVG at SHA-256 `4ddf972fc4c8082594d13a3b446537ae8a525ca35077e0605967f9c4c0ce519e` is reused byte-exactly.

Brown BA-017 proceeds under its existing scientific order. No second Brown run is created. Final S5 work starts only after BA-017 replacement panels are accepted. Brown then composes one flat native left-right SVG under its explicitly released candidate root. Historical panels may be used for one clearly labelled compatibility trial only.

## Phase 2: independent component acceptance

Each new SVG receives an input-hash manifest, zero-`image` structural check, vector-layer inspection, rendered comparison to the accepted comparator, and a non-circular acceptance record. S5 additionally requires a reversible group-and-transform proof. No component is eligible for central integration before this phase closes.

## Phase 3: Harmonizer integration candidate

The Harmonizer copies the accepted Order72h selection source into:

`audit/manuscript_nature_health/consolidated_display_repair_2026_09_11/integration_candidate/`

Only the sealed display changes are applied there:

- S5 references one flat left-right SVG.
- S7 contains two stacked full-width image blocks inside one `fig-s7` container.
- S15 contains two stacked full-width image blocks inside one `fig-s15` container.
- A/B tags are uppercase and placed at the left.
- Each S7 and S15 block has its own accurate alt text, while each figure retains one shared caption.
- Figure numbering, link identifiers, Table 3, S12, and the H06_daily exclusion remain fixed.

The Harmonizer renders only the candidate after explicit release. The canonical Order72h QMD and HTML are not overwritten.

## Phase 4: Writer candidate integration

Once the Coordinator accepts the Harmonizer candidate, the Writer starts from manuscript QMD SHA-256 `c6beb79ca34128c0b69d882c1e3ea332cb88664ce9a3c0883b39d1569db0a0d0`. The Writer then:

1. mirrors the accepted S5, S7, and S15 block structure and sealed captions in the Supplementary Information source;
2. updates the capture path so S2 uses one 14-column horizontal set with vertical continuation only;
3. applies an explicit Arial capture contract to Supplementary Tables S5, S6, and S10;
4. gives S2 a dedicated A3 landscape Word section;
5. expands the SVG manifest and Word drawing map to the accepted 22 SVG sources and 23 drawing appearances; and
6. embeds only byte-exact accepted SVG assets.

All Writer outputs remain within:

`audit/manuscript_nature_health/revision_2026_09_11/consolidated_display_repair_order72j/`

The stopped Order72d DOCX is never opened for save or used as an output destination.

## Phase 5: serial visual QA

1. Browser review of the full candidate figure/table Quarto document.
2. Native Word review of every changed display, page break, caption, drawing description, and cross-reference.
3. LibreOffice review focused on S17 and confirmation that S5, S7, and S15 remain visible.
4. Package-level checks of page sections, media relationships, hashes, counts, alt text, and the S2 capture parts.
5. Independent central acceptance with a non-circular manifest.

Only after all five checks pass may the Coordinator authorize promotion of a new canonical selection page and manuscript candidate. The user is then shown the complete integrated Quarto page.
