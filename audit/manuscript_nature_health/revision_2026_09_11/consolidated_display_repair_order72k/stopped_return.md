# Non S5 manuscript display integration stopped return

Date: 2026-09-11. Status: STOPPED_ENVIRONMENT, incomplete candidate preview.
Return gate: REPORT018-ORDER72K-NON-S5-INTEGRATED-PREVIEW-REVIEW.
Writer: 019ffb39-372e-7262-bfac-192751fd0e63.

## Outcome and exact blocker

The initial selection and manuscript HTML previews exist. The original S5,
S6 and S10 table captures have passed their initial font inspection. The
initial S2 capture preserves every source cell and distribution image but
clips some numerical text. Scoped numerical-span wrapping is prepared in
the copied helper and both candidate stylesheets, but has not been rendered
into either completed HTML preview.

The S2-only correction invocation exited 1 before creating
`capture_s2_attempt2/`. Chrome launched as PID 36917 and exited with SIGABRT.
Playwright reported `Target page, context or browser has been closed` and
`Error: kill EPERM` during cleanup. The invocation used `use_default`.
No tool permission request was denied, and there was no browser URL security
rejection. These observations establish a launch failure, not its precise
cause. No elevated retry, alternative browser/runtime, manual Chrome profile
repair, or additional capture was attempted.

Exact records: `capture_s2_attempt2_command.json` and
`capture_s2_attempt2.log`. The Harmonizer has requested a separate narrow
same-command environment disposition. That proposal has not been executed.

The first main DOCX render previously failed on missing local Lua modules.
All three dependencies are now copied byte-exactly beside the unchanged
author filter, under the accepted Lua dependency addendum. No Word rerender
or assembly has yet used them.

## Fullest available preview and change map

Manuscript HTML:
`project/render_html_attempt1/ZaunerEtAl2026_NatHealth_phase3_brown.html`.

Selection HTML, relative to the project root:
`audit/manuscript_nature_health/consolidated_display_repair_2026_09_11/integration_candidate_order72k/project/render_attempt1/selection.html`.

No candidate DOCX has been produced by this order. The stopped Order72d
document is preserved and is not substituted as a completed Order72k result.

The existing `candidate_change_map.json`, three source diffs,
`source_copy_map.json`, `expanded_svg_manifest.json`, helper diffs and
`dry_run_manifest.json` record the original integration. The new
`correction_preflight/correction_manifest.json` and its three exact diffs
record the unrendered S2 wrapping correction and S2-only capture scope.
`correction_preflight/lua_dependency_copies.csv` records the three additional
unchanged Lua modules. No title, abstract, scientific prose, caption other
than the already released S7/S15 synchronization, number, bibliography,
table cell, scientific SVG byte or source-owned helper was changed.

Main Table 3 remains the exact approved fragment. S7 and S15 each use two
independent source SVG blocks with external tags and a shared caption.
S12 and S17 retain their prescribed exact identities. H06_daily remains
excluded from numbered displays. Existing standalone editable table Word
documents were not rebuilt or overwritten.

## Completed checks and their limits

- Original source checks: 30/30 passed before rendering.
- Initial HTML structure: 12/12 passed, including 19 tables in each page,
  resolving table headers, local images, internal anchors, no duplicate IDs,
  and two images in each S7/S15 container.
- Final preservation: all 429 original authority/input rows, 138
  source-resource copy pairs, three additional Lua copies, 12 served files
  and all three candidate QMDs remained exact.
- Independent R 4.6.1 string/payload comparison confirms every original
  table body row occurs once across its initial capture parts, and every
  distribution-image payload is unchanged. This is content preservation,
  not a visual pass for the clipped first S2 capture.
- Initial S5/S6/S10 captures: Arial checked in title, header, stub, body and
  available notes, with visual inspection of all four PNG parts. Reuse these
  captures without repetition.
- Initial S2: 14 columns, 17 metric rows, six group rows and 17 distributions
  preserved across three vertical parts. Numerical text clipping failed the
  visual criterion. The correction produced no replacement PNGs.
- Permitted in-app browser: S7 and S15 independent blocks observed at a
  1440 by 1000 CSS viewport. These were partial initial inspections only.
  Complete component/caption inspection, 708/390-pixel responsive checks,
  642-pixel figure checks and final corrected-page QA remain pending.

R session and checks are in `stopped_final_checks/`. No model, Brown checker,
scientific preparation or numerical analysis was executed.

## Native Word availability and author action

The initial and final CUA inventories returned this exact native-app error:

> Native apps: Error: The Mac is locked and automatic unlock could not unlock it. Ask the user to unlock the Mac manually before continuing.

An asynchronous user-unlock request was accepted by the app:

> The Mac is currently locked, so native Word inspection is unavailable. Please unlock it when convenient; I can continue the candidate build and table checks meanwhile.

No user unlock confirmation was received before this return. No native Word
document was opened or inspected. This is unavailable evidence, not Word QA.
The permitted in-app browser remained available and was used only for the
existing loopback preview. Unlocking the Mac would address native-app access;
it is not established as a remedy for the separate headless Chrome failure.

## Remaining bounded work

1. Obtain the explicit same-command environment disposition for the aborted
   S2 correction. Do not infer another retry from the unused adjustment slot.
2. Produce and inspect the corrected S2 capture within the released allowance,
   preserving all completed captures and adding no scientific changes.
3. Use the single remaining consolidated correction pass for selection HTML,
   main HTML and main DOCX. The accepted Lua copies are already in place.
4. Seal the final table manifest and explicit drawing map, then perform the
   bounded Word assembly/native-SVG embedding and office render.
5. Complete the prescribed browser and native Word visual checks, or return
   their exact unavailable evidence without acceptance.
6. Rehash and return for independent review. No canonical promotion is implied.

The exact consumed and remaining allowances are in `trial_ledger.csv`.

## Teardown and lease release

The one task-created preview tab, ID 4, was closed and the viewport override
reset before the failed capture. Final CUA inventory showed only the two
pre-existing user tabs, IDs 1 and 2. No task Word document was open.

A narrowly elevated process check identified PID 35776 as this task's
`helpers/serve_preview.py`; failed capture PID 36917 was absent. The verified
server was stopped with SIGINT. Its receipt records closed status at
2026-09-11T19:32:18Z. A subsequent `lsof -nP -iTCP:58005 -sTCP:LISTEN`
returned no listener, and a final process check found neither PID present.
The served files remained byte-identical. No unrelated process was killed.

Writer explicitly releases ORDER72K-VISUAL-LEASE-001 with this stopped return.
A subsequent recovery requires a renewed serial QA allocation. No server,
browser capture, office render or candidate generation remains running.

## Continuing holds

Historical Brown Figure S5 and Brown scientific replacement remain held.
Existing Brown content is retained as historical context, not newly accepted
science. The candidate-only notice preserves the bedside sleep-environment
qualification. The optional H11 compatibility candidate remains excluded.
Canonical manuscript, selection, build, figures, source reports, central
ledgers and the separate editable Word tables remain untouched.
