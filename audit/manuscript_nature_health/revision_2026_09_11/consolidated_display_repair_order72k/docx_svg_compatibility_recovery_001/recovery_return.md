# DOCX SVG compatibility recovery

Date: 2026-09-12, Europe/Berlin. Status: **VISUAL_QA_PENDING**.
Return gate: `REPORT018-ORDER72K-NON-S5-INTEGRATED-PREVIEW-REVIEW`.
Owner: Writer `019ffb39-372e-7262-bfac-192751fd0e63`.

## Result

The exact released assembly and embedding commands each ran once and passed.
A complete candidate Word package now exists. It is structurally verified,
not visually accepted or final. No render, capture, office viewer, browser,
server or native Word operation was performed under this assignment.

Candidate: `../Nature_Health_non_S5_preview_attempt1.docx`.
SHA-256: `f055f0c0f6225e372ed83ddb6bbe189e64f35d7306e7e54af563687991f6492c`.
Intermediate assembly: `../manuscript_assembled_attempt1.docx`.
SHA-256: `939283e46539d85611acc54ac1be86aac25b65136ede7f3c482e9401110fb88b`.
The raw input, corrected HTMLs and all old candidates remain unchanged.
Absolute output paths and sizes are recorded in the completion seal.

## Exact authorized transition

The controlling order is
`audit/report_harmonization/owner_orders/72k_docx_svg_compatibility_recovery_001.md`,
SHA-256 `b49a7d5f184d060624f157da0732dede453d09e286ec7e481901f8a01a035ab2`.
All 822 input, 880 release and 16 dispatch rows reproduced before execution.

Only `../helpers/embed_accepted_svg_figures.py` was changed, from the exact
9,614-byte 23cf9ee0 preimage to the sealed 16,998-byte postimage:
`75432be9c10f042b9f46a0207c8fc898db274f483fd4065ab3a823bf6416c38b`.
The preimage was retained before editing. Python syntax passed; zero-fuzz,
no-offset reversal reproduced the exact original bytes. The assembly helper,
capture helper, both CSS files, reference document, sources and maps stayed
unchanged. No package installation or runtime patch occurred.

The new helper leaves the three valid main native SVG drawings, existing
member names rId21/rId28/rId35, complete DPI/SVG extensions and relationships
unchanged. It adds nineteen supplemental SVG member names, preserving S8's
shared source and two crop windows. No figure was rasterized or re-exported.

## Verification

The unchanged assembly helper passed its author-block, 22 bookmark-repair,
link, section-start, orientation, table-removal and 52-drawing checks. Its
complete output is preserved in `assembly_execution_receipt.json` and the
combined stdout/stderr log. The SVG helper passed all eight preservation
guards, including exact XML reversal and unchanged unrelated package members.

Independent verification then passed:

- All 57 R 4.6.1 document checks, including 52 drawings, 23 accepted SVG
  appearances from 22 exact sources, 29 exact table PNG parts from nineteen
  keys in the approved order, 31 sections, exact per-axis image dimensions,
  the separate A3 S2 section, all thirteen top-level page starts, all 22
  precise repaired bookmark ID/name pairs and 126 resolving internal links.
- All twelve independent OOXML checks, including whole-document XML reversal,
  unchanged main drawing XML and relationships, nineteen consistent
  supplemental relationship retargets, documented SVG additions/removals,
  byte-identical unrelated package members, resolving relationships and
  valid content types, with no new raster parts.
- All 7,347 preservation rows before the patch, after the patch and after
  embedding. This includes the 1,718 release-chain rows, 444 prior owner
  members, 828 central stopped members, eighteen durable proposal members
  and 4,339 historical versions. All six execution inputs and thirteen
  command arguments matched, with only the expressly released helper-hash
  override. All 29 PNG parts and 22 SVG source/copy pairs remain exact.

The one new source-version alias matches the exact embedder path and old
23cf9ee0 hash, giving five source-version aliases in total. Older aliases are
unchanged. The eight Quarto runtime states retain their exact accepted
pre/post identities; no new cache or xref change occurred. Temporary proposal
paths resolve only through their exact durable path/hash mapping, not through
new runtime dependencies. All nineteen standalone editable table DOCX files
are preserved separately and were not replaced by this manuscript's approved
PNG table display route.

The complete first R checker and its 54/57 result are retained. Three checker
assumptions were corrected without changing a document: the reference uses
styleId `berschrift1` for its named heading style; six main bookmark names
are restored names rather than net-new names; and exact image dimensions
must account for integer EMU conversion on each axis. A separately retained
style lookup then needed `which()` to exclude unnamed styles from an XML
nodeset subscript. The final separately named checker uses the coordinator's
explicit `!is.na(style_names)` condition as well. It retained all other tests and strengthened
the image test to exact frozen geometry. A separate checksum serialization
error was corrected only by stripping the openssl hash class before JSON
serialization. No producer command was rerun for any checker issue. See
`checker_corrections.md`, `checker_style_nodeset_correction.json` and all
preserved checker sources, results and execution receipts.

## Remaining visual and scientific holds

No visual inspection is claimed for the new candidate. Office QA remains
0 of 2 and was not invoked. Native Word requires author unlock and separate
bounded authority. Leases004 and005 remain closed. The binding browser
security denial was not retried or bypassed; corrected HTMLs are available
only for manual author review under the existing disposition.

The historical Brown Figure S5 and its known Word limitation remain held.
The existing Brown-linkage notice and bedside sleep-environment qualification
are retained unchanged. No Brown result, figure replacement, optional H11
compatibility candidate or scientific computation was introduced. No title,
abstract, narrative, citation, number or table content was edited. Canonical
sources, the accepted website, standalone editable tables, locks, profiles
and central ledgers remain unchanged. Nothing was committed, pushed or uploaded.

## Processing closure

The exact candidate-helper process check at 22:46:06 UTC on 11 September
returned no matching process. No process was signalled. No visual lease was
created or used. Writer releases this exclusive file-processing assignment
with the present return. Assembly and embedding invocation counts are one
each; no automatic second trial is implied.

`completion_manifest.csv` and `completion_seal.json` form one non-circular
owner snapshot, excluding only the manifest and seal themselves. This record
requests independent review at the existing gate, not canonical promotion.
