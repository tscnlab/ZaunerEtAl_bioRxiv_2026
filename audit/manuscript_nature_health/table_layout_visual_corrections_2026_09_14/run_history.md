# Actual run history and retained limitations

14 September 2026. R 4.6.1 and the existing accepted user library were used for content and structural reconciliation. Package versions are recorded with each executed script. Browser measurements are layout infrastructure, not scientific calculations.

1. Read Order 007 and the applicable Quarto/local-visual instructions. Checked the 37-member dispatch identity set and the immutable accepted attempt_04 baseline.
2. Built attempt_01 and passed 3,266 source/static checks. The initial default localhost bind was denied by the shell sandbox. The normal explicit escalation permitted the same exact read-only server, with no browser-surface or security change. A partial S2 review was retained. A stale helper reference to closed task tab 4 was corrected by passing the current tab explicitly. Neither issue affected source content.
3. Stopped attempt_01's server on port 61111, checked unchanged protected inputs and no listener, and retained its screenshots and inspection notes. The initial scroll evidence preceded completion of the scroll gesture; later settled evidence was used for conclusions.
4. Built attempt_02 with the same scientific table content and a CSS-only inherited-print-width proof control. Passed 3,266 source/static checks. Served only six allowlisted self-contained pages from a symlink-free root on 127.0.0.1:61388.
5. Actually inspected all six pages at 1440 × 1000 and 708 × 1000. Used keyboard scrolling inside the accessible table region. Checked S2 outside-region scrolling. Retained all left/right, bottom-row and note evidence. S2 parts 2 and 3 have earlier intermediate right-scroll captures and later `_right_final` evidence; the final files control the right-edge verdict.
6. Actually inspected all six inherited physical-width proofs at 1600 × 1000. These use 96 CSS pixels per inch and are not final Word proofs. Source font sizes and secondary S2 text sizes were measured. Complete source PNGs and small embedded axis text were inspected rather than inferred from pixel dimensions alone.
7. Used a 2200 × 1250 viewport to obtain all columns and notes in six complete unscaled screenshots. The documented cropped-screenshot API produced a half-scaled table plus outside content. The full-page clip variant reproduced the defect. Both were retained, not presented as valid table-only captures. An attempted PNG-header read exposed the actual JPEG encoding; the true formats are now explicitly inventoried without altering evidence bytes.
8. Closed only task tab 5, reset the viewport and retained all user tabs. Stopped exact task server PID 85811 through the normal permission flow. The server exited successfully. Postflight reported all protected inputs exact. No listener remained on ports 61388 or 61111.
9. The coordinator confirmed that exact R-only crops of already permitted full screenshots were allowed under clarification 007a. A strict R guard stopped the optional crop loop at S2 part 1 because the JPEG dimensions differ from the CSS viewport. The prior Table 2 crop passed decoded-pixel equality and is retained only as partial evidence. The author then clarified that full-page layout is the priority; the coordinator explicitly removed optional crops as an acceptance gate. No further crop workaround or generic capability test was pursued.
10. Re-ran the complete 1,118-check inherited baseline suite with only its output directory redirected to this new root. It passed. The first visual-evidence finalizer stopped on an openssl hash object's JSON class, and the second stopped on a relative baseline-manifest path. Both are utility errors, not source discrepancies; their partial outputs and executed scripts remain. The corrected third execution passed 854/854 checks.
11. The first post-QA source suite caught an integer-versus-double CSV column-type change when measured fractional CSS heights were inserted. No reused image, table content or scientific value changed. Candidate map reconciliation restored all unaffected CSV records byte-for-byte and compared exact text records. The final 3,266/3,266 suite passed. Failed intermediate output remains explicitly superseded by `static_verification_postqa_final`.
12. Final maps distinguish completed full-page review from later production. No final Word/native-table assembly, office conversion, live source replacement or integrated website render has been performed in this order.

No new scientific estimate, uncertainty interval, p-value, FDR decision, sample definition, diagnostic or claim was created or modified.
