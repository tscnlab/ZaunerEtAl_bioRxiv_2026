# Complete source-bound production specification

Status: proposed layout and production inputs, not implementation or visual
acceptance. The Brown source choices are resolved. All production allowances
remain zero under `writer_continuation_disposition_001.md`.

## Resolved Brown changes

The changed Brown native-table set is **B = {Table_2}**. Table 2 is expanded
from one historical combined table to two exact, unmodified accepted gt
excerpts: panel A contains 12 rows of adherence levels across both coverage
samples, and panel B contains six rows of free-minus-work contrasts and their
coverage sensitivity. It remains one numbered main table and one separate
editable `Table_2.docx`, containing two genuine native tables. Both panels
retain all columns, intervals, p-values, headers and applicable notes.

This expansion is explicit. The current single-table-per-label exporter and
one-image Table 2 mapping cannot be reused unchanged. A future bounded
implementation must group both `#tbl-brown-adherence table.gt_table` elements
under Table 2, preserving the other 18 labels. It must not shift later labels,
drop panel B or present the old Table 2 image as current. The native package
will still have 19 DOCXs, but 20 table elements across those documents.

Table S3 remains the descriptive pooled-minute table. Table S4 remains the
accepted four-effect exploratory association table. Their current native
DOCXs, exact source fragments and all three existing image parts are reused.
Their scientific scope and exact values were reconfirmed, not regenerated.

Figure S4 replaces its historical source with the accepted current adherence
levels SVG. Figure S5 replaces its historical composite with two independent
accepted SVGs: work-day levels (A) and free-minus-work contrasts (B). The
caption distinguishes comparisons against zero from the separate site-minus-
average family. Figure S6 reuses the exact anonymous participant raincloud.
Neither participant ranking nor a new participant summary was created.

## Complete maps and counts

- `native_table_dispositions.csv`: all 19 labels, original native identities,
  source identities, reuse/new status and the resolved Brown set.
- `complete_table_part_map.csv`: all 31 proposed image parts, source hashes,
  zero-based half-open tbody row ranges, full column counts, notes, dimensions
  and exact reusable images. Row ranges include existing group-label rows.
- `complete_SVG_source_map.csv`: 23 unique SVGs, including the three exact
  current Brown replacements and every unchanged non-Brown source.
- `complete_figure_appearance_map.csv`: 24 appearances. Both complementary S8
  crops remain tied to the same SVG and are verified from the old Word XML.
- `complete_55_drawing_order_PROPOSED.csv`: the complete proposed manuscript
  order. Removing only the newly inserted Table 2B, S7 part 4 and Figure S5B
  recovers the exact 52-position prior Word order.

These are expected counts, not evidence of an assembled document. No image
identity is invented for a missing artifact. S7A/B and S15A/B remain separate,
and the latest non-Brown display overlay must not be overwritten with older
canonical PNG/composite references from the prose preimage.

## Seven missing source-matched table images

The existing image-table manuscript workflow needs seven replacement files,
not five: three S2 parts, two S7 tail parts and two Table 2 panels. Exact
source identities and row ranges are in
`seven_missing_source_matched_artifacts.csv`.

S2 uses all 14 columns at 1490 CSS px, with widths
`[200,60,103,96,96,96,96,96,96,96,96,96,78,185]`. Parts are `[0,9)`,
`[9,17)` and `[17,23)`. All 170 whole mean ± SD spans must stay on one line,
with 70, 50 and 50 in the respective parts. Preserve all 17 distribution
images, explanatory text and hidden descriptions. First trial: no font
reduction. All three Word images use 15.55 inches width and must fit within
9.20 inches height. Their new heights are not yet measured.

The native S2 table remains one real table on A3 with 15.44 inches available;
use the same relative column vector, Arial and single line spacing. Preserve
exact punctuation and values, allowing NBSP only inside whole mean ± SD units.
The numeric base is 10 pt, optionally 9 pt only after the width-first layout
fails and the correction is separately released. Existing smaller explanatory
and sample text is unchanged. Do not make a cumulative or global reduction.

S7 retains existing parts 1 `[0,7)` and 2 `[7,13)` byte-exact. Its new tail is
part 3 `[13,19)` and part 4 `[19,23)`, all eight columns at 896 CSS px, fixed
Word width 10.55 inches and maximum height 6.20 inches. Notes belong on the
last part only. A two-file tail return must be merged into the four-part map.

The proposed Table 2 panels use their exact complete accepted HTML, 896 CSS
px, fixed Word width 10.55 inches and maximum height 6.20 inches. Panel A
contains all 12 rows/four columns; panel B all six rows/five columns and its
notes. These dimensions require verification. If either fails to fit, return
a bounded layout decision rather than silently shrinking, cropping or adding
parts. Reuse exact source styling; a future native export may map those styles
to consistent Arial, single spacing, cell shading and borders without changing
scientific content or precision.

The browser/capture restriction remains binding. No alternative browser,
CDP, loopback, indirect execution, office converter or screenshot route was
used to bypass it. Source-matched author-provided artifacts are the documented
safe option after the revised layout is implemented and pinned. Existing
captures do not prove the new layout. This specification is not an instruction
to the author to capture unsettled layouts now.

## Other retained layout work

The previously archived helper diffs still require explicit candidate-only
implementation. Keep captions left aligned and single spaced; keep headings
with first parts and final supplementary captions with final parts, without
chaining oversized multi-part blocks. Remove only the two identified empty
section transitions, not arbitrary blank paragraphs. Preserve the 13 top-level
section page starts, all four Table 3 images, numbering, authors, affiliation
markers, AI statement, CRediT, funding and declarations. S5/S6/S10 font repairs
already represented in the retained display overlay are not undone.

## Finite QA proposal, not an allowance

The resolved initial automatic DOCX QA set is S2, Table 2 and the complete
main document: three document commands, serially. If the packaged renderer's
three-office-process fallback is explicitly retained, budget at most nine
serial office processes for those commands, not three. Previously consumed
budgets do not reset; the unused old 0/2 office allowance is not repurposed.
No command has run. Readability and native compatibility remain unverified.

Seventeen unchanged native DOCXs can be copied byte-exact into the new final
candidate package; only S2 and Table 2 require export. The ZIP must contain
all 19 documents and matching README, table manifest and export provenance.
Whole-document assembly, SVG embedding, complete page QA and one separately
released focused native Word compatibility check follow, not precede, valid
inputs. No old preview is a final deliverable.

## Website and return

The 37-route audit remains an inspection, not a clean production release.
The coordinator/Harmonizer still need the source-transition authority bridges,
runtime/root/library pinning and 157 absolute read identities, protected
Descriptives thumbnails, final source/navigation/download postimages and finite
resource-write bounds. No profile, root or whole-project render is authorized
here. The delivered site must link to the exact new Word/ZIP, not the old
preview. The Brown review itself is closed and is not a remaining blocker.
