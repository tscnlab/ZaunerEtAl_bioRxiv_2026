# Verified R 4.6.1 lockfile snapshot

## Outcome

On 2026-07-31, `renv.lock` was updated from the verified R 4.6.1 project
library. It now records R 4.6.1 and all dependencies required by the authored
project scan and the explicit diagnostic dependency policy.

The update retained earlier package records, updated only installed versions
needed for R 4.6.1 compatibility, and added the installed dependency closure
for model diagnostics and parameterized Quarto reports. It was not a blanket
package update.

## Before and after

| Item | Before | After |
|---|---:|---:|
| R version in lockfile | 4.5.0 | 4.6.1 |
| Package records | 226 | 246 |
| Matrix | 1.7-3 | 1.7-6 |
| sf | 1.1-1 | 1.1-2 |

The new records include `see`, `DHARMa`, `shiny`, and their installed
dependency closure. The lockfile SHA-256 is
`3bf99c633fb123626eb14d29f0847b91f71030c92e90331fe401e3204bca8350`.

## Verification

- `tests/test_renv_lockfile.R`: PASS.
- `tests/test_environment_audit.R`: PASS.
- `tests/test_environment_smoke.R`: PASS.
- `tests/test_renv_status_timeout.R`: PASS.
- Deterministic environment audit: PASS with zero missing packages, zero
  version mismatches, and zero expected packages missing from the lockfile.
- Source-aware bounded `renv::status()`: PASS,
  `RENV_STATUS_SYNCHRONIZED=TRUE`, exit status 0, no timeout, about 5 seconds.

The retained machine-readable evidence is in `audit/environment/`, including
the dependency reconciliation and the bounded-status stdout and metadata.

## Remaining final-stage check

This snapshot resolves the shared environment mismatch now. The final clean
project run will repeat the same tests and resnapshot only if later approved
work changes the dependency set or an installed version.
