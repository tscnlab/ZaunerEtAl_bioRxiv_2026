--- a/tests/hypotheses/H05/test_h05_report017_source_harmonization.R
+++ b/tests/hypotheses/H05/test_h05_report017_source_harmonization.R
@@ -255,9 +255,18 @@
   for (chunk in chunks) {
     expressions <- parse(text = chunk$body)
     for (expression in expressions) {
+      operator <- if (
+        is.call(expression) && is.symbol(expression[[1L]])
+      ) {
+        as.character(expression[[1L]])
+      } else {
+        NA_character_
+      }
       if (
         is.call(expression) &&
-          as.character(expression[[1L]]) %in% c("<-", "=") &&
+          length(expression) >= 3L &&
+          !is.na(operator) &&
+          operator %in% c("<-", "=") &&
           is.symbol(expression[[2L]])
       ) {
         name <- as.character(expression[[2L]])
