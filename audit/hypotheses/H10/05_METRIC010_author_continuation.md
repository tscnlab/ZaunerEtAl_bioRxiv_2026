# H10 METRIC-010 author continuation

Date: 2026-08-11  
Status: **SUPERSEDED — RESOLVED BY FIND-049/CHG-101**

This record is retained as historical evidence of the temporary author gate.
The shared preparation repair is now sealed: the identified chest day has zero
viable momentary-ratio minutes and a reason-coded missing MDER, every finite
gap-timing-unaware MDER is strictly positive, and H10 has consumed the repinned
release. The instructions below no longer control current H10 outputs.

After H10 reported that the pinned gap-timing-unaware chest frame contains one
estimable MDER value equal to zero (`THUAS_S002`, 2025-03-09), the author
instructed H10 to **continue**.

H10 therefore applies the following bounded disposition:

- use the pinned shared gap-timing-unaware artifact exactly as supplied;
- do not drop, recode, impute, or repair the zero within H10;
- retain Gaussian identity models, for which the stored zero is in-domain;
- identify the zero explicitly as a comparator-specific provenance limitation;
- retain the strict-positive METRIC-010 definition for the primary dataset;
- fit only MDER-dependent models and comparisons; and
- preserve all non-MDER model results.

This authorization does not amend shared preparation, establish that zero as a
scientifically valid mean of strictly positive ratios, or close the upstream
shared-data question. Any shared correction still requires a coordinator-owned
decision and repinned shared artifacts.
