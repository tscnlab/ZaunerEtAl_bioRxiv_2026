# Order 60c execution notes

The controlling order identity matched its dispatch manifest at 6,852 bytes.
The 10,934-byte value in the dispatch message was a transcription error. The
SHA-256 and manifest row were exact.

Temporary harness assertions were stopped and classified before continuing:

- an occurrence vector failed only because R retained names;
- a reverse proof failed when `readLines()` and `writeLines()` changed file
  serialization, then passed with the accepted raw-byte method;
- OpenSSL retained a hash class until normalized with `paste0()`;
- the verifier diff has eight textual hunks implementing five logical
  classifications, rather than six textual hunks; and
- an in-memory build inventory used `NA` for directory hashes while the CSV
  representation used blank strings. Its persisted 1,180-row inventory was
  exact.

Air 0.4.1 was verified. A check-only formatter probe reported that it would
reformat the already sealed file globally. No formatting was applied because
Order 60c requires the exact three-line postimage.

The sandboxed loopback bind was denied before a listener existed. The one
authorized elevated server then completed the entire QA session. Direct CSV
display was blocked by the browser client after the server returned HTTP 200;
the complete verifier had already resolved every source-data path.

None of these representation or environment observations changed the test
contract, verifier contract, rendered page, scientific content, or QA verdict.
