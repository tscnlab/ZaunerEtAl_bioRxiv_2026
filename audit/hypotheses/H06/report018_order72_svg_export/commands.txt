REPORT-018 Order 72 H06 command record
Date: 2026-09-11

Environment used for every R command:
RENV_CONFIG_AUTOLOADER_ENABLED=FALSE
R_LIBS_USER=/Users/zauner/Library/R/arm64/4.6/library
R frontend: Rscript --vanilla
R version: 4.6.1

1. Sealed-release and input preflight, before creating the evidence root:
Rscript --vanilla -e '<read the 71-row release manifest; verify exact SHA-256, byte size, uniqueness, and non-circularity>'
Result: ORDER72_PREFLIGHT=PASS exact=71/71 unique=TRUE non_circular=TRUE R=4.6.1

2. Read-only dependency and display-source checks:
Rscript --vanilla -e '<check required namespaces and versions; inspect the frozen 6-row and 27-row CSV schemas and site registry agreement>'
Result: required plotting and SVG packages available; site registry agreement exact.

3. Private renderer probe:
Rscript --vanilla -e '<write a temporary 170 mm by 120 mm svglite device probe under /private/tmp>'
qlmanage -t -s 2141 -o /private/tmp/h06_order72_device_probe.40Aev2 /private/tmp/h06_order72_device_probe.40Aev2/probe.svg
Result: the sandboxed qlmanage attempt stopped at sandbox initialization; the same private probe command then succeeded with narrow approved Quick Look access. Quick Look uses a square, top-aligned thumbnail canvas.

4. Static parse of the minimal exporter:
Rscript --vanilla -e 'parse(file="audit/hypotheses/H06/report018_order72_svg_export/export_h06_order72_svg.R")'
Result: PARSE=PASS R=4.6.1

5. Single durable candidate export:
Rscript --vanilla audit/hypotheses/H06/report018_order72_svg_export/export_h06_order72_svg.R
Result: REPORT018_ORDER72_H06_EXPORT=PASS

6. Rasterize both unchanged candidate SVGs once with approved Quick Look access:
qlmanage -t -s 2141 -o audit/hypotheses/H06/report018_order72_svg_export/qa audit/hypotheses/H06/report018_order72_svg_export/candidate/H06_paired_placement_effects.svg audit/hypotheses/H06/report018_order72_svg_export/candidate/H06_stage3_site_specific_significance_screen.svg
Result: both SVGs produced one thumbnail.

7. Raster proof preparation:
sips -c 1511 2141 <S9 Quick Look thumbnail> --out <S9 center-crop diagnostic>
sips -c 1700 2141 <S12 Quick Look thumbnail> --out <S12 center-crop diagnostic>
Rscript --vanilla -e '<read each square Quick Look thumbnail with png, retain its top-aligned figure canvas at 2141 x 1511 or 2141 x 1700, and write 643-pixel reader-width copies>'
Result: exact-dimension and 170-mm comparison rasters created. The earlier center-crop files are preserved only as non-evaluative renderer diagnostics.

8. Side-by-side visual proofs:
Rscript --vanilla -e '<compose accepted PNG on the left and candidate SVG raster on the right at accepted dimensions and at 170-mm reader size>'
Result: SIDE_BY_SIDE_QA=PASS R=4.6.1. Manual inspection PASS for both displays.

9. Protected post-QA recheck:
Rscript --vanilla -e '<rehash all 71 sealed release-manifest paths and write the exact post-QA recheck>'
Result: POST_QA_PROTECTED=PASS 71/71 R=4.6.1

No Quarto, knitr, Pandoc, builder, QMD, fitted model, prediction, inference, source-data regeneration, canonical promotion, commit, push, or upload command was run.
