# Brown wake-anchored main analysis: Stage 2 transition

Decision ID: BA-018. Change ID: CHG-157. Date: 2026-09-11.

Status: Stage 1 independently accepted; bounded Stage 2 authorized only after
the coordinator sends the sealed release and ledger identities.

Owner: existing Brown task 019fffdf-66d4-7802-9091-09283ad27b7f.
Next mandatory stop: BA-LB-G2-REVIEW, author review of replacement scientific
results and their complete diagnostic disposition. No Stage 3/4 or Writer
numerical release follows automatically.

## Authority, acceptance and controlling specification

This continues BA-017 / CHG-156 and the author's already selected wake-anchor
chronology. Do not request the linkage choice again. It does not pre-accept
unseen estimates or remove the later author-review gate.

Read the complete Stage 1 plan and companion registries under:

    /Users/zauner/.codex/worktrees/82ab/ZaunerEtAl_bioRxiv_2026/audit/analyses/brown_adherence/main_linkage_b_amendment/stage1/

The plan is SHA-256 f0ac32458527f046b0485f983e05ae5ff28c3ae3e415ee338bc91007eb23b268
(46,057 bytes), the handoff is
1d4f5aaf72e3b2414338a0f80b3b73c8e31461988886a962bfa6582f05aa21a5,
and its 89-row non-circular final manifest is
7290ad5f2e595949cde38dcd241bc5f4cb0037094c1a0ed99e03c3252e877dc0.
Independent acceptance and the full release are under:

    audit/decisions/brown_main_linkage_b_stage2_release/

Independent R 4.6.1 replay passed all 89 members, 14 support/reuse checks,
36 finalization checks and 2,066 protected identities. Sixteen substantive
replay CSVs matched their sealed owner counterparts exactly. This proves
specification and input/reuse integrity, not future model estimability.

Stage 1 and every prior scientific/reporting package remain immutable. Use
this decision with the full accepted plan; the summary below does not replace
any more restrictive gate in that plan.

## Sole owner write boundary and preservation

Require this new root absent at dispatch, then create outputs only under:

    /Users/zauner/.codex/worktrees/82ab/ZaunerEtAl_bioRxiv_2026/audit/analyses/brown_adherence/main_linkage_b_amendment/stage2/

Use the 18 output groups and exact path templates in
`prospective_stage2_output_contract.csv`. Code, compiled binaries, model
interfaces, constructed chest/calendar frames, fits, diagnostics, estimands,
multiplicity, sensitivities, point-only decomposition, paired reporting source
data, tests and new evidence belong only here. Add `code/`, `diagnostics/figures/`
and `report_qa/` beneath this root for the corresponding implementation and
internal report assets. Do not overwrite a completed or failed job.

The only report endpoints released are the new root's
`implementation_and_reconciliation.qmd` and `.html`. No existing Brown report,
manuscript, selection preview, hypothesis, website, profile, shared source,
package, lockfile, model, figure or historical manifest may change. Preserve
the existing dirty renv.lock. Stage 3 and Stage 4 directories remain absent.

Before scientific work, rehash all 89 Stage 1 members, every additional
dependency and production-library description pin, all required scientific
inputs and the existing Brown protected inventory. Record new pre/post
inventories. Only the coordinator's exact BA-018/CHG-157 append is an allowed
central-ledger transition: verify all prior bytes through this release's
pre-append copies. Do not rewrite an old manifest to make a historical row
appear current. No other unexplained drift is allowed.

Use R 4.6.1 and the accepted production library:

    /Users/zauner/Documents/Arbeit/12-TUM/MeLiDos/WP2.2.5_Data_Analysis/ZaunerEtAl_bioRxiv_2026/renv/library/macos/R-4.6/aarch64-apple-darwin23

Retain RENV_CONFIG_AUTOLOADER_ENABLED=FALSE, the accepted package versions,
BROWN_ADHERENCE_PROJECT_ROOT and BROWN_ADHERENCE_AUTHOR_ROOT. Pin loaded
runtime paths and consequential versions before fitting. Set OMP_NUM_THREADS,
OPENBLAS_NUM_THREADS, MKL_NUM_THREADS and VECLIB_MAXIMUM_THREADS to 1. Do not
install, update, restore or modify the existing library or lockfile. Narrow
access to that existing user-owned library/cache is allowed when required;
it does not authorize broader filesystem changes or a profile bypass change.

## Scientific execution

1. Reuse the completed chronology/feasibility audit. Do not rerun it. Recheck
   the exact new interfaces and fit inputs. The two near-eye primary frames
   are the sealed linkage_b member and its finite support_fraction >= 0.80
   subset: 2,298 and 2,069 rows, 140 participants each, 794 and 762 cycles,
   54 State x Site x Day-type cells each. Retain independent state eligibility,
   exact 0/1 outcomes, inclusive thresholds, valid-minute denominators and
   actual-date/UTC wake-date ownership.
2. Copy the pinned likelihood/model routines into new code paths. Adapt only
   their declared data/formula and output-path interfaces. Preserve equations,
   parameter meaning, endpoint restrictions, optimizer protocol and integration
   convention. Compile only within `code/compiled/`, using the accepted C++17
   route. Seal the driver, imported source identities and deterministic exact
   likelihood tests before the first production fit. A failed likelihood or
   mapping test stops before fitting. Do not execute historical scripts in a
   way that writes back into historical directories.
3. Cleanly reproduce the stored B endpoint fit as BA-LB-PRIMARY-ANY, using its
   stored initial parameters and the specified optimizer protocol. Require
   objective and fixed-parameter agreement within 0.001, then 54-cell means
   within 0.01 percentage points at the estimand step. This is a numerical
   reproduction gate, not a choice between scientific results. A failed
   reproduction gate stops and cannot be relabelled as a structural failure.
4. Fit BA-LB-PRIMARY-80 on the exact coverage subset. Start at F3/R3/Q2/Q1/D0.
   Only the plan's finite, failure-driven component ladder is available.
   Preserve its triggers, hierarchy, diagnostics and matched structures for
   both samples. R3's participant intercept cannot be removed. No failed
   three-state random covariance or new participant-profile model is reopened.
   If a primary rung changes, resolve provisional F3-dependent registry rows
   to the actual selected common rung before their execution and record that
   mapping. No p-value or desired conclusion triggers a transition.
5. Derive the entire accepted response-scale output and covariance chain for
   both samples. Recompute whole BA-M1 through BA-M5 families separately in
   each sample. BA-M6 is exactly one 27-test primary BH family; its identical
   B80 contrasts are unadjusted direction/interval-exclusion/estimability
   checks, not a second BH family. Preserve the full-cell covariance,
   equal-nine-site weighting including the indexed site, component identities,
   rank checks, tolerances and quadrature checks. No historical C values or
   p-values may be spliced in. No family may be resized after results are seen.
6. Apply the three-effect BA-M1 B-to-B80 claim gate, including the existing
   2-percentage-point limit. Apply BA-M6's own direction, interval-exclusion
   and non-estimability rules without adding that magnitude threshold or using
   coverage-sample significance as primary evidence.
7. Complete all planned optimizer, endpoint, overall-calibration, residual,
   denominator, actual-date temporal, site and influence diagnostics. A
   numerical success is not a clean scientific assessment. Preserve failed
   fits, non-estimability and limitations. Never turn inconclusive or
   nonsignificant results into a technical failure to unlock another model.
8. Execute only the registered sensitivities and deletion jobs. Build B chest
   and calendar frames from the original pinned coverage/diary inputs and
   reconcile them before their fits. Do not relabel incomplete C chest rows,
   guess a chest sample size, pool positions, or add a Sleep chest analysis.
   Temporal routes require the exact existing trigger and actual date gaps.
   Unresolved dependence remains a limitation. Diagnostic family comparisons
   cannot silently replace the primary endpoint model.
9. Derive the plan's point-only response-scale R2, retained-intercept and
   global/within-state Shapley decomposition only after eligible primary-model
   gates, with the accepted reference weights, denominator distribution,
   quadrature and allocation checks. No uncertainty resampling, new p-values
   or reduced-model Shapley refits are authorized.
10. Reconfirm the exact frozen exploratory subset and raincloud reuse. No
    exploratory refit, expanded pair sample, new tertiles, rank, jitter,
    density or stable-trait claim is authorized. The exploratory day-level
    claim remains withheld. Keep nominal near-eye stream identity distinct
    from protocol-defined bedside placement during Sleep.

## Exact computation limits

The accepted registry proposes 30 nominal fit slots. The only reserves are
14 paired structural-transition fits, one zero-exclusion structural recovery
and at most six triggered temporal fits. Fifty-one is an absolute ceiling,
not a target. Unused slots cannot be reassigned. Retain exactly nine LOSO and
five screened participant-deletion slots, with the specified deterministic
screen and tie handling. A failed deletion does not grant another search.

Use one computational R job at a time. All optimizer passes within a job count
towards that job's 120-second ordinary or 180-second temporal wall limit.
The entire statistical batch has a 1,200-second accumulated compute-wall
limit, including compilation, likelihood validation, construction, fitting,
diagnostics, estimand/decomposition work and scientific verification. Exclude
only passive waiting, file hashing, prose preparation and the separately
bounded report render/QA. Checkpoint the accumulator across clean resumes.
Time supervision must be effective even in compiled code and must target only
this job's process lineage, never unrelated R tasks.

At most six named diagnostic samples receive 250 conditional predictive
draws, for a maximum of 1,500. Preserve the plan's seeds and record a fixed
sample/route seed registry before drawing. These are diagnostic envelopes,
not bootstrap or sampling-distribution inference. No heavy simulation,
resampling, exhaustive deletion or larger run is released under COMPUTE-001.

Checkpoint every job with input/code hashes, identifier, trigger, command,
seed, elapsed time, exit status, gates and outputs. A clean resume may reuse
only an exact completed job and proceed to the next unconsumed slot. No
automatic replay of a crashed job, unregistered optimization restart or
second scientific batch is allowed. Stop and seal on a genuine new defect,
an exhausted limit or a model/measurement change outside the finite plan.

## One internal report and author-review handoff

Prepare `scientific_assessment.md` and the new internal Stage 2 QMD from the
frozen new outputs. Report the complete sample, model/family identities,
coverage, endpoint versus overall assessment, temporal and influence
limitations, chest comparison, decomposition and exact reuse disposition.
Internal history may be explained here. Future reader Stage 3/4 pages must
remain standalone and results-led, with only brief alignment Methods text.

After scientific gates and the complete report source/input checks pass, this
order permits exactly one narrow internal target render from the Brown root:

    quarto render audit/analyses/brown_adherence/main_linkage_b_amendment/stage2/implementation_and_reconciliation.qmd --to html

Use the same accepted R environment. Inspect the existing nearest Brown
default-project configuration and the new QMD before rendering; do not change
that configuration or use a website profile. Report chunks may only read and
display frozen Stage 2 outputs and execute reporting checks. They may not fit,
optimize, simulate, construct frames, derive inference or rerun the driver.
Do not rely on --no-execute to suppress inline R. No second target, full-project
render or render retry is authorized. Keep outputs and report support files
inside the new Stage 2 root or a declared temporary render directory.

Use the established narrow access to the existing user-owned Quarto Sass cache
if required, as in the successful order-51 environment recovery. Do not reset,
relocate, copy, chmod, chown or clear it, and do not repurpose HOME. Record
environment cache changes separately from scientific preservation. Fail closed
if the scoped environment or output contract cannot be met before invocation.

Preserve raw HTML. If native gt repair is needed, use the accepted pinned
semantic engine on a candidate only, verify exact reverse/reapply plus
visible-content identity, then promote once. Require unique document IDs,
table-scoped intended-header resolution, exact tables/figures/source binding,
working links, privacy, no embedded errors and no unresolved references. New
claim values must reconcile to exact stored output cells, not just source text.

The active quarto-authoring skill requires served visual inspection rather
than treating a DOM check as visual QA. Stage only immutable report output in
a fresh narrow directory, perform the symlink-containment preflight, bind a
single read-only server to 127.0.0.1, and inspect the exact route at desktop and
narrow widths with supported browser controls and intended figure sizes.
Record actual capabilities and limitations. Never bypass a browser rejection
or substitute an iframe/emulation workaround. Close the tab/server, prove no
listener and rehash. No Word or public website integration is authorized.

Return one non-circular Stage 2 final or stopped manifest, its independent
verification, all job/runtime and preservation records, the report, assessment,
`stage2_handoff.md` and `author_gate.md`. On successful completion stop at
BA-LB-G2-REVIEW and request author review. Do not update old main values,
captions, S5, current Stage 3/4 pages, Writer prose or public downloads before
that acceptance and a later explicit reporting release.

The Harmonizer's Order72i read-only display preflight and Writer's separately
approved source-only editorial work may continue. Do not dispatch either for
Brown numerical integration. The hypothesis matrix, navigation and all other
owner render permissions are unchanged by this decision.
