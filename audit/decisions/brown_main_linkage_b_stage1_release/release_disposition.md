# BA-017 / CHG-156: Brown Stage 1 dispatch disposition

Date: 2026-09-11

The author's main pre-sleep alignment choice is recorded. This release sends
the concrete Stage 1 replacement specification to the existing Brown task,
`019fffdf-66d4-7802-9091-09283ad27b7f`. It does not release fitting or new
scientific results. No further author choice of linkage B is required.

The controlling scope is
`audit/decisions/brown_adherence_main_linkage_b_stage1_reopening.md`, under
BA-017 and CHG-156. Its final identity, both appended-ledger identities, this
record, the durable audit copies, and current preservation pins are sealed in
`release_manifest.csv`. The manifest excludes itself and the later dispatch
receipt. The receipt records the actual application dispatch separately.

## Completed audit, not another feasibility round

The release rehashes the owner's 9-member evidence manifest, the existing
7-member linkage-B fit manifest, and six byte-identical independent replay
outputs. It verifies 14 frozen authority/input identities and 27 current owner
preservation identities. No RDS was deserialized and no audit, fit, prediction,
or inferential calculation was repeated in release preparation.

Both temporary audit packages are preserved byte-for-byte under
`completed_owner_audit/` and `completed_central_replay/`. Their historical paths
remain unchanged inside the copied records. The explicit mapping is
`historical_evidence_copy_inventory.csv`; these durable copies remove any need
to depend on the continued existence of temporary audit directories.

The earlier draft and both pre-append ledgers are preserved under `baseline/`.
`ledger_append_verification.csv` must prove each current ledger is its exact
old byte prefix followed by exactly one new row, with unique BA-017/CHG-156.
No historical central decision was edited.

## Owner boundary and next gate

Only new files under the following Brown worktree root are authorized:

`/Users/zauner/.codex/worktrees/82ab/ZaunerEtAl_bioRxiv_2026/audit/analyses/brown_adherence/main_linkage_b_amendment/stage1/`

Produce `plan.md`, `stage1_handoff.md`, the exact future model/compute and
dependency specification, preserved completed audit evidence, input and
preservation inventories, checks, commands/session information, and a
non-circular final manifest. Return at `BA-LB-G1-REVIEW` for coordinator review.
Capture all pre-existing Brown analysis file identities before creating the
new root and prove that every path outside it remains unchanged at the seal.

Keep near-eye membership grounded in the sealed linkage-B frame. Identify and
pin the original chest coverage dependency for the prospective B chest plan;
do not form it by incomplete relabeling of C chest rows. Preserve BA-M6 as one
27-test primary BH family plus the identical support-80 stability/estimability
contrasts without a second FDR family or a new magnitude threshold.

No production fit, optimization, TMB/objective reconstruction, prediction,
contrast, p-value/FDR computation, simulation, R2/Shapley calculation, display
generation, current QMD/HTML edit, Quarto execution, manuscript update, package
or lockfile change is released. Existing model outputs remain benchmarks, not
replacement-primary acceptance. A separate Stage 2 transition follows only
after the concrete plan passes; replacement results later require author
review at `BA-LB-G2-REVIEW`.

Use R 4.6.1 with the established existing library and disabled renv autoloader
for any bounded Stage 1 verification. Do not install, restore, or alter the
environment. Inputs in the release manifest are exact at dispatch; central
ledger identities are dispatch-time authority, not permission to edit them.

## Independent work and holds

The Writer holds affected Brown main claims, numerical displays, and final
integration until replacement evidence is accepted. Its unaffected work may
continue. Orders 72d and 72h remain independent and unchanged. The new S7/S15
split requests and S10 font investigation are queued separately and receive
no implementation authority from this release. The 15-row hypothesis matrix
and production website are untouched.

The measurement-methods audit skill governs the read-only evidence check.
This administrative release routes the approved next specification to its
owner without changing author code, data, or scientific artifacts.
