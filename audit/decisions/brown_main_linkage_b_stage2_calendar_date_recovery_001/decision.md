# Brown saved-calendar validation and finite continuation

Date: 2026-09-12. Identity: BA-018-CALENDAR-DATE-RECOVERY-001.
Parent: BA-018 / CHG-157 and BA-018-CHEST-PREFIT-RECOVERY-001.
Status: release only after the exact non-circular dispatch seal passes.
Owner: Brown recommendation adherence strategy, task
`019fffdf-66d4-7802-9091-09283ad27b7f`, isolated 82ab worktree.
Relative paths below start at its existing
`audit/analyses/brown_adherence/main_linkage_b_amendment/stage2/` root.

The author has instructed completion of the Brown analysis and both reader
reports. This is a technical validation recovery within the approved finite
scientific contract. It adds no model, sample, estimand, prediction target,
multiplicity family, draw allowance or runtime budget.

## Accepted stopped-state classification

Preserve package008 and all earlier evidence byte-for-byte. Its 1,215-member
final manifest is
`dfae407c449323549930428f73f93792c7c4d2dd895038234fecb2807c5aeab3`.
Retain all 77 job histories and all seven nonzero exits as failures at their
actual execution stages. The original CONSTRUCT-CALENDAR-B run remains exit 1.
It must not be relabelled PASS, removed or repeated.

The calendar frame is unchanged at
`calendar/frames/model_input.rds`, 72,152 bytes,
`a5001792d45e1e0dbc823f2ea94721c9f701c4a8ed01faff4be5e8e2a17f117d`.
Its `accepted = FALSE` field remains unchanged. The new validation certificate
establishes input eligibility, not model or author acceptance.

The independent R 4.6.1 audit passes 64 checks, including raw UTC minute
recounts, true-local-date intersections, all 2,298 parent Y/N/E/projected
counts, all 2,894 eligible chunk dates, wake-date ownership and inherited
metadata. The sole failed original assertion compares Date vectors whose
values are exact but whose descriptive label attributes differ after parent
subsetting. The saved label equals the original full parent label. Preserve
that label. Do not strip metadata, coerce the payload or rewrite dates.

The chest fit and two contrasts separately pass 30 independent checks.
Saved chest and benchmark diagnostic tables pass 511 independent checks,
without new fits, predictions or draws. Chest has three applicable passing
endpoint envelopes, but retains numerical cautions and one temporal trigger.
The noninflated B benchmark has none of five applicable passing endpoint
envelopes while mean calibration is acceptable. Its variance-boundary
limitation remains and it is not a matched-random-structure test of inflation.

## Exact write-once copies and validation

Use `exact_owner_copy_map.csv`. All four destinations must be absent. Copy
bytes exactly from durable `independent_evidence`, checking size and SHA before
and after. Do not retype or format these copies.

1. `code/25_validate_saved_calendar_b.R`, 7,659 bytes,
   `c9b0c9b0ffe05006aa1fba49a31798c03bd6874a1539d4890724c8c7b58b19fe`.
2. `code/run_bounded_job_v9.py`, 35,998 bytes,
   `fbc7f7d86352bdf6db2faabff54765b473aa86d8c0ecb44409b88f48f2f39691`.
3. `preflight/calendar_date_recovery_001/job_registry.csv`, 1,372 bytes,
   `f5edc5791907ac111ecab1106b81f7b671a06a70faac3c731b50344fa254c93c`.
4. `preflight/calendar_date_recovery_001/source_manifest.csv`, 6,410 bytes,
   `b7fce272085b794da3e840b2cebe7bb51ccc30917cc6b5d41dadcd5e079c2eb3`.
   All 26 source rows must resolve exactly after copying, without circularity.

Execute only `VERIFY-SENSITIVITY-CALENDAR-DATE-RECOVERY-001` with V9 and the
exact driver25, no arguments, at most 120 seconds and the remaining total.
It reads the existing frame, parent and preserved validation evidence, checks
typed dates and the inherited label separately, rechecks all existing input
gates and writes only new `calendar/saved_frame_validation_001/` evidence.
It does not load raw coverage, recount, construct a model frame or fit a model.

The entire prospective driver was replayed with writes confined to temporary
audit output: 14 core checks, seven positive/negative date fixtures and the
complete five-member output manifest passed. The fixture explicitly rejects
shifted/missing dates, changed/dropped labels and untyped/printed replacements.
V9 has 44 process-only passing fixtures, exact six-change reverse to V8, no
real R children or signals in those fixtures, the same exclusive lock and
unchanged stop/reaping/cap behavior. V9 recognizes only the exact preserved
calendar stop and forbids calendar construction, completed drivers and
validation retry aliases.

## Continue the unconsumed finite work in this order

After validation PASS, continue without requesting another permission for
the following already enumerated registrations and implementations:

- Register and fit the single CALENDAR-CHUNKS comparator from the exact existing
  saved frame. Bind both its immutable original manifest and the new validation
  manifest/certificate. Do not demand a passing original date assertion or an
  edited `accepted` flag. Its formula is the accepted beta-binomial F3/R0 with
  participant diagonal state/day-type terms, behavioral-cycle intercept and
  participant-calendar-date intercept. Use the existing declared dispersion,
  fitting, numerical gate and export interfaces. No endpoint inflation of
  chopped windows and no calendar-specific simplification ladder.
- Preserve and report the actual calendar gate. An ineligible candidate is
  status-only, with no derived estimates or substitute. If eligible, use the
  existing response-scale/equal-site comparison convention and full covariance;
  check every downstream grid, formula/parameter order, variance and source
  row before exporting. No new p-value family.
- Complete the already-triggered actual-date temporal sequence for ANY and 80:
  ENDPOINT-R3, then BB-R0, then BB-R3, each successor only after a recorded
  structural failure. At most six temporal fits and one selected diagnostic
  sample per primary sample. Time uses real calendar gaps, never compressed
  visit order. Do not add a chest temporal model or replace the primary.
- Preserve failure and unavailable statuses and assemble the full finite-route
  diagnostic/sensitivity disposition. No completed chest, benchmark, primary,
  sensitivity, deletion, estimand or diagnostic output may run again.

New implementations, deterministic interface tests and exact pre-execution
driver/input/output/seed registrations may be written once under the existing
code, tests, preflight, calendar, temporal, diagnostics and sensitivity roots.
Before each execution, exercise the complete applicable source, grid, model
interface, export and eligibility path. Source the specific accepted formula
helpers rather than a historical executable driver. Do not export or treat
mock outputs as scientific results. No extra nominal or conditional fit slot.

## Accounting and safeguards

The 77 supervised histories total 492.651775416045 seconds. V9's fixed audit
debit is 59.03137896001269 seconds, including all prior audit charges, the
owner's 0.686-second audit and these four new timed independent audits exactly
once. Starting total is 551.6831543760577 of the original 1,200 seconds.
Do not add previous fixed debits or budget-used-before fields again. Charge
new scientific audits once. Each ordinary job is capped at 120 seconds and
each temporal fit at 180, clamped to the remaining total.

Draws remain 1,000 logical / 1,250 attempted, with ceilings 1,500 / 1,750.
Only the two selected temporal 250-draw slots remain, using the frozen seed
map, predictive seed 20260814 and residual seed 20260815. No new seeds,
bootstrap or inferential resampling.

Use R 4.6.1 and the existing author library with
`RENV_CONFIG_AUTOLOADER_ENABLED=FALSE`, unchanged Brown/author root variables
and single-thread OMP/OPENBLAS/MKL/VECLIB settings. No library, cache, package,
binary, lockfile or profile change. Freshly require no competing Brown
scientific/render process and absent computation lock before dispatch.

The Pre-sleep ANY-to-80 claim gate remains FALSE and coverage-sensitive.
Do not claim no association or a formal difference between estimates merely
from differing CI exclusion. INFLUENCE-4 and DIAG-BINOMIAL remain failed.
Exploratory day-level claims stay withheld, participant associations remain
qualified and non-causal, and no hard participant tertiles or rankings are
released. Retain complementary chest and bedside Sleep measurement limits.

## Combined return and finishing boundary

Write the next combined package only in absent
`completion_v2/continued_final_package_009/`. Include exact jobs, elapsed/draw
accounting, every route's fit and inference eligibility, all failures,
preservation checks and one non-circular manifest. Stop once on a genuinely
new discrepancy or exhausted budget, with bounded downstream diagnosis.
Do not retry a failed execution or expand the scientific contract.

The next checkpoint remains BA-LB-G2-REVIEW. R2/Shapley, exploratory exact-reuse
verification, the internal report and both reader reports remain required
next steps, to be released from independently accepted analytical inputs.
No Quarto/browser, current reader-source/HTML, shared website, manuscript/S5,
writer numerical handoff, other task, commit, push or upload in this recovery.
The coordinator continues the author's finishing objective after this return.

Dispatch once only after the full seal passes and the owner is idle. Keep the
receipt separate from the dispatch manifest.
