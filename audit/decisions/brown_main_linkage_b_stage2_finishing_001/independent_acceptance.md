# Independent finite-analysis acceptance

2026-09-12. BA-018-ANALYSIS-FINISHING-001, BA-018 / CHG-157.

Verdict: the completed finite sensitivity package is accepted with its recorded
qualifications. It is not yet the complete Stage 2 or reader-report package.
R2/Shapley, final exploratory reuse verification and reader-source assembly are
still required. The separate decision releases those tasks only.

The package010 manifest d040431c40b440d9613b0ca2ac7517a55ca558fae9cd5facf88a8d420867111f
contains 1,466 exact non-circular members. The 34 owner finalization checks,
4,533 protected rows / 2,287 distinct files and 91 completed execution histories
are retained. Eight nonzero historical executions remain recorded. They do not
authorize another fit or convert their failed scientific gates into a PASS.

## Independent saved-output audit

All numerical verification used R 4.6.1 and the accepted project library.
Commands, versions, input manifests, console outputs and exact execution times
are copied byte-for-byte under independent_evidence. No author file, model,
frame, diagnostic draw, figure or report was changed. No model fit, predict
method, objective evaluation or fresh random draw was invoked.

- temporal_completed_audit_output_003/checks.csv: 60/60 PASS. This reconciles
  both sample memberships, true calendar-date coordinates, random covariance,
  54 marginal cells per sample, full conditional fixed-effect covariance,
  15/30-node quadrature, three equal-site day-type contrasts per sample, primary
  comparisons and route eligibility from saved objects.
- temporal_diagnostic_audit_output_001/checks.csv: 604/604 PASS. This reconciles
  both saved 250-draw packages, mixture probabilities, residuals, calibration,
  endpoint envelopes and date-ordered temporal diagnostics without new draws.
- The preceding accepted calendar audit has 33/33 checks; its evidence and all
  prior simple, deletion, family, weighting and chest reviews remain pinned
  through the prior central dispatch, not replaced by this acceptance.

Two stopped audit attempts are retained. The first used an overrestrictive
covariance-failure check: a finite indefinite matrix also fails the registered
gate. The second compared CSV-inferred integer 80 with character 80 in the model
object. Only the temporary auditor was corrected. Both failures are charged in
the exact fixed audit debit, together with both successful audits.

## Qualified scientific disposition

Both endpoint-inflated temporal routes failed convergence, gradient, Hessian,
covariance and endpoint uncertainty requirements. The 80% covariance was finite
but indefinite. Their first eligible fallback was BB-R0, with no BB-R3 run.
Both fallback fits retain near-boundary cautions. All six contrasts preserve
the corresponding primary direction and interval-exclusion status, but the
fallback changes family and random structure as well as temporal treatment.

Both fallback endpoint-probability checks remain not acceptable, with zero of
five applicable envelopes passing. Any-valid retains its Daytime Pearson
temporal trigger. The 80% absence of a trigger is not proof of independence.
Their overall calibration classifications remain separate from those endpoint
failures. No failed or sensitivity model replaces the main model.

The main Pre-sleep coverage gate is FALSE: its direction persists but its
coverage-sample interval includes zero. INFLUENCE-4 and DIAG-BINOMIAL have no
derived inference. Chest is a separate sample, calendar and temporal comparisons
remain qualified, and bedside Sleep is not ocular exposure. Exploratory
day-level claims remain withheld. These are scientific limitations to report,
not software errors to remove.

## Remaining work and control

Four finite, no-fit/no-draw finishing jobs are specified in decision.md. Their
runtime starts from accounting_summary.json, using actual execution metadata
and charging each audit once. Canonical reader endpoints remain unchanged at
reader_preservation_pins.csv until a separate coordinator report release.
BA-LB-G2-REVIEW stays open. Dispatch requires a fresh exact non-circular seal.
