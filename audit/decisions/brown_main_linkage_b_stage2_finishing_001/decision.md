# Brown finite-analysis acceptance and bounded finishing release

Date: 2026-09-12. Authority: BA-018-ANALYSIS-FINISHING-001.
Parent: BA-018 / CHG-157 and BA-018-TEMPORAL-TRANSPORT-RECOVERY-001.
Owner: Brown recommendation adherence strategy,
019fffdf-66d4-7802-9091-09283ad27b7f, isolated 82ab worktree.
Release only after the independent non-circular dispatch seal passes.

The author directs completion of the Brown analysis and reader reports. This
release implements the already specified finishing calculations and source
preparation. It does not select another model, change a sample or estimand,
reopen any consumed fitting or diagnostic slot, or imply unseen author approval.
The coordinator will continue directly to the bounded report releases after
independent acceptance of these finishing outputs. No further author permission
is needed for that ordinary implementation of the standing completion request.

All relative owner paths below begin at
audit/analyses/brown_adherence/main_linkage_b_amendment/stage2/.

## Accepted finite analytical package

Preserve package010 final manifest
d040431c40b440d9613b0ca2ac7517a55ca558fae9cd5facf88a8d420867111f,
all 1,466 exact members, its 34 finalization checks, 4,533 protected rows /
2,287 unique files, every previous package and all 91 supervised histories.
Independent R 4.6.1 saved-output verification passes 60 temporal estimate and
604 diagnostic checks without a fit, predict call, objective evaluation or draw.
The prior independent calendar verification passes 33 checks.

Both temporal endpoint-inflated candidates failed their registered structural
gates. The first eligible fallback is BB-R0 in both samples; neither BB-R3 was
run. The 80% endpoint covariance is finite but indefinite, which is still a
covariance failure. Do not describe both failed covariance matrices as nonfinite.
Both BB-R0 models retain near-boundary numerical limitations. All six temporal
contrasts reproduce from the full conditional fixed-effect covariance and
integration of participant, cycle and stationary actual-date OU variances.
Variance parameters are held fixed in that delta-method uncertainty convention.
Both quadrature checks and corresponding primary direction/interval-status
comparisons pass. This fallback also changes family and random structure; it
does not isolate the effect of temporal correlation alone.

Both temporal fallback endpoint-probability assessments are not acceptable:
zero of five applicable endpoint envelopes pass. Any-valid retains the Daytime
Pearson temporal trigger. The 80% sample has no registered trigger, which is not
proof of independence. Overall calibration is acceptable with limitations in
any-valid and acceptable in the 80% temporal sample. None replaces the main model.

The main Pre-sleep coverage claim gate stays FALSE and explicitly qualified.
INFLUENCE-4 and DIAG-BINOMIAL remain failed and have no derived inference.
The calendar comparator retains its variance-boundary caution. Chest remains a
separate eight-site/two-window sample, not pooled with the primary. Bedside
Sleep measurement is not ocular exposure or a whole-bedroom measurement.
Exploratory day-level claims remain withheld; participant associations stay
limited and non-causal. No hard participant tertiles or ranks.

Two initial independent audit stops are preserved: one incorrectly required
nonfinite covariance rather than allowing finite indefinite covariance to fail;
the other compared the CSV-inferred integer sample label 80 with its character
RDS label. Only the temporary auditor changed. The corrected complete audit
passes. These failures and their actual runtime are retained, not relabelled.

## Exact write boundary and preparation

Create only these absent implementation sources, plus their new evidence:

- code/36_finishing_contract.R
- code/37_verify_finishing_interfaces.R
- code/38_derive_r2_shapley.R
- code/39_verify_exploratory_reuse.R
- code/40_export_finishing_sources.R
- code/run_bounded_job_v11.py
- preflight/analysis_finishing_001/
- tests/analysis_finishing_001/
- r2/
- reuse/
- source_data/
- scientific_assessment_finishing.md
- completion_v2/continued_final_package_011/

Existing paths, including every historical model, figure, source CSV, code,
manifest, failed output and canonical QMD/HTML endpoint, remain immutable.
If a proposed directory already contains files, inventory them and preserve
them; use absent names inside the named scope and stop on an overlapping target.
No broad historical manifest builder, deletion, rename, package installation,
lockfile, profile, library, cache or binary change.

Before any scientific job, prepare and parse all five R files, Air-format only
these new files, and inspect the complete downstream path. They must contain no
fit, optimizer, objective, predict-method, simulation, bootstrap or RNG call.
Read only the exact frozen models/frames and stored outputs required below.
Reuse pure mathematical definitions from the pinned historical R2 driver if
helpful, but do not execute that historical driver or import its numerical output.
Do not source a file with top-level production actions.

Seal job_registry.csv, source_manifest.csv and registration_manifest.csv under
preflight/analysis_finishing_001/ before execution. Register exactly the four
jobs below with driver path/hash, empty argument list, output root, zero fits,
zero diagnostic draws, and source-manifest identity. Use a leaf-up, non-circular
seal: source manifest contains source/input files, not itself or a later mutable
execution output; registration manifest contains the two registry leaves.
Do not guess dynamic discovery row counts or include a live manifest self-row.

V11 is a copy of V10 with only the new exact four-job dispatch classification,
registration/hash checks, updated fixed audit debit, additional package010/
central pins and the finishing-authority metadata field. Preserve all historical
failure identities, accumulator semantics, exclusive lock, process-tree reaping,
single-thread environment and 1,200-second total. Keep old temporal definitions
needed to verify history but deny every temporal, primary, diagnostic or other
fit job for current dispatch. Do not weaken a historical assertion to admit a
new job. Seal an exact diff and reverse proof to V10, then exercise the complete
V11 path with inert process fixtures: allowed jobs, consumed job, alias, wrong
driver/hash/arguments, changed source, missing/failed prior history, exhausted
budget, concurrent lock and timeout/reaping. Fixtures must create no R child or
real signal and belong only in a fresh temporary directory. This metadata work
does not itself authorize another scientific execution.

## Four finite scientific jobs, no further fitting or draws

Use V11 and the unchanged accepted environment, one R process at a time:

1. VERIFY-SENSITIVITY-FINISHING-INTERFACES-001:
   code/37_verify_finishing_interfaces.R, no arguments.
   Exercise deterministic synthetic mixture, denominator, random-integration,
   subset/permutation and failure cases; verify actual model interfaces and
   all downstream source/claim dependencies without producing R2 results.
2. DERIVE-SENSITIVITY-R2-SHAPLEY-001:
   code/38_derive_r2_shapley.R, no arguments. Produce both sample decompositions
   under the exact point-only contract below, once.
3. VERIFY-SENSITIVITY-EXPLORATORY-REUSE-001:
   code/39_verify_exploratory_reuse.R, no arguments. Reconcile the existing
   exploratory and anonymous profile outputs exactly, without refitting or
   replacing any exploratory value or figure.
4. DERIVE-SENSITIVITY-FINISHING-SOURCES-001:
   code/40_export_finishing_sources.R, no arguments. Assemble only traceable
   aggregate tables/source data and provenance from frozen completed outputs,
   including all failed and qualified sensitivity statuses.

Each job has a 120-second maximum, clamped to the remaining total. Each output
is write-once. Preserve a failed attempt and stop once with a consolidated
diagnosis if a genuinely new defect occurs. Do not retry a consumed job, add
an alias or change the scientific definition to get a PASS. Parse/format and
inert infrastructure checks may be corrected before execution without asking
for another permission. No new model, contrast, p-value, interval, multiplicity
adjustment or diagnostic draw is included in these four jobs.

## Point-only R2 and Shapley contract

Use only the frozen F3/R3/Q2/Q1/D0 primary model pair:
ANY 494b4647e7394b8efcaaf7cd7ae2922ba0669ac609529328d0a5e67cab60c245;
80 72cc742f3d60941a5a4fc44361f09a671fea00a4183b85ab932d1d7292a02a74.
Frame 189e8acf90dd44c4f58a74cd7cc9166bd7ecc451c0da8c150c9197bffd5df035.
Their accepted reporting qualifications do not change their fitted parameters.

Retain the 54-cell balanced recommendation-window/site/day-type reference,
equal 1/54 weights globally, each window's equal 1/18 reference, empirical
within-cell mean inverse valid-minute denominator and new-participant normal
integration. Use the exact endpoint mixture and active parameters only.

V_fixed = Var_X(E_b[m]); V_random = E_X(Var_b[m]);
V_observation = E_X,b,N[r]; V_total is the sum. Use population/reference-weighted
variance, not sample variance with an n-1 divisor. Report fixed, participant and
observation shares, marginal/conditional R2, their participant-intercept
increment and the retained logit-scale SD. Inactive terms are not zero estimates.

Global Shapley has three players, window/site/day type and all eight subsets.
Within each window, site/day type use all four subsets. Allocate full-model
interactions by the exact Shapley rule without reduced-model refits. Export all
global and within-window subset values, absolute contributions and relative
percentages of fixed-effect variance, with their denominator explicit.
No CI or p-value is added. Undefined relative weights are withheld, not clamped.

Compute 15 and 30 normal nodes; use 30 for final values. Require at most 0.05
percentage-point difference for every reported R2 and relative weight, finite
components, nonnegativity within 1e-10 and total/allocation identities within
1e-8. Test permutation invariance, exact reference membership and empirical
denominator coverage. Preserve failures rather than forcing components or
weights to sum correctly.

Required r2 leaves: variance_decomposition.csv, random_effects.csv,
reference_cell_components.csv, shapley_global.csv, shapley_within_state.csv,
subset_values.csv, quadrature_check.csv, validation.csv, exact input/source
manifest and a non-circular final manifest. Additional deterministic check
evidence may be stored under tests/analysis_finishing_001/.

## Exact exploratory reuse and reader-source preparation

Verify the frozen exploratory samples: 1,376 primary pairs, comprising 758
Sleep and 618 Pre-sleep; 1,199 coverage pairs, comprising 697 and 502.
Preserve their exact cluster/date/window/count/coverage membership and every
stored within/between predictor, fitted model, four-member family and diagnostic.
Do not expand to the main construction's 743/581 potential Pre-sleep pairs.
Reproduce the 139 complete three-window profiles/417 points by deduplicating
Daytime within participant-cycle before averaging and comparing the complete
anonymous mean/count vector multiset within 1e-12. Preserve the anonymous mapping,
jitter, density, source CSV and PNG/SVG bytes. No participant ID in reader data.
Reuse scope must distinguish descriptive profiles from model estimates and
retain the withheld day-level claim and limited participant-level interpretation.

Required reuse leaves: cross_state_reconciliation.csv,
cross_state_protected_manifest.csv and raincloud_reconciliation.csv plus the
non-circular input/evidence seal. These are checks, not replacement analyses.

Export privacy-safe source_data/<endpoint>_source.csv leaves and provenance_map.csv
covering the complete current main report, diagnostics/sensitivities, R2, chest
and exact exploratory reuse. Each row/claim maps to its frozen source, model,
sample, key and units. Do not rederive inferential quantities in a display
builder. Every main number must come from the new joint B fit, including
Daytime/Sleep rows whose input observations did not change. Include full
multiplicity families before selecting reader rows, not only supported rows.
Keep CI exclusion separate from FDR evidence and both site-forest tests distinct.

The scientific assessment must state actual numerical/diagnostic dispositions,
the Pre-sleep coverage-sensitive interval, finite failed sensitivity routes,
temporal endpoint failures, residual trigger, chest sampling and bedside Sleep
qualification. Describe the final cycle construction in planned reader language;
reserve B/C and repair history for internal evidence. Daytime/Pre-sleep/Sleep
are recommendation windows and the outcome remains recommendation adherence.

## Accounting, return and report sequence

All 91 prior jobs total 708.9808902080695 seconds. The previous fixed debit
66.58152158500906 and all four new independent audit executions are charged
exactly once through audit_compute_accounting.csv in this central directory.
The exact current fixed debit, cumulative runtime and remaining allowance are
in accounting_summary.json. V11 must read the sealed debit without reset or
double counting previous budget-before fields. No diagnostic draws remain:
1,500 logical and 1,750 attempted are preserved, including the unsaved attempt.

Rehash the complete dispatch and owner-input pins immediately before starting.
Return one combined completion_v2/continued_final_package_011/ with all four
actual execution outcomes, independent implementation checks, exact manifests,
runtime/draw accounting, old-package preservation and a qualified handoff.
Do not mark BA-LB-G2-REVIEW closed or a failed diagnostic PASS.

The next action is coordinator verification of these finishing outputs, then
the internal Stage 2 report and the two reader reports. No report edit/render
is released in this scientific-finishing order. The coordinator owns this
serial boundary and will not stop at dispatch or mistake an idle worker for
completion of the user's request. Canonical Stage 3/4, historical 07_results,
website, navigation, manuscript/S5/Word, writer numerical propagation, shared
ledgers, commit, push and upload stay held.
