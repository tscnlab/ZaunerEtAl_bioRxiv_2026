# Brown chest prefit stop: independent acceptance

Date: 2026-09-12. Finding BA-018-CHEST-PREFIT-001 is confirmed, medium severity,
and confined to an implementation identity guard. It is not model convergence
failure or evidence against a scientific effect.

The owner package007 manifest 946038dbd06d694d27873f47bac8d742dd0cf27be55a2e2d8090ebd522b30865
is independently verified at 1,080/1,080 unique non-circular members. All 4,533
protected group rows, representing 2,287 unique paths, and current authority
pins reproduce exactly. The corrected chest construction retains all eight
expected payload identities and twelve support checks. The failed CHEST-ANY
job remains an exit-1 execution of 0.9314868329965975 seconds, reaped before
rank/input export, DLL loading, objective construction or optimization.

The source assertion compared `design_object$frame` with the unprepared
`input$frame`. The established `make_chest_design()` explicitly prepares the
frame using `ba_boundary_prepare_frame()`. That operation adds the all-yes
state field and fixes factor/contrast metadata. Exact identity is required
against that specified prepared representation, not against raw input.

Independent R 4.6.1 audit passed 40/40 checks against immutable input. It
executed the complete corrected prefit block, all five design-rank checks,
exact object data/parameter/random-component mapping up to MakeADFun, and
the full caller export path with synthetic eligible and structural-failure
bundles. Each fixture exported seven exactly verified members. Five negative
input fixtures were rejected in each scenario. These fixtures are not fitted
models or estimated results. No likelihood, optimization, prediction, random
draw or owner write ran. The exact historical two-window design and its
32-cell, eight-site grid reproduce; its all-yes matrix remains Pre-sleep-only.

The audit took 1.895528666995233 seconds and is charged once. Its script,
input inventory, execution journal, scope, session, checks and fixture exports
are retained in independent_evidence. The input counts, model specification,
optimizer and fit gates remain unchanged. No clinical, causal or substantive
claim follows from the synthetic fixtures.

The exact versioned driver contains four reversible replacements: the prepared
frame assertion, new registry directory, explicit preserved payload-manifest
path, and versioned self hash. The process-only V8 audit passed 42/42 checks,
including all 71 prior execution charges, exact failed-history identities,
single-slot recovery, no aliases, next-job ordering, failure and timeout
stops, exclusive lock, original 1,200-second budget, and remaining-budget
clamping. No real R child or signal was used in those supervisor fixtures.

Scientific limitations remain unchanged: Pre-sleep coverage sensitivity,
failed INFLUENCE-4 and DIAG-BINOMIAL, and the withheld exploratory day-level
claim. The remaining chest, calendar and temporal results are not yet accepted.

Disposition: accept package007 as a preserved technical stop; release only
the exact prospective correction and continuing finite scope in decision.md.
