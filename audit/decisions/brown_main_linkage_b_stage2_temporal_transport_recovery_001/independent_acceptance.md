# Independent temporal transport stop disposition

Verdict: the stopped analytical package is valid evidence. The temporal
input-classification defect is confirmed and the bounded recovery is ready
for release after the final identity/process seal. The Brown analysis and
reader reports are not yet complete.

BA-LB-TEMPORAL-FACTOR-GUARD-001 is a medium-severity, confirmed implementation
guard defect with no demonstrated scientific-data change. At the final
factor_levels_preserved assertion in code/30_temporal_fit_contract.R, only
the 80% behavioral_day_id factor differs: 794 declared versus 762 observed
levels. The two frozen constructors and the accepted 80% primary design all
use those same 762 observed levels. Values, counts, date classes, labels,
row permutations, real gaps, five full-rank matrices and starting parameters
are preserved. The original job remains exit 1, with an exact partial ANY input.

The complete prospective no-fit interface was independently replayed under
R 4.6.1, with writes only in temporary audit locations: 21 synthetic, three
exact likelihood, 44 actual sample-interface and 16 independent checks pass.
The final recovery reuses the old ANY RDS after exact object reconciliation,
constructs only the missing 80% RDS and writes fresh certificates. Changed
observed grouping values and unexpected retained levels are rejected.

Two successful complete prospective replays and one intermediate audit-only
failure are preserved. The failure followed a successful complete driver and
was caused by the audit's variable being shadowed by a driver variable.
The corrected independent audit uses its own path name and passes. No owner
source or output was changed by these audits and no real fit or draw ran.
All actual audit wall time, including that failure, is charged once.

The completed calendar sensitivity independently passes 33 checks from exact
saved input through every retained random variance component, the 54-cell
response-scale grid, fixed covariance, all three contrasts/intervals and
primary comparisons. All three directions and interval-exclusion statuses
are retained; the maximum shift is approximately 0.419 percentage points.
Its recorded random-boundary caution is retained. This is not permission
to refit or reinterpret the calendar comparator.

V10 process-only fixtures pass 52 checks without real R children or signals:
exact finite job/driver/argument allow-list, eight historical failures,
no repeat or alias, exclusive lock, failure/timeout stops, reaping,
unchanged draw ceilings and remaining-budget clamp. Five exact supervisor
substitutions reverse to V9. R source changes are restricted to the grouping
guard, stronger accepted-primary level check, explicit ANY reuse and fresh
recovery routing. Original code and all historical partial outputs remain.

Audit details and sessions are retained under independent_evidence. The
scientific definition, samples, likelihoods, formulas, structural ladder,
optimizer limits, primary models, multiplicity and genuine qualifications
are unchanged. Continue only via the sealed decision and exact copy map.
