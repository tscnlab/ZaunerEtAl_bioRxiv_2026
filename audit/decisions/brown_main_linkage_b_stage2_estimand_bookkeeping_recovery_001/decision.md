# BA-018 estimand bookkeeping recovery 001

Date: 2026-09-12. Continuing authority: BA-018 / CHG-157 and
BA018-AUTHOR-EXECUTION-CONFIRMATION-20260912.
Recovery ID: BA018-ESTIMAND-BOOKKEEPING-RECOVERY-001.
Status: stopped state independently accepted; the exact versioned recovery
below may be dispatched once to the existing Brown owner. No scientific
result, report, Figure S5 or manuscript replacement is accepted here.

## Verified stopped state

The only owner write root remains
`/Users/zauner/.codex/worktrees/82ab/ZaunerEtAl_bioRxiv_2026/audit/analyses/brown_adherence/main_linkage_b_amendment/stage2/`.

Preserve the complete stopped package
`completion_v2/continued_final_package_001/`. Its final manifest is
`b15ce21db75fe4eaab7a2f74019611bbe4048a4172626a6cc1fba563fce23067`,
133/133 exact, unique and non-circular. Its verification is
`1be8b830831f0daca427748e88bdf7d044daf1386bd7e120e2be42757a2887eb`
and handoff is
`23605325d066fed6f05dd4ed929cedecb8663c6c78827d5656174665e5fb7c56`.
All 13 stored finalization checks pass. Independent rehash also reproduces
all 4,533 protected-group rows, representing 2,287 unique files, and both
current primary-model manifests. No author file changed in this review.

Both primary fits completed once and retain their numerical disposition
acceptable_with_cautions, not author acceptance. Their exact RDS identities are:

- ANY: `494b4647e7394b8efcaaf7cd7ae2922ba0669ac609529328d0a5e67cab60c245`.
- 80: `72cc742f3d60941a5a4fc44361f09a671fea00a4183b85ab932d1d7292a02a74`.

The failed driver remains
`code/06_derive_primary_estimands.R`,
`dc88dfca95189435dcf456069d4d69857ecba5c5d2b35abba188c1fca7fc1ea0`.
The exact expression `family[family$sample_id == sample, ]` resolves the
character `family` column inside the data.table mask, not the outer table.
This is a confirmed new-driver bookkeeping defect. Both per-sample derivations
had returned only in memory; no estimand or multiplicity output was written.
Those lost values are not accepted findings. Real downstream scientific gates
have not yet been evaluated.

The six recorded jobs consume exactly 68.001416874991264 seconds, including
31.04514845801168 seconds from the failed derivation. All children were reaped,
there was no timeout, and there is no computational lock. The two model slots
remain consumed. No budget, fit slot, batch or render allowance is reset.

## Complete prospective review

Independent R 4.6.1 testing reproduces the original masking error before any
output, then executes the complete corrected downstream driver with invented
synthetic fixtures. All 29 checks pass: both 54-cell grids, every M1 through M5
family, M6 construction and reconciliations, coverage, compact-table export,
named-input export, the complete 23-member versioned output manifest, exact
reverse, ten missing-family-member cases, both nonfinite-p cases, and repeat
write rejection. A deliberately failed scientific gate still writes its
evidence and stops. This synthetic PASS is not a scientific result or a waiver
of the same gates on the actual model outputs.

The ten mathematical functions in `estimand_interface.R` remain byte-identical
at `1a5a18f55231c6176d84dffdf62bab9e2479d97eb711180179d5356c1ff64a99`.
No actual model, compiled binary, objective, derivative, prediction, optimizer,
research data or report was evaluated by the independent replay. The existing
data.table shallow-copy warnings are reproduced and retained in audit evidence.
Do not suppress them in the real job or use their classification to waive an
actual numerical, covariance, coverage or output check.

A further dispatch prerequisite was found in the unchanged process supervisor:
it refuses all later jobs if any previous finish record is nonzero. Therefore
the original supervisor cannot execute a centrally approved recovery. An exact
versioned supervisor recognizes only this one sealed failed job and continues
to count its time. Nineteen infrastructure-only tests pass with every child
process mocked: exact recovery and next-job accounting, duplicate rejection,
three historical-identity tamper cases, wrong driver, missing failed history,
an additional failure, a premature future job and failure of the new recovery.
No R process or scientific function was started by those tests.

## Exact additive code boundary

Do not edit or rerun either failed original. Copy only the supplied files from
this release's `prospective_code/` to these previously absent owner paths:

1. `code/06_derive_primary_estimands_v2.R`, SHA-256
   `729ce699c57d9ab91cc16668c074742cc4929035102a079866390291964037ba`,
   14,641 bytes. It already parses and passes Air. Only two parsed assignments
   differ: the family-count check converts its local table to a base data.frame
   and selects with `[["sample_id"]]` and `drop = FALSE`; the final artifact list
   names the executed v2 driver. The exact three literal reverse substitutions
   recover the failed original. All mathematical and actual gate expressions
   remain unchanged.
2. `code/run_bounded_job_v2.py`, SHA-256
   `1d108833c89ac2815b01075db8d43a1f527c4fabdd09968c4f89a77efe4f1698`,
   6,103 bytes. Exact reverse recovers the original supervisor
   `8e24c1f43f9586f6f4d491ffda7fceed0875184fd86504de7e12671eb3b5ff3b`.
   Its only added authority is the sealed failed DERIVE job, identified by
   exact start, finish and log hashes, original driver/supervisor, exit 1,
   no timeout, reaped child and exact elapsed time. The v2 recovery driver hash
   is fixed. Future unconsumed plan jobs require this recovery's recorded PASS.
   Every other failed or unfinished job still stops execution.

Do not adapt or reformat either postimage. The original interface, runtime
contract, model bundles, frame/design, estimator DLL, histories, six original
job directories and all earlier stopped manifests remain immutable. No old
manifest row is rewritten or waived as a self-transition.

## One retry, then only original unconsumed work

1. Freshly rehash the release, stopped 133 members and complete protected groups,
   both model manifests, all six job records and exact code. Require both new
   owner code paths, new job directory and new final-package root absent.
   Verify no Brown job or lock is active. The four separately classified old
   R metadata probes do not authorize terminating any process or waiving a new
   competing scientific process.
2. Copy the exact code pair. Record its hash/size, syntax/Air status and the
   sealed inverse proof in new evidence under
   `completion_v2/estimand_bookkeeping_recovery_001/`. Do not rerun the old
   frame preparation, derivative gate, models or synthetic replay as production.
3. Using the accepted Python infrastructure runtime, invoke the versioned
   supervisor once with job ID `DERIVE-PRIMARY-ESTIMANDS-V2`, limit `120`, and
   driver `code/06_derive_primary_estimands_v2.R`. Use the same R 4.6.1 library,
   `RENV_CONFIG_AUTOLOADER_ENABLED=FALSE`, both exact Brown/author root variables
   and all four single-thread variables set to 1. Keep the 1,200-second total
   accumulator starting with all 68.001416874991264 consumed seconds. The
   supervisor's only scientific child remains the specified Rscript command.
4. This one retry may recalculate the already authorized post-fit estimands
   from the two preserved models and existing estimator binary, because the
   failed process lost its transient results. No fit, refit, recompile,
   derivative regeneration, model selection, frame reconstruction, alternative
   covariance, sample change or inference-definition change is released.
5. On real complete PASS only, continue the remaining originally authorized
   finite BA-018 plan through the versioned supervisor and original conditions.
   No consumed job is rerun. All diagnostic, family, coverage, sensitivity,
   conditional-ladder and runtime/fit ceilings remain binding. A genuinely new
   failure stops and seals once; there is no additional retry authority.

Actual estimand/multiplicity outputs retain the original Stage 2 paths. Write
the eventual complete or stopped package to the new
`completion_v2/continued_final_package_002/`, not over any existing gate,
handoff, final manifest or verification. This is only a final-evidence path
substitution. Preserve the complete stopped 001 package and prior histories.

## Mandatory next stop and parallel holds

The scientific author stop remains `BA-LB-G2-REVIEW`. There is no automatic
acceptance, Stage 3/4 revision, Figure S5 replacement, Writer numerical update,
manuscript, selection-page or website change. The original conditional one
internal Stage 2 report still requires the safe serial slot before execution;
this order creates no additional render or browser authority. Browser-policy
denials remain binding, with no alternate-surface workaround.

The Harmonizer and Writer continue their already dispatched read-only final
document preflights. They must not incorporate unseen Brown results. No central
ledger append, package/lock/profile change, upload, commit or push is released.
Only an actual accepted dispatch to Brown owner
`019fffdf-66d4-7802-9091-09283ad27b7f` changes this order from sealed to delivered.
