# Independent final preparation-report visual review

Reviewed 2026-09-13 Europe/Berlin, 2026-09-12 UTC. This record supplements
the exhaustive independent R checks and the owner's native served-page review.
It is not a new browser run, a mobile screenshot claim, or scientific approval.

The reviewer directly inspected these retained FINISHING006 captures under
the isolated Brown analysis root, in
main_linkage_b_amendment/stage4/reporting/finishing_006/visual_qa/:

- diagram_top.png, diagram_middle.png and diagram_end.png;
- formula_output.png;
- diagnostic_qualifications.png and grouping_and_chest_limits.png;
- site_localizations_row_27.png;
- final_section.png.

The complete diagram and its branches are readable. The displayed formula
outputs, qualifications, terminal table rows, notes, links and closing limits
are present without identified clipping, overlap or missing content in these
views. No additional report defect was identified. Numerical reconciliation
comes from the separate R audits, not visual estimation.

The owner's native_completion.json records 52 captures, a full-page review
and a 1280x720 viewport. Its HTTP verification records exact status, bytes and
SHA-256 for all 47 finite resources. Mobile coverage remains deterministic
responsive-structure checking only. No mobile viewport emulation or screenshot
is claimed. The actual result and internal-report link interactions and the
source-download activation are retained separately.

The first lifecycle checker incorrectly treated the browser viewport size as
the returned raster dimensions and read the .png evidence suffix as its binary
format. The immutable provider captures actually contain 1265x712 JPEG rasters.
The corrected evidence-only checker verifies all 52 with image metadata and
keeps the DOM's 1280x720 viewport as a separate fact. All 29 lifecycle gates and
eight deterministic responsive rules pass. The initial failed assertion and
its exact reverse proof are preserved. No image was resized, rewritten,
recaptured or promoted to a scientific/report asset by this correction.

The owner accurately records that its synchronous preview server did not exit
on SIGTERM and was forcibly stopped at its exact owned PID 26749. This is a
teardown event, not a normal graceful exit or a report defect. The reviewer
independently executed read-only /bin/ps -p 26749 and lsof for TCP listener
64642 on 2026-09-12 before 22:44:39 UTC. After the sandbox denied the first
process query, the same narrowly elevated read-only checks returned no
matching process or listener. No unrelated process was stopped by this review.
The owner's retained browser inventory shows its QA tab closed and user tab 1
preserved. No further browser, render or source mutation is required by this
review. Final technical acceptance still requires the independent post-QA
preservation and non-circular package rehash.
