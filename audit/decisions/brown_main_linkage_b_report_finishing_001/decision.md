# BA-018-REPORT-FINISHING-001

Date: 2026-09-12. Parent BA-018 / CHG-157. Status: RELEASED after exact dispatch preflight.
Owner: Brown task 019fffdf-66d4-7802-9091-09283ad27b7f in the isolated 82ab root.
The latest direct author request is to resolve remaining issues until Brown analysis
and both reader-facing reports are complete and error-free. This releases the
stored-output reporting implementation below, without another routine author
permission request. It is not acceptance of an unseen report or new science.

## Outcome and scientific boundary

Complete the internal Stage 2 implementation/reconciliation report, then the
reader-facing Stage 3 results and Stage 4 preparation/provenance pair. Technical
completion means current values, traceable sources, correct interpretation,
valid semantic HTML and verified presentation, not unconditional scientific
success or unseen author approval. Keep a truthful final author-review state.

All 95 completed supervised histories, all earlier failed routes,
frozen current model/estimand/diagnostic/sensitivity/R2/reuse files, raw inputs,
libraries, packages and renv.lock are immutable. No fitting, predict method,
optimization, TMB objective, contrast, CI/p-value/FDR derivation, RNG, simulation,
model-frame rebuilding or sensitivity execution in any report or display code.
The 95-job history and eight preserved nonzero attempts remain exact.

Use all main values from the current joint primary model pair. There is no
selective substitution limited to the Pre-sleep rows. Preserve exact exploratory
reuse and all distinct main versus exploratory limitations. The four main
figures must use current frozen paired outputs; the raincloud remains exact.

## Exact write boundary

Owner root is the isolated 82ab worktree. Under
audit/analyses/brown_adherence/main_linkage_b_amendment/ permit absent files in:

- stage2/reporting/ and stage2/implementation_and_reconciliation.qmd/.html;
- stage3/code/, stage3/source_data/, stage3/figures/, stage3/evidence/;
- stage4/code/, stage4/source_data/, stage4/evidence/.

The only existing author endpoints permitted to transition are the canonical
13_cross_state_association_results_amendment.qmd/.html and
14_cross_state_association_preparation_and_provenance.qmd/.html. Preserve exact
four-file copies and identities before editing. Existing historical source,
display, HTML, test, handoff and final manifests are not rewritten. Their live
canonical endpoints transition through exact historical copies in a new ledger.
07_results and the prior main and exploratory scientific/display trees stay exact.

Historical preservation is checked by identity, not by forcing live endpoints
to retain their old bytes after authorized replacement. Record one exact four-
endpoint preimage-to-new-history-copy map and use it only for the corresponding
rows in every older protected/final/dispatch manifest. Each other path remains
live-exact. Do not repin an old manifest, exempt an entire directory, or mistake
the new verifier's own transition for scientific drift. New final manifests
exclude themselves and their subsequently written verification files.

Keep the existing nearest Brown _quarto.yml, stage1.css and report.css unchanged.
If a demonstrated layout defect requires CSS, use one amendment-owned CSS leaf
and explicitly inventory it, without changing a shared stylesheet. No website,
navigation, manuscript, S5, Word, ZIP, writer numerical propagation, package,
lockfile, commit, push or upload.

## Source preparation and complete preflight

Prepare the complete three-document dependency chain before the first render.
Use pinned aggregate CSVs only for report execution. Do not load model RDS or
restricted participant tables into report cells. Minimal aggregate display
formatting/transposition is allowed, with R verification against its parent.
Each substantive cell presents a defined result and concise interpretation.

Preserve existing meaningful reader anchors and all main/exploratory endpoint
IDs. New sensitivity or provenance endpoints are permitted when required to
report the completed contract. Capture the exact resulting endpoint inventory,
chunk and inline-expression inventory, captions, alt text, relative link map,
source hashes, and full title/heading order before execution. Use that actual
QMD-position inventory for order, never discovery-table row order or retired
fixed counts. Exactly one document main node must be tested with html_elements.

Require a machine-readable claim/source map for each main table, figure,
inline quantitative statement, sample, diagnostic disposition and limitation.
Provide the source path/hash, model/sample/key, units and report location.
Bind any textual counts of supported site effects to the complete new source
families. Do not use stale hard-coded counts or names from an old caption.

Every report setup manifest is a leaf-only source/display manifest with no
current QMD, HTML, verifier or later execution output as a self-dependency.
Historical self-pins remain historical and byte-identical. New evidence comes
after source inventory or is excluded by its exact declared evidence subtree.
Derive the finite member set explicitly rather than guessing discovery counts.

Before rendering, parse every R chunk and inline expression; inspect called
sources recursively; confirm no prohibited scientific/side-effect operation;
check all local links and source-data targets; exercise every formatting helper
on frozen aggregate inputs; compare full table data/labels to those sources;
and test all later semantic/link/privacy/protected and QA selectors against
temporary or existing HTML where possible. Do not replay a report builder that
writes a frozen output. A stale legacy test must not drive an unrelated source
change; write an exact current verifier with historical evidence preserved.

## Required final scientific and reader content

Follow reader_dependency_review_001.md RPT-01 through RPT-18. In particular:

- Describe the final preceding Sleep, Daytime, following three-hour Pre-sleep
  windows with wake-date Work/Free ownership, actual timestamps and independent
  validity. Reader prose explains the final method without linkage-B/C labels
  or repair history. Expected interval duration governs coverage; valid minutes
  govern model denominators. Use recommendation adherence and Brown et al.
  recommendation windows, not contexts for these three categories.
- Preserve the primary 140-participant versus separate 153-participant chest
  distinction and the exploratory pairwise eligibility subset. Near-eye,
  complementary chest and bedside Sleep measurement are genuine sensor context.
  Bedside Sleep is neither eye-level exposure nor a whole-bedroom measure.
- Display exact main ANY and 80% samples, all six adherence levels, the compact
  table, full global/state/site comparisons, endpoint versus overall calibration,
  coverage gate, random SD and fixed/random/observation R2 shares, global and
  within-window Shapley with clear denominators and point-only uncertainty scope.
  The participant R2 increment is conditional minus marginal R2, expressed as
  percentage points of explained response variance, not an increase in adherence.
  Relative Shapley percentages use fixed-effect variance as their denominator,
  not total variance or percentage adherence. The logit-scale SD is not a
  response-scale percentage. Distinguish these units in labels and captions.
- Main Pre-sleep coverage interval crosses zero; direction persists. Do not
  call it coverage-robust, evidence of no effect, or a significant difference
  between the two estimates. Retain the false gate and validation limitation.
- Keep M1-M5 BH families within each sample, the primary-only M6 BH family and
  identical unadjusted 80% M6 claim gate, and the separate four-member exploratory
  family. Explain them in reader language. CI exclusion is distinct from FDR.
- Work-day site forest plots levels while marking site-minus-average tests.
  Free-minus-Work forest plots effects with distinct against-zero and
  departure-from-equal-site-reference encodings. The indexed site remains in
  the equal-site average. All 27 rows and nonsignificant rows remain present.
- Report every completed sensitivity, including failed INFLUENCE-4,
  DIAG-BINOMIAL and both temporal endpoint routes as status-only. Temporal
  BB-R0 results have near-boundary, family/random-structure and endpoint-fit
  limitations, with the any-valid residual trigger retained. No-trigger is not
  proof of independence. Calendar and chest qualifications remain separate.
- Exploratory day-level claims stay withheld; participant associations are
  limited, exploratory and non-causal. No stable-trait wording, ranking or hard
  tertiles. Exact 139-profile/417-point raincloud is descriptive, not a time
  trajectory. Do not re-jitter, regenerate density, re-map profiles or change its
  source/PNG/SVG bytes. Explain the exact frozen eligible pair subset.

The internal Stage 2 report retains implementation history, model/route IDs,
gate/recovery/failure accounting and reproducible provenance. The Stage 4 reader
companion explains final methods, samples, weighting, families and output paths
without exposing participant identifiers or substituting a file list for method.

## Candidate-first four main displays

Build in one new temporary candidate directory using only current frozen
aggregate source CSVs. Candidate iterations may repair layout/typography only;
record each version, reject rather than silently overwrite evidence, and perform
the complete numerical/marker/source check after each iteration. Do not change
any scientific value, legend meaning, sample, family, source row or threshold.

Main figures: adherence levels; 27 Work-day site levels with equal-site reference;
27 Free-minus-Work effects with two distinct test markers; six ANY/80% effects
with visible x guides and zero line. Preserve nine country-coded sites, state
panels, non-colour cues, informative captions and alt text. Essential text must
reach 7 pt at 170 mm and be legible at 708 CSS pixels. Check every label,
confidence interval, marker, reference line and internal note for clipping.

After complete candidate PASS, promote only the declared four PNG/SVG pairs
to absent amendment-owned paths. Source manifests pin those candidates exactly.
The frozen raincloud is referenced as-is, not promoted over its original path.

## Narrow serial render and semantic gate

Installed Quarto is 1.9.37. The nearest Brown _quarto.yml is project type default,
execute-dir project, not the outer website. Retain knitr, embed-resources and
freeze false. No profile, full-project command or --no-execute workaround.

Use the accepted R 4.6.1 library with RENV_CONFIG_AUTOLOADER_ENABLED=FALSE,
BROWN_ADHERENCE_PROJECT_ROOT and BROWN_ADHERENCE_AUTHOR_ROOT, single-thread
environment and an unused temporary session directory. On macOS installed
Quarto routes Sass to the existing user-owned Library/Caches/quarto/sass/sass.kv;
XDG_CACHE_HOME does not redirect it. Do not change HOME, caches, binary or library.
Use established narrowly elevated access to the existing cache for normal
Quarto transactions, verified before the sole target command. Do not wait in
a known profile/cache loop. Record the process tree and bounded timeout/reaping.

Serial order, with exactly one initial render per separately released target:
stage2/implementation_and_reconciliation.qmd; canonical Stage 3; canonical
Stage 4. After each raw render exits zero, retain its exact copy and logs. Run
the unchanged accepted gt semantic engine on a candidate only. Require exact
composed reverse and reapply, unchanged visible DOM/content/order outside
id/headers, unique IDs and table-local intended-header resolution before one
canonical candidate promotion. The next target remains held until coordinator
independent acceptance of the current one.

No source patch or second render is implicit in a render allowance. On an
unexpected defect, preserve the current state, diagnose the complete remaining
path and send one consolidated finding. The coordinator will resolve ordinary
reporting/harness defects within the author's standing completion request, not
ask the author to re-approve routine repairs. Science stays frozen.

## Source, HTML, privacy and permitted visual QA

Verify every actual native gt endpoint and image, data/label agreement, distinct
table bodies where expected, captions, alt text, references, units, absence of
stale numeric claims, all preserved limitations, required reciprocal/source
links, no worktree-absolute/file URLs or unpublished participant data, no broken
resources, unresolved cross-references or embedded execution errors. Internal
technical code may carry relative source names but not identifying source data.

Build the narrowest temporary serve tree from final self-contained HTML plus
only its exact linked privacy-safe aggregate CSVs and reader HTMLs needed for
checks. No project root, RDS, raw participants, secrets or symlinks. Bind one
read-only GET/HEAD server only to 127.0.0.1. Record symlink preflight, root, PID,
port, target URL and lifecycle. Use no Quarto preview, tunnel or LAN binding.

Keep the approved browser surface. A prior data-URL iframe/viewport workaround
was expressly blocked; do not evade it with CDP, another browser or Computer Use.
Use the genuinely available native 1280x720 served-page inspection and the
previously accepted deterministic 390-pixel responsive equivalent, clearly
labelled. Never claim a mobile screenshot or emulated viewport that was not
actually captured. Stop and request exact author screenshots if the permitted
loopback route itself is denied. Do not substitute DOM checks for all visual QA.

Inspect complete figures at native and intended final size; every table and
horizontal scroller; disclosure open/closed states; Mermaid if present; headings,
captions, notes, axes, legends, wrapping, overlap, overflow and page-console
state. Preserve screenshots and an endpoint-by-endpoint observation table.
Close owned QA tab, stop only owned server, prove no listener, and rehash source,
config, HTML and protected inventories. Restore or safely retain any temporary
inspection copies only under their exact controlled scope.

## Final package

Create source/display/HTML/semantic/link/privacy/visual/runtime/protected checks,
exact historical transition map, final route inventory, source-to-claim map,
single-render records and non-circular completion manifests. Record genuine
scientific limitations separately from unresolved software defects. Final handoff
must identify both current reader HTML/QMD paths and hashes and their common
scientific authority, with all results verified and no unresolved in-scope
reporting error. Do not notify the writer or propagate values to shared pages
without the separately recorded downstream authority.

## Exact controlling acceptance and current release count

The independently rehashed package011 final manifest is
`c1ce90bf88ff3597d6a75419ee331f091d0a867735117e8785973e1f172af696`,
1,705 exact, unique and non-circular members. All 28 owner finalization checks
pass. Independent R4.6.1 verification passed 157 point-only R2/Shapley checks
and 166 exploratory-reuse/source-copy checks. See independent_acceptance.md,
input_pins.csv and dispatch_manifest.csv in this central directory for exact
paths, identities and audit evidence. Preserve all 145 complete source exports.

This order releases complete stored-output source/display preparation for all
three documents, candidate-first promotion to the new amendment paths, and
exactly ONE initial internal Stage 2 target render after complete preflight:

`quarto render audit/analyses/brown_adherence/main_linkage_b_amendment/stage2/implementation_and_reconciliation.qmd --to html`

Run from the 82ab project root, using the nearest unchanged Brown default
project configuration and the environment specified above. Bound the command
at 180 seconds with process reaping; any startup failure is preserved and
returned rather than rerun. A successful render does not release another target.
Stage 3 and Stage 4 may be prepared but remain unrendered until their individual
coordinator releases. Do not start the internal render if a competing render or
visual lease appears. Recheck before execution. Current boundary has no Brown,
Quarto, Pandoc, semantic or loopback process; writer is idle and Harmonizer and
navigation tasks are inactive. Existing native Word visual lease is closed.

Before source replacement, create exact four-endpoint preimage copies under
stage3/evidence/report_finishing_001/history/ and a non-circular map linking each
canonical path to its expected preimage and copy. Use this map only for those
four paths when auditing prior manifests. New independent or owner final
manifests pin the new current sources and the exact old copies, never weaken
scientific or unrelated historical rows.

Return a complete source/display acceptance package and the internal Stage 2
render/semantic/visual package. The coordinator will verify them and release
only Stage 3 next. Final technical gate is BA-LB-REPORTS-REVIEW after both reader
reports are independently verified. Scientific qualifications remain explicit;
a final author review is not falsely marked as already supplied.

