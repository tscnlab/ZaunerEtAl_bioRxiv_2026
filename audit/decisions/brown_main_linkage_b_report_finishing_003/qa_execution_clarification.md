# Existing QA boundary clarification

The exhaustive link contract permits one bounded read-only R GET/HEAD batch
against the existing 127.0.0.1 server and its already privacy-audited finite
allowlist, recording status and exact bytes per URL. It does not require a manual
browser click on every CSV. Native browser inspection of every visual endpoint,
real peer navigation and representative download interaction remain required.
All prior completed manual checks remain evidence. No new served path, browser
surface, viewport simulation, blocked-content workaround, source edit, render,
or scientific computation is authorized. Any actual tool denial remains binding.
This clarification was delivered during the active FINISHING-003 turn and is not
a new stop gate or a change to the immutable dispatch identities.
