# BA-018 single diagnostic-export recovery

Date: 2026-09-12. Identity: `BA-018-DIAGNOSTIC-EXPORT-RECOVERY-001`.
Parent authority: BA-018 / CHG-157 and
`BA-018-M1-QUALIFIED-CONTINUATION-001`.
Status: AUTHOR-APPROVED; EXECUTION RELEASE ONLY AFTER EXACT DISPATCH PREFLIGHT.

Owner: Brown recommendation adherence strategy,
`019fffdf-66d4-7802-9091-09283ad27b7f`.
Owner root:
`/Users/zauner/.codex/worktrees/82ab/ZaunerEtAl_bioRxiv_2026`.
All owner-relative paths below start at
`audit/analyses/brown_adherence/main_linkage_b_amendment/stage2/`.

## Purpose and approval

The direct author response `authorized`, recorded in `author_approval.md`,
releases one same-seed repeat of the diagnostic computation whose return was
lost before export. It does not release a model refit, changed estimator,
additional inferential family, changed primary result or more favorable
scientific classification. The already approved remaining diagnostics and
finite sensitivities may continue after this recovery only within the parent
qualified-continuation decision and its unchanged scientific prerequisites.

The exact stop is `BA-LB-DIAGNOSTIC-EXPORT-001`. The driver requested `status`
instead of the stored `fit_status` field after diagnostics had returned.
The original driver, all eight job histories, all three nonzero exits and the
failed package remain immutable. They are not marked successful retrospectively.

Independent stop disposition:
`audit/decisions/brown_main_linkage_b_stage2_diagnostic_status_export_stop_001/disposition.md`,
SHA-256 `c665fd5aafc13f0a1ae92397075eefc37436b132e5e93cf69b3241a91d86f81d`.
Its 271-member manifest is
`2eb0131608191f045b22f1f6abd53df666c7b51d26705eba6a7b7bd0edd81ff6`.
Owner package `completion_v2/continued_final_package_003/final_manifest.csv`
is `ef06de7af67271cfdd2b6ab4958aede173e5707d1a5d6c8c20f23f63db4877fe`,
223 exact members. The new final return must use
`completion_v2/continued_final_package_004/` instead.

## Exact bounded implementation

Copy the five exact files identified by
`independent_preflight/exact_owner_copy_map.csv` to their previously absent
owner destinations, once. Rehash before and after each copy. Do not edit the
accepted candidates, old drivers, old source manifests, seed registry or job
registry. No scientific function implementation changes.

1. New `code/07_run_primary_diagnostics_v2.R`:
   `ed06bbdc1257efbbd543dfd2cedd321d2796c2c1d0baad043e279511deeb5428`,
   7,977 bytes. Six exact changes reverse to the immutable 6,688-byte driver
   `20c8a54701b7cd1772ce78aa5b18588dfa9459d08a13dd879ac4716ba58eb5f3`.
   The changes are a unique recovery job ID, new registry directory, versioned
   self-pin, reference to the unchanged historical seed registry, a frozen
   fit-status schema check followed by an immediate raw checkpoint, and the
   corrected technical-status field.
2. New `code/run_bounded_job_v4.py`:
   `c6be41215a27a3ed76f75367336915bbba25537fafa2a4a962de9fa038f994ae`,
   13,469 bytes. Seven exact infrastructure blocks reverse to v3
   `f4cfa0953a9dabb8a4c76d3847107507657b8e1ec51a35edbbfbd6f67be3e1d9`.
   V4 recognizes only the exact additional diagnostic failure, requires its
   preserved start/finish/log identities, charges its elapsed time, and permits
   only the unique recovery job with the exact driver and `PRIMARY-ANY`
   argument. No alias or second recovery is allowed. Later jobs remain blocked
   until the recovery completes successfully. Any new failed execution still
   stops subsequent jobs. Python supervises processes only; scientific work is R.
3. New `preflight/diagnostic_export_recovery_001/job_registry_0001.csv`:
   exactly two rows, the recovery and the unconsumed primary-80 diagnostic job.
   The models, seeds, purpose, sample slots and 120-second caps remain unchanged.
   The new rows add exact fit-gate pins and checkpoint provenance. The old
   two-row registry remains unchanged as historical evidence.
4. New `preflight/diagnostic_export_recovery_001/primary_diagnostic_source_manifest.csv`:
   14 exact rows. It retains all nine historical source/registry identities and
   appends only the new driver, supervisor, two-row registry and two frozen
   fit-gate CSVs. It excludes itself. It is not an update of the historical
   manifest and is not a scientific artifact regeneration.
5. New `preflight/diagnostic_export_recovery_001/diagnostic_draw_recovery_allowance.csv`:
   six named slots with the retained 250 lost attempted draws for B_any and
   the one explicitly authorized repeat. The old seed/draw records are immutable.

The versioned driver checks the exact fit-gate file hash, one-row schema,
model/sample identity and nonempty `fit_status` before any prediction or
residual work. Immediately after `derive_diagnostics()` returns, it saves the
unmodified returned object once to
`diagnostics/<sample_id>/raw_diagnostic_checkpoint.rds`, before later checks
or metadata/table assembly. That checkpoint is controlled internal evidence,
not reader data. It contains the original return, including draws and bundle.
The existing processed exports remain separate. Each completed primary
diagnostic manifest has 15 members, the prior 14 plus the raw checkpoint,
with no self-row or duplicate. An existing output path blocks overwriting.

The original nine diagnostic functions, conditional predictions, residuals,
draw algorithm, calibration, temporal checks, participant screening and
scientific assessments are unchanged. The actual diagnostic outcome has not
yet been observed or accepted. A construction-check PASS must not conceal a
failed endpoint/overall/temporal assessment. Any newly required scientific
stop follows the frozen plan and is returned honestly.

## Draw and runtime accounting

The lost attempt's 250 draws and 1.0988522499974351 seconds remain charged.
All eight historical jobs total 100.50703887498821 seconds. The same 1,200-second
cumulative scientific ceiling continues, without resetting elapsed time.
Ordinary jobs remain at most 120 seconds, temporal jobs at most 180 seconds,
both limited further by remaining cumulative time. All computational
verification, derivation and diagnostic work remains inside that budget.

Exactly one repeat is authorized at predictive seed 20260814 and residual
seed 20260815. These are repeated execution of the same logical diagnostics,
not independent evidential replicates. The ceilings are now 1,750 attempted
diagnostic draws, including the lost 250, and at most 1,500 logical final draws
over the original six slots. Every slot retains at most one final 250-draw
diagnostic set. This is not permission to run otherwise untriggered chest,
benchmark or temporal diagnostics, omit a required diagnostic, reuse a slot
for a different purpose, or add bootstrap/resampling inference.

Before each later draw job, write a new versioned accounting record in the
new recovery directory that reconciles all execution jobs to the original
six-slot seed map and this allowance. Count each completed or conservatively
charged failed attempt, not only saved outputs. Require attempted totals no
greater than 1,750, logical completed totals no greater than 1,500, no repeated
slot except this B_any recovery, and exact seeds. Do not rewrite the old ledger.

V4 records the exact budget/allowance in job metadata. The owner must still
enforce the frozen scientific trigger and draw accounting before every job;
an allowed technical job prefix is not authority for an unplanned computation.
After a new nonzero exit or timeout, stop and retain all evidence. There is no
second retry allowance, checkpoint recomputation, or silent rerun here.

## Frozen scientific authority and preflight

Retain primary-any model
`494b4647e7394b8efcaaf7cd7ae2922ba0669ac609529328d0a5e67cab60c245`
and primary-80 model
`72cc742f3d60941a5a4fc44361f09a671fea00a4183b85ab932d1d7292a02a74`.
Retain all 23 estimand/family exports under manifest
`baff7f3cb89565235193a2a74dc64afd82a58e3fea3d519c55fce08318d4863a`.
`M1_80_claim_gate` stays FALSE; scientific validation stays 12/13. The
Pre-sleep coverage-sensitive qualification remains approved and unchanged.
No repeated permission for that same qualification is needed.

Before any owner copy or job, rehash this complete dispatch manifest and its
exact copy map, owner 223-member package, independent 271-member stop,
254-member prior dispatch, 23 primary exports, 89 Stage 1 members and 2,287
distinct protected paths. Confirm eight complete jobs and three preserved
failures, no computational lock, no diagnostic outputs, and all five copy
destinations/new job/final-package paths absent. Check no competing Brown
scientific process; unrelated metadata R processes must not be terminated.
Verify the accepted package versions and loaded paths under the same R 4.6.1
library. There is no package, lockfile, profile, library or cache change.

Central validation reproduced all of those identities and 20/20 owner
finalization checks. Full prospective driver execution with fixed synthetic
fixtures passed 32/32, including both primary sample branches, all later
exports, missing/duplicate/empty metadata, bad identities/seeds, retained
checkpoint after later errors, M1 FALSE and separate synthetic endpoint failure.
Mocked supervisor tests passed 44/44, including exact reverse, tampered histories,
duplicate/aliased jobs, wrong driver/arguments, failure, timeout, lock and budget.
No real model RDS, fit, prediction, residual or diagnostic draw was evaluated
by these tests. Air and R parse pass for the exact driver. Infrastructure syntax
passes. They prove implementation behavior, not scientific diagnostic success.

## Sole recovery command and subsequent scope

Use the existing bundled Python runtime to invoke owner
`code/run_bounded_job_v4.py`, job
`DIAGNOSTICS-PRIMARY-ANY-RECOVERY-001`, requested wall limit `120`, exact owner
`code/07_run_primary_diagnostics_v2.R`, and sole driver argument `PRIMARY-ANY`.
Run it exactly once. It launches `/usr/local/bin/Rscript --vanilla`.
The separately registered, still-unconsumed primary-80 job uses the same
driver, job `DIAGNOSTICS-PRIMARY-80` and sole argument `PRIMARY-80`, only after
the recovery and all applicable scientific gates permit continuation.

Retain `RENV_CONFIG_AUTOLOADER_ENABLED=FALSE`, the existing project R 4.6.1
library in `R_LIBS`, exact Brown/author roots and all four BLAS/OMP thread
limits at 1. No alternate execution environment or profile bypass is added.
`dispatch_message.md` supplies the exact paths and the parent authority.

After successful recovery, the prior qualified-continuation decision remains
controlling for all remaining already registered diagnostics, sensitivities,
finite model/deletion/temporal slots and required assessment. This new order
does not expand those routes or permit primary reselection. New task-owned
unconsumed drivers/registries/output paths must be sealed before their one
execution as that prior decision requires. Keep all existing files immutable.

Return one combined new completion or stop under
`completion_v2/continued_final_package_004/`, including exact provenance,
raw checkpoint and processed output manifests, all assessments/qualifications,
attempted versus exported draw accounting, actual cumulative runtime,
ineligible/failed branches, no competing child and protected-identity checks.
The mandatory review remains `BA-LB-G2-REVIEW`.

R2/Shapley, internal report/QMD execution, Stage 3/4, reader figures/source data,
S5, manuscript/Word/ZIP/website integration and Writer numerical notification
remain held. No Quarto, browser, office, full-project render, commit, push,
upload, deletion, new task or alternate scientific delegation is authorized.
