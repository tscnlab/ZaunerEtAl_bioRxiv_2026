# BA-018 qualified diagnostic and sensitivity continuation

Date: 2026-09-12. Decision identity:
`BA-018-M1-QUALIFIED-CONTINUATION-001`, within BA-018 / CHG-157.
Status: AUTHOR-APPROVED, BOUNDED EXECUTION RELEASE AFTER DISPATCH PREFLIGHT.
This is an additive continuation, not a rewrite of the original plan, a passed
coverage gate, or final scientific acceptance.

Owner: Brown recommendation adherence strategy,
`019fffdf-66d4-7802-9091-09283ad27b7f`.
Owner project root:
`/Users/zauner/.codex/worktrees/82ab/ZaunerEtAl_bioRxiv_2026`.
Only the existing task continues. Do not create or delegate a new scientific task.

## Author decision and scientific qualification

The author's direct "Yes, approved" accepts continuation of the already planned
diagnostics and sensitivities while preserving the failed Pre-sleep coverage
gate and qualifying its claim. `author_approval.md` records the exact question
and response. This releases only the scope below.

Preserve `BA-LB-M1-COVERAGE-001` as a genuine robustness limitation.
`M1_80_claim_gate` stays FALSE and `estimands/validation.csv` stays 12/13.
Pre-sleep's primary equal-site Free-minus-Work estimate has a positive interval;
the at-least-80% interval includes zero while the point-estimate direction is
retained. Report this as coverage-sensitive, not robust under the prescribed
gate, not evidence of no association and not a test of a difference between
the two estimates. The failed gate cannot trigger a more appealing model,
cutoff, confidence level or multiplicity definition.

The independently sealed disposition remains unchanged:
`audit/decisions/brown_main_linkage_b_stage2_m1_coverage_gate_stop_001/disposition.md`
SHA-256 `de8f2b17b965a3d229cac5f108ad482afa59d7c545750ed01e5c121314a82d95`;
its 198-member manifest is
`db5cc06168c99ec3f0997483357a2cca6c70f3a54fe5673d58510e98822f52b8`.

## Frozen completed work

Paths below are relative to the owner Stage 2 root:
`audit/analyses/brown_adherence/main_linkage_b_amendment/stage2/`.

- `models/BA-LB-PRIMARY-ANY.rds`:
  `494b4647e7394b8efcaaf7cd7ae2922ba0669ac609529328d0a5e67cab60c245`.
- `models/BA-LB-PRIMARY-80.rds`:
  `72cc742f3d60941a5a4fc44361f09a671fea00a4183b85ab932d1d7292a02a74`.
- `estimands/manifest.csv`, 23/23 members:
  `baff7f3cb89565235193a2a74dc64afd82a58e3fea3d519c55fce08318d4863a`.
- `estimands/boundary_estimands.rds`:
  `b171074e003d565219327482329df4189258a2e2cfaa4d6c2c644b6b0a857490`.
- `estimands/validation.csv`:
  `bf182db7ccc2546972e1e7352ef40e82a913832c0a9432488e79ffd7c03f831f`.
- `estimands/B_to_B80_claim_gate.csv`:
  `1dea9c6364e8cb87bf8d52ecb9b7ac8e4d12a739b248d4c96e71cc7df38f1dd6`.
- `completion_v2/continued_final_package_002/final_manifest.csv`, 182/182:
  `5753221dd6a001c1715e2ba8e3e34ec2a83976e6d4ed0f637fa2cf3ac22baa80`.

All 182 members, every prior stop/package, primary sample, complete primary
family, code/binary, model and diagnostic already created, original/updated
supervisors and all seven execution-job directories remain byte-identical.
The current primary model pair and 23 exports are reused, not rerun or refitted.
No primary structural-ladder reserve is activated by this author decision.

The immutable scientific specifications remain the Stage 1 `plan.md` at
`f0ac32458527f046b0485f983e05ae5ff28c3ae3e415ee338bc91007eb23b268`
and the Stage 2 transition at
`f8bd6762e731ef1024762daea3113f3970eab21794b4f0c4d7316c020aee2178`.
Their formulas, thresholds, membership, sample construction, technical gates,
full multiplicity families, finite sensitivities and privacy limits are not
amended. The only scientific process change is continuation past this exact,
retained M1 claim stop with the author's accepted qualification.

## Released remaining scientific work

1. Run the already prescribed primary-model diagnostics from the frozen pair:
   conditional mean and endpoint calibration, denominator/coverage strata,
   Pearson and randomized quantile residuals, within-cycle cross-window
   dependence and actual-date/gap-based temporal assessment. Keep technical
   model status, overall adherence assessment, endpoint calibration and M1
   coverage qualification as separate fields. Do not collapse them into a
   single all-PASS status.
2. Execute only the 28 remaining nominal model slots listed in the exact
   Stage 1 registry, copied for convenience as
   `independent_preflight/remaining_nominal_model_jobs.csv`. These comprise
   the registered eight sample/threshold sensitivities, common dispersion,
   chest, calendar, three diagnostic family comparisons, nine site deletions
   and five screened participant deletions. These are ceilings and scientific
   prerequisites still apply, not a demand to run an ineligible branch.
3. Retain deterministic participant screening from the B primary diagnostics,
   average-rank tie handling and stable-key tiebreak. Run only the five selected
   deletions and the nine registered site deletions, never an expanded search.
4. Construct chest and calendar sensitivity frames only by the exact frozen
   source/chronology rules in Stage 1. Reconcile their integer counts,
   boundaries, eligibility, day ownership, memberships and support before
   fitting. Never reassign the old C chest frame or invent a chest sample
   count. Keep chest complementary, two-window and separate from primary
   near-eye; no Sleep chest estimate and no pooled placements.
5. The existing B beta-binomial benchmark may be reused after its exact
   identity/membership check. The registered observed-support weighted
   estimands and internal frozen C-to-B comparison may be derived as separate
   sensitivity outputs. Neither changes the primary estimand exports or
   multiplicity families. No C fit or exploratory cross-state fit may rerun.
6. The only conditional extra fit slots are the existing zero-exclusion
   structural recovery and the six actual-date OU candidates, at most three
   per sample. Trigger and route requirements in Stage 1 remain binding.
   No temporal candidate is run unless the actual-date residual trigger is
   met. No endpoint/rank/variance/decay gate is weakened. A completed primary
   model is not replaced by a temporal sensitivity. Retain unresolved
   dependence and the separately withheld exploratory day-level claim.
7. Maximum diagnostic simulation is the six named samples times 250 draws,
   1,500 total. Base seed 20260814 and residual seed 20260815 remain controlling.
   Seal the deterministic sample/route seed map before draws. No bootstrap,
   resampling inference or simulation-recovery experiment is authorized.
8. Derive only the diagnostic and sensitivity estimates/comparisons already
   required by those routes, with their registered uncertainty interpretation.
   No additional p-value family, selected-subset FDR, primary inference rewrite,
   participant ranking or hard tertile analysis is released.

An observed diagnostic or sensitivity failure is recorded honestly. Expected
finite conditional routes may follow only their frozen triggers. A new
nonzero process exit, timeout, code defect, frozen-input discrepancy,
unexplained membership, unplanned structural problem or required scientific
change stops the continuation. No blind retry. Do not stop merely to request
the same already-approved Pre-sleep qualification again.

## Exact infrastructure continuation and cumulative budget

Copy only the pinned prospective infrastructure file
`independent_preflight/run_bounded_job_v3.py` to the previously absent owner
`stage2/code/run_bounded_job_v3.py`, once. SHA-256:
`f4cfa0953a9dabb8a4c76d3847107507657b8e1ec51a35edbbfbd6f67be3e1d9`,
10,120 bytes. Preserve v1/v2 and all old drivers. The exact two-block reverse
to v2 and 33/33 mocked infrastructure checks are sealed. No R command, real
child process or signal was executed by those checks.

V3 recognizes only the two exact sealed nonzero histories:

- `DERIVE-PRIMARY-ESTIMANDS`: original bookkeeping stop, all three file pins
  retained; elapsed 31.04514845801168 seconds.
- `DERIVE-PRIMARY-ESTIMANDS-V2`: the real M1 coverage stop, start
  `10c6750009e87bdb316835932b68121daa462c5813872ece6631d22812808577`,
  finish `c21046b8e617853ef032a62ba8461c14c7a684e37771963027bafbe90bd19ad2`,
  log `cc48e937c460281eeec2887676cf2b0a5b5788aa7cfb95c4fe9024f8ca7675ac`;
  elapsed 31.406769749999512 seconds.

Both retain exit 1, no timeout and reaped children. V3 includes all seven
historical jobs and all 99.408186624990776 seconds in the same 1,200-second
accumulator. No budget reset. Ordinary jobs stay at most 120 seconds and
temporal jobs at most 180 seconds, further limited by remaining budget.
Computational verification and derivation count; pure file hashes, static
code inspection, prose and passive waiting do not consume scientific time.

Only one Brown scientific R process tree may execute at a time under V3.
Each fit has its own registered job ID and output identity; do not hide
multiple fits under one support job. V3's permitted non-fit prefixes are
`DIAGNOSTICS-`, `CONSTRUCT-CHEST-`, `CONSTRUCT-CALENDAR-`,
`SCREEN-INFLUENCE-`, `DERIVE-SENSITIVITY-`, `VERIFY-SENSITIVITY-`,
and `COMPARE-SENSITIVITY-`. These are technical routing labels, not authority
to invent additional work. Before any new job, seal its exact logical purpose,
frozen-plan row, driver hash, seed, inputs, outputs and cap in a new write-once
`preflight/qualified_continuation_001/job_registry_<sequence>.csv`.

The 28 fit IDs are the unchanged Stage 1 IDs. The conditional zero-removal ID
is `SENS-EXCLUDE-ZERO-NOZERO`. Temporal IDs are `TEMPORAL-ANY-ENDPOINT-R3`,
`TEMPORAL-ANY-BB-R0`, `TEMPORAL-ANY-BB-R3` and their corresponding
`TEMPORAL-80-...` IDs. The original absolute 51-fit ceiling is not expanded;
its unused primary-reselection reserve is not released here.

Use the existing accepted R 4.6.1 library and recorded production environment:
`RENV_CONFIG_AUTOLOADER_ENABLED=FALSE`; `R_LIBS` equals
`/Users/zauner/Documents/Arbeit/12-TUM/MeLiDos/WP2.2.5_Data_Analysis/ZaunerEtAl_bioRxiv_2026/renv/library/macos/R-4.6/aarch64-apple-darwin23`;
both Brown root variables retain their exact original values; OMP, OPENBLAS,
MKL and VECLIB thread limits are 1. Invoke the versioned supervisor with the
bundled Python runtime; it launches `/usr/local/bin/Rscript --vanilla`.
Python performs infrastructure only. R remains authoritative for every
analytical operation. Pin consequential package versions and actual loaded
paths before execution. Do not install, restore, update or change renv,
profiles, packages, lockfiles, caches or model libraries.

## Exact write boundary and preservation

Only new task-owned files inside the existing Stage 2 root may be written:

- new unconsumed diagnostic/sensitivity R drivers and their required scoped
  tests under `code/` and `tests/`, plus the exact supervisor copy;
- `preflight/qualified_continuation_001/` and new job directories;
- previously absent registered sensitivity model outputs, and new
  `diagnostics/`, `influence/`, `sensitivities/`, `placement/`, `calendar/`
  and `temporal/` outputs;
- new checks, logs, non-circular manifests and aggregate assessment evidence;
- final return under the previously absent
  `completion_v2/continued_final_package_003/`.

Never overwrite an existing path. If a generic original registry or summary
path already exists, create a versioned continuation counterpart and explicit
cross-reference instead. All current primary `frames/`, `models/` members,
`estimands/`, `multiplicity/`, binaries, original scripts and completed evidence
remain frozen. Newly required conditional code follows the already approved
Stage 1 mathematical interfaces; no recompilation or replacement of the
accepted primary likelihood is allowed. Do not add participant identifiers to
reader outputs; controlled diagnostic keys remain inside owner evidence.

Before computation, independently reproduce this dispatch seal, the 182 owner
members, 23 estimand members, 198 central stop members, all original Stage 1
pins and 2,287 distinct protected files (4,533 group rows). Verify the absent
computational lock and no active competing Brown scientific job. The old
unrelated R metadata probes are not Brown analysis and must not be terminated.
Retain exact snapshots and compare again at finalization. Any unexpected
protected delta stops; do not broaden an allow-list after a mismatch.

## Required return and continuing holds

Return all planned diagnostic/sensitivity status rows, exact model/input
provenance, gate results, failed or ineligible branches, seed/draw usage,
each job's real status/runtime and the cumulative budget. Explicitly retain
the failed Pre-sleep coverage gate and author-approved qualification beside
the separate endpoint/overall/temporal assessments. A verification PASS may
mean that the recorded failure was correctly preserved, never that it passed
scientifically. Seal a unique, non-circular completion or stopped manifest
and new handoff at `completion_v2/continued_final_package_003/`.

The mandatory next review is `BA-LB-G2-REVIEW`. This order does not accept
either model or the complete science. Point-only R2/Shapley, the internal
Stage 2 QMD/report render, all reader source-data/figure integration, Stage 3,
Stage 4, S5, manuscript/Word/ZIP/website changes and Writer numerical
notifications remain held for a later exact decision. Zero Quarto, report,
browser, office or full-project render is released. The separate returned
Writer/Harmonizer preflight packages remain planning evidence only.

No commit, push, upload, deletion, new task, alternate execution route or
unbounded retry. Seal first, confirm the owner is idle, and dispatch once.
