# REPORT-018 post-navigation display queue

Date: 2026-08-24

Status: **QUEUED, NO EDIT OR RENDER RELEASED**

The Nature Health navigation integration is independently accepted and its protected promotion window is closed. The following author-directed display work is now queued for separately sealed, path-bounded orders. This record is coordination authority only.

## 1. Brown Stage 3 terminology and display package

Priority: first, because the new author decision controls a pending package that has not been independently accepted or rendered.

Required consolidated scope:

- replace the three recommendation-window labels Wake / Pre-sleep / Sleep with Daytime / Pre-sleep / Sleep in every affected Brown Stage 3 figure;
- state in each affected caption that Daytime, Pre-sleep, and Sleep identify the Brown et al. recommendation windows;
- do not call these three categories contexts;
- replace Evening with Pre-sleep only where Evening denotes the exact three-hour pre-sleep window;
- retain recommendation adherence;
- incorporate the pending reader-facing replacement of unexplained BA-M4 and BA-M6 labels before source acceptance;
- preserve genuine sensor-position context language;
- preserve every scientific value, interval, p-value, multiplicity decision, sample, model, marker, reference line, source-data row, legend meaning, and non-causal qualification; and
- require one complete read-only inventory, one exact replacement matrix, candidate-first asset generation, independent source/display acceptance, and only then a separately bounded Stage 3 render.

The earlier Wake-label recommendation is superseded. No Brown file or accepted HTML may change before the consolidated order is sealed.

Owner: `019fffdf-66d4-7802-9091-09283ad27b7f`.

## 2. H06_daily FDR-overview display

The blank H06_daily table in the static manuscript-selection preview is accepted only as a selection-copy layout issue caused by the retained `column-page` class. The accepted reader endpoint, table values, source, and scientific decisions remain unchanged.

The future display-only order must:

- remove only the MDER-specific magenta-diamond class and legend entry from the accepted FDR-overview display;
- encode the six estimable, FDR-not-supported MDER cells with the ordinary Not FDR-supported symbol and legend entry;
- retain the separate L10 non-estimable encoding;
- read only the frozen figure source data;
- preserve every estimate, raw p-value, BH/FDR value, rank, decision, sample, metric label, diagnostic, source-data row, geometry, panel, dimension, and typography;
- use candidate-first PNG/SVG generation with a non-circular display manifest and visual QA; and
- keep any reader-page or selection-page render separately gated.

Owner: `019fec6a-20d3-7710-ab9b-a035e0874182`.

## 3. H03/H04 Supplementary display and language

The future shared Supplementary Information order must address endpoints by identifier:

- pair `fig-h03-primary-estimates` and `fig-h04-primary-estimates` as one Supplementary display;
- place `fig-h04-temporal-near-eye` immediately below it;
- select `tbl-h04-near-site-factorization`, supported by `tbl-h04-category-support`;
- use category-by-site interaction model for H03 and activity-by-site interaction model for H04;
- use site-average estimate for the accepted equal-site fitted-log-mean estimand;
- preserve every value, interval, p-value, FDR decision, category, order, geometry, palette, non-colour cue, missing or non-estimable cell, source-model flag, 1/k weight, and display-only status; and
- preserve the exact accepted five-category H04 near-eye n contract supplied by the H04 owner.

No H04-owned edit or render is authorized by this queue entry. Implementation requires a separately sealed owner order and a separately bounded Supplementary Information integration route.

Owner: `019febf4-4868-72f3-bd97-31a85e86f8f0`.

## Common boundary

No model fit, prediction, inference, resampling, source-data regeneration, package or lock change, broad language loop, manuscript edit, commit, push, upload, or full-project render is authorized. Each item must return one consolidated inventory and order package before any owner is activated.
