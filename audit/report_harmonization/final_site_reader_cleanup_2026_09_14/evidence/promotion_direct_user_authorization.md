# Direct user authorization for Order017

14 September 2026. The user directly instructed this task:

> Apply Order017’s 62 checked local operations, including the 15 backed-up public-file retirements and matching navigation, configuration and manifest updates.

This resolves the direct-user authorization requirement recorded in `promotion_permission_blocked.json`. The two earlier pre-execution denials remain preserved as historical evidence. No live operation had started before this authorization.

The same frozen helper set, candidate, preimages and 62-operation transaction plan apply. No scope expansion, rerender, scientific recomputation, source/artwork change, remote publication or Writer wakeup is authorized. The existing once-only promotion will recheck exact current preimages, protected files and competing processes, then retain the checked rollback boundary. Brief live browser/HTTP verification, teardown, final integrity checks and a non-circular completion record follow a successful transaction.
