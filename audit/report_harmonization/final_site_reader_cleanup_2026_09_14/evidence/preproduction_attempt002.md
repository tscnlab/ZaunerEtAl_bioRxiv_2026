# Consolidated inherited semantics classification

The second preproduction check confirmed that the old `V0` header token is absent, not duplicated within its table. Both original failed check CSVs and validators remain preserved. No HTML or candidate content was changed.

The Coordinator requested a complete inherited header, ARIA and ID failure-set enumeration across all 44 survivors, instead of serial discovery. `verify_semantics.R` independently enumerates every failure and binds it to the exact route, table serialization, token, multiplicity and accepted page preimage. The static validator independently compares the same complete old/new failure multiset and still requires full non-entry tables and content to be identical.

Index and Supplementary Information remain strictly free of semantic failures. Any new, concealed or changed token state anywhere fails. Previously valid registered scientific tables remain valid. The bounded baseline classification does not authorize any scientific, HTML or table repair.
