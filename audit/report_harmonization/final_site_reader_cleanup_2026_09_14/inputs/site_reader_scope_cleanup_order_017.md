# Order017: remove internal utilities and the sensitivity planning page

14 September 2026. Owner: Harmonizer task 019ff52e-48ac-77b3-9a0e-9a87749a3bba. Coordinator: 019faf58-3df3-7383-8034-f715cdfdd154. This order is executable only after the coordinator seals its dispatch manifest and explicitly releases it. It implements the author's overall approval with three website corrections recorded in `reader_scope_author_direction_017.md`.

## Exact scope

Start from independently accepted Order016: 914 public files, index `af87852a1a5e2e5739eb87870fafcf0166e6c79e262c6e1f1f0eb9f3eed7e651`, Supplementary Information `0a1da925d6bb63d2be7139ddcf0c511fb2c5ba10eacf2d340cfed6e61ee8b286`, and 37-row corpus `642161dfc1ec18572b8b8c124546060c8f889683ebf7ceb8e5e43dc785f77669`. Reproduce the complete archived read-only impact package and the exact replacement/retirement preimages bound by this dispatch before work.

The sole production boundary is:

1. The 45 public replacements enumerated in the impact matrix: 43 existing HTML files, `search.json` and `sitemap.xml`.
2. The 15 public retirements enumerated in that matrix: `manuscript_changes.csv`, `manuscript_changes.md`, `notebooks/sensitivity_battery.html`, and its 12 exclusively owned support files. No source-side sensitivity file is retired.
3. One exact `_quarto-nathealth.yml` patch, from `e54c71794f4f763a8b50417ab83ff3db37bc9af3fef3f4d1910576ab12c61bc7`: remove the positive sensitivity render entry, its two-line Supporting material menu entry, and the now-empty robustness sidebar definition. Preserve every other configuration byte. Do not edit `_quarto.yml`, style files or browser scripts.
4. One final direct replacement of `audit/report_harmonization/phase4_corpus_manifest.csv`: remove only the sensitivity row, retain the remaining 36 rows and their historical source hashes, update the 36 current HTML identities, and correct only render positions affected by removal of the old render-position 35 entry. Index becomes render-position 35 and Supplementary Information 36. Remaining source/title/role/logical/sidebar values stay exact.

No public file additions are required. The resulting public tree has 899 files, with 854 unchanged survivors and 45 replacements. The current reader corpus has 36 routes, with result/provenance adjacency preserved. The 15 retired preimages remain recoverable outside the served site. Preserve unregistered historical pages except for their exact references to the withdrawn sensitivity page. Broader retirement of ambiguous historical workflow/analysis pages is not released by this order.

## Permitted transformations

On index, remove the complete `section[data-site-utility="true"][aria-labelledby="site-downloads-title"]`. Keep the separate Other Formats Word link, the approved Word payload, manuscript body, scientific tables/figures and their links exactly.

On standalone Supplementary Information, remove only the passage-change download paragraph from its downloads utility, the complete `section[data-site-utility="true"][aria-labelledby="site-passage-changes"]`, and its one matching TOC list entry. Retain the manuscript Word download, all 19 editable-table links and payloads, and return-to-manuscript navigation. The removed internal revision table is not a scientific table.

Across the 43 enumerated surviving HTML files, remove the exact 47 incoming sensitivity references in their existing navigation roles: 39 navbar, four sidebar, two head rel-next and two previous/next references. Remove only their minimal self-contained navigation nodes. Preserve all other navigation blocks and main content. Do not invent a replacement scientific destination or replace a scientific sensitivity result with a planning statement.

Remove only the four search objects whose href route is `notebooks/sensitivity_battery.html`, retaining the other 938 objects, their order and values. The existing search projection already excludes utility blocks. Do not rebuild search from historical source QMDs. Remove only the matching sensitivity URL entry from sitemap; preserve other entries and timestamps. The sitemap intentionally changes from 39 to 38 URLs, not 36: its two pre-existing unregistered workflow URLs are outside this retirement boundary. Do not conflate sitemap membership with the current reader-corpus/search roster.

Use exact byte-span changes rather than wholesale HTML reserialization. Record removed spans and sufficient offset/context information for exact reversal. Independently show that stripping precisely these allowed nodes from the old DOM yields the new DOM, with no additional attribute, text, ID, table, figure, caption, source-data or numerical-content change.

## Candidate and implementation boundary

Write new helpers, copied audit inputs, backups, isolated fixture/candidate, matrices, evidence and final records only beneath `audit/report_harmonization/final_site_reader_cleanup_2026_09_14/`. Never edit sealed historical integration helpers or manifests. This order grants the owner the narrow production profile/build/corpus writes above as an explicit exception to its ordinary ownership boundary, and no others.

Create a byte-exact candidate from the current public tree. Capture and hash all 62 production preimages before any production mutation: 45 public replacements, 15 retirements, profile and corpus. Verify exclusive ownership of the 12 support files again. Preserve every input including authoring QMDs, source data, scientific/display artifacts, Word files, approved table downloads, CSS, JavaScript, bibliography, packages and lockfile.

Implement and freeze one complete helper set before promotion. Parse/compile it, run the complete forward, reverse, retirement, rollback and postflight path on an isolated full-tree fixture, and require exact recovery of all 914 original public files plus the old profile/corpus. Check path-keyed sets rather than inconsistent sorts or imported storage types. Classify intended transitions consistently in all nested historical checks using exact path, prehash and retained backup; do not repin or rewrite historical evidence. In particular, do not execute the old `test_navigation_contract.R`: it sources the manifest-writing 37-route builder. Preserve both existing files and their historical PASS records, and implement the new 36-route contract in this order's read-only validator instead.

Candidate preparation and verification may be corrected within this bounded removal-only scope before the single live transaction. Preserve failed check evidence and do not weaken content or file-set invariants. A new scientific or source-content defect stops; it does not authorize a repair.

## Complete acceptance before production

Require 899 exact candidate files, zero symlinks, 45 changed survivors, 15 exact retirements, 854 byte-identical survivors, and no additional path. Require the exact 36-route profile/corpus relationship and 938 retained search records. Recheck the retained 36 historical source hashes rather than substituting current authoring states.

Use R 4.6.1 with the existing approved library and `RENV_CONFIG_AUTOLOADER_ENABLED=FALSE` for scientific/content reconciliation. Reuse accepted content/targeted checks in new copies with only the expressly retired utility/search/roster classifications changed. Protect the complete manuscript/Supplementary scientific content, single Table 2, all figures, all retained tables and semantics, S2's accepted secondary text, S4/S7 corrections, S8 whole SVG, S15-S18 destinations and 19 editable-table payloads. No computation or reformatting of results.

Audit every surviving public HTML/reference and asset dependency, not just the 36 registered routes. Require no remaining link/search/sitemap/profile pointer to the retired endpoint or passage-change public resources, no new unresolved reference, and no new duplicate or unresolved semantic ID. Preserve exact inherited duplicate-ID multisets in non-entry reports; utility removal must not introduce or conceal a new semantic failure. Retain only the exact documented inherited font/navbar/source-hash exceptions. The old 37-route, 942-record, 914-file, two-download-utility, 22-download-file and revision-table expectations are historical preconditions, not current postconditions. Twenty download payloads remain: complete Word plus the 19 native table files.

Inspect the candidate with the supported internal browser on a single read-only GET/HEAD server bound strictly to 127.0.0.1 after mandatory symlink preflight, serving only the candidate rendered root. Review index, Supplementary Information and H11 companion serially at desktop 1440 x 1000 and narrow 708 x 1000 or 390 x 844. Confirm the index starts with its manuscript heading without the box; Supporting material exposes Supplementary Information without Sensitivity checks; Supplementary Information has no revisions section/TOC but retains downloads; H11 companion has no withdrawn next link. Check representative table/content preservation and actual navigation. Exercise search to prove no withdrawn page result, but do not reopen the inherited B1 desktop search-layout issue.

HTTP-check all 36 reader routes and retained download identities; the three retired reader/log URLs must return 404, not a copied audit page or redirect to unapproved content. Stop the server, prove no listener, close task-created tabs, reset viewport, and rehash the candidate/protected state.

## One guarded production transaction and final return

After all complete candidate and fixture gates pass, freeze the exact candidate promotion and rollback plan. Confirm no competing render, semantic hook, site writer or loopback process owns the scoped targets. Unrelated read-only R processes are not a blocker.

Stage/hash every replacement before the first live write. Retain all 62 preimages outside the served build. Journal each exact operation, check its current preimage immediately before writing or retiring it, and atomically replace each of the 47 replacement targets once. Retire only the 15 exact pinned public files; remove only their now-empty, exclusively owned support directories if desired, never a broad tree. Put the 36-row corpus replacement last. Same-directory stage files must be explicitly journaled and absent at completion.

Require final public equality with the candidate, exact profile/corpus postimages, recoverable preimages, no unexpected protected delta, all static and R content checks, and brief live index/Supplementary/menu smoke QA under the same bounded localhost procedure. Verify all withdrawn URLs are absent, retained routes/downloads work, and the publication site remains immutable across QA. Teardown, prove no listener and rehash. Produce one non-circular completion manifest and one combined return with exact changed/retired sets and inherited qualifications.

If a live transaction or genuinely new postflight defect fails, restore only this order's exact installed postimages/retirements from the retained preimages, verifying identities and absence before every restore. Prove the original 914-file build and profile/corpus, retain the failed transaction, and stop. Do not overwrite concurrent work, render again, create a second production candidate, or silently continue after a new defect.

No Quarto, Pandoc, knitr/QMD execution, Office conversion, model, prediction, inference, data regeneration, new language or typography loop, remote publication, manuscript alias update, package/lock change, commit, push or upload. Do not wake scientific or Writer owners. Overall approval and the narrower S2/Word qualifications remain unchanged.
