# Order017 combined local completion return

14 September 2026. Owner: Harmonizer task `019ff52e-48ac-77b3-9a0e-9a87749a3bba`. Coordinator: `019faf58-3df3-7383-8034-f715cdfdd154`.

## Outcome and authority

The exact checked Order017 cleanup was applied once after the user directly authorized all 62 operations, including the 15 backed-up public-file retirements and matching navigation, configuration and manifest updates. The prior permission denials occurred before process creation, caused no live mutation and remain preserved. `evidence/promotion_direct_user_authorization.md` and `evidence/promotion_permission_resolution.json` record their resolution.

The main manuscript no longer has the downloads utility box. Supplementary Information retains the complete Word file, 19 editable-table downloads and return navigation, without public passage-change records. The Sensitivity checks planning page and its exact navigation, search, sitemap, profile and reader-corpus entries have been removed. Scientific sensitivity results elsewhere and the source planning QMD remain unchanged.

The site now has 899 public files: 45 checked replacements and 854 byte-identical survivors. Fifteen pinned public files were moved into `live_retired/`, outside the served site, and are also retained in `preimages/`. One profile replacement and one corpus replacement complete the 62 operations. The corpus was installed last. There are 36 reader routes, 938 unchanged retained search records, 38 sitemap entries and 20 unchanged download payloads.

## Exact operation and recovery records

`evidence/planned_operations.csv` and `evidence/transaction_plan.json` enumerate every changed and retired target with its preimage, postimage, byte size, backup and sequence. `evidence/preimage_manifest.csv` records all 62 retained preimages. `evidence/raw_span_ledger.json` records the exact reversible byte changes and DOM-removal proofs. `evidence/live_transaction_journal.jsonl` records all staged and installed operations, with no second live transaction or live rollback. Every same-directory staging file is checked absent at completion.

Only the specified navigation/utility nodes and exact configuration/corpus records changed. No retained scientific prose, table, caption, figure, SVG or numerical content was modified. All authoring QMDs, accepted Word/table files, source artifacts, packages, lockfile and historical evidence remain protected. No Quarto/Pandoc/knitr execution, Office conversion, scientific recomputation, remote publication, manuscript alias update, commit, push, upload or Writer/scientific-owner wakeup occurred.

## Verification

The 14-helper set and copied input inventory were frozen and rechecked before use. Candidate static checks passed, and a full isolated fixture ran the complete forward transaction, recoverable retirements, static and four R checks, concurrency-rejection guards and exact rollback to the original 914-file tree plus profile/corpus. All candidate evidence, including three corrected preproduction validator attempts, was retained.

The live transaction rechecked exact preimages and process ownership, staged all 47 replacements before the first live write, installed all 62 operations once, and passed complete postflight. `evidence/live_static_summary.json` records 204,873 checks across all 44 surviving public HTML files and 57,390 reference rows. `evidence/live_checks.json` records all four R checks, and `evidence/live_verified_R/` retains the complete roster/content, accepted Writer-content, targeted entry and all-HTML semantic evidence with R 4.6.1 session and command provenance. The frozen runner's stdout phrase "three R checks" is only an old label; its recorded commands and fields confirm four executed R validators.

Both candidate and live HTTP checks passed 79 checks each: exact responses for all 36 reader routes, GET/HEAD identity for the complete Word file and 19 native tables, and 404 responses for the three withdrawn reader/log URLs. The accepted SVG payloads remained unchanged.

Candidate browser review covered manuscript, Supplementary Information and H11 companion at 1440 x 1000 and 708 x 1000, including the H11 footer and actual Supporting material-to-SI navigation. Live smoke repeated manuscript, SI and menu checks at both widths. The exact CUA archives contain 14 candidate and five live screenshots. Each read-only server bound only to 127.0.0.1, passed mandatory zero-symlink preflight, served only its rendered root, and was stopped afterward. Teardown proved connection refusal, all 899 rendered files unchanged across QA, unchanged scoped source/configuration hashes, closed task-created tabs and reset viewport. Existing local deliverable tab 10 was retained.

Post-QA protection passed 14,756 checks across 5,691 unique paths. Its 271 historical manifest-row transitions represent only the exact 62 approved paths, with pinned prehash and retained backup identities. Finalization rechecks this boundary, exact live/candidate equality, all recovery files, completed gates and staging absence before writing a non-circular completion manifest and seal.

## Inherited qualifications, not new acceptance claims

- The B1 desktop search-dropdown clipping/overflow remains unchanged. Candidate search returned no result for the withdrawn title, but no unqualified search-layout PASS is claimed.
- The SI return link's candidate click/keyboard attempts did not expose navigation or a new tab. This remains **inherited/not newly browser-verified**, not PASS. Its exact pre/post node, target and handler are unchanged; the manuscript destination was HTTP-verified. The Coordinator expressly confirmed no additional successful-activation gate for this exact inherited behavior. Positive Supporting material-to-SI navigation is separately recorded. Any generated-summary wording about a handler that may open another tab does not claim successful return-link activation in this review.
- S2's accepted secondary text, S8's whole SVG/internal whitespace, dense-phone artwork enlargement needs and the approved Word S18 footnote/right-edge limitation remain fixed. No Office or typography loop was opened.
- The 16 historical source-hash gaps, four exact inherited font-reference exceptions and five classified unregistered historical links are retained. No new unresolved reference was introduced.
- All 12,839 inherited semantic-token failure occurrences across 11 non-entry reports are exactly preserved, including some registered preparation/descriptive reports. This is not a claim that all historical tables are semantically valid. The two manuscript entries remain strictly valid, with no new semantic defect anywhere.
- The two pre-existing unregistered workflow sitemap URLs remain in scope-preserved state. Thus 38 sitemap entries do not imply 38 current reader-corpus routes.

These qualifications are documented in the candidate and live browser acceptances and frozen classification records. No newly introduced defect was identified within the released removal-only scope.
