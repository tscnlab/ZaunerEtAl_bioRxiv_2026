# Final website and document integration reconciliation

14 September 2026. Harmonizer. **Read-only return for central disposition. No implementation release is inferred.**

Recommendation: use a bounded, reversible static packaging operation, not a 37-report render. The proposed target set is complete in `proposed_integration_matrix.csv`: four existing website files, 21 new download files, and one two-row corpus-hash reseal. Preserve all 35 analytical/preparation/report HTML pages and their existing resources exactly. The final Word identity remains **PENDING_ORDER010C_ACCEPTANCE**.

All paths below are relative to `/Users/zauner/Documents/Arbeit/12-TUM/MeLiDos/WP2.2.5_Data_Analysis/ZaunerEtAl_bioRxiv_2026` unless absolute. C denotes `audit/manuscript_nature_health/final_format_completion_2026_09_14/`; P denotes `audit/manuscript_nature_health/final_review_production_2026_09_14/`.

## Verified present state

- All **893 live build files** reproduce the accepted Order71c inventory exactly, not merely the 37 HTML hashes. The build has no symlinks. All **37 reader HTML endpoints** match the current corpus manifest `01a2fdc1f1d11db45e28834893c79d620aa5321be1f49cff612b34589e068008`.
- All **271 C package members** reproduce their recorded hash and size. C's integrated HTML is `752ee27291e7970d28fc369c1ad3c396dc8894f483aa9864e65b9edea638df17`. All 19 native table files are pinned individually in `editable_table_bindings.csv`. Their accepted source bytes are the proposed download bytes. No export or Office conversion is needed.
- The existing website manuscript is still the September 3 artifact, index `9c1beea41a0b203b8a8b032eade2ba7e4ab5067857d39e296a8503231af46eec`. Its Word download is `74193a7a787ea18d70933b1742e588304a6bf6050c8b43e8a0a0cc352b44a2b8`, not the new final document.
- The supplementary website route remains a placeholder: `4f80761abfa29148d50c4d9c60754c19625de6d0e8505b29530901ed3204e7cd`, with no tables or images. C already contains the complete `section#supplementary-information`, including 16 semantic tables. Its fragment references are self-contained within that section. No new supplementary scientific prose is required.
- The website has no manuscript editable-table download set or passage-change artifact. The existing accepted revision record has **20 ordered positions**. Its CSV is `abaa25cc7f25ecfc8a54895d6e375030ed08dbbd63fac8b7eccf68bd23b298d8`; its Markdown counterpart is `341a8d7a92843c5d8188a433a628bc943eeae7572c54af248f5ce8559e58ff24`. Both are under `audit/manuscript_nature_health/table_layout_visual_corrections_2026_09_14/attempt_02/evidence/cumulative_passage_changes.*`.
- Search is a separate stale bridge. `search.json` has 558 records, including **122 invalid fragment targets** across 25 routes and entries for two non-reader workflow pages. Updating only the manuscript search records would leave broken results. Reindex the 37 accepted reader HTML pages statically, without rendering or changing them.

These are file/markup checks. The coordinator's C acceptance and Writer's visual evidence remain the visual/scientific authority; no browser, render, Office application or scientific verifier was run here.

## Source authority is not the same as delivered HTML authority

`reader_routes.csv` records every historical source pin, current source pin, historical HTML pin and current HTML pin. **Sixteen QMD hashes differ from the historical corpus**, but none changed since the September 12 preflight. All 54 files in that preflight's static code frontier are also unchanged. The cited source-authority records are unchanged. `source_transition_status.csv` retains the individual distinctions:

- H01 companion: the current endpoint is independently accepted, but it was not the source used for the historical HTML pin.
- H04 result/companion and H06 result/companion: exact current source-only endpoints have acceptance records. Their older baseline transitions and/or report execution bridges are not erased by acceptance of manuscript formatting.
- H01 result: the recorded independent source acceptance precedes the current endpoint.
- Descriptives and the H02/H03/H05/H06_daily/H07/H08/H09/H10/H11 results: the prior audit did not establish complete historical-to-current source/output bridges.

This does not establish an analytical error. It also does not authorize a new analysis, render or owner review. For this integration, the accepted historical HTML is the authority for the 35 unchanged report routes. Any later source-based reproduction must address these recorded gaps separately. The old scientific input frontier and runtime/library constraints are historical evidence, not a freshly validated execution closure.

There are additional manuscript-specific gaps. Root `index.qmd` and `supplementary_information.qmd` still have their historical hashes, but are not the current Writer sources. Live Writer QMDs, display CSS and canonical output aliases are older than P/C. P's staged main QMD is `d33fe83296dac0b83dabffff30e5f21bb42143f40906615f94af4146329030eb`; its SI outline is `7eaa706556703ce9e78b93d927aae1ca83865174a7be2dca0799689eb4efaec9`. Both have zero detected executable-cell openings or inline-R tokens. C then applies accepted reversible HTML packaging changes, including Table2/S2 colgroups and S15B, rather than claiming a new QMD render. The final Word has further assembly/formatting and the pending single-element Order010c correction. None is a reason to overwrite live Writer sources in this website operation.

The general report pipeline is not a no-compute substitute: the archived preflight includes inline R, stored-summary operations, manifest identity reads and deferred Descriptives thumbnail generation. `--no-execute` is not an adequate proof that inline R or the whole render closure is inert. No Quarto, Pandoc, knitr, post-render hook, model loader or thumbnail generator belongs in the proposed static operation.

Brown remains closed. Its accepted current Stage3/Stage4 sources and HTML were identity-pinned from the isolated Brown worktree, not substituted with shared-checkout snapshots. They are not members of the 37-route profile and are not silently added as new public routes here. C contains the accepted final Brown manuscript content. H09's corrected S15B is accepted for the Writer display only; preserve its occurrence in C without changing the historical H09 report or scientific figure authority.

## Exact proposed public bindings and allowlist

The matrix provides a full path, current existence/hash, source/hash and check for every target. New derived output hashes must be computed and sealed after an authorized candidate operation, before promotion; they are not invented in this return.

| Target | Proposed operation |
|---|---|
| `_build/nathealth/index.html` | Insert C's exact main content and updated manuscript metadata into the accepted site shell; use C's exact display-specific CSS and TOC content. Add a clearly separate website download panel. |
| `_build/nathealth/supplementary_information.html` | Replace the placeholder body with C's exact complete SI section; preserve the supplementary route's accepted shell. Add the resource links and existing position/old/new passage table as explicitly labelled website utilities, not new manuscript prose. |
| `_build/nathealth/ZaunerEtAl2026_NatHealth_phase3_brown.docx` | Copy only the independently accepted final Order010c Word. Its exact path, size and SHA must be supplied before candidate staging. C's `d6dd4180...` is expressly not the final source. |
| `_build/nathealth/search.json` | Static reindex of the exact 37 reader HTML pages using the existing `objectID`, `href`, `title`, `section`, `text`, `crumbs` schema. No non-reader workflow results. |
| `_build/nathealth/editable_tables/{Table_1,Table_2,Table_3,Table_S1,Table_S2,Table_S3,Table_S4,Table_S5,Table_S6,Table_S7,Table_S8,Table_S9,Table_S10,Table_S11a,Table_S11b,Table_S12,Table_S13,Table_S14,Table_S15}.docx` | Nineteen individually enumerated byte-exact copies from C, with one visible link for each. No regeneration, restyling or archive expansion. |
| `_build/nathealth/manuscript_changes.csv` and `.md` | Exact copies of the accepted 20-position record. The visible SI utility table uses its `position`, `old_text`, `new_text` fields in the same order, escaping literal markup safely. No rewriting. |
| `audit/report_harmonization/phase4_corpus_manifest.csv` | After acceptance only, change the index and SI HTML hash cells. Preserve all 37 rows/order and historical source metadata. Add a separate current-artifact authority ledger in the new integration evidence root. |

This yields 914 website files: the 893-file baseline plus 21 additions, with four existing files replaced. All other **889 baseline files**, including all **35 report routes**, remain exact. There is no new reader HTML route, shared CSS/script change, profile change, canonical Writer-file change, `docs/` update, deployment or remote upload in this proposal. Do not publish C's raw provenance JSON or copy its whole package: those records contain internal paths and are not requested downloads.

## Navigation and public dependency closure

Reuse the current accepted shell independently accepted under Order71c: navbar/menu grouping, route order, search interface, footer, right-side desktop TOC, mobile TOC behavior and page navigation. Preserve each entry route's own shell, rather than transplanting a notebook or standalone-document shell. Component DOM identities are in `integration_component_identities.csv`; all raw containing files are pinned. The current mobile include is `153967734707cccf0fb2e910327cebfadb552f3c752b70e67b5346fb9acd8980`.

The static public reference scan resolves 45,706 local references and identifies 450 existing build dependencies. C's only non-fragment local dependency is its adjacent Word download; its display payloads are embedded. The scan is not proof of a minimal JavaScript runtime closure. Preserve the entire exact 893-file published baseline, including the other 443 files, rather than pruning it. This is a finite public-output closure, not permission to copy or serve the repository, research inputs or audit roots.

Four inherited CSS references to `Datatype.woff2` are unresolved at three paths: build root, `notebooks/hypotheses/`, and `audit/hypotheses/H06_daily/`. The font is also absent at repository root. Record these exact baseline exceptions, do not invent a successful font load or add a font-repair loop. Existing remote KaTeX, MathJax, polyfill and font stylesheet references are inventoried but were not fetched. An offline/self-contained website is not being certified. Freeze existing font choices, including S2's explicitly accepted secondary type size.

The older `order71c_resync_verify_promote.R` is a useful static-transplant/atomic-copy/reverse-proof pattern, **not a runnable entrypoint for the new package**. It hard-pins superseded inputs, assumes identical old/new display CSS, index-only replacement, a two-file promotion, old figure counts and a single-row reseal. `navigation_integration_support.R` also has obsolete mobile-include/corpus pins. Adapt new copies only under the new bounded integration root. Retain equivalent current checks instead of deleting failing guards. Do not run `build_phase4_corpus_manifest.R` against today's live QMDs to manufacture source acceptance.

## Finite staging, checks, promotion and rollback

Proposed execution/evidence root, not created by this return: `audit/report_harmonization/final_site_integration_2026_09_14/`, limited to `helpers/`, `candidate_build/`, `evidence/`, `backup/` and `rollback/`. The live target allowlist is exactly the 26 matrix rows. A new central release must name the implementation owner and the final Word authority, then authorize these operations together:

1. Recheck all current input/target pins, the 893-file baseline, C's seal, the 19 native identities and final Order010c acceptance. Confirm the current Writer operation has completed. Snapshot the four replaceable build files and corpus manifest into five exact backups. Record the absence of all 21 addition targets. Never overwrite a newly appeared file.
2. Copy the exact published build into an isolated candidate. Perform the two raw-byte-bounded HTML integrations. Preserve C's manuscript/SI text, all table structures/notes/values, 17 S2 plot payloads, accepted Table3 order, 24 figure appearances from 23 SVGs, S8 crop styles, capital panel tags and accepted S15B. Replace the entry-local display CSS with C's accepted block, not the shared site CSS. Retain every untouched shell/script byte. New UI panels must reverse away exactly. Preserve C and all historical packages.
3. Add the final Word, 19 native tables and two revision files. Generate a fresh 37-route search index from static DOM text only. No report source execution or scientific calculation. Produce complete candidate hashes, an explicit preimage/postimage operation ledger and the additive source-authority ledger before promotion.
4. Run structural checks over all 37 pages and the complete proposed file set: exact 35-route/889-file preservation; accepted content/payload comparison in R; table/header/IDREF/fragment checks; no new duplicate IDs; 19 editable links and hashes; final Word equality; all 20 ordered revision rows; no internal filesystem links or audit metadata exposure; valid search schema/unique IDs and every search target resolving. Preserve exact historically accepted duplicate-ID multisets on unchanged report pages, not a new blanket zero-duplicate gate. The two changed entry pages must be clean. Allow only the four recorded legacy CSS-font misses, not new unresolved local targets. Keep old search and all reverse mappings.
5. After static PASS, perform bounded localhost QA through the permitted in-app browser: one GET/HEAD-only server on `127.0.0.1`, serving only `candidate_build`, mandatory symlink/pre/post hash checks, no `quarto preview`. Inspect both changed entry pages at 1440, 708 and 390 CSS pixels, including their complete content and full-width table scroll endpoints. Verify all figures/captions, S2's rightmost plots and unchanged type size, utility table and download links. Exercise navbar, search, keyboard/collapsed TOC and report navigation, with Preparation01 and H06 as unchanged-route desktop/narrow smoke checks. Identity-bound prior QA covers the other unchanged report pages. Do not reopen accepted font/cosmetic or scientific cases. Stop the exact server, reset viewport overrides, close only task-created tabs and prove teardown. No fallback surface may evade a tool denial.
6. Seal the full candidate and obtain central acceptance before a single promotion transaction. Copy only the 25 website targets, then perform the two-cell raw-reversible corpus reseal. Write the additive authority ledger and transaction record in the new evidence root. The ledger must explicitly distinguish historical QMD provenance, current QMD pins, accepted HTML authority and static integration. It must not claim the changed current sources generated the preserved report HTML.
7. Postflight: compare the full live file set with the sealed 914-file candidate, all 37 live route hashes with the resealed corpus, all 19 native downloads and final Word with their authorities, and replay structural/link/search checks. A short production localhost check of the two entry routes verifies delivered navigation/download bindings, with the same teardown rules. On copy, reseal or postflight failure, restore the five verified preimages and move only the 21 transaction-created additions to the new rollback directory. Confirm exact recovery of the 893-file baseline and corpus. Preserve failed candidate/evidence; never recursively delete broad paths or roll back another owner's work.

The operation is at most one candidate packaging pass, one centrally accepted promotion and one documented rollback if necessary. A failed new invariant returns the consolidated evidence; it does not silently authorize new rendering, source edits or additional cosmetic rounds.

## Ownership, limits and return evidence

Writer remains the sole owner of Order010c and the final Word correction. A read-only task snapshot during this audit showed Writer active (`01a09f79-b917-74e2-ac82-a3d93a38c678`) and the navigation task not loaded, with its last turn completed. No task was dispatched or awakened. These are task statuses, not an OS-wide process clearance. An attempted read-only `ps` inventory was denied; no alternative process-enumeration route or permission escalation was used.

Harmonizer wrote only this new reconciliation directory. There was no live-source change, report/model execution, Quarto/Pandoc/Office call, browser/server/capture, staged website, owner dispatch, promotion or final-Word inference. Infrastructure scripts used Python 3.12.14 and lxml 6.1.1 for file hashes, URL resolution and markup structure only. The final read-only checkpoint is **1,298/1,298 exact**, including expected-absent proposed targets. All scientific and accepted visual assertions are attributed to the existing C/Coordinator evidence rather than represented as newly audited results.

The Quarto skill informed the separation between authoring sources, accepted rendered artifacts, resource closure and actual visual QA. It did not cause a render or a source change. The controlling next decision is central approval of this finite static integration plan after the final Word identity arrives, not another Brown review or author font approval.
