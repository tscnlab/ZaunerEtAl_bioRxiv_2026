# Order016 initial authority check

The complete Order016 and complete Order015a independent acceptance were read before implementation. R 4.6.1 independently verified all four controlling file pins and both 58-row manifests at their exact current preimages before creating the new implementation.

The initial read-only release check used the installed user library. All transaction preflight and production content verification commands are explicitly routed to the accepted project library `renv/library/macos/R-4.6/aarch64-apple-darwin23`, with `RENV_CONFIG_AUTOLOADER_ENABLED=FALSE`. No packages are installed or modified.

The new helpers preserve the accepted content and targeted scientific predicates. Only input/output routing and immutable preimage baselines change. Infrastructure copies are narrowed to seven replacements, corpus last, and use a classifier that matches both the exact live path and exact authorized prehash. Four previously pinned, read-only Brown provenance files in the existing 82ab worktree remain explicit external protected inputs; no external write is permitted.

All new work is confined to this Order016 directory until the separately invoked guarded transaction. The Order015 failure and Order015a pending packages remain unchanged. The accepted historical source-gap, search-shell, S2 and Word qualifications remain in force.
