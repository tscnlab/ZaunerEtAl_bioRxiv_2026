# REPORT-018 Order72j independent component disposition

Date: 2026-09-11.

Verdict: ready for candidate-only non-S5 integration, with visual acceptance
still pending. This is not production promotion or final manuscript acceptance.

The coordinator independently replayed the unchanged H07 and H09 static
reviewers and complete combined reviewer under R 4.6.1. Every write from those
reviewers was intercepted into `/private/tmp/order72k-independent.o4jCUF`.
H07 passed 32/32, H09 passed 41/41, and the combined review passed 30/30, with
identical check tables to their sealed counterparts. The 28-member component
review manifest and every member stayed exact. The combined replay includes
all 123 release, 110 execution-input, and 2,548 preservation members.

The additional independent preflight passed 13/13. All 62 original source and
authority inventory rows are exact. The expanded integration input inventory
has 186 unique paths and pins current manuscript sources, table fragments,
existing table captures, bibliography/style resources, helper sources, all
20 accepted/historical SVG mapping inputs, and the three new static components.
Six directly relevant native SVGs parse with zero raster image, script,
foreignObject, or duplicate-ID nodes. S12 contains no MDER text.

H07 and H09 were actually dispatched once and returned. Their loopback bind
attempts failed before content load. The optional H11 comparison encountered
a browser policy rejection. All three visual leases are returned. Those
records establish no visual acceptance, and this disposition does not bypass
the H11 rejection or authorize its optional replacement.

One evidence-label discrepancy is nonblocking: `component_status.csv` labels
the optional H11 static result as 7/7, while the actual sealed check table,
combined checker and review correctly show 14/14. Historical files stay exact;
the optional H11 SVG remains unpromoted. The accepted S17 is retained.

The coordinator's temporary source evidence extractor initially assumed that
the selection source used the same literal figure containers as the manuscript,
then assumed every heading used a period after the figure number. Both local
harness stops are preserved under `independent_replay/attempt_01` and
`attempt_02`. The final extractor uses the exact observed heading forms.
No author source, output, model, scientific value, or historical record changed.

Brown is a separate held scientific dependency. The attempted derivative-gate
recovery dispatch was rejected and was not delivered. Neither this disposition
nor Order72k permits that checker, fits, prediction, science, or indirect
execution through another task. Final Supplementary Figure S5 is held.

The current selection source also retains historical Brown sleep wording that
conflicts with the approved placement decision. It must not be newly endorsed
by this display preview. Preserve its bytes as historical content and add the
explicit preview-status qualification specified in Order72k outside the held
figure/caption. No new Brown result or scientific claim is inferred.

Used: `$measurement-methods-audit` for read-only provenance/identity verification
and `$quarto-authoring` for the isolated render and visual boundary.
