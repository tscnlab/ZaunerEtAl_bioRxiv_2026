# Downstream static review

The prospective diff affects the helper's source/clone/visible-text guard only.
It adds a Node-core SHA256 import, fixed source contract, pure predicate and
descriptor collection/validation at the existing source/capture callbacks.
No dependency is installed or runtime/browser flag changed.

The following portions compare exactly with the preimage: active table specs,
all column and row partition settings, browser launch/context/navigation,
scientific-figure disabled loop and closure, image containment/aspect check,
exact body-cell comparison and all existing visible-cell selectors. Static
tests also show no extra browser, new-page, navigation, set-content or capture
invocation. Source/capture extraction remains inside the original invocation.

The same validation logic necessarily appears in isolated browser callbacks
and in the Node source gate. The static test extracts those production
functions and requires all copies to match after indentation normalization.
The test does not import or execute the full capture helper.

The existing Word helper prepare_word_manuscript.py manifest_map function
reads only each entry's key and files[].path and checks that files exist.
The prospective helper retains that interface and adds only verification
metadata. No Word helper, page geometry or SVG embedding changes are needed
for this guard correction. Actual image dimensions remain determined by the
future successful capture and must undergo the existing intended-size review.

The historical prepare_correction_manifest.py intentionally still records
attempt2 and width1400. It is prior evidence, not the current S2 path contract.
Do not rerun or edit it to make it current. A future release needs a new exact
mapping to the successful new capture manifest and the preserved accepted
S5/S6/S10 parts, without overwriting any prior trial record.

All 335 owner files, both live CSS postimages and all 2,309 historical rows
remain exact at the end of this static work. No browser/Word surface or server
was opened and no owner implementation file was changed. Writer remains idle
with lease003 closed. A central seal, explicit new capture allowance and fresh
lease are still prerequisites to implementation.

Limitations: this is not DOM execution or visual QA. Synthetic computed-style
fixtures test the fail-closed predicate, not the actual styles returned by the
pinned Chrome runtime. The prospective capture checks those actual styles
before any PNG. SVG/native Word review remains pending under the original
integration scope and the existing unlock requirement.
