# Independent disposition: exact S2 accessibility-guard recovery

Date: 2026-09-11. Coordinator accepts the stopped state and prospective
guard-only implementation. This is not S2 visual or document acceptance.

The central stopped acceptance and its 353-member seal remain unchanged.
Writer remains idle after attempt4; visual lease003 is closed. The attempted
capture generated its source HTML only and no PNG or completed manifest.

Coordinator independently repeated installed-Node syntax checking and the
404 extracted-function/synthetic tests in a fresh temporary directory. R 4.6.1
then verified the original 19-member proposal seal, reproduced the immutable
17-record source contract from the frozen S2 HTML, and applied the reverse
patch with zero fuzz. It recovered helper7f5c exactly from prospectiveecf752.
All 335 stopped owner members and 2,309 historical rows were rehashed exactly,
with only the three already authorized version-specific preimages.

Durable byte-exact proposal copies are in `prospective/`. The original TEMP
manifest and seal are retained as historical provenance, not as a dependency
on the future availability of that temporary directory. Independent replay
code, outputs, exact reversed helper, commands and session details are retained
in `proposal_replay/`. `durable_copy_mapping.csv` records every copied file.

Review covered the complete forward diff, synthetic tests, source descriptor
construction, and downstream static review. The guard's exact span identities
are preserved during overflow clearing. Only text belonging to those same
validated hidden spans is exempt from visible geometry checks. Every visible
cell, including Unit, Scaling, distribution text, header and note, retains all
four containment boundaries. Widths, fonts, partition ranges, payloads, browser
calls and capture/manifest interface remain unchanged. No browser was launched
by the proposal or this independent review.

Computed-style fixtures are synthetic and cannot establish actual browser
layout. Accordingly, one newly explicit attempt5 must still pass the runtime
source/style, containment, image, R cell/payload and original/intended-size
visual gates before document completion. A failed runtime gate has no retry
allowance. No scientific computation, Brown fit or S5 replacement is released.

The separate owner order is
`audit/report_harmonization/owner_orders/72k_s2_accessibility_guard_recovery_001.md`.
Dispatch requires its exact release seal, Writer idle confirmation, a fresh
exclusive visual lease004 and an actual owner-delivery receipt. Neither this
record nor creating the order is evidence that Writer has started.
