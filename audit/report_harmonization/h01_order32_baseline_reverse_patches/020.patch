# order32 file-change event: exec-086a9ef6-11ee-49fb-83fd-c724936b47f1
--- a/audit/hypotheses/H01/H01_analysis_preparation.qmd
+++ b/audit/hypotheses/H01/H01_analysis_preparation.qmd
@@ -917,6 +917,6 @@
   "Predictor centres", "Recorded separately within the permitted analysis frame", "Makes zero correspond to the recorded analysis-specific mean while preserving original photoperiod and latitude fields.",
-  "Participant-level engine", "lm, maximum likelihood", "Fits one outcome per participant without a redundant participant random effect.",
-  "Gaussian participant-day engine", "lmer; ML for comparisons, REML for final estimates", "Accounts for repeated participant-days with a participant random intercept.",
-  "Tweedie participant-day engine", "glmmTMB, log link, maximum likelihood", "Handles non-negative continuous outcomes with a mass at zero without silently changing the response family.",
-  "Site-average marginalisation", "Equal weight for every represented site", "Defines the site-average estimate and prevents large sites from dominating the site contrasts.",
+  "Participant-level engine", "Participant-level model (ordinary Gaussian linear model, lm), maximum likelihood", "Fits one outcome per participant without a redundant participant random effect.",
+  "Gaussian participant-day engine", "Participant-day Gaussian mixed model (lmer); ML for comparisons, REML for final estimates", "Accounts for repeated participant-days with a participant random intercept.",
+  "Tweedie participant-day engine", "Participant-day Tweedie generalized mixed model (glmmTMB), log link, maximum likelihood", "Handles non-negative continuous outcomes with a mass at zero without silently changing the response family.",
+  "Site-average marginalization", "Equal weight for every represented site", "Defines the site-average estimate and prevents large sites from dominating the site contrasts.",
   "Confidence level", "95%", "Used for model effects, site contrasts, R² summaries, and sensitivity estimates.",
@@ -926,3 +926,2 @@
   gt::gt() |>
-  gt::cols_label(Why_separate = "Why separate") |>
   gt::cols_width(
@@ -1177,7 +1176,3 @@
   gt::gt() |>
-  gt::cols_label(
-    Entry_point = "Entry point",
-    Main_outputs = "Main outputs",
-    Run_when_page_renders = "Runs when page renders"
-  ) |>
+  gt::cols_label(Why_separate = "Why separate") |>
   gt::cols_width(
@@ -1213,2 +1208,7 @@
   gt::gt() |>
+  gt::cols_label(
+    Entry_point = "Entry point",
+    Main_outputs = "Main outputs",
+    Run_when_page_renders = "Runs when page renders"
+  ) |>
   gt::cols_width(

