# order32 file-change event: exec-e22a293d-2bbe-4b02-bcc1-7047f9feb081
--- a/audit/hypotheses/H01/H01_analysis_preparation.qmd
+++ b/audit/hypotheses/H01/H01_analysis_preparation.qmd
@@ -312,3 +312,3 @@
 #| label: tbl-h01-prep-inputs
-#| tbl-cap: "Verified model-data inputs entering the H01 implementation."
+#| tbl-cap: "Verified analysis datasets entering the H01 implementation."
 
@@ -323,12 +323,23 @@
 input_summary |>
+  transmute(
+    Dataset = .data$Input,
+    Role = recode(
+      .data$Input,
+      `Primary dataset` = "Primary metric analysis",
+      `Gap-timing-unaware dataset` = "Sensitivity to remaining-gap timing"
+    ),
+    Verification = recode(
+      .data$Status,
+      PASS = "Verified",
+      CHECK = "Review needed"
+    )
+  ) |>
   gt::gt() |>
   gt::cols_width(
-    Input ~ gt::pct(20),
-    Path ~ gt::pct(34),
-    Manifest ~ gt::pct(31),
-    `SHA-256 prefix` ~ gt::pct(11),
-    Status ~ gt::pct(4)
+    Dataset ~ gt::pct(28),
+    Role ~ gt::pct(52),
+    Verification ~ gt::pct(20)
   ) |>
   gt::tab_source_note(
-    "Both manifests were checked row by row against the current files during rendering."
+    "Exact paths and SHA-256 identities are retained in the technical provenance section."
   ) |>
@@ -336,16 +347,2 @@
 ```
-
-<details>
-<summary>Show the complete pinned manifest identities</summary>
-
-- Primary model-data manifest:
-  `r input_contract$main$manifest_sha256`
-- Gap-timing-unaware model-data manifest:
-  `r input_contract$manuscript_prepared_data$manifest_sha256`
-- Shared H01 implementation contract:
-  `r input_contract$implementation_contract_sha256`
-- Shared implementation source:
-  `r input_contract$shared_implementation_sha256`
-
-</details>
 
@@ -353,3 +350,3 @@
 
-Each H01 data bundle contains the 17-metric contract, long model rows, sample
+Each H01 analysis dataset contains the 17-metric contract, long model rows, sample
 flow, exclusions, predictor centres, scenario availability, a variable

