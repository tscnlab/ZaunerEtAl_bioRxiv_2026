# Order011 final website candidate return

14 September 2026. Harmonizer to Coordinator only.

Status: **CANDIDATE_COMPLETE_FOR_INDEPENDENT_ACCEPTANCE**. No live promotion has occurred. Writer remains finished and was not woken. No other owner or subagent was dispatched.

## Delivered candidate

`candidate_build/` contains exactly 914 files. It combines the accepted C manuscript and complete supplementary content with the two existing website shells. The exact accepted final Word, nineteen native editable tables and two unchanged passage-change files are linked as separate download utilities. The supplementary utility includes all twenty ordered position/old/new rows. The scientific search index contains 942 unique records across exactly 37 accepted routes.

The complete website identity set is `evidence/candidate_inventory.csv`, SHA-256 `0610b27e315cf73fd2d55bb4116d87c6706ad7de92d21e5f5dfd767034dbb862`. The independently recomputed postflight inventory has the same hash.

| Future website target | Bytes | Candidate SHA-256 |
|---|---:|---|
| `index.html` | 25,367,535 | `588bf4f60c7e1ca9cf409e86696632b8a14cc9d79e8aecf2aa105c8190b7cdd2` |
| `supplementary_information.html` | 15,674,678 | `649bbc1796cad3bbd2ff1b372a46599a297cef98e3a338b4985824bb5764c53e` |
| `ZaunerEtAl2026_NatHealth_phase3_brown.docx` | 14,372,157 | `325c3a8e3a76ea225970f80e177ceed0bbdd592b72e79196d154597f3581250b` |
| `search.json` | 2,350,000 | `7469b089dddcedf93b7acd7e414ca1f200180d134a9edffdbf7224db7925c686` |

The exact 25-file future website allowlist is `evidence/website_promotion_manifest.csv`: four replacements and twenty-one additions. All nineteen DOCX additions match the accepted C native-table identities. The two passage files retain the accepted CSV/Markdown bytes. The full Word is solely the accepted pagination-completion document, not the superseded candidate or a QA PDF.

## Implementation and preservation

The controlling Order011 and dispatch47 hashes were replayed. Preflight verified the accepted proposal25, C271, final145 and all1298 input pins. Five exact future-replacement backups were taken and all twenty-one future additions were absent from live paths.

One initial static packaging pass and the one allowed consolidated correction pass were used. The correction addressed two integration-slot layout defects: the manuscript's imported grid required C's accepted main grid classes, and the utility panel had to be a section rather than an aside to avoid Quarto's margin-placement behavior. The first candidate's changed files, implementation and checks remain under `attempts/01_initial_candidate/`. No third candidate was made. The correction changed only the two candidate HTML files, without changing scientific content, shared shell files or live paths.

The raw operation ledger and exact reverse checks are in `evidence/raw_operation_ledger.json`. The candidate preserves all 889 unaffected baseline files and all 35 non-entry report HTMLs byte-for-byte. Existing navbar, footer, page navigation, shared scripts and route-specific shell behavior remain. No source QMD, shared CSS/JS/profile, package, lockfile, model, figure source, manuscript alias, central live corpus or remote resource was edited. No rendering, scientific recomputation, Office conversion, commit, push or upload was performed.

## Verification

- Static verification: 53,703 checks passed. Changed entry pages have clean IDs and intended table IDREFs; unchanged reports retain their exact accepted duplicate-ID multisets. Every new local reference resolves. The only retained misses are the exact four inherited Datatype.woff2 references.
- R 4.6.1 reconciliation: all 75 checks passed. Exact C reader text, all nineteen index and sixteen supplementary table structures/cells/annotations, approved Table 2 and Table 3 displays, S2 typography/column contracts, seventeen S2 distributions, 170 complete mean/SD runs, all twenty revision rows and the SVG payload/crop contracts are preserved. Package versions and the actual command are recorded in `evidence/content_R_sessionInfo.txt` and `evidence/content_R_provenance.txt`.
- Figure-count distinction: C HTML has 23 SVG image elements with 23 distinct payloads, all preserved. The accepted expanded map counts 24 delivery appearances because Word assembly uses S8 twice. The candidate does not invent a duplicate HTML image. The supplement has 20 SVG image elements in seventeen figure groups.
- Browser verification: complete entry-page inventories and display review at 1440, 708 and 390 CSS pixels; local table scrolling, rightmost S2 distributions, utilities, all twenty revision rows, navbar, contents and search interaction checks passed. Preparation 01 and main hourly H06 passed unchanged desktop/narrow smoke checks. See `evidence/browser_qa_report.md`, the 267-row capture-identity ledger and `evidence/browser_measurements.json` for exact observations and limitations.
- Download verification: all 22 loopback GET/HEAD targets returned exact accepted bytes with status 200.
- Postflight: 3,632 checks passed at `2026-09-14T12:54:35.793268+00:00`, including all 1,771 recorded preflight hash rows, the complete live893 and candidate914 sets, protected absences, the five exact backups and unchanged live preimages. The candidate inventory is unchanged by QA.

The preview server is stopped, the port has no listener, only the task-created tab was closed and the viewport reset method was called. No browser screenshot file is fabricated: actual tool captures are identified by timestamp, route and dimensions, with one invalid and five limited records called out in the QA report.

## Corpus, authority and future rollback

`evidence/phase4_corpus_manifest.prospective.csv` is an evidence-only postimage. Exactly the two changed entry-page HTML-hash cells differ; all 37 historical source-hash cells and other bytes are preserved. `evidence/prospective_corpus_reverse.csv` supplies their exact old/new values. The live corpus still has SHA-256 `01a2fdc1f1d11db45e28834893c79d620aa5321be1f49cff612b34589e068008`.

`evidence/source_artifact_authority.csv` distinguishes the historical accepted report renders, current sources, the accepted C static packaging and final Word authority. Historical source/render provenance differences are not turned into new source acceptance. This return does not claim a fresh 37-report render or a fully offline website.

`evidence/five_backup_manifest.csv` binds the four website replacement preimages and the corpus preimage to exact files under `backup/`. The twenty-one additions remain absent live; any later rollback must address only those exact additions and the five backed-up replacements under the centrally released promotion operation. Nothing has been removed from live paths in this task.

## Central next step

Independently verify `completion_manifest.csv`, `completion_seal.json`, the 914-file inventory, exact25 website allowlist and prospective two-cell corpus postimage. The completion manifest excludes itself and its seal to remain non-circular. It includes this return, all candidate files, helpers, evidence, backups and the preserved initial attempt.

Only a separate central release of these exact accepted postimages may authorize live promotion and the short production check. There is no remaining Writer action and no new author choice requested by this candidate return.
