# A4 candidate HTML visual inspection release

14 September 2026. Continuing Writer task019ffb39-372e-7262-bfac-192751fd0e63. Status RELEASED_ONCE only upon coordinator dispatch after the exact Order013 teardown check. This order permits read-only candidate browser QA, not another render, edit pass, website update or final scientific acceptance.

## Frozen candidate

Read `writer012_nonbrowser_independent_review.md` and its seal. Candidate root is `audit/manuscript_nature_health/a4_display_revision_2026_09_14/`. Preserve every member of `word_candidate_manifest.csv`, SHA-256 `996e138233eff74da4754aae6c2fbc1a6b5785c9d196fa2c64df2fe8eeccb464`, exactly318 unique members. Preserve `word_candidate_handoff.md` at `277a7f0646ccdfd3fa3cbce1a30762b1826e0d458b74a35e3ab6cc52e1886fc4`.

The exact reader candidate is `html_candidate_round2/ZaunerEtAl2026_NatHealth_phase3_brown.html`, SHA-256 `d7ee926c0910227004581915040f30ae01142290f0759839e897fa2b3f5f10aa`. Main Word stays `6f0ce7a50b608f91d24c31ae0b9b0edefe15828ed4f966732ea616c6e395a570`. All19 native exports and accepted C/N history remain fixed. Do not edit the old candidate manifest or handoff merely to change its historical browser-held wording.

## Serial safe point and allowed writes

Order013's durable `audit/report_harmonization/final_site_promotion_2026_09_14/evidence/production_safe_point.json` records13:47:36 UTC, port55182 closed, task tab3 closed, viewport reset and live914/candidate914/owner1026/fixed closure exact. The coordinator independently repeats the closed-port and live/candidate identity checks before this release. Harmonizer reports no further browser use. Its final prose sealing can continue disjointly; do not contact or interrupt it.

Create new QA scripts, observations, captures, lifecycle, checks and final QA handoff only under `audit/manuscript_nature_health/a4_display_revision_2026_09_14/html_visual_qa/`. A temporary directory under private/tmp may hold only an exact rendered candidate copy and any strictly required accepted rendered resources. Record and remove only that task-owned serve copy after teardown. Do not serve the project root or a parent of the rendered output. Serve only the exact HTML route and indispensable read-only resources; do not expose the old/new delta fragments or source/evidence directories through directory listings.

Before serving, rehash all318 members and relevant accepted source/profile inputs; inventory the chosen narrow output root and perform mandatory symlink preflight. A failed symlink or identity check stops without browser use. Use one GET/HEAD-only server bound strictly to127.0.0.1 and one unused high port. Use the supported internal browser and ordinary permission route, never a profile bypass, remote tunnel, different browser or raw CDP. Record exact command/root/PID/port/URLs and actual viewport dimensions. If access is rejected, report that exact rejection without a workaround.

## Complete bounded visual check

Inspect all six changed endpoints: `supp-table-s4`, `supp-table-s7`, `fig-s15`, `fig-s16`, `fig-s17` and `fig-s18`. Cover desktop1440x1000 and phone390x844, plus both tables at708x1000. Use actual supported viewport overrides, screenshots and normal reader scrolling. Inspect each complete table, caption and notes, including both horizontal edges if scrolling is needed. Inspect each complete affected figure and its caption, the associated heading and Contents entries, and the two changed Results references. An unchanged `fig-s8` reader check may confirm its single complete appearance; do not change it.

For S4, require four retained outcome rows, four columns, the complete four-test family note, natural column wrapping, no clipped values or page overflow. For S7, require one continuous17-metric/eight-column semantic web table, complete exact two-line sample cells, all group labels/notes, deliberate matching column widths and working contained horizontal scrolling. Do not impose the Word two-page split on HTML. Confirm no duplicate/new missing IDs, correctly resolved local references and intended S15/S16 separation followed by S17/S18. Use existing R4.6.1 checks for content and structure with new evidence-output routing only; do not silently replace semantic inspection with text searches.

Verify no new page-attributable console error, missing image, broken scroller, clipped table content, overlap or unexpected page-level horizontal overflow. Native small S2 secondary type is accepted and not in this correction scope. Source SVGs, all23 image identities, every value/interval/decision and the approved20-row prose history are frozen. Do not alter the inherited biological-sex SVG footnote qualification while inspecting it, and do not claim it was repaired.

## Teardown and return

Stop the exact localhost process, prove no listener remains, close only the task-created tab and reset viewport overrides. Rehash all318 original members and the served copy before removal; the candidate, main Word, native19 and scientific sources must remain unchanged. Retain actual screenshot provenance and any limitation honestly. A successful HTTP response or static DOM check alone is not visual acceptance.

Return one new non-circular QA completion manifest and a concise exact handoff for independent review, preserving the original318-member package. No Quarto, Pandoc, QMD execution, Office rerender, scientific computation, source/artwork/table edit, package/lock/profile change, website promotion, live corpus change, commit or upload. If a genuinely new visual defect appears, return one consolidated endpoint-specific finding without patching or rerendering under this inspection-only release. Any later website integration must use the accepted minimal delta against completed Order013, not replace the website from this standalone C-based HTML.
