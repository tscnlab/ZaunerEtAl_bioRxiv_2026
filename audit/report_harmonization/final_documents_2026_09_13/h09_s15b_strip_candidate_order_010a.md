# H09 S15B strip-label candidate correction

14 September 2026. Owner: H09 task 019fdc1b-b927-7fb1-ac61-88993c0a818a.
Status: RELEASED_FOR_ONE_BOUNDED_CANDIDATE_AND_VISUAL_RETURN after the accompanying dispatch seal passes. This is a new correction order, not resumption of the old Order72j visual lease.

## Authority and inputs

The author requested coordinated corrections of the final manuscript tables and reader-facing layouts. Writer's complete-page and browser review confirms three clipped S15B top-facet labels. Independent coordinator R 4.6.1 review reproduces the exact geometric cause. Controlling disposition is `writer009_independent_layout_disposition_010.md` in this directory, with exact identity in the accompanying non-circular dispatch manifest.

Read only the frozen candidate SVG at `audit/hypotheses/H09/report018_order72j_split_svg_export/candidate/H09_observed_timing_patterns.svg`, SHA-256 `c937252509c5de9448944358a9846842c973bac26b0f0fb4640728fcf8a908b3`. The paired display CSV `artifacts/11_source_data/H09/H09_observed_timing_patterns_data.csv` remains SHA-256 `34640aba210181b973902e00cd6924f79f6ae76faf01fa3c5b66078e2e2e7cee`. Hash it for protection; this correction does not require reading its rows. Keep the primary-effects companion SVG `a3f9bdb969c075d33adf9a0e6e751a1f967d5111856fcd0db07cbc36575a745a` unchanged.

Do not run `build_h09_stage3_observed_figure.R`, the old 72j export entrypoint, any model or RDS reader, a plotting builder, scientific verifier that executes analysis, Quarto, Pandoc, or the manuscript assembler. No fit, prediction, bootstrap, simulation, statistical summary, interval or p-value calculation is authorized. No historical source, QMD, HTML, native document, accepted figure, script, manifest or lockfile may be edited.

## Write boundary and exact change

Create only the new owner root `audit/hypotheses/H09/manuscript_s15b_strip_layout_2026_09_14/`, with `code/`, `candidate/`, `evidence/` and `qa/`. Fresh explicitly recorded `/private/tmp` preview directories are allowed. Preserve all old Order72j and Writer Order009 files byte-identically.

Use an R 4.6.1 script for a literal, fully reversible SVG transformation. All three selected labels retain their original full string `MCTQ MSFsc (hours)`, font family, bold weight, 17.00px size, textLength 172.83px, x/y positions and second-line metric labels. Do not alter any text or scientific graphical mark.

Each selected strip has exactly two matching rectangles: its clipPath rectangle and its visible grey background. Change only their x and width attributes, centered on the unchanged label:

| Label center x | Original rectangle x | Original width | Candidate x | Candidate width |
| --- | --- | --- | --- | --- |
| 185.13 | 105.65 | 158.96 | 89.13 | 192.00 |
| 404.21 | 324.73 | 158.96 | 308.21 | 192.00 |
| 623.28 | 543.80 | 158.96 | 527.28 | 192.00 |

All six rectangles retain y=86.06 and height=47.66. Keep their style, clip identifiers, node order and every remaining byte unchanged. The labels gain 9.585 units of horizontal inset. Adjacent widened strips remain separated; no plot panel, axis, data coordinate, canvas dimension or background outside these three strip bands moves. The right-hand boxplot strip and every lower MEQ strip stay exact.

Before writing, prove exactly two old rectangle-prefix matches for each row and no unexpected match. Write one candidate `candidate/H09_observed_timing_patterns.svg` and preserve the complete forward/reverse patch evidence. Require exact byte reversal to the accepted input hash, not merely equivalent DOM. This is the sole source-asset candidate change. Do not silently extend the patch if a precondition fails.

## Complete validation and QA

Before visual inspection, require all of the following in R 4.6.1: exact input hashes; exactly six rectangle changes; exact reverse; identical text-node sequence and attributes; all non-selected SVG markup exact; no duplicate IDs or unresolved SVG references; original dimensions/viewBox; all three full label extents inside the enlarged rectangles; no strip-to-strip overlap; the unchanged fourth matching label complete; protected original SVG/CSV/primary SVG exact. Record R and consequential package versions and exact commands. Generated evidence may use scoped infrastructure helpers, but no analysis runs.

Then inspect the actual native candidate SVG at original and intended manuscript display sizes, including 600 CSS-pixel width, and a complete desktop/narrow view. Check complete labels, legibility, the extended grey headers, clean strip boundaries, unchanged axes/panels/legend, and no clipping/overlap. Do not claim these as visual PASS based only on geometry. The current supported in-app browser and normal permission requests are authorized. This new scope supersedes the old lease's no-escalation binding restriction only to allow the normal approved permission mechanism for this local read-only preview. It does not override an explicit tool-policy denial or permit a browser workaround.

Before serving, hash candidate and original inputs and inspect all symlinks. Serve only a tiny published wrapper and this SVG on one GET/HEAD-only `127.0.0.1` endpoint. Never expose the data CSV, repository or evidence directory. Record actual viewport and image sizes, screenshot identities, server PID/port/root. Stop the listener, close task-created tabs, reset temporary viewport settings, prove no listener and rehash. Do not revive any old preview process or port.

One candidate and one bounded visual return are released. If the exact transformation exposes a genuine visual problem, retain it and return a consolidated finding without another source change. Permission-required is a normal approval state, not itself a failed output. A denied action remains denied.

## Return and holds

Return a non-circular manifest with the new script, input identities, exact six-row change map, reverse proof, candidate, complete source/geometry/protection checks, actual browser findings and teardown. Candidate acceptance remains with the coordinator. Notify the coordinator only; do not replace Writer's embedded asset or wake the Writer yourself.

Writer packaging, final HTML/Word production, live manuscript, website, H09 result/companion and all scientific files remain held under their existing authority. S2 typography is expressly outside this H09 scope.
