# Corrected manuscript and table candidate production

14 September 2026. Owner: Writer, task 019ffb39-372e-7262-bfac-192751fd0e63.
Status: RELEASED_FOR_BOUNDED_CANDIDATE_PRODUCTION_AND_FULL_PAGE_QA.
Controlling author instructions: coordinate the table corrections; retain one primary-sample main Table 2; prioritise complete-page layout; and "S2s small secondary text size is good enough for me".

## Accepted inputs and output boundary

Independent acceptance 008 is controlling: record SHA-256 `bf98c2ac5ed302f2632134d0d3a5f6dacbafddf547fa924607a96779ac91575c`, 55-row non-circular manifest `381a466441be50a3f88afbe9b8ae875f913e6302d870b647eae009faa77373ab`. All 263 owner-package members, 3,266 source/content checks, and 854 visual-evidence checks independently passed, and the actual six localhost pages were inspected. The existing S2 secondary text size is accepted, not a new permission gate. Do not reduce or enlarge it as a purported remedy.

Use the immutable selected root `audit/manuscript_nature_health/table_layout_visual_corrections_2026_09_14/attempt_02/`, under root manifest `1b5deb90c0c11cd0e931e6b13c00fedf5551920b9be1e890f86ba878ccfceb3b`. Its main QMD is `d711b2950ea8f7f893c3c09b0e7a015f657d59d1d7f966fbf7f8f89c2ab88ab8`; SI outline is `f0fa949f1a10dc0dfef3fac9fa03be9225d45966c1c25c1331250dc176cef6bb`. These are logical overlays, not a runnable project at their present location. The dispatch manifest pins the actual files and dependencies.

Create only the new candidate root:

`audit/manuscript_nature_health/final_review_production_2026_09_14/`

Call this absolute project-relative destination P below. Writes are limited to P and explicitly recorded fresh scratch/cache/QA directories under `/private/tmp`. Under P, use `project/`, `helpers/`, `production_images/`, `maps/`, `editable_tables/`, `qa/`, `deliverables/`, and `evidence/`. Retain meaningful initial failures and versions with distinct names. Never write into the sealed Order 007 package, older production packages, live manuscript/SI, canonical native documents, `_build/nathealth`, profiles, scientific artifacts, package libraries, or lockfiles.

This is an implementation-and-completion order, not a request for another proposal-only turn. Finish staging, required helper adaptations, static preflight, permitted narrow production, and full-page QA in this same bounded owner continuation. Record exact staged commands and input/output pins before execution. A source/scientific mismatch, specific tool denial, or genuinely unresolvable production defect still stops the affected action. Complete unaffected in-scope work and return one consolidated finding set.

## Frozen representation contract

Use the accepted maps, including `part_count_contract.json` at `07e6f1ff9fbd2d7746317159cfe96a41161326366698ebfb6c01aba1335406e5` and `complete_drawing_order.csv` at `ca062248f9dffb69e04c9dc36d26ac36b74c59967c4d4da39faf3841a23a4e7a`.

- Exactly 19 editable/native DOCX files, each containing its one real table.
- Exactly 30 table-image parts and 24 figure appearances from 23 unique accepted SVG sources, for 54 manuscript drawings.
- Six changed image parts only: Table 2 part 1; S2 parts 1, 2, 3; S7 parts 3, 4. Reuse the other 24 image parts exactly, including S7 parts 1 and 2.
- Export only Table_2 and Table_S2 as new native tables. Reuse the other 17 native DOCX files exactly, with explicitly rebased provenance, never claiming a new export.
- Table 2 is one primary-sample table, not Table 2A/2B. Preserve all 407 checked cells, all 170 complete mean-plus/minus-SD units, the 17 original S2 PNG payloads, all notes, intervals, p-values and labels.
- Figure S5 has the accepted A/B appearances. Preserve S7/S15 A/B, both S8 crops from one source, S17, all established captions and accessible equivalents. Do not resurrect 52-drawing or 22-source guards.

Preserve the inherited table widths and maximum heights from acceptance 008. In particular, the current S2 secondary sizes must not be reduced further. Do not invent a 170-mm placement for these wide tables. Final Word review checks actual placement, clipping, pagination, missing plots and unintended scaling, rather than reopening the accepted S2 size choice.

## Staging and helper adaptations

Copy only the required dependency closure into P/project, preserving the logical `manuscript/R0_NatHealth` location and copied `assets/`, relevant static `audit/` fragments, bibliography, CSL and local Lua dependencies. No full repository or analysis-tree copy. Resolve every render input before execution. The accepted source overlays take precedence over stale live prose. Permitted source changes are literal path relocation and the staged standalone SI wrapper's subtitle, changed to the accepted main title: "The health-relevant architecture of the everyday light exposome". All other prose, numerical strings, citations, authors, 14 affiliation markers, declarations and approved editorial records are frozen.

The local manuscript project is `type: default`, with execution disabled. The staged main QMD includes the SI outline. Confirm recursively that its render dependency closure contains no executable R/Python/Julia cell, inline computational expression, model read, or scientific regeneration call. `--no-execute` alone is not proof of this. Do not enable an analytical engine. Keep the manuscript's static HTML, DOCX reference and author-block settings, while rebasing local references to copied dependencies. Do not inherit root website pre/post-render commands.

Adapt new copies of the existing `run_stage.py`, `stage_candidates.py`, `prepare_word_manuscript.py`, `embed_accepted_svg_figures.py`, and `export_editable_tables.py` as necessary. Never run their old hard-coded entrypoints unchanged. Preserve the accepted native-SVG compatibility logic. Apply only the already reviewed caption spacing, single-spaced table paragraphs, exact empty-section cleanup, S7 continuation placement, S2 width/whole-unit handling, current single Table 2, S5 A/B and the resolved 23/24/30/54 guards. Keep S2's accepted 14-column vector `[210,86,138,146,146,154,138,146,138,146,138,130,120,252]` and the current Table 2 grid. The old optional one-point S2 reduction is not released.

The first four helper baselines are the accepted copies under `audit/manuscript_nature_health/revision_2026_09_11/consolidated_display_repair_order72k/helpers/`. In particular, assembler `e89329f6...` and embedder `75432be9...` identify those copies, not their older same-named files in `scripts/manuscript_nature_health/`. The exporter baseline is `scripts/manuscript_nature_health/export_editable_tables.py` at `4bf3a4aa...`. Preserve all baseline and live helpers; adapt only new copies under P/helpers. The archived prospective diffs are implementation guidance, not executable entrypoints or permission to restore obsolete output-count guards.

Record exact helper diffs, parse/AST checks, source reversal for path-only relocation, protected input inventory, resolved commands and finite writes. Old deliberate stop guards may be replaced only by the complete new checks specified here. Preserve checks for semantic content, number of outputs, full row partitions, relationships and input identities. Do not merely delete failing guards.

## Six production image inputs

The full-page review is accepted. A cropped browser screenshot is not a new author-acceptance gate, and no author-supplied cropping is required. The existing image-based Word assembler nevertheless needs six current, source-matched table-only bitmaps. This order explicitly releases their preparation in P/production_images.

Use the already permitted internal-browser route and/or the immutable, complete source-matched screenshots. Never run the old helper's independent Chromium/Playwright entrypoint, use another browser to evade a denial, or relabel a whole viewport JPEG as a table PNG. Preserve the full screenshots and truthful MIME and original dimensions. No source table, scientific plot or image payload may be regenerated or redrawn.

R may derive a table-only PNG by exact integer-pixel selection from the R-decoded original screenshot. If a verified one-to-one DOM mapping exists, use it. If it does not, do not guess a viewport scale factor: locate the actual complete table border and final notes directly in the full-resolution bitmap, record those pixel bounds and how they were identified, visually verify the full selected rectangle against the source-matched whole screenshot, and verify decoded output equals that exact pixel subset. This bounded production-pixel method supersedes the optional 007a viewport-equality guard only for these six new outputs. It does not permit interpolation, rescaling, sharpening, stitching, AI reconstruction, cell/text replacement, or omission of notes. Account for the existing JPEG compression honestly; PNG output is not a claim of a native lossless browser export.

A supported fresh whole-page screenshot may be taken when needed, using the same six immutable pages, exact source hashes, normal browser permission flow, stable compositor/viewport state, and the bounded symlink-checked loopback procedure. Verify actual output dimensions, rather than assuming the requested viewport or filename. If a complete source-matched image cannot be verified, leave that image unaccepted and continue the unaffected native-table and HTML stages; report the exact technical blocker together, without reviving a blanket browser-access stop.

Merge the six accepted image outputs and the 24 exact reused parts into one complete 19-key/30-part manifest before main assembly. Verify S2 and S7 row partitions `[0,23)` once, without omissions or duplicates, and the final-part notes. No partial tail-only manifest may be passed to the assembler. Final production images must contain only their table content and applicable notes, not audit wrappers, filesystem paths, hash labels or browser controls.

## Narrow rendering and assembly budget

Use the installed Quarto executable `/Applications/quarto/bin/quarto`, confirmed version 1.9.37. All calls run from P/project/manuscript/R0_NatHealth with a fresh output directory under that copied project. Use the established fresh writable scratch and Quarto cache paths outside the project, `RENV_CONFIG_AUTOLOADER_ENABLED=FALSE`, and the accepted R 4.6.1 library if an environment probe is necessary. No activation, installation, restore, profile bypass mutation or scientific execution is permitted.

Initial render commands are limited to:

1. `quarto render ZaunerEtAl2026_NatHealth_phase3_brown.qmd --to html --no-execute --output-dir render_html_round1`
2. `quarto render ZaunerEtAl2026_NatHealth_phase3_brown.qmd --to docx --no-execute --output-dir render_docx_round1`
3. One optional separate static SI wrapper HTML render, only if required for the candidate's explicit standalone SI deliverable. It does not authorise another site route.

Use the bundled Python `/Users/zauner/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3` for non-analytical OOXML/export infrastructure. Resolve the workspace document dependencies and follow the document skill's operation marker immediately before the first document-authoring operation. No system Python package installation.

After integrated HTML content/semantic PASS, seed the 17 reused native files and metadata; invoke the adapted exporter once for `--only Table_2 Table_S2`, with no size reduction. Package exactly 19 DOCX files plus README, table_manifest.json and export_provenance.json. After all six production images and native table checks pass, assemble the main DOCX once using the 30-part manifest and explicit part-count contract, then integrate the 23 accepted SVGs into the 24 mapped appearances once. No scientific SVG regeneration.

Run packaged `render_docx.py` serially, with `--dpi 150 --emit_pdf --verbose`, unique QA directories, and the bundled `/Users/zauner/.cache/codex-runtimes/codex-primary-runtime/dependencies/bin/override/soffice`. Never use desktop LibreOffice. Initial required order is native S2, native Table 2, then the whole main manuscript. The renderer can internally attempt DOCX-to-PDF, DOCX-to-ODT, and ODT-to-PDF, so three document commands allow at most nine logged serial converter subprocesses. Do not use the two-document parallel wrapper.

For the 17 byte-exact reused native files, reuse identity-bound prior complete-page visual QA where available. If a reused file has no such evidence, one serial QA command for that exact file is authorised, with no re-export. Record the missing prior-evidence reason first. This makes the maximum initial QA budget 20 documents and 60 converter subprocesses, not a requirement to rerender all 20. Inspect every page of every newly rendered document at normal print size.

One consolidated production-layout correction round is authorised inside P if the first outputs expose clipping, erroneous placement or a format-conversion defect. It may change only formatting/packaging helpers, image boundaries or staged layout, never accepted prose, data, science, table values, S2 text size or figure content. Rerun only affected stages into round2 destinations: at most one additional main HTML, one additional raw DOCX, one optional SI HTML, one changed-native export, one assembly, one SVG integration, and QA of at most the two changed native files plus the main DOCX. Preserve the failed round. A need beyond this finite layout allowance returns one consolidated defect package, not a science reopening or an unrecorded retry.

## Final verification and return

Verify exact source/table/figure provenance in R 4.6.1, all 19 native table texts and structures, all 30 table-image parts, all 24 figure appearances, 54 drawings, captions, cross-references, 13 top-level section starts, author/affiliation block, declarations, no missing glyphs, no blank section artifacts, and no unwanted scaling. S2 must retain all 14 columns, 170 whole summary units and 17 plots; S7 must retain four properly ordered parts and complete sample labels; Table 2 must remain one primary-sample table.

Inspect all rendered Word pages, not only extracted text or selected crops. Retain converter logs and distinguish converter behavior from native Word fidelity. The author need not open every intermediate table. A focused read-only native Word compatibility inspection of the new candidate is allowed through the supported UI when necessary for the retained SVG/crop appearances; do not save, print, alter fields or update links in that session.

Inspect the actual integrated candidate HTML through a GET/HEAD-only 127.0.0.1 server, with mandatory pre/post hashes and symlink check, at desktop and narrow widths, exercising contained table scrolling and checking notes, figures, navigation, semantic headers and links. Serve only published candidate HTML/assets, never P or the repository as a whole. Close task tabs, reset viewport overrides, stop the exact listener and prove teardown.

Return one non-circular candidate-production seal with exact inputs, command accounting, provenance, whole-page Word/HTML findings and output identities. No final delivery claim is made until this review passes. PDFs and page images are QA evidence, not substitute requested deliverables. Keep all live source/document/site outputs and historical seals unchanged for independent final acceptance and separately bounded promotion.

## Separate website boundary

This order produces the complete integrated manuscript-plus-SI HTML candidate, not the 37-route website. Do not run a root or full-profile render, copy a stale source set over the corpus, or reseal the live phase4 manifest. The Harmonizer receives a separate read-only reconciliation request for current source bridges, absolute identity reads, preserved thumbnails, final download bindings and a finite route/write plan. Its older Brown/capture holds are superseded, but its unresolved source-consistency checks are not waived. Website integration follows the candidate output identities and a separate exact serial release.
