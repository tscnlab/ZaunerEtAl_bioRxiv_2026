# Independent candidate browser inspection

14 September 2026. Coordinator read-only inspection of the exact Order011 candidate, supplementing rather than replacing the owner's complete browser review.

The supported in-app browser opened only the candidate at `http://127.0.0.1:54196/`. The server served exactly the candidate_build root, with mandatory zero-symlink preflight, GET/HEAD only, no directory listings and no outward resolved paths. The first sandboxed bind returned PermissionError before a listener existed. The same exact server command then used the normal narrowly approved permission route. It started as PID23635 at 13:03:32 UTC. No security setting was disabled and no browser surface was changed.

Five actual screenshot outputs were inspected in the tool conversation:

| View | Actual viewport | Observation |
|---|---|---|
| index Table2 left side | 1440 by1300 | The single primary-sample table, recommendation/descriptive and Work/Free columns are complete in a contained horizontal scroller. The surrounding prose and right-hand contents panel do not overlap. |
| index Table2 right side | 1440 by1300 | Horizontal scroll reached281 of281 px; Free-minus-Work estimates, intervals and adjusted p column remain complete, readable and accessible. |
| supplement S2 left side | 708 by1300 | Body16px and accepted small secondary text remain intact. Table width2088px is contained inside642px, without page overflow. |
| supplement S2 right side | 708 by1300 | The local scroller reached1446px, exposing rightmost site fields, scaling labels and the distribution column.17 distribution images are present; the visible plots and small labels are complete. |
| supplement download utility | 390 by1300 | All19 editable-table links, full-manuscript Word link, passage CSV/Markdown and reciprocal manuscript link are clearly separated and fit the page.20 revision rows remain in the document. |

The measured document width/scroll width pairs were1425/1425,693/693 and375/375px. The screenshots are actual emitted tool images, not invented local screenshot files. This is a five-view independent check, not a claim that the coordinator reread every owner capture or every duplicate table cell.

A first live selector using a relative href prefix returned zero links because the browser DOM exposed absolute same-origin hrefs. The visible links were present. An exact same-origin path-based observation returned all19 correct labels/targets. This was a read-only selector mismatch, not a missing-download defect. Static source/link and22 GET/HEAD identity evidence remain authoritative for the full download closure.

The final warning/error log query returned an empty list. Temporary viewport reset was called, task-created tab6 was closed, and the subsequent browser tab inventory was empty. The first sandboxed kill was denied without stopping the server; the same exact PID received TERM through the normal narrow approval route. Server stop is recorded at13:06:29 UTC, process exit0. `/usr/sbin/lsof -nP -iTCP:54196 -sTCP:LISTEN` then returned exit1 and no output. No listener remained.

The entire1026-member owner seal,914-file candidate,893-file live tree,53703 static checks and3632 protected/postflight checks were replayed again after teardown. Owner files, scientific content and live website remained unchanged. No Quarto, scientific computation, candidate edit or live promotion occurred during this inspection.
