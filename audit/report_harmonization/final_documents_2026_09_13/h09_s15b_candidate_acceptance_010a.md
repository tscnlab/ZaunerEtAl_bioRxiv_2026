# Independent H09 S15B strip-layout acceptance

14 September 2026. Coordinator disposition: ACCEPTED_FOR_WRITER_CANDIDATE_INTEGRATION_ONLY.

The sole accepted new SVG is `audit/hypotheses/H09/manuscript_s15b_strip_layout_2026_09_14/candidate/H09_observed_timing_patterns.svg`, SHA-256 `0a3d0cabcd6cdb67db072cfa566448a885a774db519bea442a973896a7d616e8`, 1,245,095 bytes. It supersedes `c937252509c5de9448944358a9846842c973bac26b0f0fb4640728fcf8a908b3` only for the next separately dispatched Writer candidate's S15B appearance. The historical accepted source is not replaced.

## Independent reproduction

R4.6.1 independently reproduced the owner's33-row non-circular manifest `cc1774067a4ef6ca95efdb5942aa6a0ca39ac4441251ffaea0cfb3403854904b`, the32-row dispatch and the full439-member stopped Writer package. Every member matched its byte count and SHA-256. The handoff is `98552d4d610e6b2a5b61fa0a1e9169b401c6df3fc05d6312460026979829f52b`.

The coordinator independently applied the exact six rectangle-prefix substitutions in memory and obtained the candidate byte-for-byte. Reversal obtained the entire old SVG byte-for-byte. Text-node sequence and all attributes, including fonts, are exact. Unique SVG IDs remain intact. All17 final owner checks and8 teardown rows pass. No scientific data rows, model or statistical output were read or recalculated by this acceptance check.

## Independent browser inspection

After the owner had closed its listener, the coordinator served an exact two-file copy of the same wrapper and candidate from `/private/tmp/h09-s15b-independent.ZOy62t/serve` at `http://127.0.0.1:65254/`. Symlink preflight passed. The first sandbox binding attempt failed before listening; the exact read-only command succeeded through the normal approved permission request. This was not a browser-policy bypass.

The supported in-app browser showed the actual candidate at600x485.71CSSpx in1280x720, original1512x1224 in1600x1400, and358x289.80 in390x844. The coordinator visually inspected all three displayed screenshots. The three full top labels are complete and centered, their grey headers remain separated, and panels, axes, points, lines, intervals, boxplots and legend remain intact. No clipping or distortion was found. At the358px overview scale, fine text is naturally small; this is not substituted for the600px manuscript-size legibility check. Narrow document width was390px, with no page overflow. The browser reported no warning/error entries. Its optional favicon request received404, a preview-only cosmetic absence, not a figure or manuscript defect.

Screenshots were emitted by the supported tool in the coordinator transcript. No persistent screenshot-file path was supplied, and no filesystem image artifact is claimed. The owner separately retains the three screenshot identities in its sealed evidence. This acceptance rests on actual image inspection plus the exact source/reversal checks, not DOM geometry alone.

Viewport override was reset; only the coordinator's created tab was closed. Server PID9161 exited0 after one interrupt. A port-specific lsof check found no listener on65254. Both served files were rehashed2/2 exact. The temporary read-only review directory is retained as audit scratch; it is not served now.

## Scope closure

H09's strip-layout candidate task is complete. No H09 source report, scientific artifact, model, accepted figure, historical manifest, Writer package or live website was changed. This acceptance authorizes no Quarto render or asset promotion. The exact candidate is now available to the separately sealed Writer format-completion order010b. S2 text size, S10 image, S12/S17 SVGs and all other closed findings remain unchanged.
