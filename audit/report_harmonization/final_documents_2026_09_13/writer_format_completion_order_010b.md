# Consolidated final formatting candidate completion

14 September 2026. Owner: Writer task 019ffb39-372e-7262-bfac-192751fd0e63.

Status: RELEASED_FOR_BOUNDED_FORMAT_COMPLETION after its final non-circular dispatch seal and coordinator dispatch. Independent H09 acceptance is recorded in `h09_s15b_candidate_acceptance_010a.md`, pinned by the accompanying dispatch. Its sole new candidate is `audit/hypotheses/H09/manuscript_s15b_strip_layout_2026_09_14/candidate/H09_observed_timing_patterns.svg`, SHA-256 `0a3d0cabcd6cdb67db072cfa566448a885a774db519bea442a973896a7d616e8`, 1,245,095 bytes, under the exact33-member owner seal `cc1774067a4ef6ca95efdb5942aa6a0ca39ac4441251ffaea0cfb3403854904b`. H09 and coordinator preview listeners are closed. Writer receives the serial candidate-production and browser slot. This order does not authorize a second H09 correction.

## Controlling review and protected inputs

The coordinator accepted Writer's complete Order009 review as truthful, including all 439 exact members of `audit/manuscript_nature_health/final_review_production_2026_09_14/candidate_package_manifest.csv` at SHA-256 `624753fabeb69a47cc9fe7262cba25849963e8ab8aca201512f47fbb5ec65b32`. Keep that entire package immutable. Use the independent disposition `writer009_independent_layout_disposition_010.md`, SHA-256 `0664e5ca1342f7decb7fde34bab9c81db6ff9e3dac20fb0a3b2aa9eb05479003`, and Writer's exact separate native/browser adjudication, `ff2c2dd82842c347f684cbe4fbbc3c5f1c4c085eecffbd425fb0f9d61e206e7d`.

The author has accepted S2's small secondary text size. Preserve its current native DOCX at `0c834387ff6462b737381d028f0482636c5e55ad0443234f1cb8c165dda75334`, its HTML secondary sizes, the 14-column vector and its complete mean-plus/minus-SD runs. There is no new S2 font-size gate. S10's complete image, S12's SVG and S17's SVG are also closed to source correction after their native/browser adjudication. Do not redraw, rescale pixels, or modify these to compensate for converter-specific appearances.

Accepted Order009 main Word is `3f33431ed4e85b042f8a98e232b25b9617d8dae16bc8eff8df9595c05a80d611`; integrated HTML is `0488130aba3ef7b043c743ee879d6e88f9c2ece8ae3c0c2eba89d90e116ea510`. The new package starts from these exact candidate identities and the already successful raw static DOCX input, not stale live documents or old 2A/2B versions. The previous 009 correction budget remains consumed; this is an explicit new, bounded format-only continuation.

## New write root and representation

Create only `audit/manuscript_nature_health/final_format_completion_2026_09_14/`, called C below, with `inputs/`, `helpers/`, `project/`, `production_images/`, `maps/`, `editable_tables/`, `deliverables/`, `evidence/` and `qa/`. Fresh recorded `/private/tmp` conversion/preview directories are allowed. Copy only the required static dependency closure, with exact source-to-copy hashes. No full repository or analysis-tree copy.

Preserve exactly 19 native one-table DOCX documents byte-for-byte from the accepted 009 set. Repackage them with rebased truthful metadata and README, but do not re-export any native table or repeat its accepted QA. In particular, Table2, S2 and the 17 other native files remain exact. There are still 30 table-image parts, 24 figure appearances from 23 unique SVGs, and 54 visible manuscript drawings.

Only one of the 30 PNG parts changes relative to 009: S3 part1. The other 29 image parts, including the six already corrected 009 outputs, remain exact. Only one of the 23 unique SVG identities changes: the independently accepted H09 S15B candidate under Order010a. Keep the other 22 SVGs, all accepted crops and all other figure appearances exact. S15A remains unchanged. Do not treat unreferenced original ZIP media parts as additional visible drawings.

## One consolidated implementation

### S3 descriptive table image

The clipped PNG is `8139eb4ea512629f6457ad4f5dc6c586304b65c14f3324f22b6707a118b1cb4b`. The unchanged authority is the static fragment `manuscript/R0_NatHealth/display_assets/table_s3_recommendation_windows.html`, SHA-256 `33ebc99f976acffb54082eb7afd4a6b44549b3f9de330dea998ce2f69c4ba6e6`. Its 116 header/body/note cells are complete and agree with the current manuscript HTML. The exact native S3 DOCX remains `29d32faf828a660ff8179f95de219aef2d69a23c1a33870a20f19dfa6cb99e5d`.

Create a new static presentation copy under C, adjusting only column widths, cell padding, safe line breaks and the canvas required to expose full denominator strings. Do not reduce font size, truncate, abbreviate numbers, alter table wording or convert pooled-minute descriptive proportions into period-model estimates. Keep all nine columns, row order, country labels, units, notes and non-identifying content. No Brown analysis owner is needed and no analysis is reopened.

Inspect the complete served table and capture one source-matched table image using the supported internal browser. Preserve the full screenshot and actual MIME/dimensions. If cropping is needed, use the approved R integer-pixel subset method, with recorded true pixel bounds and exact decoded pixel equality. No guessed viewport scaling, interpolation, stitching, sharpening or invented text. Require all 116 cells exact in R before and after presentation-only changes, and visually inspect every denominator and final note. Keep S3 as one image part. If needed give its manuscript page appropriate full-width space, rather than shrinking its contents to the old narrow placement.

### Table2 and S2 in the integrated HTML

Correct the packaging layer so Table2 retains `[176,254,230,180,180,200,100]` and S2 retains `[210,86,138,146,146,154,138,146,138,146,138,130,120,252]`. These are the accepted relative pixel-width vectors, not a request for equal columns. Restore the exact colgroups in a new copy of the already rendered HTML with a uniquely targeted deterministic transformation. Retain the accepted intrinsic table widths, body/secondary font sizes, wrappers and contained horizontal scrolling. No global font or page-grid redesign.

The source fragments already contain the correct colgroups. This correction requires no Quarto render. A new packaging helper must verify the unique table endpoint and exact full cell fingerprint before changing the two colgroups, then prove all 413 Table2/S2/S3 cells, all 19 table structures, all 170 whole S2 units, 17 distribution payloads and complete prose/captions/notes remain exact. HTML line breaks are whitespace separators, not analytical mismatches. Preserve semantic IDs/headers and recheck their unique intended targets.

Update the new integrated HTML's S15B asset binding or embedded SVG only to the separately accepted Order010a identity. Keep its alt text/caption and all other SVG payloads exact. Copy any required local public dependencies into C and check links. Do not edit the original HTML, any QMD or CSS in the live project, or run a website render. Record exact postprocessing steps and reverse proof for all unaffected content. The final HTML is explicitly a reproducible packaging candidate, not a claim of a new Quarto execution.

### Physical figure placement

Use new copies of the accepted 009 Word assembler and SVG embedder. Preserve all analytical SVG bytes except the accepted S15B replacement. Main Figure3 is 1134x1695pt and was capped at5.35in high; S16 is676.8x936pt and capped at4.85in; S7B is648x1296pt and capped at8.10in. These are tall layouts. Use full-height A3 portrait sections where useful, rather than assuming A4 landscape solves their shrinking. Fit the image, heading and caption within the actual page's printable rectangle, with no crop or distortion. Keep ordinary prose A4 portrait and preserve the already accepted S2 page/layout choices.

Provide sufficient complete-page space for main Figure3, S16, S7B and both existing S8 crop appearances. Preserve S8's exact two crop boundaries and source identity. Do not make a new crop or omit a panel/legend. Record each final page size, drawing width/height, source aspect ratio and resulting label scale. Inspect the real full page and dense labels/embedded tables at intended print size, not only zoomed crops. If the documented page rectangle cannot accommodate a heading or caption, adjust only the surrounding layout in this candidate, not the figure content.

### Captions and headings

Correct the exact reported placement defects: Table3 caption before its first part; S1's unnecessary fragmentation; S3 caption with its image; S8 table caption with its table sequence; S10 caption with its table sequence; S9 heading with its figure. Keep every caption word and existing author-required top-level new-page start. Prefer an explicit per-display heading/image/caption layout contract, not unchecked chained paragraph moves. Preserve table-part order, caption counts, cross-references and S5/S7/S15 shared-caption identities. Do not enlarge this into a prose rewrite.

The old hard-coded 15 orientation ranges and old section indices are implementation history, not the new layout contract. Replace only those stale layout guards with explicit endpoint-based checks for the newly specified sections. Preserve all content/author/reference/drawing safeguards. Never delete a substantive failing check or label a genuine clipping issue as converter-only without native/browser evidence.

## Production and verification sequence

1. Rehash the 439-member prior package, this dispatch and accepted H09 candidate. Snapshot current protected inputs and prove the new root is fresh. Record package versions and commands. Stage finite copies, all 29 unchanged images,19 native documents,22 unchanged SVGs, the one accepted S15B source and unchanged successful raw DOCX. Rebase maps without changing their endpoint/order/content meaning.
2. Complete one coherent helper/presentation implementation and prospective checks, including the exact S3 complete-row contract, HTML transformations, drawing/section plan and new guard logic. Review helper diffs before execution. Do not rerun the old `stage_candidates.py` or any hard-coded old entrypoint against the live project.
3. Obtain the source-matched S3 image and pass its full-page QA. Close that one loopback server before starting another. Build the complete30-part map with only this one new PNG identity.
4. Produce the new static integrated HTML by the narrowly defined transformation of the accepted 009 HTML. No Quarto, Pandoc, QMD execution, scientific computation or native re-export is authorized or needed. Assemble the new main Word once from the copied successful raw DOCX, then integrate the complete23-SVG/24-appearance map once. Preserve the SVG compatibility handling.
5. Run the bundled `render_docx.py` serially for the complete new main DOCX, with150dpi, retained PDF/page images and the bundled soffice override. Never use desktop LibreOffice. Inspect every page, not just the changed pages, and record actual page count rather than requiring107. Preserve 28 authors,14 affiliations,13 top-level section starts, all93 reference paragraphs and all declarations. Use focused read-only native Microsoft Word checks if the packaged converter disagrees with the accepted SVG behavior. Do not save, update fields/links or print from that session.
6. Inspect actual integrated HTML at full desktop and narrow sizes, with complete table/figure view and S2 keyboard scrolling. Use the bounded symlink-checked GET/HEAD-only127.0.0.1 route, served public outputs only, no source CSV/evidence/repository exposure. Use normal permission requests without evading an explicit denial. Retain actual screenshots, source/output hashes and teardown/no-listener evidence.
7. If the first new version exposes a defect within these exact formatting changes, one consolidated second candidate version is authorized within C, preserving the first. Change only affected formatting/packaging and repeat affected HTML transformation/assembly/SVG integration and main-document QA once. No third assembly or expansion into figure/scientific work. Prospective read-only helper checks and non-rendering layout calculations may be refined before production. After the bounded second version, return one complete defect set if any genuine issue remains.

This allows at most two new main assemblies, two SVG integrations, two static HTML transformations and two serial main DOCX QA commands. Each document renderer may use its documented PDF/ODT fallback, so at most six logged serial converter subprocesses are possible, not required. No native-table export, main Quarto render or website command is included. All19 native documents reuse exact accepted identities and prior QA. Do not run redundant conversions merely to fill a budget.

## Final return and handoff

Return one new non-circular completion manifest and a concise change list: S3 image, two HTML colgroups, exact S15B binding, physical placements and caption/heading arrangements. Reproduce all scientific/content/provenance protections in R4.6.1, all19 native identities,30 table parts,24 appearances/23 SVG identities,54 visible drawings, complete cells/notes/units, semantic/link checks, full-page Word observations, served HTML observations and no-drift/teardown. Document converter-specific differences accurately without reopening S2, S10, S12 or S17.

Do not claim final acceptance or promote any live manuscript, native table, website route or download. The coordinator independently accepts the completed candidate before the separately bounded final delivery/site-integration step. Preserve Order009, Order010a and all scientific/historical evidence. No commit, push, upload, model, data, profile, package or lockfile change.
