# Order017: exact inherited non-reader references and semantics

14 September 2026. This is a pre-production interpretation of the existing no-new-defect boundary, not a new edit, candidate, retirement or transaction authorization.

The complete all-44-HTML audit found five pre-existing unresolved links beyond the four already documented font references. The coordinator independently reproduced each under R 4.6.1 with xml2/digest: the exact href is unchanged in the accepted live preimage and candidate, and its target was already absent. The five route/reference/prehash/posthash rows are frozen in the adjacent classification evidence.

Four links target absent `notebooks/assemble_artifacts.html`, one each from `audit/H01/02_implementation_and_v0_comparison.html`, `audit/hypotheses/H03-H11_gated_workflow.html`, `audit/hypotheses/implementation_result_comparison_contract.html`, and `notebooks/descriptives 2.html`. The fifth is `notebooks/hypotheses/H03.html#diagnostics` from the unchanged `audit/hypotheses/H03/H03_analysis_preparation 2.html`. None of the five owners is a registered current reader route. Do not repair these links, create their targets or withdraw additional pages under Order017.

Classify only these five exact inherited route/href/target/status cases, in addition to the four pinned font exceptions, while requiring exact old/new unresolved-reference set equality and no remaining reference to any Order017 retirement. A root-directory href must resolve to `index.html`; treating `.` as a missing file is a checker normalization defect, not an HTML repair.

The complete R audit also enumerated 12,839 pre-existing semantic failure-token occurrences across 11 routes, with exact old/new route, table, token, multiplicity, table-hash and preimage identities. Some are in retained preparation/descriptive reports, not only unregistered pages. This records an inherited condition; it does not declare those tables fully semantic-valid or broaden the cleanup into a semantic repair. Both manuscript entry pages remain strictly valid. Require every previously valid token to remain valid and the complete inherited failure multiset to remain exact, with all non-entry table payloads unchanged. Do not infer a scientific discrepancy from a preserved markup condition, or hide any new mismatch behind this classification.

The central independent candidate audit already passes the entire 899-file inventory, 854 unchanged survivors, all 43 exact allowed DOM removals, three profile removals, 36 retained corpus rows, 938 unchanged search records and 38-entry sitemap. Retain the original pre-production checker failures and their explanations. The owner may finish its scoped validator before freezing the complete helper set, then continue the existing fixture/browser/one-transaction contract. No source, HTML or candidate-content change is authorized by this clarification, and no new production allowance is issued.
