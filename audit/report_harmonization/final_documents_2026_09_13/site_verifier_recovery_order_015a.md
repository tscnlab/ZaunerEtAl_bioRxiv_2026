# Order015a: verify the existing website candidate without rebuilding

14 September 2026. Owner: Harmonizer task 019ff52e-48ac-77b3-9a0e-9a87749a3bba. Release occurs only through the coordinator's single dispatch after this order and its non-circular manifest are sealed.

Read `site015_stopped_independent_acceptance.md` and its seal, then the original Order015. This is one consolidated verifier-only recovery. All user-facing content, source, typography, render, candidate and live boundaries of Order015 remain fixed. The user accepted S2's secondary size; do not reopen it or create a new cosmetic pass.

## Exact preserved state and writes

Base T is `audit/report_harmonization/final_site_a4_delta_2026_09_14/`. Preserve every member of its failure manifest `02a4f39eae1a29971598f3f84f03b6c854a5f6dc53774bd282e6f4e56f54161d`, all 959 unique non-circular rows, and the separate failure seal `ff359836235d441f79e7add87471b6f092211154a7b95ab1642d53a4f8054c3f`. Thus all 961 original files remain exact. This includes every candidate file, all 13 current helper files, the original eleven-helper checkpoint, seven preimages, raw/reversal/search/corpus evidence, the failed console record and the started packaging marker. Do not change that historical marker to complete.

Use the existing `T/candidate_build/` in place and read-only. Rehash it against the exact original six-row target matrix, `20c3c32eb1fea17c0cc96a9731c0f4b208a46c5f293c9707ce4c7299646a9af6`, and the accepted live914 baseline. Required state remains 914 files, six exact postimages, 908 unchanged files, zero additions/removals and 35 unchanged report pages. No candidate regeneration, rewrite, replacement, formatting, recopy or second build is allowed. Computing expected bytes in memory for verification is permitted; writing them to candidate paths is not.

Write only inside the new `T/verification_recovery_015a/`, with its new helpers, evidence and final handoff/seals, plus fresh logged scratch when necessary. Preserve the full 961-file original set; every added file beneath T must be under this exact recovery prefix. No other T path or live `_build/nathealth` path may change. The original prospective corpus and live corpus remain unchanged. Do not wake the Writer or any scientific task. No Quarto, Pandoc, knitr, source/Word/artwork edit, Office conversion, model, inference, scientific builder, package/lock change, commit, push or upload.

## Two exact verifier classifications and evidence routing

The coordinator has executed the full prospective nonbrowser path against the existing candidate: 53,724 static checks, 4617 protected hash rows, 75 R content checks and 15 targeted R checks per route. The independent scripts, first failure evidence and complete pass outputs are sealed under the coordinator's `site015_stop_independent/` directory. Use this complete diagnosis, not another narrow retry.

1. Leave `T/helpers/build_candidate.py` untouched and never invoke it. The stopped assertion is historical. In a new recovery copy of `check_closure.py`, replace only its incorrect candidate sequence assertion with a comparison that first proves unique path keys and equal cardinality, then compares the complete path-to-(bytes, SHA256) dictionaries. The tested expression is:

   `len(candidate)==len({r['path'] for r in candidate})==len(expected) and {r['path']:r for r in candidate}==expected`

   Do not change `common.inv()`'s existing ordering. Generate `candidate_inventory.csv` from that actual unchanged inventory, so all remaining inventory-to-inventory comparisons use the same established order. Produce the missing six-row `website_promotion_manifest.csv` from the sealed target matrix and exact existing candidate files. This creates verification metadata only, not another package.

2. In a recovery copy of `verify_targeted.R`, replace the single `No_new_broken_internal_links` assertion with the exact tested fragment in `site015_stop_independent/R_complete/exact_new_targeted_fragment.txt`. This accepts only the five named `href="#"` controls in their recorded order and requires byte-equivalent parsed nodes to accepted live Order013. Every nonempty fragment remains checked. Keep all other targeted gates, counts and scientific content comparisons. The tested prospective source before path routing is `59f910d4352708669ce44d3bf8b2bbac2b0454bde60febe32f9e599f7de05132`, with exact reverse to `3104ed2ab2b603f0fff11cd426fa8dea02443f64e66a7804b22180da62c5b966`. It is a logical template, not permission to execute its historical output paths.

3. Adapt copied helper path bindings only as required to make every new output land in the recovery prefix. In the new Python common module, OUT is the recovery root, E its evidence directory, and BUILD remains the original T/candidate_build. Preserve original ROOT, LIVE, CORPUS, donor roots and input matrix. Adapt the helper-location assertion consistently. In the R content checker route `out` to recovery but keep `candidate` at the original candidate. In the targeted R checker keep its original candidate lookup and route only write.csv/capture.output destinations to recovery evidence. No numerical/scientific predicate change beyond the exact empty-fragment classification above.

4. Copy the inherited raw ledger, search delta, seven-backup manifest, prospective corpus and corpus reverse records into recovery evidence if the copied verifiers require local names. Require byte-exact copy checks. Preserve the original implementation preflight as historical evidence. Make a distinct new recovery helper manifest after all copied/helper code is complete and parsed. Recheck both the original eleven-helper pins and all new recovery helper pins before validation and finalization. Do not overwrite the original checkpoint or mistake the deliberately copied new path bindings for drift in the original helpers.

5. Adapt finalization only to its new output routing, new helper checkpoint and preservation/accounting of the original stopped package. The final non-circular seal must explicitly bind the 959-member failure manifest and separate seal, rehash all 959 members, bind the exact six candidates and seven preimages through their complete inventories, and include all completed recovery evidence/helpers. Paths must have an unambiguous declared base or be project-relative. Exclude the final manifest and its separate seal from its own rows. Prove the full old 961-file set is unchanged and no added T path lies outside the recovery prefix. Do not claim the original build exited successfully; state that the one built candidate was subsequently verified without rebuilding.

Record exact copied-helper diffs and reverse proofs for the logical predicate changes. Allow read-only code preflight/parse and output-routing refinement before the final helper checkpoint. Do not run sealed donor scripts directly when that would overwrite donor evidence, execute a build or create a server prematurely. Do not simulate or premark any browser/HTTP/final acceptance evidence.

## One complete continuation

Rehash the release, original961, candidate914, live914, all 4617 protected/input rows, original/new helper checkpoints and donor closures first. Verify no competing writer or target render/browser server through the normal narrowly scoped process check. Unrelated read-only R help/package commands are not conflicts. Use ordinary tool permission where required and never evade an actual browser rejection.

Then run the complete copied static checker once, the complete R4.6.1 content checker once, and the complete targeted R checker once for both routes. Expected counts are static 53,724, content 75, targeted 15+15, with all domains passing. Require unchanged exact six postimages, raw reversals, seven preimages, all 19 native downloads, 20 revision rows, 942 search records and all 37 routes. Keep only the exact four inherited font-reference exceptions. Preserve source/figure/type/claim history and the inherited Word S18 converter qualification. No general broken-link or mutable-file exemption is allowed.

Only after full nonbrowser PASS, perform the previously released bounded localhost QA against the unchanged candidate. Serve only T/candidate_build after its mandatory full symlink preflight, through one GET/HEAD-only server on 127.0.0.1, using the supported in-app browser and normal permissions. Inspect index and Supplementary Information at 1440x1000, 708x1000 and 390x844. Complete the six revised endpoint checks, two Results references, S2 and Table2 preservation, full S8, menu/Contents/search behavior, horizontal table edges/notes and download utilities. Verify the 22 exact GET/HEAD resource identities. Retained Writer visual evidence supplements, but does not replace, the integrated-site browser checks.

Stop the exact server, confirm no listener, close only task-created tabs, reset viewport overrides and perform the single final protected/candidate/live closure check. Complete the recovery handoff and non-circular seal. The pending production operation remains six public replacements plus a prospective two-cell corpus, with no live write under this order.

If a genuinely new defect appears, return one consolidated stop with preserved evidence. No additional correction, package, render, source change or promotion is authorized. Otherwise return the verified exact candidate and completed visual acceptance for independent central review and a separate one-time production release.
