# Central pre-seal protected-count reconciliation

The first central seal invocation exited at `owner_summary$protected_paths == 5691L`, before creating the final independent-evidence directory or manifest. The independent owner checker had passed all 2,318 members, all 62 operations, 15 exact retirement archives and its entire 5,681-path initial protected snapshot.

The complete owner closure has 5,691 unique paths because it additionally includes the sealed Order017 inherited-classification manifest and its nine members. The discrepancy is ten accounting paths, not a changed or unverified website/scientific file. The corrected central sealer retains the 5,681 assertion for the initial snapshot, independently verifies the classification manifest's pinned SHA and all nine exact member identities, and proves that these ten paths are exactly the full-minus-initial set. No protection check is removed or broadened.

The original central sealer is retained as `central_seal_attempt001.R`. No live site, profile, corpus, source, owner package or prior accepted manifest was edited during this correction.
