# Independent Order72k cache-stop disposition

The stopped first candidate render is accepted as an environment-only stop.
The frozen source/dependency/authority bytes reproduce. No candidate HTML was
produced, and no scientific execution or canonical-output change is shown.

The installed macOS cache implementation was inspected directly. It ignores
QUARTO_CACHE_DIR and opens the Sass SQLite KV database in the normal existing
user-owned Quarto cache. A permission limitation is consistent with the
failure, but database corruption has not been established. No database probe,
cache mutation, runtime modification or Quarto render was performed by this
independent review.

The smaller continuation is the same exact candidate target using the
unmodified installed runtime, subject to narrowly scoped execution-tool
approval for normal cache access. A copied/modified runtime, changed HOME,
cache reset or Sass-content workaround is not authorized. A tool denial or
another environment failure returns one stop, without another retry route.

R 4.6.1 audit: 23/23 checks; original authority rows 216/216, input rows
186/186, dependency rows 21/21; 166 candidate files and 175 environment pins.
Installed Quarto version command returned 1.9.37. The two temporary audit
extractor corrections and their original scripts are retained as coordinator
evidence; neither involved an owner-file edit or a render.

Brown fitting and Figure S5 replacement are deliberately separate and remain
held. The original candidate-only preview and visual-acceptance gate apply.
