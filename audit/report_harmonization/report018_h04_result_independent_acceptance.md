# REPORT-018 H04 result-page independent acceptance

Date: 2026-08-20

Disposition: **ACCEPTED.** The H04 companion remains the next held serial
target.

## Independent verification

R 4.6.1 independently verified the order-42b owner package at
`/private/tmp/H04-order42b-evidence.6YflMH`:

- the 117-row evidence manifest is exact, unique, and non-circular;
- all 28 visual and cleanup gates are `PASS` or the exact sealed
  `PASS_CLASSIFIED` held-companion dependency;
- all 15 order-42b dispatch identities match;
- all 836 build files and all 260 protected paths are byte-stable through QA;
- the build contains zero symlinks;
- all 17 native tables and seven figures were reached at every required
  viewport;
- no page-overflow step, table-usability failure, endpoint clipping, unloaded
  image, empty alt text, empty caption, console warning, or console error was
  observed;
- all five disclosures and the mobile navigation control worked; and
- all seven stored publication PNGs passed final-size inspection.

The supported in-app Browser inspection covered 1440 by 1000, 708 by 1000,
and the 720 by 500 200-percent-equivalent viewport. Independent inspection of
the sealed overview screenshots confirmed readable typography, intact
responsive layout, and working navigation at the visible page level.

One actual read-only loopback listener served only `_build/nathealth` on
`127.0.0.1:54904`. A preceding restricted command failed before binding and
created no listener. The actual listener was stopped, `lsof` found no listener,
the viewport was reset, and the QA tab was closed.

## Accepted result and held dependency

The accepted result HTML is
`da5f7f7195da843e46014d4796d35381f74d223ba79c37bb78fb8ce6dfa67c9f`,
387,968 bytes. The semantic hook repaired 17 tables with 132 IDs, 670 header
associations, and 802 substitutions. Static structure, source-data links,
deviation links, navigation, country-coded sites, problem-node checks, and
protected scientific identities pass.

The exact result-to-companion fragment classified in
`report018_h04_order42a_held_companion_link_classification.md` remains deferred
to the next H04 companion render. Its anchor exists once in the accepted
companion QMD but not in the intentionally stale held companion HTML. This is
not a result-page source or visual defect. No other unresolved internal link
is allowed.

No source, scientific artifact, profile, package, lockfile, ledger,
manuscript, or later page changed. The H04 result is accepted. Only the H04
companion may now be prepared for a separate serial render order.
