# Navigation production concurrence after the H09 source/display package

Date: 2026-08-24

Disposition: **APPROVED FOR ONE EXACT PRODUCTION PROMOTION AFTER A FRESH BOUNDARY SEAL**

## Independent classification

The protected-inventory delta that stopped the first production-boundary
check is accepted as one bounded, author-authorized H09 source/display
package owned by task `019fdc1b-b927-7fb1-ac61-88993c0a818a`. It is not a
navigation mutation and it is not yet a rendered corpus transition.

The package consists of exactly six added paths and two changed paths. Their
complete before/after identities are frozen in
`h09_concurrent_package_production_allowlist.csv`. Any ninth path or any
identity different from that allow-list remains fail-closed.

Independent R 4.6.1 verification passed:

- the focused H09 test reproduced six frozen, FDR-supported primary near-eye
  associations, 4,701 exact participant-day rows, 606 stored line/ribbon rows,
  371 de-identified chronotype rows, accepted site conventions, and no model
  fitting;
- the 24-member H09 source seal rehashed 24 of 24 exact, unique, and
  non-circular members;
- the accepted H09 result and companion HTML files remain exact at
  `901fd63b45182be70ef77166c418633220a7226d86f52e14d1c79afbfc7cdd16`
  and
  `09b604b011ef14c138c15720b419a9ca200e15f71ce3e40a9b85c23f6803f96d`;
  and
- the H09 owner is idle. No H09 render is active or authorized by this
  concurrence.

The current H09 result QMD
`notebooks/hypotheses/H09.qmd`, SHA-256
`9db9671d61b39a81fa83b234e08832cef516ee91688ac8bc4150385629648df9`,
remains classified as
`AUTHORIZED_CONCURRENT_SOURCE_ONLY_NOT_RENDERED`. The historical Phase 4
source identity remains the substantive provenance of the accepted H09 HTML.

## Candidate acceptance

Candidate root:
`/private/tmp/nathealth-navigation-integration-6e1e958a273/candidate_build`.

Fresh R 4.6.1 replay passed all 40 candidate promotion identities and all
11 final candidate checks. The controlling candidate identities are:

- 40-row promotion manifest:
  `7ad23be76994e4189c9c589fe0403231f087395eaf37e3815ad5686dd21adbf2`;
- final checks:
  `a737669d12d271b2f09c9772de012e7359bc20f5bf947a63d49eebd9a002e83f`;
- all-route browser QA:
  `d5017ab041fa51655c857ab3334da000e15f29d325f776b7fa28c92378def8a0`;
- representative manual QA:
  `1f92b6203333ba5fbeaba6bc52618f28589301f5359efcdb81dfdc9807c98c49`;
  and
- original concurrent-source allow-list:
  `9e538a36fb9b8454fa3fbd870f1aa5911d4480aa10194d612239862f86f333ca`.

The candidate has 37 routes, 572 native `gt` tables, 160 figures, 86
deviation anchors, zero symlinks, and no unresolved local reference. The
34 main and 34 title-metadata layout-class transitions are exact. The three
accepted alternate layouts remain unchanged, and all previous/next blocks
remain byte-identical. Candidate regeneration is neither required nor
authorized unless an accepted HTML preimage, a candidate member, or a shell
input changes.

## Exact production boundary

Before promotion, create `candidate_production_seal.csv` and require all of
the following in one fresh R 4.6.1 check:

1. the 40 candidate members and the 11 final checks remain exact;
2. the 37 accepted production HTML preimages and the complete 1,180-member
   accepted build remain exact, with zero symlinks;
3. the historical Phase 4 manifest remains exact at
   `983b16136c1115d5a6b8dceb135c10de8d347743c694c5027c4ead9b2c7aa605`;
4. the H09 24-member source seal remains 24 of 24 exact;
5. the protected delta since navigation preflight is exactly the eight rows in
   `h09_concurrent_package_production_allowlist.csv`, while the already
   classified H09 QMD source transition remains exact at `9db9671d...`;
6. every other protected path remains exact; and
7. no Quarto, Pandoc, R render, semantic hook, build writer, or loopback
   server is active.

If and only if that seal passes, promote the 40 candidate files once: 37 HTML
shells and three navigation assets. Do not delete any build member. Require
exactly 1,183 build members after promotion, consisting of the 1,180 accepted
members plus the three approved assets, with exactly the 37 approved HTML
transitions and no other changed member.

## One-time corpus-manifest reseal

Do not run `build_phase4_corpus_manifest.R` against the live authoring
checkout. Run one navigation-specific R 4.6.1 resealer exactly once after
promotion. It must:

1. read the preserved historical Phase 4 manifest;
2. retain all 37 source hashes, roles, titles, and ordering as the substantive
   provenance of the accepted page bodies;
3. replace only the 37 HTML hashes with the promoted candidate identities;
4. require all 37 promoted HTML files live-exact and preserve the existing
   37-row, 10-column schema;
5. write a separate `live_authoring_source_checkpoint.csv` containing the
   current 37 live source hashes and the exact concurrent-source
   classifications; and
6. state explicitly that the H09 live QMD and source/display package are
   source-only and are not claimed to be rendered by this corpus manifest.

## Postflight and hold

Complete the previously approved all-route DOM, semantic, link, download,
navbar, desktop/mobile TOC, breadcrumb, previous/next, build, protected, and
browser checks. Serve only `_build/nathealth` after a fresh symlink scan.
Teardown must prove no process or listener remains, followed by a complete
post-QA rehash and one non-circular completion seal.

The eight H09 package paths and all 24 H09 source-seal members are hard
post-classification pins. The navigation owner must not edit or render them.
Any new protected path, accepted HTML drift, candidate drift, unexpected build
delta, failed QA domain, or process conflict returns one combined stopped
state. No patch, second promotion, manifest retry, real QMD render, scientific
execution, commit, push, upload, or publication action is authorized.
