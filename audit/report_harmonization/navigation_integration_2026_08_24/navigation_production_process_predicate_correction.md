# Navigation production-boundary process-predicate correction

Date: 2026-08-24

## Disposition

The stopped production-boundary check is accepted as an evidence-only process-classification stop. It made no candidate, production build, phase-4 manifest, H09, source, scientific, render, or loopback mutation.

The two captured processes are nonblocking:

1. PID 29342 read and validated YAML front matter in `/Users/zauner/.codex/skills/review-shiny-ux/SKILL.md`.
2. PID 31323 inspected R library paths and installed metadata for `glcdp`.

Neither command references this project, `_build/nathealth`, the navigation candidate, Quarto, Pandoc, knitr rendering, the semantic hook, promotion, manifest resealing, or an HTTP server. The process inventory separately records no navigation loopback listener.

## Authorized correction

The production-boundary predicate must classify processes by conflicting purpose, not by the presence of an R or Rscript executable alone. It must continue to fail closed on any command that invokes or contains:

- Quarto or Pandoc as an executable;
- `quarto_render()`, `rmarkdown::render()`, or `knitr::knit()`/`knit2html()`;
- `post_render_gt_html_semantics`;
- `python -m http.server` or `python3 -m http.server`;
- `build_navigation_shell_candidate`;
- `promote_navigation_candidate` or `promote_navigation_candidate_once`; or
- `reseal_navigation_manifest` or `reseal_navigation_manifest_once`.

The independent loopback-port checks remain unchanged. The checker must still exclude only its own PID and must not exempt an R process that matches any conflicting-purpose expression above.

The sole authorized checker transition is:

- preimage SHA-256 `7685cf0538bc57fabe3fa4c71c3ad8ea7383d4f030dbeac046e6bfdc7107973e`;
- postimage SHA-256 `80c86fc292d772548993864e2ebe2cb9d40b69000dec34b7c4494084396d01dc`;
- postimage size 12,738 bytes.

The final postimage also incorporates the separately accepted, exact external
protected checkpoint. It does not broaden the process predicate. The external
checkpoint is limited to the held manuscript-selection builder and the
postflight classification correction required to recognize the already sealed
H09 package.

The original stopped inventory is preserved at `production_seal_process_inventory_overbroad_stop.txt`, byte-identical to the stopped `production_seal_process_inventory.txt` preimage with SHA-256 `82a32ae604763a1cf00f5859f9755600ed1d277795f96af51c1dedf7e828af0d` and 756 bytes.

## One-run authority

Exactly one fresh R 4.6.1 execution of `run_navigation_production_boundary_check.R` is authorized from the unchanged candidate and accepted production preimages. The execution may overwrite only the live process-inventory evidence and may create the already specified production-boundary checks, protected/source checkpoint evidence, and `candidate_production_seal.csv` on complete PASS.

Any matched conflicting-purpose process, listener, candidate mismatch, accepted-HTML/build drift, unauthorized protected delta, H09 package mismatch, or other failed gate must stop without promotion. If the recheck passes, the existing production concurrence remains controlling for the separately bounded one-time promotion and reseal. This correction does not itself perform or broaden promotion.
