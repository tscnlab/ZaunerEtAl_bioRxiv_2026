# Navigation final production-boundary recovery and concurrence

Date: 2026-08-24

Status: **APPROVED FOR ONE FRESH FINAL BOUNDARY SEAL, THEN THE ALREADY AUTHORIZED ONE-TIME PROMOTION IF AND ONLY IF ALL GATES PASS**

## Process classification

The earlier process stop is accepted as an overbroad predicate defect. PID 29342 validated YAML front matter for the `review-shiny-ux` skill. PID 31323 inspected R library paths and `glcdp` package metadata. Both were unrelated read-only R processes. Neither was Quarto, Pandoc, a knitr or R render, the semantic hook, a build writer, a navigation command, or a loopback server.

The corrected predicate is purpose-based and remains fail-closed for all rendering, semantic, navigation-build, promotion, resealing, and HTTP-server commands listed in `navigation_production_process_predicate_correction.md`. An independently elevated process replay found zero matching commands and zero navigation listeners.

## Exact protected-path disposition

The post-concurrence protected delta is resolved as follows.

1. The previously accepted H09 source/display package remains exactly six added and two changed paths under `h09_concurrent_package_production_allowlist.csv`.
2. The H09 owner confirmed that the two suffixed paths below were unsealed, obsolete, unreferenced snapshots outside the 24-member accepted package:
   - `scripts/hypotheses/H09/build_h09_stage3_observed_figure 2.R`;
   - `tests/hypotheses/H09/test_h09_stage3_observed_figure 2.R`.
3. Those two files were moved intact, without deletion, to `/private/tmp/navigation-h09-unsealed-duplicates.ok3rz3/`. Their exact identities and original paths are preserved in `h09_unsealed_duplicate_recovery_inventory.csv`.
4. The Harmonizer completed and froze the author-authorized shared planning builder at `scripts/report_harmonization/build_manuscript_figure_table_selection.R`, SHA-256 `fb78bcc465897b11f2111e3d110e601e89e86c1005769574a029d1ec0afaf9e1`, 45,886 bytes. It also completed and froze the paired verifier at `scripts/report_harmonization/check_manuscript_figure_table_selection.R`, SHA-256 `f6bb5c5fa9eb23656cc3ae85f85e505704e2202c08ffd9f0d5f7b5b7d32cd861`, 13,740 bytes. These planning sources are outside the 37-page corpus and did not produce the accepted navigation HTML candidate.
5. The postflight checker transition to SHA-256 `349e29c66592a92a08a25f0d2dc29d96348f9ba72b06232918ce32e436cf8bbb`, 23,702 bytes, recognizes only the exact sealed H09 additions/transitions, the held planning builder and verifier, its own central classification correction, and the navigation-only Phase 4 manifest transition. It fails on any additional added, removed, or changed protected path and rehashes all eleven classified live paths.

The three exact external transitions are frozen in `navigation_external_protected_checkpoint.csv`, SHA-256 `7e633d5c2829ee9fad2cfa167ddb87777160cbba5e9ce8fa4b84737c100c749c`.

## Promotion-script call adjudication

The reported malformed `readr::write_csv(drift, drift, file.path(...))` call is not present in the shared checkout. Direct source inspection and recursive search confirm that `capture_authoring_boundary()` already uses the intended two-argument form `readr::write_csv(drift, file.path(...))`. The exact promotion script is therefore preserved without a needless edit at SHA-256 `d96b8601b3b67fa4ed97c84ebc241e6ffbffd1a118839d2d0353bc5efa405d06`, 8,567 bytes. The final boundary checker pins that identity as its own fail-closed gate.

## Invalid diagnostic evidence

A temporary diagnostic correctly exposed the additional protected-path gate but, because its stop-line substitution did not insert the intended immediate exit, wrote seal-shaped files after printing a failed check. Those files were moved intact to `invalid_diagnostic_boundary_run_2026_08_24T084959Z/` and are explicitly classified as never authoritative. No valid production seal exists at the navigation evidence root from that diagnostic. The diagnostic did not change the candidate, production build, Phase 4 manifest, H09 package, or authoring source.

## Final one-run boundary

Exactly one fresh R 4.6.1 execution of `run_navigation_production_boundary_check.R`, SHA-256 `61f2af81883cc090dc0c1502dbee2d1682f39577b258a756a86ef9479ae43320`, is authorized with narrow read-only process-inventory access. It must require:

- the exact promotion script at `d96b8601b3b67fa4ed97c84ebc241e6ffbffd1a118839d2d0353bc5efa405d06`, 8,567 bytes;
- all 40 candidate members and 11 candidate checks exact;
- all 37 accepted HTML preimages and all 1,180 build members exact;
- historical Phase 4 manifest SHA-256 `983b16136c1115d5a6b8dceb135c10de8d347743c694c5027c4ead9b2c7aa605`;
- H09 source seal 24/24 exact;
- protected delta exactly six H09 additions and five exact transitions: two H09, the held planning builder and verifier, and the postflight correction;
- H09 live QMD exactly `9db9671d61b39a81fa83b234e08832cef516ee91688ac8bc4150385629648df9` as source-only, not rendered;
- no removed or additional protected path;
- zero conflicting processes and zero loopback listeners.

On complete PASS, the existing production concurrence authorizes the separately bounded one-time promotion of 37 HTML shells plus three navigation assets, one navigation-specific manifest reseal, and complete postflight/browser QA. The builder and all other non-navigation protected paths must remain byte-identical throughout that window. Any new drift or failed domain stops without retry or promotion broadening.

No Quarto render, QMD execution, scientific computation, model/artifact change, content/language loop, commit, push, upload, or publication is authorized by this recovery.
