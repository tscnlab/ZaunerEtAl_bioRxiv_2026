# Order72k visual lease 002

Fresh exclusive lease: `ORDER72K-VISUAL-LEASE-002`.
Owner: Writer `019ffb39-372e-7262-bfac-192751fd0e63`.
Issued following the 2026-09-11 19:44:31 UTC preflight.

The former lease 001 is closed. No task listener was found at port 58005;
Harmonizer has opened no browser or office surface. This renewed lease is
exclusive to Writer for the released S2 capture environment recovery 001 and,
only after S2 passes, the remaining serial Order72k browser/table/native Word
and office QA within the original trial budgets.

The one new S2 capture consumes the final existing adjustment slot. The
installed browser, helper, viewport and data remain frozen. Restart serving
only through the new minimal helper and immutable 12-file preview root
specified in the recovery order. Obtain actual tool permissions as required;
this lease is not OS or browser-security permission.

Stop on denial, repeated launch/capture failure or another order stop rule.
No alternative browser/runtime, security workaround, automatic unlock,
scientific action, optional H11 replacement or Brown Figure S5 change is
released. Do not hold the lease indefinitely for a locked native surface;
report unavailable Word evidence as specified by the order.

Writer must explicitly close task contexts/listeners/documents and release
this lease with its next consolidated return. No other task receives a
visual allocation while this lease is active.
