# Independent completed SVG handoff

Date: 2026-09-12, Europe/Berlin.
Gate: `REPORT018-ORDER72K-NON-S5-INTEGRATED-PREVIEW-REVIEW`.
Disposition: **STRUCTURAL_PASS_VISUAL_QA_PENDING**.

The Writer's completed recovery is independently verified as a file-processing
candidate, not as visually accepted publication output. No owner file was
edited and no producer, render, capture, browser, server, office application
or native Word operation was invoked during this handoff review.

## Candidate and owner seal

Candidate:
`audit/manuscript_nature_health/revision_2026_09_11/consolidated_display_repair_order72k/Nature_Health_non_S5_preview_attempt1.docx`.

SHA-256: `f055f0c0f6225e372ed83ddb6bbe189e64f35d7306e7e54af563687991f6492c`.
Size: 13,498,777 bytes.

The owner return, 507-member completion manifest and companion seal match:

- Return: `dda8061bf2133e17a510f16ae22d6532d22c802eb5da5fb4f857607c33e87c35`.
- Manifest: `aa4ccbbd1bf9f2e796dd18bc3e7d228d99b3031130c902b95af06e862cba5e01`.
- Seal: `a63fed0cd0c8597a557679cd3946b48c7aa9213a5e22af057fe1a411b12c3a83`.

## Independent checks

The final handoff verifier passed all 24 checks. It reproduced all 507 current
owner members directly and independently resolved and rehashed all 7,347
historical/current rows. Resolution uses exactly five source path/version
aliases, the sealed durable proposal mapping and the single previously
accepted Quarto xref transition. All eight runtime-cache pre/post identities
remain exact. There is no new transition. All six execution inputs, thirteen
frozen command arguments, 29 table PNG parts and 22 SVG source/copy pairs match.

The earlier independent Harmonizer document audit passed 64/64 checks. Its
five output files remain exact against the nested evidence-pin ledger sealed
by the Writer. The final owner R checks pass 57/57 and its independent OOXML
checks pass 12/12. The eight built-in embedding guards passed. No document
was rebuilt to perform or correct any verification.

The candidate contains 52 drawings: 23 SVG appearances from 22 exact sources
and 29 PNG table parts from 19 keys. It has 31 sections, thirteen named
top-level page starts, 22 precise repaired bookmark ID/name pairs and 126
resolving internal links. Table 3 retains its approved content, styling and
harmonized metric order. Nineteen standalone editable table documents remain
separate and unchanged; the approved manuscript table-display route is PNG.

The three main native SVG drawing XML structures, existing SVG/DPI extensions
and relationships are exact relative to the assembled intermediate. The
supplemental embedding adds nineteen physical SVG member names for twenty
appearances, preserving the two S8 crop windows. The independent package
audit verified exact XML reversal, documented former-member removal,
unchanged unrelated members and exact table dimensions on both axes.

## Preserved checker corrections

All original failed checker sources and evidence are retained. The completed
owner checker corrects reference-style resolution (`berschrift1` for the
named heading 1 style), distinguishes six restored names from sixteen net-new
names within 22 repaired bookmark pairs, and checks exact integer EMU image
dimensions instead of a derived aspect-ratio tolerance. The separately
retained style-node lookup correction excludes unnamed styles. The final
checker includes the coordinator's explicit `!is.na(style_names)` predicate.
The openssl JSON serialization correction removes only the hash object's S3
class. None is a scientific or document change.

The first handoff checker incorrectly required the Harmonizer evidence to be
direct owner-manifest members. The owner correctly seals the five evidence
files through `harmonizer_independent_evidence_pins.csv`. The separate verified
checker checks that sealed nested ledger and each exact file instead. It pins
the original checker hash and changes only that assumption, output directory
and recorded command name. Both the original partial results and verified
24/24 results are retained. No producer was rerun.

An initial standalone Rscript JSON inspection waited in the project's renv
lock acquisition and was interrupted before its expression ran. Subsequent
verification used the already recorded R 4.6.1 runtime and autoloader settings.
The current owner and versioned-history identities subsequently reproduced.

## Closure and holds

The Writer's recorded assembly and embedding commands each ran once and
exited successfully. Its exact-helper process check found no matching
process. A compact task snapshot independently observed the Writer idle,
with turn `01a09295-7294-7032-a1fe-490972a70b3a` completed and no error,
cursor `77b03eaa-d0ba-4fa4-870a-a7fb1dc45167:22`.

The exclusive file-processing assignment is released. Leases 004 and 005
remain closed. Office QA is still 0 of 2. Native Word visual acceptance
requires author unlock and separately bounded authority. The binding browser
security denial remains in force with no retry or alternate route.

Historical Brown Figure S5 is unchanged and held with its known Word
limitation. No new Brown result, optional H11 candidate, H06_daily insertion,
scientific recomputation or canonical promotion is implied. This handoff
requests coordinator reconciliation of the completed structural candidate,
not a new producer invocation or visual assignment.
