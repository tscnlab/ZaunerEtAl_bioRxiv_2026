# Order72k visual lease004

Fresh exclusive lease: `ORDER72K-VISUAL-LEASE-004`.
Sole owner: Writer `019ffb39-372e-7262-bfac-192751fd0e63`.
Issued: 2026-09-11 21:14:09 UTC.

Lease003 is closed. Writer was freshly observed idle at cursor
`77b03eaa-d0ba-4fa4-870a-a7fb1dc45167:18`. The completed prior turn is
`01a09227-ed1e-7613-9229-d781df4e8eff`. A fresh lsof check found no
listener on port58005. No other task receives a visual allocation.

The complete sealed accessibility-guard recovery order was read. Independent
R 4.6.1 preflight reproduced 4,339 preservation rows across 691 unique files,
including current input632/release688/dispatch15, the classified central353,
owner335, independent7 and historical2309 records. The older three same-hash
path-spelling pairs are preserved, not rewritten. New seals are canonical-
unique. Version aliases key on both exact path and expected hash.

The immutable served12 and all previous captures are preserved. New attempt5
and recovery evidence destinations are absent. The task TMPDIR exists and
is not a symlink. Writer alone may preserve the new exact helper preimage and
apply the centrally sealed prospective helper; neither CSS file changes.

This lease covers exactly the one released S2-only attempt5 and, only after
all required actual S2 checks and visual acceptance pass, the previously unused
Order72k correction/document/office work. It adds no other trial or render
budget. The actual hidden-span, visible-text, image and exact-content gates
must pass before each PNG; no separate browser probe or tuning run.

Restart only the immutable minimal12-file preview set with a fresh receipt.
Actual narrow tool permission remains required. This lease is not OS or
browser-security permission. Denial or failure stops without a workaround.
Close the exact task server/capture context after bounded inspection.

Native Word remains subject to the user's unlock. Do not unlock automatically
or claim native acceptance from another viewer; return VISUAL_QA_PENDING if
necessary. Do not reserve a locked surface indefinitely.

Writer must explicitly release lease004 with its consolidated return and
provide verified task-context/listener/document teardown. Approved Table3,
SVGs, Brown/S5 and optional H11 holds, old evidence and all canonical files
remain protected. No other owner wake or hypothesis-matrix edit is released.
