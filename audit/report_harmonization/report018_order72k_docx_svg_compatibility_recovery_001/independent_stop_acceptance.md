# Coordinator acceptance of the stopped DOCX compatibility state

Date: 2026-09-11. Status: accepted stopped state, not document acceptance.
No compatibility implementation or assembly is released by this record.

Coordinator independently rehashed the Harmonizer's 18-member seal
57586ee563a086950a846ced8dcd9287cf2af8ea3aaf38de861b749f00af610b and replayed
its complete verified R 4.6.1 audit in a fresh temporary location. All 17 checks,
444 current Writer members and 4,339 historical versions passed. Exact
version-specific aliases and all eight cache pre/post states were reproduced.
Only the already recorded main xref transition is present.

Writer's stopped return is 3da07f7f1eec17c850321ff0c84f35c7c4cc8df6575e2069c80b94268645f746;
the 444-member owner manifest is ddb53080c0eca05ed499752c6c5741fed725a46edf9a6c01049bad793e0a1596.
The raw attempt2 DOCX remains a36009b0e7f52f61a89650a795dfcc0f7854b1c8be4dff235336135f68ee0d82.

The complete raw-document classification is three accepted main SVGs plus
17 byte-exact S2 distribution miniplots, not three drawings in total. Main
docPr IDs 22/29/36 have native SVG image relationships rId21/28/35 and both
standard DPI and SVG extensions, with absent base image embed. Their embedded
SVG bytes are exact. Every image relationship is internal and resolving.

Inspection of the frozen embedding helper confirms both incompatible
assumptions: it resolves a base image relationship before inspecting a native
SVG extension and then requires only one extension. The assembly helper
preserves those main extensions while moving/resizing the figure paragraphs.
Its supplemental base-only SVG path, S8 repeated-source crops, exact 23 SVG
appearances from 22 sources, 29 table PNG parts and final 52-drawing contract
must be preserved. Native-only main drawings should not be unnecessarily
rewritten or given fabricated fallback images.

Only a temporary exact-diff and isolated-function/OOXML-fixture proposal has
been requested from Harmonizer. It may not invoke the full assembly or
embedding entry point, save a candidate DOCX, modify an owner helper, install
a renderer, or run an office/browser surface. A separate reviewed order and
non-circular seal are required before implementation.

All correction render slots are consumed. Assembly/embedding and office QA
remain 0 of 2 and held. The future command must use the existing raw attempt2
DOCX and accepted word_table_manifest_attempt5.json, never rerun the historical
runner with its stale table map. No further HTML/DOCX render is authorized.

Lease004 is released. Harmonizer's subsequent file-URL browser action was
security-rejected and lease005 is released. Preserve the complete browser
stop: no loopback copy/server, alternate surface, indirect execution, raw CDP
or open-in-app workaround may be used. Corrected HTML visual acceptance is
pending manual author review. Native Word remains pending author unlock, and
XML validity must not be described as native Word rendering acceptance.

The canonical website, manuscript sources, standalone nineteen editable table
documents, SVG/image payloads, numerical results, science, packages and locks
remain untouched. Brown scientific work and Figure S5 replacement remain held.
The current return gate remains REPORT018-ORDER72K-NON-S5-INTEGRATED-PREVIEW-REVIEW.
