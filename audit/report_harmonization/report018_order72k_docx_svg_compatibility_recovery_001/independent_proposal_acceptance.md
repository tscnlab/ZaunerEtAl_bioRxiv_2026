# Independent acceptance of the temporary compatibility proposal

Date: 2026-09-12, Europe/Berlin. This accepts the exact helper proposal for
the separately sealed candidate-only assembly order, not a final document.

Main-agent review covered the complete original embedder and full prospective
diff, all 139 isolated test definitions, both exact future command vectors,
the preservation/reverse evidence, and the unchanged assembly helper's image,
manifest, layout, bookmark and final-save/postcondition paths.

In a fresh temporary directory, installed Python 3.12.14 reproduced all 116
relationship/OOXML fixtures and 23 preservation guards. The resulting three
test-output files exactly match the original proposal. Syntax validation
passed without importing a full entry point. No DOCX package was saved.

R 4.6.1 verified the original 18-member proposal seal, exact pre/post images,
zero-fuzz/no-offset reverse proof, all 1,290 rows across the 828/444/18 central,
owner and independent manifests, six input pins, all 13 command-vector arguments
and the 52/23/22 drawing contract. All output destinations remain absent.
The existing owner helper remains 23cf9ee0..., with no owner edit by coordinator.

One coordinator command assertion initially used argument4 instead of
argument3 for the raw DOCX and stopped after preservation/reverse checks passed.
That first script and partial outputs are preserved. A separately named checker
then checked the entire exact assembly and embedding vectors by input role,
without changing the proposal or relaxing a document check. Both scripts and
the classification are retained in `proposal_replay/`.

The reviewed approach leaves the three native-only main SVGs and their DPI/SVG
extensions and relationship IDs intact. It keeps the supplemental base-only
path, validates immutable pre-mutation payloads, rejects conflicting shared
retargets, preserves exact extension reversal, and strengthens final physical
SVG/drawing counts. Static fixture success does not establish actual Word
rendering. The complete assembly/embedding and postconditions still must pass.

Durable proposal copies are in `prospective/`; independent code and outputs are
in `proposal_replay/`. `durable_copy_mapping.csv` records original temporary
paths and byte-identical durable destinations. Temporary paths in old proposal
metadata are preserved as provenance, not future execution dependencies.

Only the separate exact order may release one first assembly/embedding chain.
All correction renders are consumed. Office/native Word and automated HTML
review remain held. No browser-security workaround, raster generation,
scientific work or canonical promotion is authorized.
