# Harmonizer 37-route transitive preflight

Date: 2026-09-12. Disposition: read-only preflight returned with explicit execution blockers. This is not a render release or a final-document acceptance.

Evidence root: `/private/tmp/nh-report-closure.Ts4qel`.

Controlling disposition is `audit/report_harmonization/final_documents_2026_09_12/consolidated_planning_disposition_001/disposition.md`, SHA-256 `027312e79404c42838c1bcc35f74e650d67843b26364e3066f5664a932e0cbc3`. All 34 members of dispatch `c2a946ec8c8c1d3ca3be715695dc7c3e4af908b5287a7dee985eae0c784c292f` were independently rehashed, including byte sizes. No historical checker was executed.

The author's SVG preference is already represented by the completed 22-source, 23-appearance manuscript figure handoff. Preserve those sources, crop windows and uppercase left-side tags. This preflight does not replace them with PNG figures or regenerate approved Table 3. Historical S5 remains held for its separately accepted Brown replacement and native compatibility repair.

## Work performed and scientific boundary

R 4.6.1 parsed source as data. No QMD, project helper body, model object, scientific test, metric producer, fit, prediction, diagnostic producer, renderer, browser, capture tool, office application, assembler or SVG embedder was executed. Only newly authored inspection scripts and their derived evidence were written, all in this temporary directory. No production source, helper, candidate, library, cache, canonical artifact or old process was changed.

Inspection commands used:

`RENV_CONFIG_AUTOLOADER_ENABLED=FALSE R_LIBS_USER=/Users/zauner/Library/R/arm64/4.6/library Rscript --vanilla <temporary inspection script>`

The package contains complete current chunk/inline text, parsed expressions, calls, symbols, local definitions, source edges, source hashes and conditional branches. Current signatures are evidence of the current code, not invented acceptance of a historical source transition.

The static call graph is deliberately conservative. It follows named helper calls and function references through loaded sources, includes syntactic branches and callback bodies, and excludes explicitly disabled chunks. Its path decoder is not a branch-sensitive R interpreter. Root substitution is conditional on the future root contract. It cannot prove arbitrary S3/package dispatch safe, derive an approved final source that does not yet exist, or certify a minimal closure by treating every mentioned path as a file read.

## Coverage and reconciled counts

- All 37 report routes plus the semantic post-render hook are inventoried.
- 54 distinct code files were parsed: 37 QMDs and 17 R files. All 54 current hashes remain identical to their initial parse pins.
- 993 units parsed successfully without evaluation; 44,094 calls and 590 named function definitions are retained as inspectable evidence.
- All 45 discovered `source()`/`sys.source()` edges are recorded. Four symbolic edges have explicit manual bindings: H06's root locator and the posthook's pinned repair-engine path.
- 291 reachable conditional/iteration/exception branches are recorded with full expressions.
- There are four QMDs without R units, 33 R-bearing QMDs, but only 32 with executable R units. The sensitivity-battery page's sole R chunk is `eval: false`. H11's companion also has four disabled formula-example chunks. Thus the proposed commands use `--no-execute` for the four static pages and sensitivity-battery page. The semantic posthook still runs in R for these pages.
- The conditional staging/read frontier has 1,397 unique existing file candidates with current SHA-256 and size. This includes code, direct inputs, manifest-member identity reads, metadata/resources and explicitly marked literal/link frontiers. It is not an instruction to copy all 1,397 files or a claim that all are necessary at runtime.
- Twenty-four report-consumed manifest seeds expand to 621 member-identity reads. Of these, 607 match the historical member hash. The remaining 14 all belong to H01's historical L10 integration manifest and exactly satisfy the current helper's explicit exception predicate after restricting the transition set to that manifest's 67 members. No unexplained mismatch is left in this expanded identity subset. No statistical result was inspected or recalculated to make that determination.
- Of those manifest paths, 157 are literal absolute paths inside the current canonical project: 122 in Preparation 01 and 35 in Preparation 04. A staged copy of the manifest would still read those original canonical paths. Do not silently rebase them. Either expressly approve that exact immutable canonical read boundary or approve a separate path-only staging adaptation with original-identity proof.
- Package DESCRIPTION metadata covers 129 package names and 245 installed-location records, with no missing named requirement in the inspected library set. Twenty-five package names have different project/user-library versions. This is consequential for device/font/layout reproducibility. For example, project `ragg` is 1.5.2 and user-library `ragg` is 1.5.0. `gt` is 1.3.0, `knitr` 1.51, `rmarkdown` 2.31, `ggplot2` 4.0.3, `LightLogR` 0.10.3, `xml2` 1.6.0 and `digest` 0.6.39 in the relevant metadata. Quarto's installed version file is 1.9.37. No package was loaded merely to inspect its version.

See `route_37_commands_and_expected_delta_NOT_EXECUTED.csv` for every route's exact prospective command, working directory, source pin, output path, helper closure, runtime directory, unique semantic-audit directory, expected writes and remaining conditions.

## Sixteen source transitions: acceptance is not inferred from a hash

`sixteen_source_authority_disposition.csv` preserves each old corpus pin, current pin, candidate authority record and remaining gate. The bounded search included ignored audit/manifests. Later Order72 SVG-export preservation records establish a frozen input for that export, not blanket approval of an earlier QMD rewrite.

1. H01 companion: the exact current endpoint `c6435a5f...` is an `accepted_companion_qmd` member of the independent H01 METRIC-010 acceptance. Its transitive input/runtime conditions still require the current execution preflight.
2. H06 result and companion: independent source-only acceptance explicitly names both exact current endpoints and their stored employment-eligibility integration. The companion remains held until independent acceptance of the result-page render. The result's acceptance preimage `013496ae...` is newer than the corpus baseline `d65c197c...`, so that earlier bridge must still be reconciled rather than silently omitted.
3. H04 result and companion: the owner handoff and acceptance manifest name the exact current endpoints, the author-approved `Other` correction and frozen scientific outputs. Shared integration/render authority remains with the Coordinator. A byte-exact old companion source was found; its 42 units contain 40 identical ASTs, with the changed AST subtrees limited to the recorded `Other` display labels and label-column adaptations. This supports the owner's stated scope but does not issue a new execution allowance.
4. H01 result: independent acceptance pins `5e0bcf31...`, not live `4618b80e...`, and expressly requires a new pin for any later source transition. The current endpoint therefore remains unresolved.
5. Descriptives, H02/H03/H05/H07/H08/H09/H10/H11 results and H06_daily result have no complete accepted old-to-current source/scientific-expression bridge established by the inspected records. They remain unresolved for execution, not reverted or declared scientifically wrong.

The bounded 81-QMD archive scan found three byte-exact corpus preimages: H04 companion, H06 companion and the H05 result's retained stopped snapshot. Their complete unit/AST comparison is included. H06 has 33 exact ASTs of 35 compared units; the accepted stored-sensitivity addition is explicit. H05 has 60 exact ASTs of 63 units; the changed subtrees are table-font constants and the source-note parameter/wording. That narrows H05's review substantially, but a historical stopped snapshot plus a later export pin is not itself source acceptance. All current expressions and changed subtrees remain available for the Coordinator's bounded disposition. The other thirteen old QMD preimages were not found in this bounded scan.

## Consequential execution paths

Descriptives is not a byte-exact image reuse path. Its QMD calls `load_descriptive_render_bundle()`, which imports the ten existing descriptive modules, checks package availability and reads stored descriptive tables, `metric_plot_values.csv`, `figure_alt_text.csv` and the descriptive artifact manifest. It does not invoke the producer entrypoints, and missing stored files stop execution rather than triggering a rebuild.

However, its table calls `metric_thumbnail_plot()` and the deferred `gt::text_transform()` callback calls `metric_thumbnail_html()` / `gt::ggplot_image()`. This re-creates density ridges and median markers from stored metric values, with dose scaling and circular/symlog display transformations. It does not simply reuse the accepted 17 embedded density PNG payloads. The current S2 contract requires those payloads to remain exact. A render cannot be released on the assumption that this path is display-byte-preserving. Resolve exact existing-payload reuse or separately approved deterministic display generation and comparison first. Do not run `build_descriptives`, `run_descriptive_display_refresh` or the broad manuscript-selection builder to repair nowrap.

Several report chunks also compute descriptive summaries from stored frames, for example coverage quartiles in Preparation 02, a 90th percentile in H02's companion, and support quartiles in H03/H04 companions. These are not fits, but they are still numerical report operations. Their exact expressions and stored input identities are in the package; future permission must include their accepted result-preservation checks. This preflight did not execute them.

The only identified direct report `system2()` call is H01 companion's `quarto --version`. A future command must bind the actual executable and count this metadata subprocess rather than leave an uncontrolled PATH lookup. No direct fit/predict/bootstrap entrypoint was found in the conservative reached project-helper graph. That negative scan is not a package-internal side-effect proof.

## Root, library, hook and resource contract

Every future route must run in its own approved staged project root with `QUARTO_PROJECT_DIR` and, where used, `NATHEALTH_PROJECT_ROOT` set to that exact root. Root-marker files, `execute-dir: project`, H06's locator, and H03's explicit `renv/library/macos/R-4.6/aarch64-apple-darwin23` existence/precedence check must be satisfied honestly. Do not create a dummy library directory merely to pass a guard. Some packages are loaded before local `.libPaths()` changes, so a later path change is not enough to establish the intended runtime.

The prospective command table names a staged library path, but no library was created, copied or approved. An implementation order must select and pin the actual package binaries, dependencies and precedence. The existing project/user version differences, loaded namespace lifetime, fonts and device choice prevent treating the two library roots as interchangeable. Disabling renv's autoloader alone is insufficient to exclude startup code: the reviewed `renv/activate.R` can still source the user profile in its disabled branch. Prospective commands therefore also disable that branch and set user profile/environment files to `/dev/null`. `--vanilla` was used for all actual audit scripts.

The current semantic wrapper is `28c1105840834e95a37f84eaded9a2c595b9febd568fbc763daef247a53d7205`. It checks R 4.6.1, gt 1.3.0, root/output identity, the exact repair-engine hash and the Quarto-declared output list. Only declared HTML under staged `_build/nathealth` may be repaired. The engine changes native-gt `id`/`headers` values, proves exact raw reversal and checks DOM/text structure. Unknown/partial namespaces stop.

The wrapper creates its own exact temporary staging directory, writes staged HTML and audit CSVs, commits declared outputs, and rolls back originals on failure. Its cleanup applies only to its own staging directory or audit files created during that invocation. Default finalization callback only. It refuses an existing audit destination. Therefore the 37 serial calls require 37 distinct, initially empty semantic-audit directories outside the staged project, not one shared directory with a reused summary filename. Old evidence directories must never be supplied as destinations.

Prospective writes are limited to new staged route HTML and declared resources, isolated Quarto xref/search/navigation metadata, staged knitr temporary figures, and the unique external runtime/audit directories. No write to canonical data/models/manifests/figures, packages, existing build or manuscript candidates is allowed. The exact tool-owned generated-resource/cache membership is not yet proven for the new flattened stage, so that remains a release prerequisite rather than a blanket cache exception.

Current navigation must retain the Nature Health navbar grouping, hidden sidebar, right-side TOC and mobile TOC include, not the old RQ navigation. Preserve the exact CSS and font resources, including `Datatype.woff2`. Current root index uses the local authors-block extension; its `_extension.yml` and four Lua modules are pinned and inspected as metadata/DOM transformations, without executing them. Current index also names an external MathJax 2.7.9 URL. No fetch was made. The eventual synchronized source must explicitly settle offline/resource behavior rather than silently fetching it during a build.

## Source-consistent landing, supplement and downloads

The accepted 37-page HTML baseline and 893-file build inventory remain the controlling read-only preimages. Matching an old HTML hash does not make it a render of a changed live QMD.

- `index.qmd` must be replaced in the approved stage by the accepted current Writer manuscript source and the latest candidate display overlays. The accepted website index is a direct manuscript overlay, whereas root QMD retains older prose. A blind root rerender would regress it.
- `supplementary_information.qmd` is still a placeholder. Its staged source must be assembled from accepted Writer supplementary material, exact numbered displays/captions and dynamic report links, not copied unchanged and called complete.
- Preserve current S7/S15 independent SVG components, S8 crop windows, accepted S17, Table 3's approved Descriptives order/design and all protected caption/claim text. Keep H06_daily's two report routes, but no numbered manuscript display insertion.
- Rebase only verified resource paths into the new stage. Exact final source hashes and figure/table/claim mappings do not exist yet because Writer layout changes and Brown integration remain pending. No honest exact final minimal closure can be sealed before those postimages are defined.
- Synchronize the main/selection/standalone pages, 19 editable table files, ZIP provenance and website download links to the accepted final files. Do not restore the older site Word download or mislabel current f055 as final.
- `_quarto.yml` and `_quarto-nathealth.yml` render arrays are union-merged. Use a separately approved flattened stage configuration and explicit per-route commands; do not copy the base legacy render list or issue a whole-project render. The CSV commands are conditional on that configuration, not executable approval by themselves.

## Writer return and consolidated next gates

Writer supplied `/private/tmp/nature-health-layout-preflight.yMAKmA`. All 45 unique non-circular package members and all six announced return/manifest/seal/diff/plan identities were independently verified. The return and serial QA plan were read. This is package-identity verification, not approval or runtime validation of the prospective diffs. No package was archived into the author project during this temporary-only assignment. The Coordinator should archive both temporary packages before any implementation order.

Writer's proposed repairs preserve the SVG source set and Table 3. They cover S2 nowrap/width/native spacing, actual empty-section removal, paragraph spacing and caption keeps, equal-width S7 continuation with an explicit four-part map, and ZIP `export_provenance.json`. Five non-Brown table images need a permissible new capture route: three S2 parts and two S7 tail parts. Other reusable parts/native tables remain byte-exact. The browser denial remains binding; no alternate renderer/capture path has been approved here.

Writer's initial automatic DOCX QA is `2 + |B|` document commands for S2, changed accepted Brown native tables and the main document. Its packaged renderer can internally make up to three serial office conversions per command. Those must be explicitly budgeted or replaced by a separately reviewed no-fallback route. Old office 0/2 and consumed render/capture/assembly/embedding allowances do not reset.

Outstanding gates, to be resolved by the Coordinator in one consolidated disposition:

1. Accept or obtain the remaining exact source-transition bridges, including H01's newer result endpoint; preserve the existing H06 serial gate. This audit must not manufacture acceptance by editing a manifest.
2. Select the exact runtime/library/root contract and explicitly bind all 157 canonical absolute identity reads. Convert the candidate/frontier map into an exact minimal stage only after final source postimages are approved. No broad project copy.
3. Resolve the Descriptives thumbnail-generation contract and other report numerical-summary preservation requirements without invoking a producer.
4. Approve source-consistent manuscript/supplement/nav/resource/download postimages and their finite Quarto/posthook write sets. Retain the SVG choice.
5. Receive accepted Brown results and exact downstream table/figure scope. Brown is already active under its own scientific gate and was not redispatched. S5 and dependent numerical integration stay held.
6. Reconcile Writer's prospective changes, a genuinely permissible capture route or explicit author-supplied source-bound artifacts, and fresh finite execution/office/native-QA allowances. No new visual session or final document has been claimed.

No additional author choice is required merely to use SVG. If the approved workflow cannot provide the necessary table captures within the binding restrictions, the concrete missing artifacts must be requested in chat rather than hidden as an internal task trigger.

## Evidence integrity and limitations

All authored scripts are included. Parser-development corrections addressed missing R argument nodes, namespace matching, disabled-unit reachability, nested-function visibility, constant path/registry binding and a metadata-only query typo. These affected only temporary evidence scripts. No failed query executed author code or modified a research file. The corrected H01 exception check uses the exact historical-manifest subset, not unrelated transition rows.

The path and Markdown inventories are conservative candidates, with explicit tiers for direct reads, identity reads, constructors, metadata and link/literal frontiers. They are not a full Quarto resource resolver or a package interpreter. The 107 symbolic records are individually classified as reader formals, finite reviewed registries/contracts, posthook runtime bindings, the conditional site registry, or non-read constructor frontiers. None is silently treated as permission for an arbitrary read or write.

The final seal records current input/source rechecks and package membership. No final HTML/DOCX/figure/table output was produced or promoted by this preflight.
