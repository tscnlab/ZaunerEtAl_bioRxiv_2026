# Prospective layout change matrix

Status: read-only proposal, not implemented. All row numbers below are zero-based,
half-open body-row ranges. All live sources and f055 remain unchanged. The
prospective Python and JavaScript entrypoints are deliberately disabled.

| ID | Native finding or dependency | Proven cause / exact live binding | Proposed change | Verification and stop boundary |
| --- | --- | --- | --- | --- |
| L01 | S2 mean ± SD wraps, Word pp. 63-65 | `project/order72k_layout.css` in both Order72k projects forces all numeric descendants to normal wrapping. The accepted source already contains 170 complete nowrap spans. | Apply the supplied S2-only CSS diff to the future manuscript and selection projects from one pinned copy. Preserve existing whole spans and descendants; do not force nowrap on complete cells. Use the 1490 px widths `[200,60,103,96,96,96,96,96,96,96,96,96,78,185]`, retaining all 14 columns. Numeric-cell side padding 4 px. | Compare all source text, 23 rows, six groups, 17 metrics, 170 whole units, 17 distribution payloads and hidden descriptions. All whole units must occupy one line and lie inside cells. The 185 px distribution column is a prospective size, not a visually accepted size. Stop if containment/readability fails. |
| L02 | Same S2 wrapping in image representations | `helpers/capture_word_tables.mjs`, the `fixedWidths` branch, overwrites the same span styles. | Supplied prospective diff protects each exact mean/SD span and children. Retain three parts `[0,9)`, `[9,17)`, `[17,23)` and all columns. Require 70, 50 and 50 no-break units respectively. Fixed Word width 15.55 inches on every part, maximum image height 9.20 inches. | A permissible capture route or exact author-supplied artifact is a prerequisite. The disabled headless entrypoint is not permission to circumvent the denial. No capture/render trial has occurred here. A failed browser-layout attempt still counts against any future trial allowance even when no PNG is exported. |
| L03 | Conditional S2 font permission | Existing 12 px capture base at 1490 px, future fixed Word width 15.55 inches. | Width/no-break first. Default reduction zero. Only if still necessary, subtract at most one **physical printed point** from S2 numeric base text: `1490/(72*15.55)` = 1.33083244 CSS px. Capture base 12 px, conditional base 10.66916756 px, equivalent to 9.01691275 and 8.01691275 printed points. Smaller subordinate text is frozen, not cumulatively reduced. | Record actual CSS font and DOCX extents, not an assumed CSS-pixel-to-point identity. No reduction to other tables, captions, figures, headers or explanatory stub text. Do not silently use the conditional setting without evidence that the width-first candidate failed. |
| L04 | Native S2 export also loses no-break behavior | `scripts/manuscript_nature_health/export_editable_tables.py`: `add_run()` collapses whitespace; `render_inline()` ignores nowrap. | Preserve full source mean/SD units with NBSP within those exact numeric spans. Retain native single-line spacing and Arial; widths use the same relative column vector within 15.44 inches available on A3. Native numeric base 10 pt, optional 9 pt only if needed. Existing smaller explanatory/sample text remains 9 pt. | Export S2 only, plus the separately accepted changed Brown table set. Require 170 full no-break units; exact whitespace-normalized cell text, punctuation, units, source order, notes and caption. The individual table remains one editable Word table with 17 images in its distribution cells, not a screenshot table. Render all its pages. |
| L05 | Editable ZIP provenance inconsistent with its exporter | Exporter ZIP loop currently includes README and manifest but omits `export_provenance.json`. | Include all three metadata files, reject missing metadata, and verify every record points to the expected exact DOCX inside the new package directory. Seed unchanged DOCXs byte-exact. Prepare README and provenance intentionally before the selected export. | Exactly 19 DOCXs plus the three intended metadata members; no duplicate/extra archive member; per-member checksum equals the new candidate file. Do not mislabel reused DOCXs as newly exported from the new full HTML. Whole-fragment hashes and serialized-table hashes are distinct identities. |
| L06 | Main Table 2 caption pp. 10-11; Table 3 caption pp. 16-17 | Captions inherit 1.5-line body spacing. Main-table captions have keep-next but not direct keep-lines. | `format_display_caption(..., above=True)`: single spacing, left aligned, keep lines together and keep with first table part. First part has no forced page start inside the same block; later parts retain their separate starts. All caption runs, font sizes, words and links remain exact. | Preserve Table 3's order/design and all four existing images. No table-content update here. Table 2 remains Brown-dependent. If a heading/caption/display block still cannot fit at preserved scale, stop for an explicit size/part decision rather than silently reducing another table. |
| L07 | Main Figure 3 image p. 23, caption p. 24 | Inline-image paragraph and ImageCaption style inherit the body's 1.5-line spacing. | Set only figure/image and caption paragraphs to single spacing, keep figure with complete caption; retain existing image extents and bytes. | Figure 3 scientific source and accepted composite unchanged. Automated intended-size check must confirm caption adjacency and label readability. This is not an authorization to redesign the plot. |
| L08 | Supplementary Table S1 pp. 59-61; S3 pp. 67-69; S4 pp. 76-77; S5 pp. 79-80; S7 pp. 86-87; S8 pp. 88-90; S10 pp. 98-101 | Images and captions inherit 1.5-line spacing; supplementary captions are not explicitly kept with the last image. | Single spacing for display paragraphs, matching heading/caption keeps. Keep each heading with its first part and only the final part with the caption. Do not chain all parts into one oversized block. The same safe paragraph rules apply consistently through S1-S10. | Preserve caption text and font, all continuation starts, landscape ranges and display numbers. Visual QA decides whether each block actually fits. Single spacing and keep rules are a candidate fix, not proof of final pagination. Brown-dependent S3/S4 still need scope confirmation. |
| L09 | Blank pp. 62 and 66 around S2 | `wrap_landscape()` inserts a portrait section before each independent range. Adjacent S1/S2/S3 leave two empty portrait sections, separated only by bookmark nodes. In f055 these are body nodes 514 and 525, old section positions 8 and 10, both `nextPage`, not odd/even breaks. | Remove only the two empty portrait section carriers proven by the strict S2/S3 heading guards, preserving every intervening bookmark. Direct A4-landscape → A3-landscape → A4-landscape. Make empty section carriers 1 pt exact with no paragraph space. | Expected section sequence becomes 29 rather than 31. Fail if the exact empty-node mechanism no longer matches. Preserve all 13 legitimate top-level fresh-page starts, title page, page sizes, margins, line numbering and footers. No generic deletion of empty paragraphs. |
| L10 | Table S7 last continuation is smaller | Last old part is 896×767 CSS px. The 6.2-inch height cap shrinks it to 7.24276356 inches wide; first two are 10.55 inches. | Reuse parts 1 `[0,7)` and 2 `[7,13)` byte-exact. Replace old tail with part 3 `[13,19)` (timing) and part 4 `[19,23)` (exposure history, spectrum and notes). All four at 10.55 inches, same 896 px capture width and original capture typography, maximum height 6.2 inches. | Two genuinely new images, no new native S7 export. Require full source-cell equality and notes only on final part. Fail the new capture before PNG export if fixed-width height budget is exceeded. A four-part map is explicit, not squeezed into the old 29-part total. |
| L11 | Figure S7 caption p. 83 | Its independent B component is tall; one shared caption follows both images and inherits body spacing. | Keep both accepted independent SVGs. In Word only, place the existing complete shared caption directly after A, then preserve B's separate page start. Keep A and caption together. No caption duplication or rewriting. | Exact shared caption text, A/B order, independent image payloads and cross-reference anchors; B is not shrunk to make room for the caption. This specific caption relocation is proposed for layout approval. HTML still retains the accepted shared-caption structure. |
| L12 | Dense labels in Figure 3; S1, S7, S8 lower crop, S10, S11, S16 | Native presence was confirmed, not universal readability at intended print size. | No source/image redesign. Verify at intended physical size in the new automatic page images/PDF. | Record actual unreadable labels if any and return a bounded owner correction request. Do not treat panel visibility or an enlarged screenshot as a print-size pass. |
| L13 | Blank historical Figure S5 | Native Word compatibility defect confirmed, but current main Brown alignment results are not yet accepted. | No display or scientific replacement in this proposal. Future accepted Brown result and Word-compatible scientific source must be supplied and pinned. | `BA-LB-G2-REVIEW` plus explicit integration acceptance. Preserve main/exploratory hierarchy and sample/diagnostic restrictions. A cosmetic relabel or unseen result cannot fill the placeholder. |
| L14 | Drawing/part validators assume old total | Old assembly hard guard assumes 29 table image parts. | Require a separate positive-integer, complete 19-key part-count contract. All unresolved Brown entries are null in the proposed contract and deliberately fail execution. Fixed non-Brown parts total 26. Final table parts = `26 + b(Table 2) + b(S3) + b(S4)`. Old logical figure contract is 23 appearances. | With unchanged Brown part counts 1/1/2, total would be 30 table images and 53 drawings. This conditional arithmetic is not final output approval. Final counts, source identities and any figure expansion must be sealed before assembly. |
| L15 | Full website and download coherence | Website baseline is the accepted Order71c 37-route corpus, not the older REPORT-018 snapshot. Root source is stale, supplement placeholder, download older than f055. | Harmonizer's separate read-only source/closure reconciliation controls future route staging. Writer supplies this one CSS change and exact table/figure maps. Both manuscript and selection wrappers must take the same accepted S2 behavior. | No root/profile render, hook run or site promotion here. Final Word, 19 table files/ZIP, manuscript source, supplement and all 37 routes must share the approved source/output identities at delivery. |

All changes are downstream layout/export work. The Descriptives source generator,
accepted hypothesis figures/tables, scientific computations and central registers
must not be rerun or changed for these repairs. The optional H11 S17 replacement
remains excluded; accepted S17 is reused.
