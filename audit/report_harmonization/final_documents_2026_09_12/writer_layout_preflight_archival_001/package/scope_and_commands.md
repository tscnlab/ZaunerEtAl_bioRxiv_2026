# Actual preflight operations

Scope: document structure, source identity, protected display tokens and
prospective layout text only. All new records and proposed postimages were
written inside `/private/tmp/nature-health-layout-preflight.yMAKmA`.

The live coordinator disposition and its 34-member dispatch manifest were
read completely/verified. The existing proposal, native review, assembly,
capture and export code were inspected. Continuing Quarto and document skill
instructions and the relevant layout references were read; no new review,
scientific-analysis or rendering skill was silently activated.

Read-only R invocations used R 4.6.1 with:

```text
RENV_CONFIG_AUTOLOADER_ENABLED=FALSE R_LIBS_USER=/Users/zauner/Library/R/arm64/4.6/library Rscript --vanilla /private/tmp/nature-health-layout-preflight.yMAKmA/verify_dispatch.R
RENV_CONFIG_AUTOLOADER_ENABLED=FALSE R_LIBS_USER=/Users/zauner/Library/R/arm64/4.6/library Rscript --vanilla /private/tmp/nature-health-layout-preflight.yMAKmA/inspect_layout.R
RENV_CONFIG_AUTOLOADER_ENABLED=FALSE R_LIBS_USER=/Users/zauner/Library/R/arm64/4.6/library Rscript --vanilla /private/tmp/nature-health-layout-preflight.yMAKmA/build_structural_contracts.R
RENV_CONFIG_AUTOLOADER_ENABLED=FALSE R_LIBS_USER=/Users/zauner/Library/R/arm64/4.6/library Rscript --vanilla /private/tmp/nature-health-layout-preflight.yMAKmA/seal_preflight.R
```

The original inspection script's missing-XML-node error was retained in
`inspect_layout_attempt1.R` and `inspection_attempt1.md`. It stopped before
producing its inspection output. The corrected read-only inspection produced
the geometry and style records. Consequential infrastructure package versions
are in `structural_session.txt` and `preflight.json`.

The static diff check used the bundled Python with bytecode writing disabled:

```text
PYTHONDONTWRITEBYTECODE=1 /Users/zauner/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 /private/tmp/nature-health-layout-preflight.yMAKmA/validate_prospective_diff.py
```

This parsed Python ASTs without importing the proposed modules and generated
round-trippable unified text diffs. It invoked bundled Node with `--check` only,
not the prospective module entrypoint. Its exact arguments and output are in
`static_diff_checks.json`. The check was repeated after one prospective error
message was clarified to distinguish a failed trial from an unexported PNG.

Manual prospective edits used `apply_patch` on temporary copies only. No
Quarto render, browser operation, office conversion, Word UI action, PDF/image
generation, DOCX transformation, native export, model operation, commit, push
or canonical promotion was performed. The serial commands in the separate
future plan are explicitly not executed.
