# Order 72e standalone-command stop: independent disposition

Date: 2026-09-11

The coordinator accepts the reported Order 72e stop as an output-command
construction defect, not a source, scientific, SVG or rendered-page defect.
The coordinator supplied the invalid --output-dir command. The owner correctly
stopped when standalone Quarto rejected it before conversion.

Independent R 4.6.1 read-only rehash finds exactly one change among the 63
historical release members: the already authorized selection QMD promotion.
The other 62 members remain exact. Both durable endpoint preimages remain
exact. The current 14-file owner state is inventoried separately. All 20
stored source checks pass. The candidate output locations are absent and the
owner rendered directory contains no file.

The promoted QMD is 49,865 bytes, SHA-256
9acec033d0c24cbb0ee7649c38f55d5cea90052e11b6be7fc5f9e7310890d8f3.
The canonical HTML remains 30,925,051 bytes, SHA-256
82100e0d3990dec39f61e94970e7a434b02cf94a6d25819ede1be4d24f4130a6.
The original Order 72e order, release seal, contracts, source candidate and
endpoint preimages have not been changed by this recovery.

The installed Quarto 1.9.37 code also rejects --output containing a relative
or absolute path. Consequently, that suggested alternative is not released.
Instead, the coordinator executed one non-scientific standalone fixture under
/private/tmp, with an SVG at a relative parent path and a relative HTML table
include. The basename-only --output command exited 0, wrote only the new
requested HTML basename, left the source exact, embedded the exact SVG bytes,
included the table once, did not create the default canonical HTML, and
required no persistent support directory. No project report was rendered.

The fixture files, installed option-handler excerpt, command/version record,
20-check preflight, current execution pins and historical transition
classification are sealed beside this disposition. Fixture testing establishes
command and path behavior, not the project's later visual acceptance.

The rejected Order 72e call produced no HTML and is preserved as a failed
pre-conversion attempt. Order 72f releases one actual narrow render invocation,
not an unbounded until-success loop. Any failure of that invocation stops
without another render or source patch.

