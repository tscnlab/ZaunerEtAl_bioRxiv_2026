# REPORT-018 Order72k S2 capture environment recovery 001

Date: 2026-09-11. One same-browser S2-only capture using the last existing
S2 adjustment allowance. No additional render, capture or scientific budget.

Dispatcher: Harmonizer `019ff52e-48ac-77b3-9a0e-9a87749a3bba`.
Implementation owner: Writer `019ffb39-372e-7262-bfac-192751fd0e63`.

## Accepted stop and unchanged authority

The Writer's stopped return is
`audit/manuscript_nature_health/revision_2026_09_11/consolidated_display_repair_order72k/stopped_return.md`,
SHA-256 `7e45b0b3398f9c3d6abcbb8ef6880329a2f48f1390b032ed6ba6e86804dc9d46`.
Its 263-member non-circular `stopped_owner_manifest.csv` is
`445ba04dd59cd3ef1be2d1d7bc625c8b4f8ce3852b97d48a9d80f86c3f667d25`.
Preserve the manifest, return, trial ledger, failed command/log and all
completed initial captures as historical evidence.

The S2-only attempt 2 failed at Chrome launch, before output-directory
creation and before navigating to the source URL. The log records target
closure, PID 36917, SIGABRT and cleanup kill EPERM under `use_default`.
No execution-tool permission request was denied and no browser URL rejection
is reported. These facts support a bounded environment trial; they do not
prove that permissions or the locked desktop were the sole cause.

The old server receipt records PID 35776 closed at 19:32:18 UTC. Fresh
coordinator read-only process and listener checks found neither stopped PID
and no listener on 127.0.0.1:58005. No process was killed by the coordinator.
The prior visual lease has been released and must not be treated as active.

The original Order72k, SHA
`359d8d730930fb0008779b3aca8db840f24663a100b61b62807f766b51561332`,
and its cache recovery 001 remain controlling. All canonical source, science,
Brown, Figure S5, optional H11 and no-promotion holds remain unchanged.

## Preflight and renewed serial lease

Harmonizer must reproduce the attached recovery input pins and release seal,
verify the 263 stopped-owner members and 429 original preservation rows,
confirm Writer is at its stopped safe point, and issue a fresh exclusive
Order72k visual lease before dispatching this continuation once. No other
task is woken. Use the existing Harmonizer dispatch directory for the lease,
receipt and rehash evidence, not the hypothesis matrix.

The currently corrected capture helper is frozen at
`audit/manuscript_nature_health/revision_2026_09_11/consolidated_display_repair_order72k/helpers/capture_word_tables.mjs`,
SHA-256 `8dd21ba1d978e36d1cafa0669b7c2456306b66e996f013f44b86ebc2549e0174`,
21,399 bytes. Do not edit it, its browser flags, viewport, target width,
row ranges, columns, checks, installed Node, Playwright or Chrome. Its
`only-s2` argument restricts this invocation to the S2 table, and its old
broad scientific-figure capture loop remains disabled.

Freshly rehash the frozen served HTML/resource set and all 19 existing
`capture_attempt1/` files. The failed `capture_s2_attempt2/` destination
and new `capture_s2_attempt3/` destination must both remain absent before
invocation. Preserve the failed command JSON at SHA
`7404fc887fa732c6618c35e0aed42a58b3db99bc0ae480faff2c92e876208c8d`
and failure log at SHA
`d75fc4aa3decec0cddb8cc4a9c7ab24a9d223e876b87c45be3009780254951e9`.

## Minimal server restart, no content generation

Restart only a read-only static GET/HEAD server over the already staged
12-file `preview_attempt1/` rendered-output root beneath the Writer Order72k
directory. Reproduce its exact membership and hashes and perform the mandatory
symlink/private-resource preflight before binding. No symlink, extra file,
project root, research data, raw source or credential may enter the served set.
Do not render, restage or replace HTML to perform this capture.

The existing `helpers/serve_preview.py` cannot simply be rerun because it
refuses an existing root and receipt. Preserve it and its old session record.
A fresh minimal restart helper may be created only in
`environment_capture_recovery_001/` under the existing Writer candidate root.
It may serve the immutable existing root, disable directory listings, bind
only to 127.0.0.1, and write a new PID/port/root/start/stop receipt. It may not
copy content, change the old receipt, expose write endpoints or widen the root.
Obtain narrow tool permission before binding if required. Use port 58005 if
still unused, or record one fresh ephemeral port. Do not take over an occupied
port or terminate another owner's process.

Record and verify HTTP success for the exact immutable main route before
capture. This is the same accepted local preview artifact, not an alternative
route around a security rejection. Any server or browser-policy denial stops
the route; no tunnel, browser switch, CDP workaround or indirect capture is
authorized.

## Exactly one final S2 adjustment invocation

Working directory: the shared project root. Use the same installed Node,
unchanged copied helper, installed Chrome and ephemeral Playwright profile.
Do not use the personal Chrome profile or change HOME or browser security
settings. Request this exact capture via the execution tool with
`sandbox_permissions=require_escalated`, explaining permission for the
existing headless Chrome capture and its task-owned temporary profile. Omit
a broad reusable approval prefix. The order is not OS permission.

```sh
TMPDIR=/private/tmp/order72k_3ok569jd /Users/zauner/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node audit/manuscript_nature_health/revision_2026_09_11/consolidated_display_repair_order72k/helpers/capture_word_tables.mjs http://127.0.0.1:58005/main/ZaunerEtAl2026_NatHealth_phase3_brown.html audit/manuscript_nature_health/revision_2026_09_11/consolidated_display_repair_order72k/capture_s2_attempt3 only-s2
```

Only the recorded port may differ if a fresh server port is necessary. The
new output argument is deliberate attempt accounting, not a new capture
implementation. Do not precreate that directory: the helper enforces a fresh
destination. The recorded task-specific TMPDIR must exist, be task-owned and
contain no substituted symlink.

This consumes `S2_vertical_adjustment_2`, the final remaining S2 allowance.
The first capture and failed first adjustment remain counted. No extra S2
trial, S5/S6/S10 recapture, screenshot workaround or browser/runtime switch
is released. If tool permission is denied, the runtime fails again, a capture
check fails or a genuine new defect appears, return one consolidated stop.
Do not turn this into a fresh environment retry budget.

## Required capture acceptance and continuation

Preserve all 14 columns, 17 metric data rows, six group rows, all source cell
text and 17 exact distribution-image payloads across the same three vertical
parts. Require the existing exact cell-text comparisons, image containment
and aspect ratios, numerical text-rectangle checks and complete headers/notes.
Inspect all parts visually at original and intended Word display size. No
clipping, missing rows/images, horizontal splitting or shortened numerical
content may pass. No scientific calculation or data regeneration is involved.

Reuse the four completed S5/S6/S10 Arial PNG parts byte-for-byte. After S2
passes, only candidate input-path records may point from the absent attempt-2
capture to the sealed attempt-3 manifest. Do not alter the original dry-run,
correction manifest, failed trial or old stopped seal; add a path-mapping
record. No unrelated table or figure may be regenerated.

Then continue only the already authorized consolidated correction pass:
selection HTML once, main HTML once and main DOCX once, using the exact Lua
dependency copies and normal-cache permission strategy already released.
No such correction render has yet been consumed. Original assembly/embedding
and office-QA budgets, complete independent acceptance and no-promotion gate
remain in force. Any generated candidate/cache transition must retain its
prior identity and be classified separately from frozen source/resource pins.

The Mac lock is a separate native-Word access blocker. Do not attempt an
automatic unlock, authentication workaround or substitute static/LibreOffice
checks for native Word acceptance. Continue only work not requiring the
locked native surface. If Word remains unavailable, return VISUAL_QA_PENDING
with the exact missing observations and existing author-unlock request.

Close capture contexts, stop the restarted server immediately after bounded
capture/inspection, verify the exact task processes and listener are gone,
and rehash served outputs and completed captures. Record tool approval or
denial, argv, URL, output, PID where observable, exit, trial use and teardown.
Keep all new owner evidence inside the two existing Order72k candidate roots.

Mandatory return gate remains
`REPORT018-ORDER72K-NON-S5-INTEGRATED-PREVIEW-REVIEW`.
No canonical replacement, science, Brown fit, S5 replacement or new language
loop is authorized by this environment continuation.
