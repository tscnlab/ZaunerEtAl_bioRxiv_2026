# Owner order 56a: H09 consolidated figure typography repair and result rerender

Date: 2026-08-22

Owner: H09 task `019fdc1b-b927-7fb1-ac61-88993c0a818a`

Status: `RELEASED_ONCE_AFTER_EXACT_PREFLIGHT`

## Authority and objective

This order implements the complete central concurrence in
`audit/report_harmonization/report018_h09_order56_consolidated_display_repair_concurrence.md`,
SHA-256 `d874576ef3250e139dfc65b68af54742cb0f0ccc444cd1ec19cfbf3b5115d192`,
13,031 bytes, with its 26-row non-circular seal SHA-256
`23c38a47b6d177218f734ac2f09b3ad4f36bc67627da5891941b8a5203ba0265`,
4,123 bytes.

Order 56 is independently accepted at a fail-closed stop. The fresh H09
result page is scientifically, semantically, structurally, and visually
acceptable except that essential text in all four figures falls below 7 pt at
the required 170-mm display width. Correct all four figure typography defects
in one candidate-first display pass, promote the eight PNG/PDF outputs once,
and then rerender the H09 result exactly once.

Do not edit either H09 QMD. Do not render the companion. Do not open a
language or cosmetic cleanup loop.

## A. Exact preflight

Before any mutation, rehash and require:

1. central concurrence `d874576e...` and its 26-row seal `23c38a47...`;
2. independent stopped acceptance `1c470541...` and its 41-row seal
   `02dcf649...`;
3. owner stopped record `6f1d5900...` and 98-row seal `a9ed20cd...`, with all
   98 members exact;
4. result QMD `c738a436...` and stopped HTML `dbc9122c...`;
5. companion QMD `7563a933...` and held HTML `4054dfc6...`;
6. builder `a234fcaa...` and figure manifest `0ad64452...`;
7. frozen source CSVs `3972ae75...`, `ecc9fe09...`, and `b3119775...`;
8. all eight current PNG/PDF identities from the 41-row seal;
9. both historical H09 tests `a0309e55...` and `9a243e39...`;
10. profile `80dd0557...`, semantic wrapper `28c11058...`, semantic engine
    `7c949930...`, and lockfile `3bf99c63...`; and
11. R 4.6.1, Quarto 1.9.37, Air 0.4.1, no competing Quarto or Pandoc process,
    and zero build symlinks.

Create complete pre-edit project-protected and `_build/nathealth`
inventories. Preserve exact recoverable preimages of the eight affected
display files, builder, and figure manifest in the new order evidence root.
Do not overwrite historical order-56 evidence.

If any preflight condition fails, stop without editing.

## B. Exact owned paths

This order may create or edit only:

- `scripts/hypotheses/H09/refresh_h09_order56_figures.R`;
- `tests/hypotheses/H09/test_h09_order56_display_repair.R`;
- the approved display literals in
  `scripts/hypotheses/H09/run_h09_stage2.R`;
- the four affected rows of
  `artifacts/12_manifests/H09/H09_figure_manifest.csv`;
- the eight affected durable PNG/PDF files in
  `artifacts/10_figures/H09/`;
- the canonical result HTML and normal target-owned result resources changed
  by the one authorized render; and
- bounded new evidence under
  `audit/hypotheses/H09/report018_order56a_display_repair/`.

No other source, test, manifest, artifact, report, profile, ledger, build
target, or evidence file may be edited intentionally.

## C. Dedicated refresh implementation

Write the refresh in R for R 4.6.1. It must require an explicit output
directory outside durable targets. It may read only:

- `H09_primary_effects_data.csv` at `3972ae75...`;
- `H09_paired_placement_effects_data.csv` at `ecc9fe09...`;
- `H09_primary_diagnostic_figure_data.csv` at `b3119775...`; and
- the current H09 figure manifest at `0ad64452...` before its bounded update.

It must not source the full builder, read a model or RDS, inspect an analysis
frame, or derive a scientific result. Add a fail-closed static audit that
rejects model fitting, prediction, inference, p-value/FDR calculation,
resampling, simulation, bootstrap, and inputs outside the exact allow-list.

Implement the four plot families from the accepted builder code. Provide an
exact historical-theme mode and a repaired-theme mode. Use the historical
mode to reproduce the current outputs from the frozen CSVs before applying
the repair.

## D. Baseline and candidate proof

Use fresh temporary directories outside the project. Require all of the
following before promotion:

1. historical-theme PNGs decode pixel-identically to the four current PNGs;
2. historical-theme PDFs have identical page geometry and rendered visible
   content after excluding nondeterministic PDF metadata;
3. historical and repaired ggplot builds have identical scientific layer
   data, including coordinates, groups, panels, values, intervals, reference
   lines, categories, scales, breaks, labels, colours, shapes, and ordering;
4. every source row used by every layer reconciles exactly to the frozen CSV;
5. no point, interval, panel, axis meaning, legend item, or label meaning is
   added, removed, or reordered; and
6. candidate work has not touched any durable target.

Candidate changes are limited to essential typography, plot margins, panel
spacing, label wrap width, and canvas height. Width, export scale, and 300 DPI
remain fixed. Base canvas height may increase by at most 25 percent.

Require minimum nominal essential text of 17 pt for the primary-effects and
both diagnostic figures, and 13 pt for the paired-placement figure. Maximums
are 20 pt and 16 pt, respectively. R 4.6.1 must calculate and record an
effective minimum of at least 7.0 pt at 170 mm and at the stopped QA's
643-pixel final-size display.

Complete visual candidate QA at original dimensions, 170 mm, 708 pixels, and
720 by 500. Require every label, axis, legend, mark, panel, and caption to be
present and legible, with no clipping, overlap, broken wrapping, or blocking
whitespace. Candidate iterations remain temporary. If the full four-family
set cannot pass, seal one stopped return before promotion.

## E. Reproducibility and one durable promotion

After the final candidate set passes:

1. apply only the corresponding display literals to
   `scripts/hypotheses/H09/run_h09_stage2.R`;
2. update only the four affected rows of `H09_figure_manifest.csv` with
   truthful final height, nominal text, effective text, and QA status;
3. preserve both V0 rows and the complete schema, order, paths, alt text,
   widths, DPI, and unaffected values;
4. prove exact forward diffs and reverse reconstruction for builder and
   figure manifest; and
5. promote all eight PNG/PDF candidates together exactly once.

Create an exact preimage-to-postimage ledger, source inventory, current
display manifest, scientific-layer audit, no-scientific-call audit, candidate
history, geometry and typography audit, PDF audit, and visual evidence. The
current display manifest must be unique and non-circular.

Do not run the full H09 builder or any broad manifest builder. Keep every
historical H09 test and manifest byte-identical. Record their exact transition
sets in order-specific evidence and fail on any unclassified path.

## F. Complete pre-render gate

Before rendering, run one complete focused verification that requires:

- the result and companion QMDs exact and all 17 result R chunks parse;
- the three source CSVs exact;
- the eight durable files candidate-identical;
- exact scientific-layer equality and source-row reconciliation;
- the four updated figure-manifest rows exact and both V0 rows unchanged;
- exact builder and figure-manifest reversals;
- the historical tests and historical manifests byte-identical and
  unexecuted;
- all unrelated scientific and protected artifacts exact;
- Air 0.4.1 and R parse PASS for both new R files;
- scoped `git diff --check` PASS; and
- zero forbidden scientific call or input.

Do not render if this gate fails. Return one stopped package without a retry.

## G. Sole result render

After the complete pre-render gate passes, create one fresh, empty, absolute
semantic-audit directory and issue exactly once:

```sh
RENV_CONFIG_AUTOLOADER_ENABLED=FALSE \
R_LIBS=<accepted-R-4.6.1-project-library> \
GT_HTML_SEMANTIC_AUDIT_DIR=<fresh-absolute-directory> \
quarto render notebooks/hypotheses/H09.qmd --profile nathealth
```

Use R 4.6.1 and Quarto 1.9.37. Do not bypass the accepted profile or semantic
hook. No second render is authorized.

## H. Post-render nonvisual acceptance

Against the fresh HTML, rerun all order-56 nonvisual contracts and require:

- exactly 11 native gt tables and four figure endpoints in accepted order;
- exactly 23 reader-link occurrences and 21 unique targets;
- exact semantic reverse and reapplication, document-wide unique IDs, and
  every explicit header token resolving once within its own table;
- all accepted H09 score direction, MCTQ MSFsc versus MEQ distinction,
  four five-outcome FDR-family, hierarchy, qualification, deviation,
  reciprocal-link, navigation, and country-label contracts;
- exact rendered-image reconciliation to the eight promoted files and three
  frozen CSVs;
- zero embedded error, warning, stderr, raw trace, unresolved reference,
  unsupported target, or local path; and
- exact preservation of companion source and HTML, historical tests and
  manifests, profile, semantic tools, source data, science, and unrelated
  document sets.

Classify the build delta. Only the result HTML, source-identical result QMD
copy, eight target-owned figure copies, search, sitemap, ordinary
source-identical target resources, and bounded semantic output may differ.
Do not update `phase4_corpus_manifest.csv` in this result-only order.

## I. Secure-loopback QA

After every nonvisual check passes, preflight for zero build symlinks. Start
one read-only static server rooted exactly at `_build/nathealth`, bound only to
`127.0.0.1` on an unused high port. Inspect only the H09 result route.

Inspect the complete page at 1440 by 1000, 708 by 1000, 720 by 500, and exact
170-mm figure width. Review every table and all four figures. Require at least
7.0 effective points in each figure and check axes, legends, shapes, labels,
panels, captions, disclosures, navigation, links, clipping, overlap, wrapping,
and page overflow.

Stop the server, prove no listener remains, reset the viewport, close the QA
tab, and require complete build and protected inventories to remain exact
across QA.

## J. One combined return

Return one non-circular acceptance package containing exact commands,
versions, identities, source and reverse proofs, preimages, postimages,
candidate history, source and layer reconciliation, semantic reversal,
screenshots, build and protected reconciliation, and server lifecycle.

If a genuinely new defect appears, complete every safe read-only check and
return one consolidated stopped package. Do not patch or rerender inside this
order.

## Prohibitions

No QMD edit, companion render, source-data edit, model, fit, refit,
prediction, simulation, bootstrap, resampling, estimate, interval, p-value,
FDR decision, diagnostic calculation, sensitivity, scientific artifact
regeneration, full builder, broad manifest builder, historical test or
manifest rewrite, profile, package, lockfile, ledger, later render,
full-project render, commit, push, upload, or publication action is
authorized.
