# REPORT-018 Order72k: DOCX SVG compatibility recovery 001

Date: 2026-09-12, Europe/Berlin.
Dispatcher: Harmonizer `019ff52e-48ac-77b3-9a0e-9a87749a3bba`.
Sole implementation owner: Writer `019ffb39-372e-7262-bfac-192751fd0e63`.
Status: one exact candidate-helper transition and the first existing
assembly/embedding trial, after current release pins reproduce. No render,
capture, office-QA or browser allowance is released by this order.

## Independent disposition

Coordinator accepts the stopped state, not the final documents. The stopped
return is `consolidated_stopped_return.md` under the Writer's
`s2_accessibility_guard_recovery_001/`, SHA-256
`3da07f7f1eec17c850321ff0c84f35c7c4cc8df6575e2069c80b94268645f746`.
Its 444-member seal is
`ddb53080c0eca05ed499752c6c5741fed725a46edf9a6c01049bad793e0a1596`.
All 4,339 historical versions, four existing path/version source aliases and
eight cache pre/post states were independently reproduced. Only the recorded
ordinary main xref transition is present. Table3, S2 and all SVG bytes are exact.

Central stopped acceptance is
`audit/report_harmonization/report018_order72k_docx_svg_compatibility_recovery_001/independent_stop_acceptance.md`,
SHA-256 `afc54dc93e3ac84e4bad3da2c42bbd47bed2b8caf816aa38a325dcfef03ec3cb`.
Its 828-member non-circular seal is
`d796831bbe226284ab2102e133ee3e399cadb13ea9135b547fd62bd3988edb84`.

The three accepted main SVG drawings have native SVG relationships
rId21/rId28/rId35 and both standard DPI and SVG extensions, but no base embed.
The old embedder assumes a base embed and only one extension. This is a
packaging incompatibility, not a numerical or scientific discrepancy. All 20
raw drawings are classified: three exact main SVGs and 17 exact S2 miniplots.

The precise proposal and independent replay are retained under the same
central recovery root in `prospective/` and `proposal_replay/`. Its original
18-member proposal manifest is
`5ed0062db9055614151e55fc1cf1f60e7ff76b6750c9f4002089a224daeb8f29`.
Coordinator independently repeated all 139 isolated tests, syntax validation,
zero-fuzz reverse proof, 1,290 preservation checks and all 13 command-vector
arguments. No full helper entry point or DOCX save was executed. One erroneous
coordinator argument-index assertion is transparently retained, with a separate
strict complete-vector verification; the proposal did not change.

## Exact edit and preservation boundary

Abbreviate Writer's existing candidate root as
`audit/manuscript_nature_health/revision_2026_09_11/consolidated_display_repair_order72k/`.
New recovery evidence is restricted to `docx_svg_compatibility_recovery_001/`
beneath that root. Preserve every prior source, render, image, capture, failed
checker, receipt and manifest. No canonical or accepted website path may change.

The sole implementation edit is Writer-root
`helpers/embed_accepted_svg_figures.py`:

- Exact preimage: `23cf9ee0f46fc9b79962b76c19c6bc3af03d70af6fe580e0120294c2b5b726fe`, 9,614 bytes.
- Exact postimage: `75432be9c10f042b9f46a0207c8fc898db274f483fd4065ab3a823bf6416c38b`, 16,998 bytes.

First retain the exact preimage as
`docx_svg_compatibility_recovery_001/embed_accepted_svg_figures.preimage.py`.
Use the sealed `prospective/embed_accepted_svg_figures.proposed.py` bytes
without formatting or tuning. Require Python syntax PASS and an exact
zero-fuzz/no-offset reverse proof on a separate temporary copy before assembly.
Do not run the full embedder as a preliminary test.

Historical manifests must remain byte-identical. Add only this exact candidate
helper path AND expected preimage-hash alias to the already accepted four
source-version aliases. Old references to 23cf9ee0 resolve through the new
exact preimage; the live execution helper must be 75432be9. Preserve every
older helper/CSS alias and the sole existing cache/xref preimage classification.
Do not exempt the whole helper directory, rewrite a historical manifest or
classify an unexpected version. No Quarto cache change is expected here.

The assembly helper stays
`e89329f60bb477bf726fb1ccc0f3acecd107bab81d4541a1fca2633cf54415aa`,
54,987 bytes. The capture helper, both CSS files, raw DOCX, corrected HTMLs,
all PNG/SVG sources and nineteen separate editable table documents stay exact.
Neither historical `run_stage.py` nor `prepare_correction_manifest.py` may be
edited or invoked for this continuation.

## Validated compatibility behavior

Inspect original relationship records and original exact payloads before any
retargeting. For each existing valid native main SVG, preserve its complete
blip, DPI/SVG extensions, geometry/crops and base/native relationships, and
reuse its existing embedded member. Do not invent a base fallback or raster.

Keep the base-only supplemental path and S8's shared source/two crop windows.
Retarget each base relationship consistently once, reject a conflicting shared
target, and add only its SVG extension. If a DPI list already exists, exact
reversal must remove only the new SVG node and preserve that list. Reject
malformed, duplicate, unexpected, external, unresolved or mismatched inputs.
The tested generic fallback fixtures do not authorize changes to the exact
current three native-only main drawings.

The final package must retain 22 physical byte-exact SVG sources and 23 SVG
appearances, using 19 new supplemental member names and three unchanged main
members. Alongside the 29 approved table PNG parts, this gives 52 drawings.
Keep relationship-based orphan cleanup, SVG content-type checks, exact XML
reversal, unchanged unrelated package members and no new raster-part guards.
No scientific figure export or image manipulation is permitted.

## One bounded assembly and embedding chain

Harmonizer must reproduce the release and input pins, freshly confirm Writer
idle, and send this complete order once to that same Writer. Record an exclusive
file-processing assignment and actual delivery/activation separately. Leases004
and005 remain closed. Do not create a visual lease or reserve a native surface
for these headless operations.

Preflight must reproduce the current stopped package and exact path/version
history, all six inputs in `prospective/future_commands_NOT_EXECUTED.json`,
and all 29 mapped PNG parts and 22 mapped SVG sources. That JSON's helper hash
records the historical preimage; this order supplies the sole execution-time
postimage override. Every other input hash and every command argument stays
exact. Require absence of the three new output destinations immediately before
execution and no competing task-owned assembly/embedding process. Use narrowly
scoped process checks, never a blanket prohibition of unrelated R processes.

Use the existing bundled Python 3.12.14 with lxml 6.1.1, python-docx 1.2.0 and
Pillow 12.3.0. Set `PYTHONDONTWRITEBYTECODE=1`; no installation or runtime patch.
If OS permission is required, request only the exact operation. A permission
denial stops the route and does not authorize a workaround.

Execute the exact `assembly` argument vector from the sealed JSON once, using:

- Input: `project/render_docx_attempt2/ZaunerEtAl2026_NatHealth_phase3_brown.docx`,
  SHA-256 `a36009b0e7f52f61a89650a795dfcc0f7854b1c8be4dff235336135f68ee0d82`.
- Table map: `s2_accessibility_guard_recovery_001/word_table_manifest_attempt5.json`,
  SHA-256 `54b6246876b0ec91ca125465d1b9fe847042e23137e45024ae067844176db18e`.
- Figure map: `word_figure_svg_manifest.json`,
  SHA-256 `f6876a274afe8578528aeb196d18b06e237aa6ef91cb7d03abe3f88772ab3873`.
- New output: `manuscript_assembled_attempt1.docx`.

Only after that command and its complete existing checks pass, execute the
exact `embedding` vector once, with the new assembled input, postimage helper
and `expanded_svg_manifest.json`, SHA-256
`1d68cd537f0aceea9e33a94efcdc531880d586b3d474c165d7b7b9907d34398d`.
The new outputs are `Nature_Health_non_S5_preview_attempt1.docx` and
`svg_embedding_attempt1.json`. Never overwrite or replace an existing output.

Before each command, write a fresh command/start/allowance record under the
new recovery evidence root. Record exact argv, working directory, environment,
stdout/stderr, exit status and output hashes. A started failure consumes that
operation's slot. This is the first existing assembly/embedding trial, not a
new two-trial budget. No automatic second trial, source patch or rerender is
released if a genuinely new defect occurs. Preserve all partial outputs.

## Required postconditions and return

Run complete read-only R 4.6.1/package-structure verification after successful
embedding. Non-analytical OOXML/checksum tooling may supplement it. Require:

- 52 drawings: 23 accepted SVG appearances from 22 exact sources and 29 exact
  table PNG parts from nineteen table keys; no native gt or float-wrapper
  tables left by assembly.
- The unchanged helper's author-block, 22 precise bookmark repairs, unique
  resolving internal links, headings, captions, page starts, orientation
  sequence and 31-section layout checks.
- Main native drawing XML and relationships unchanged relative to the assembled
  input; exact S7/S15 independent A/B structure and S8 crop geometry; all display
  labels and source bytes unchanged.
- Original-to-embedded document XML reverse proof and byte equality for every
  unrelated package member, canonical internal image targets, unique IDs,
  valid content types, no dangling relationship and only documented orphan
  removal. Retained main SVG names need not become nh_main_figure names.
- A3 S2 geometry and the existing 15.55 by 8.90 inch maximum, aspect ratios,
  every mapped table part, approved Table3 order and all historical holds.
- Full current and version-resolved historical rehash, exact corrected HTMLs,
  immutable raw DOCX and source maps, no unclassified package/source drift,
  and completed task-process teardown without signalling unrelated processes.

Create one bounded, non-circular completion or stopped seal under the new
recovery evidence root. Original render logs and all old manifests remain
unchanged. Report actual assembly and embedding invocation counts separately.
Office QA remains 0 of 2 and is not invoked under this order. A successful
structural candidate must be labelled VISUAL_QA_PENDING, not final or accepted.

The browser-security denial remains controlling: no alternate browser,
loopback server/copy, indirect execution, raw CDP or open-in-app workaround.
Corrected HTMLs remain available for manual author review only. Native Word
requires author unlock and separate bounded surface authority; do not unlock
automatically or infer native acceptance from XML or another viewer.

Return to `REPORT018-ORDER72K-NON-S5-INTEGRATED-PREVIEW-REVIEW`. Brown scientific
work and Figure S5 replacement, optional H11 work, canonical promotion, source
language changes, science, packages/locks/profiles, commit, push and upload all
remain held. No additional Quarto, HTML, DOCX or image render is authorized.
