# REPORT-018 owner order 53a: H07 companion no-rerender completion

Date: 2026-08-21

Owner: H07

Status: sealed for one no-rerender continuation

## Controlling disposition

The central stopped-state acceptance is `audit/report_harmonization/report018_h07_order53_stopped_independent_acceptance.md`. It classifies exactly one removed source-side historical HTML and its complete 15-file local asset tree as expected canonical-output cleanup under REPORT-018. The canonical reader endpoint is the existing `_build/nathealth/audit/hypotheses/H07/H07_analysis_preparation.html`.

The removed source-side HTML and asset tree must remain absent. Their historical hashes remain preserved in the order-53 record and inventories. Do not restore, recreate, or add them to the current preparation manifest.

Three `javascript:void(0)` Quarto code controls are excluded from filesystem and reader-navigation link checks. All other links remain in scope.

## Preflight

Before browser or server activity:

1. Reproduce every non-matrix row of the order-53a dispatch manifest. The coordination matrix is dispatch-time evidence only.
2. Require the owner stopped-state record at SHA-256 `22a0f7e6bd5ba808496138420a75c097600e4ce4276248233ce609d04211ee95`, 12,411 bytes, and its 47-row manifest at SHA-256 `0b059a86079ceea2a4e0922185903a24537065e5683efe976def238770eb7c85`, 9,273 bytes.
3. Run `scripts/report_harmonization/check_h07_order53_stopped_acceptance.R` exactly once under R 4.6.1 and require the exact `PASS` disposition with 41 of 41 checks.
4. Require the accepted result source and HTML at `c779c57ffc64a15c82e519ef38e865a39108a6563d82e408e4522253c61e8226` and `7814860467f71311c56e22960e757059524ccc3890b0721708eda7a5c661ab40`.
5. Require the companion authoring and build QMDs to remain byte-identical at `a6c05e81333bb612e99ef431f42f47860ac26b6d5c0f0f44ecbce019ee470d1b`, 49,762 bytes.
6. Require the canonical companion HTML at `4c03a3e3cdfa1c6eac13d5785ceee8306e6d0941278b679a1eb88e357b270d93`, 654,955 bytes.
7. Require the 1,235-row preparation manifest at `db1b00058848d27eed9d5ece9d9eae97851d1bdc997c4f2fe28d2ce9abd3c8e2`, 335,146 bytes, unique, non-circular, and fully live-exact.
8. Require the profile, helper, both tests, semantic summary and ledger, result acceptance, order-53 release/order/dispatch, and every scientific input and artifact to remain exact.
9. Require the source-side historical HTML and all 15 local assets to remain absent, zero build symlinks, and no Quarto, Pandoc, semantic-hook, H07, or loopback process.

Stop before browser activity on any unclassified drift.

## Complete static acceptance against the existing HTML

Without editing or executing any QMD, helper, test, or manifest:

1. verify exactly 21 native gt tables in source order, two PNG figure endpoints, and one top-down Mermaid;
2. verify the semantic summary and 850-row ledger, exact reverse to pre-hook SHA-256 `7dadaaaa92c89346af7a46490419c2af98602235cf655daa49a756c9b76dec4a`, exact reapplication to the canonical HTML, unique document IDs, and all 1,144 table-header tokens resolving once within their own table to `th` elements;
3. verify captions, notes, alt text, source-data relationships, formula registry, sample and diagnostic contracts, METRIC-011 reconciliation, headings, deviation anchor, active H07 preparation navigation, country-coded sites, and zero embedded errors, warnings, or unresolved cross-references;
4. verify all 405 applicable links and required fragments, including the three dynamic result links, while recording the three `javascript:void(0)` code controls separately as non-file controls;
5. verify the 1,235-row live manifest, accepted result, profile, helper and tests, protected scientific identities, and classified build delta; and
6. verify that the source-side canonical cleanup remains exactly the accepted 16-path set and that no other path is absent or unexpectedly changed.

The stale preparation test remains historical evidence. Do not edit or execute it. Do not rerun the already passing result reader test.

## Secure-loopback visual QA

After the static checks pass, serve `_build/nathealth` read-only on one unused high port bound only to `127.0.0.1`. Inspect the exact H07 preparation/provenance route at:

- 1440 by 1000;
- 708 by 1000;
- 720 by 500 as the 200-percent-equivalent view; and
- both exported PNG figures at their intended 170-mm final size.

Inspect all 21 tables, both PNG figures, the Mermaid, captions, notes, links, navigation, table scrollers, wrapping, clipping, overlap, and page overflow. Exercise every contained narrow table scroller and return it to its initial position. Require essential exported-figure text of at least 7 points at final size. Require no page-attributable console warning or error.

Close or reset the QA surface, stop the server, prove no listener remains, and rehash the complete build and protected inventories. Post-QA inventories must be byte-identical to the pre-QA inventories.

## Return

Return one concise completion record and one unique, non-circular, live-exact evidence manifest, or one consolidated genuine-page-defect stop. Do not patch, restore, rerun the helper or tests, or render if a defect appears.

No Quarto command, QMD execution, source/helper/test/manifest/profile/HTML edit, scientific computation, artifact regeneration, result-page change, H08 or later render, Brown action, full-project render, package or lock change, commit, push, or upload is authorized.
