# REPORT-018 Order72k: S2 Unit and Scaling width repair

Date: 2026-09-11.
Status: one explicit additional S2-only display capture is released after the
attached non-circular seal reproduces. This is not a general retry budget.
Dispatcher: Harmonizer `019ff52e-48ac-77b3-9a0e-9a87749a3bba`.
Sole implementation owner: Writer `019ffb39-372e-7262-bfac-192751fd0e63`.

## Independent stopped-state acceptance

The prior S2 attempt3 executed successfully but is NOT visually accepted.
The Unit column clips HH:MM and the Scaling column clips threshold. The
40-pixel and 50-pixel widths and the numeric-only columns 3-12 containment
check explain the omitted protection. All three failed PNGs remain evidence.

The owner stop record is
`audit/manuscript_nature_health/revision_2026_09_11/consolidated_display_repair_order72k/environment_capture_recovery_001/stopped_visual_return.md`,
SHA-256 `b5a24e0c4bdd47a18fee444ac01339eb4fd295268b64a2bd31e4cf745c15a13f`.
Its 295-member manifest is
`852fca237ffb23c1e294095aebd7894e150d350f4c41be32463a857711446a7e`.
Coordinator R 4.6.1 verification reproduced all 295 members, all 854 prior
preservation rows, 244 exact rendered-source body cells and 17 exact image
payloads, with 18/18 checks. The three older text flags are separately
reconciled as whitespace-only differences between original fragment markup
and browser serialization. They do not waive the genuine clipping failure.

Harmonizer's independent review is
`report018_order72k_non_s5_integration_dispatch/s2_visual_stop_independent_review.md`
under `audit/report_harmonization/`, SHA-256
`6b63fcbdf0906f22b620e94d604f1e7c3f0579843c6682609b392d0a75251474`.
Its six-member seal is
`5345d8fea206671b50f8956e361aacce2863b008de67c6525de9abea08218645`.
The old listener is closed and visual lease002 is released.

Original Order72k `359d8d730930fb0008779b3aca8db840f24663a100b61b62807f766b51561332`
and its cache and capture recoveries remain controlling except for the
precisely enumerated changes below. Brown scientific execution, Figure S5
replacement, the optional H11 route and canonical promotion remain held.

## Exact implementation and preservation boundary

Use the shared checkout and existing two Order72k candidate roots. Abbreviate
the Writer root as
`audit/manuscript_nature_health/revision_2026_09_11/consolidated_display_repair_order72k/`.
Create new recovery evidence only beneath `s2_width_repair_001/` there and the
fresh capture destination `capture_s2_attempt4/`. Do not precreate the capture
destination: the helper must enforce its absence. Preserve every older capture,
manifest, failed checker, log, receipt and stopped package unchanged.

Before editing, copy the exact current helper and both candidate CSS files
to distinctly named preimage files in the new evidence root. Pin those copies.
For historical manifest reconciliation only, these three explicitly authorized
live transitions resolve to those exact preimage copies. Do not rewrite an
old manifest, treat an old postcondition as current, or exempt any fourth path.
All other 295 stopped members and 854 preservation entries remain exact.

The only implementation changes permitted are:

1. Writer-root `helpers/capture_word_tables.mjs`:
   preimage `8dd21ba1d978e36d1cafa0669b7c2456306b66e996f013f44b86ebc2549e0174`,
   21,399 bytes; exact postimage
   `7f5cdeb35280de680dfc6135dca462ba5e16cbc2e5085539810805d34f70331a`,
   21,562 bytes.
2. Writer-root `project/order72k_layout.css`, and
   `audit/manuscript_nature_health/consolidated_display_repair_2026_09_11/integration_candidate_order72k/project/order72k_layout.css`:
   each preimage `44b5a324d14ede154901a28005f042a7b1edc544a80e589a30ccaebe82a087c5`,
   2,492 bytes; each exact postimage
   `03d9ff7f9cbe1d258d335f262b948f32f6b8e59dba46dbe9cf336c9d6708a4e9`,
   2,493 bytes.

The exact prospective files are retained under
`audit/report_harmonization/report018_order72k_s2_unit_scaling_width_repair/prospective/`.
Use their sealed bytes to avoid formatting iterations. Coordinator has not
applied them to the owner paths. Require Node syntax validation and exact
reverse proof to each preimage before capture.

Unit column 2 changes from 40 to 80 CSS pixels. Scaling column 13 changes
from 50 to 100 CSS pixels. Every other column width remains unchanged:
`[200,80,105,86,86,86,86,86,86,86,86,86,100,231]`.
The table, capture wrapper and S2 CSS width become 1,490 pixels, the exact sum.
The existing 3,000-pixel capture viewport and device scale factor 2 stay fixed.
No font, padding, row range, miniplot size, label, numeric string or image
payload changes. Vertical continuation stays [0,9), [9,17), [17,23), with all
14 columns in each part. No crop, horizontal split or text shortening.

The helper's text-rectangle guard expands to all thead, tbody and tfoot th/td
text, including Unit and Scaling. It checks left, right, top and bottom with
the existing 0.5-pixel tolerance. Its existing JSON key
`numericalTextOverflow` is retained for compatibility but now represents this
explicit all-cell scope. Its diagnostic records both axes. All other checks,
browser flags and disabled scientific-figure capture logic remain unchanged.

## Preflight, one capture and required visual acceptance

Harmonizer must first reproduce the release and input pins, confirm Writer's
stopped state and issue one new exclusive visual lease. Lease002 stays closed.
Do not activate another owner or change the hypothesis coordination matrix.
Record actual delivery and observed owner execution state separately.

Rehash the existing immutable 12-file `preview_attempt1/` served set and all
19 completed `capture_attempt1/` files. Preserve the old source HTML and all
attempt3 output. Do not render or restage content for this S2 capture.

One restart-only server helper may be copied to `s2_width_repair_001/` from
the previous recovery, changing only its new evidence/receipt location and,
if necessary, one unused port. Reproduce its 12-file membership and mandatory
no-symlink/private-resource preflight. Serve only the existing rendered-output
root through GET/HEAD on 127.0.0.1, with no directory listing or write endpoint.
Never serve the project or recovery evidence directory. Keep old receipts
unchanged. Record command, PID, port, root, start, exact target URL and teardown.

This order grants one new S2-only capture attempt, not another environment
retry. Use the same installed Node, Playwright and Chrome with the existing
ephemeral task profile. Request the capture with narrow
`sandbox_permissions=require_escalated`, without a broad approval prefix.
The order is not OS permission. A permission or browser-policy rejection
stops the route; do not change browser surface, tunnel, use CDP or bypass it.

```sh
TMPDIR=/private/tmp/order72k_3ok569jd /Users/zauner/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node audit/manuscript_nature_health/revision_2026_09_11/consolidated_display_repair_order72k/helpers/capture_word_tables.mjs http://127.0.0.1:58005/main/ZaunerEtAl2026_NatHealth_phase3_brown.html audit/manuscript_nature_health/revision_2026_09_11/consolidated_display_repair_order72k/capture_s2_attempt4 only-s2
```

Only the recorded port may differ if 58005 is occupied. Verify the task-owned
TMPDIR exists without a substituted symlink. Do not take over another listener
or kill an unrelated process. No S5/S6/S10 capture or scientific image export.

Before each part's PNG is written, the repaired helper must measure actual
text rectangles in the laid-out cells and pass all-cell containment, including
the Unit and Scaling strings and their unchanged padding. It must also pass
the unchanged image-containment/aspect-ratio and exact source-cell checks.
The increased canvas must not clip the distribution column. A failed measure
stops the run; these prospective widths are not permission to accept a failed
fit check or tune a second candidate. This invocation includes that measurement
gate. No separate uncounted browser trial is authorized.

After capture, use R 4.6.1 to reconcile all 244 body cells character-for-character
against the frozen rendered source, with a separately labelled whitespace-only
comparison against the original fragment. Preserve 17 metric rows, six groups,
14 columns, three complete headers, final notes and 17 exact image payloads.
Inspect every part at original size and intended Word size, not just the two
reported labels. Record actual capture dimensions and display dimensions from
the unchanged Word helper: S2 maximum width 15.55 inches and maximum height
8.90 inches, aspect ratio preserved, in its existing A3 landscape section.
Verify label legibility, complete values, miniplots, notes and absence of
clipping/overlap. DOM success alone is not visual acceptance. Do not modify
Word page geometry or the Word helper to make this width repair pass.

Stop the server immediately after bounded capture/inspection, close task
capture contexts, prove the exact process and listener are gone and rehash
the immutable served set and older captures. Record any limitation honestly.

## Remaining document allowance and mandatory return

On S2 PASS only, append an exact path-mapping record pointing the candidate
assembly inputs to the new sealed attempt4 manifest. Do not edit historical
dry-run, correction, failed-trial or owner seals. Reuse the four completed
S5/S6/S10 PNG parts byte-for-byte.

The existing consolidated correction pass remains one selection HTML, one
main HTML and one main DOCX, each still unused. This order adds none. Use only
the previously approved isolated candidate targets, normal-cache permission
strategy and three exact Lua dependency copies. Original assembly/SVG embedding
and office-QA allowances remain 0 of 2 consumed at this release. No canonical
file or accepted build may be replaced.

Native Word still requires the author to unlock the Mac. Do not unlock it
automatically or substitute another viewer for native acceptance. Work that
does not require the locked surface may continue under the existing bounds;
return VISUAL_QA_PENDING if the required native views remain unavailable.

A new clipping defect, unexplained drift, failed execution or exhausted slot
requires one consolidated stopped return, not a new implicit retry. The return
gate remains `REPORT018-ORDER72K-NON-S5-INTEGRATED-PREVIEW-REVIEW`. Brown fits,
models, predictions, inference, data pipelines, S5 replacement, optional H11
compatibility work, manuscript-language changes and canonical promotion stay
held. This release resolves only the two-column S2 display stop.
