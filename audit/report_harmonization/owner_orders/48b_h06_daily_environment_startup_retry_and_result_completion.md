# REPORT-018 owner order 48b: H06_daily environment-startup retry and result completion

Date: 2026-08-21

Owner: H06_daily task `019fec6a-20d3-7710-ab9b-a035e0874182`

Status: **AUTHORIZED ONCE AS AN ENVIRONMENT-PERMISSION RETRY**

Target: `notebooks/hypotheses/H06_daily.qmd`

The H06_daily companion and every later REPORT-018 target remain held.

## Authority and exact purpose

Order 48a completed its source correction, frozen-source display repair,
candidate validation, one-time six-file promotion, and full pre-render gate.
Its sole normal-profile target invocation then entered the documented
restricted `renv` transient-cache loop before knitr, Pandoc, the configured
semantic hook, or target HTML creation.

The accepted startup stop is recorded in:

- `audit/report_harmonization/report018_h06_daily_order48a_environment_startup_stop_independent_acceptance.md`,
  SHA-256
  `70427f41c9a9bcc4579ce29a10a659d985139673b0eae901c6ec1f02db130d87`;
- its 35-row non-circular manifest, SHA-256
  `e400e585c6a2ad60e1ef1f81aaade3e3825c5cdb964c022f7ffa855a63121795`;
- the owner 49-row startup-stop manifest, SHA-256
  `cfdaaa7d3fae5a89706b824495b48bc40884579553aab0d4a8822bc40ce64aba`;
  and
- the accepted read-only checker, SHA-256
  `48680b3937befb6f0261a5222f26e06a8c2c4f75f54ac72839eb356c6fd80255`.

Order 48b authorizes one environment-startup retry of the unchanged H06_daily
result target with the established narrow elevated access to the existing
user-owned `renv` cache. It does not authorize another pre-render repair,
candidate iteration, durable display write, promotion, or cleanup loop.

## Hard preflight pins

Before the retry, require every row of the 35-row independent acceptance
manifest exact and rerun once:

```text
Rscript --vanilla scripts/report_harmonization/check_h06_daily_order48a_environment_stop.R
```

Require the exact PASS line:

```text
H06_DAILY_ORDER48A_ENVIRONMENT_STOP=PASS owner_rows=49 build=846 protected=3369 promoted=6 knitr=FALSE pandoc=FALSE hook=FALSE html_changed=FALSE
```

This checker is an infrastructure rehash. It does not start the project
profile, execute a QMD, rerun the focused display test, or calculate a
scientific result.

The following identities are hard pins:

- repaired result QMD:
  `8f696f3f89fe9ea18c8066b756a01fc5da6b41e86f8ac7d37d1e16f9c10ba639`,
  65,349 bytes;
- held companion QMD:
  `ef4fde67d5dd2ebbb14deb76c025b5ac2ac9b6a34f7b34ca62c2fdb382bde709`,
  35,409 bytes;
- unchanged stopped result HTML:
  `15c537269ac0be96ce06c6b574946dc0b98d46afe36c3c696f804a216a7d0c76`,
  11,691,631 bytes;
- held companion HTML:
  `7f3adfd0d80c9e083baa34f6aa056bc12e0a30e233f786fbc55a6ad6ac176259`,
  4,613,650 bytes;
- profile:
  `80dd05573b6c764272d0d0aa1c82571bdaab3cb2bee9ee34441e2456318708e3`;
- `.Rprofile`, `renv/activate.R`, and `renv.lock`:
  `3f9d62fc3f1bf5888a09816101b705f9844ef4cd44ee5ad157f4168dac4af4d4`,
  `51798a35c2772b94b4a73ae7a7cc928975ff6e628a17bad1bc73d30defca82e6`,
  and
  `3bf99c633fb123626eb14d29f0847b91f71030c92e90331fe401e3204bca8350`;
- accepted display refresh implementation:
  `154091b99da11b51b8e41a53f1532c0bf7a57f44ec6e58b35d0fa19913bf2ce6`;
- accepted focused display test:
  `aca33f5815bb34979513caa7a50a9a38ba66e7bb22d228c8c04f6afb010e42f7`;
- current display manifest:
  `395c112968c00294cbc085246894feae9cd7746e4e5c5714b96cb9e59fd90fc6`;
- unchanged Figure 5:
  `e28c639f23b3f33687ca046a77069153bb66073d29cd03d96fd03408c4317d98`;
  and
- the six promoted Figure 1 through Figure 4 files at the exact identities in
  the 35-row independent acceptance manifest.

Require the 846-row pre-render build inventory and 3,369-row protected
inventory to reproduce against the live workspace. Require the stopped
semantic directory `/private/tmp/H06_daily-order48a-semantic.T6899t` to remain
present and empty. Confirm PIDs 5083, 5097, and 5106 are absent and that no
competing Quarto, Pandoc, semantic-hook, H06, or H06_daily render process is
active. Stop before execution on any drift.

Do not run the display refresh, focused display test, QMD parser, candidate
generator, source verifier, package check, or any preliminary project-profile
startup. Do not copy or promote a display again.

## Sole environment-startup retry

Create one fresh absolute empty semantic-audit directory under `/private/tmp`
with mode 0700. Preserve the failed directory unchanged. Run exactly once:

```text
GT_HTML_SEMANTIC_AUDIT_DIR=<fresh-absolute-directory> quarto render notebooks/hypotheses/H06_daily.qmd --profile nathealth
```

Execute this invocation with `sandbox_permissions=require_escalated`, or the
exact task-equivalent narrow permission that permits transient writes only to
the existing user-owned `renv` cache. Preserve normal `.Rprofile`,
`renv/activate.R`, R 4.6.1, Quarto 1.9.37, the project profile, and the
configured post-render semantic hook.

Do not set `R_PROFILE_USER=/dev/null`, change a library path, use
`--no-execute`, remove or alter a cache lock, edit the cache, install or update
a package, change `renv.lock`, or bypass the profile or semantic hook.

This is the only retry. If startup stalls again, if the render exits nonzero,
or if a later gate fails, interrupt only that exact session, prove complete
process teardown, and return one fail-closed package. Do not retry, patch, or
rerender inside this order.

## Completion after successful target execution

After exit 0, continue the unchanged order-48a result-page acceptance contract
without reopening the pre-render stage:

1. Require a nonempty semantic audit with `REPAIRED` or a proven already-valid
   equivalent, exact reverse and forward evidence, document-wide unique IDs,
   and every explicit `headers` token resolving exactly once to its intended
   `th` within its own table.
2. Require exactly 14 native gt tables, five figures, seven dynamic links, and
   the accepted endpoint order.
3. Reconcile Tables 5, 6, and 7 to distinct predictor-specific bodies with 15
   metric rows and four placement columns each, using the already accepted
   frozen-source contract.
4. Require all five figures, captions, alt text, paired source links, values,
   marks, order, and labels. Figure 5 remains unchanged. Figures 1 through 4
   must retain the accepted promoted display identities as their durable
   sources and pass at least 7.0 effective points at 170 mm and 708 pixels.
5. Require the accepted complementary-daily hierarchy, hourly-main links,
   reciprocal companion link, deviation anchors, active navigation, all nine
   country-coded sites, zero embedded errors or warnings, and zero unresolved
   internal targets.
6. Require the held companion source and HTML, profile, semantic tools,
   accepted source data, scientific artifacts, and unrelated hypotheses
   unchanged.
7. Classify the build delta exactly as in order 48a. Keep
   `audit/report_harmonization/phase4_corpus_manifest.csv` unchanged.

Do not execute a broad manifest builder or rewrite a historical manifest.

## Secure-loopback visual QA and teardown

After all nonvisual gates pass, serve only `_build/nathealth` through one
read-only server bound to `127.0.0.1`. Inspect only the H06_daily result page at
1440 by 1000, 708 by 1000, and 720 by 500, plus the original and 170-mm figure
sizes. Inspect all 14 tables and five figures, especially Tables 5 through 7
and repaired Figures 1 through 4. Verify typography, scrollers, legends, axes,
labels, symbols, panels, captions, disclosures, links, navigation, clipping,
overlap, wrapping, and page overflow.

Stop the server, prove no listener remains, reset the viewport, close the QA
tab, and require post-QA build and protected stability.

## Return and prohibitions

Return one complete non-circular acceptance package or one final environment
or page stop. Record the exact elevated invocation boundary without exposing
private cache contents.

No QMD edit, source-data edit, model, fit, refit, prediction, simulation,
bootstrap, resampling, inference, p-value, FDR, diagnostic, scientific
artifact regeneration, display regeneration, candidate iteration, promotion,
companion edit or render, later render, full-project render, profile, package,
lockfile, ledger, manuscript, commit, push, upload, deletion, or publication
action is authorized.

The H06_daily companion and every later REPORT-018 target remain held pending
independent result-page acceptance.
