# REPORT-018 Order72h: one coordination-table wrapper and replacement render

Date: 2026-09-11

Status: SEALED FOR ONE HARMONIZER CONTINUATION DISPATCH.

Executor: Harmonizer task 019ff52e-48ac-77b3-9a0e-9a87749a3bba.

This order releases only the reviewed mobile Coordination status table fix,
the explicitly classified dependent verifier copy, and one replacement
standalone preview render. All Order72e scientific/display contracts, browser
policy boundaries and production holds remain controlling.

## Authority and exact state

Read and rehash this order and the complete unique, non-circular release at:

    audit/report_harmonization/report018_order72h_mobile_coordination_scroll/

Read independent_stopped_disposition.md, current_execution_pins.csv and the
source/table/lineage/replay evidence. The package explicitly records, rather
than hides, the checker transition missing from the old Order72f release.
Historical manifests must remain byte-identical, with exact old rows resolved
through their retained historical copies where applicable.

The only existing source permitted to change is:

    audit/manuscript_nature_health/manuscript_figure_table_selection.qmd

Preimage SHA-256
9acec033d0c24cbb0ee7649c38f55d5cea90052e11b6be7fc5f9e7310890d8f3,
49,865 bytes.

The current canonical HTML remains protected at SHA-256
82100e0d3990dec39f61e94970e7a434b02cf94a6d25819ede1be4d24f4130a6,
30,925,051 bytes, until full replacement-candidate acceptance.

The first rendered SVG candidate must remain untouched:

    audit/manuscript_nature_health/figure_table_selection_svg_revision_2026_09_11/rendered/manuscript_figure_table_selection.html

SHA-256 7688f8ea58e0045fca25c6de971e418b7c6c873dc83f75ed34d942fdcb1c36b4,
29,370,696 bytes.

Use only this new owner recovery root:

    audit/manuscript_nature_health/figure_table_selection_svg_revision_2026_09_11/order72h_scroll_recovery/

Require it absent before starting. Preserve exact local copies of the current
QMD, current checker and old QA/stop records there before any promotion.
Do not overwrite any earlier owner evidence, candidate, checker or preimage.
Preserve the reconstructed original checker in the central release.

The previous loopback server must be stopped and its tab closed before any
new render. Reconfirm no listener on its recorded port 8765 and no competing
selection render or preview server. Do not terminate unrelated processes.

## Exact source and verifier implementation

The source change is only the central coordination_table_preimage.txt replaced
once by coordination_table_postimage.txt. It wraps the complete existing
14-row Markdown table in:

    <div class="local-table-scroll" role="region" aria-label="Coordination status" tabindex="0">

and its closing div, with the sealed blank lines. Preserve every table byte,
the heading, all other prose, the 20 SVG references, 19 table includes, Table 3
ordering and all CSS. No text, caption, number, value, interval, p-value,
selection or source-asset change is authorized.

The exact prospective QMD is available as prospective_selection.qmd in the
central package. Its SHA-256 must be:

    197e571f3aaa2be34747ee742a20018f98d541eed87851250b4f3eda131c72a9

49,967 bytes. Reproduce it in the new owner root; require exact reversal to
the full source preimage. Do not make formatting iterations or unrelated edits.

Copy the sealed verify_selection_svg_scroll_recovery.R into the new owner
root, unchanged at SHA-256:

    ae2e8a3a4a43c99dc7e0bccf49b812d26cfd695d775e6da001822dbb97983cd7

38,251 bytes. The current ef2fd6bf checker and reconstructed 0e97b329 checker
are frozen history. The new copy handles the exact wrapper source transition
and writes new evidence only. It must not write into the old qa directory.

Create the new qa and rendered subdirectories explicitly before any execution.
Use R 4.6.1, RENV_CONFIG_AUTOLOADER_ENABLED=FALSE and the accepted library
/Users/zauner/Library/R/arm64/4.6/library. Parse the copied verifier and run its
complete source phase on the prospective QMD once. The expected source result
is 21/21. For owner execution, leave ORDER72H_VERIFY_OUTPUT_ROOT unset so the
default is exactly this recovery root; the central simulation used only a
temporary output override and is not an owner write destination.

On source PASS and complete protection checks, promote the QMD once to the
exact 197e571f postimage. Do not rerun any old builder, extraction helper,
scientific code, historical verifier or broad manifest builder.

## One replacement render with precreated output destination

Keep the current standalone Markdown-only, execution-disabled,
embedded-resource configuration. Use installed Quarto 1.9.37 and the established
narrow Quarto cache access, without package, lock, cache, HOME or profile edits.

Require this temporary sibling absent before the sole invocation:

    audit/manuscript_nature_health/manuscript_figure_table_selection_order72h_candidate.html

The final new recovery destination below must also be absent, and its parent
directory must already exist and be verified within the owner root.

From audit/manuscript_nature_health, invoke exactly once:

    quarto render manuscript_figure_table_selection.qmd --to html --output manuscript_figure_table_selection_order72h_candidate.html

No --output-dir, path-valued --output, website profile, alternate target,
full-project render, quarto preview or computational engine is allowed.
The earlier successful 72f render remains historical. This order adds exactly
one replacement render; failure stops without another attempt.

After exit 0, record the new sibling SHA/bytes, confirm all frozen inputs and
old canonical/candidate HTMLs, and move the new file intact once to:

    audit/manuscript_nature_health/figure_table_selection_svg_revision_2026_09_11/order72h_scroll_recovery/rendered/manuscript_figure_table_selection.html

Require exact post-move SHA/bytes and absence of the temporary sibling.
Do not overwrite the prior candidate. Unexpected persistent sidecars or
changes outside the exact output boundary stop before acceptance.

## Complete candidate acceptance and QA

Run the copied verifier's complete HTML phase against the new QMD and candidate.
Expected result is 43/43, including all 20 decoded SVG hashes, 19 native gt/22
total tables, 11 disclosures, header semantics, all retained values/captions,
exact old-candidate visible text and all 22 table-cell vectors, and exactly one
accessible Coordination status scroller with all 45 cells preserved.

Order72e's conditional candidate-only semantic normalization, with exact
reverse/reapply proof and no visible-content change, remains the sole allowed
HTML normalization. Do not patch a genuinely new finding or rerender.

Repeat the complete served visual review at 1440, 708 and 390 CSS pixels and
intended display sizes, using the same supported browser surface and honest
native-versus-wrapper viewport disclosure. Check all 20 SVGs, all table content,
all 11 disclosures and Table 3 order. Add explicit Coordination status checks:
the region is contained, keyboard-focusable and horizontally scrollable when
needed; every cell is reachable and legible; its scrollbar is local.

At every width, compare documentElement.scrollWidth with
documentElement.clientWidth, not only window.innerWidth. Require no page-level
horizontal overflow, including the 375-pixel usable width encountered inside
the 390-pixel review viewport. No tolerance may hide the observed 8-pixel defect.

Stage only immutable rendered output in the new recovery root, perform the
mandatory symlink-containment preflight, and use one read-only GET/HEAD server
bound strictly to 127.0.0.1. Never serve source trees or bypass a browser denial.
Record actual browser limitations. Teardown the server/tab, prove no listener
and rehash all inputs and rendered assets.

Only complete PASS permits the single canonical preview HTML replacement
already authorized in Order72e, using exact accepted candidate bytes. Preserve
all old HTML/source/evidence snapshots and create a new non-circular combined
completion seal in the recovery root. No historical manifest is rewritten.

Mandatory return remains REPORT018-ORDER72E-SELECTION-SVG-PREVIEW-REVIEW.
Coordinator independently accepts before any Writer handoff of this preview.
Writer's separate Order72d is unchanged. All production website/corpus,
production manuscript, hypothesis/Brown, science, packages/lockfiles,
commit/push/upload and Brown scientific-reopening holds remain in force.
