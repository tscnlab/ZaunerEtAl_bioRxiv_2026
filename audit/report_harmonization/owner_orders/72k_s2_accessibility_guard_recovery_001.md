# REPORT-018 Order72k: exact S2 accessibility-guard recovery 001

Date: 2026-09-11.
Dispatcher: Harmonizer `019ff52e-48ac-77b3-9a0e-9a87749a3bba`.
Sole implementation owner: Writer `019ffb39-372e-7262-bfac-192751fd0e63`.
Status: one new S2-only capture allowance after this order and its non-circular
release and dispatch seals reproduce. No other new execution allowance.

## Accepted stop and controlling history

Attempt4 is accepted as a fail-closed accessibility-guard stop, not as visual
acceptance. Its seven reported overflows are precisely the seven intentional
screen-reader descriptions in the first partition. The generic overflow
clearing also changed those hidden spans before the containment check. No S2
PNG or completed capture manifest was created. No numerical or scientific
discrepancy was found. All previous S2 capture allowances remain consumed.

The Writer stopped record under
`audit/manuscript_nature_health/revision_2026_09_11/consolidated_display_repair_order72k/s2_width_repair_001/`
is `stopped_check_return.md`, SHA-256
`ec2e2841de79d52ba8cb19dbeb7b1e3aa72d8b7ef2aa75476f8df00a1ea82a51`.
The 335-member `stopped_check_owner_manifest.csv` is
`75259255f98f19a291471e617a42be6bd304db523450167a1eb2c2b9a90045f9`;
`stopped_check_owner_seal.json` is
`abae2f7983fb83a02b4abc5d8563dce5413c4b90f0b880306dd160cd73dcf7e2`.
Teardown receipt
`e46d1d96d5f753309291222eb8b401ac5da4f3fddd0eafc3900de42a188d717d`
and the Harmonizer lease003 closure remain historical and immutable.

Coordinator independent stopped acceptance is
`audit/report_harmonization/report018_order72k_s2_accessibility_guard_recovery_001/independent_stop_acceptance.md`,
SHA-256 `16184fe6fda0971309a26e58135a2003e1441616fc8de448977e9eeb3642cb17`.
Its non-circular 353-member seal is
`370c1a4a1942906138d6e709371a05ca3dc7a74b39032b14baa397c186018674`.
R 4.6.1 reproduced 335/335 owner members, 2,309/2,309 historical rows with
only the three existing exact preimage aliases, and the 17-description source
contract. A fresh independent static replay passed 404/404 checks and an
exact zero-fuzz reverse proof. These are not browser or visual checks.

Original Order72k `359d8d730930fb0008779b3aca8db840f24663a100b61b62807f766b51561332`,
its cache/dependency closure, and the width order
`982dc5b6213ca813c0883455197140025d367388a7956b19747af61763b13d3f`
remain controlling except for this exact helper transition and new attempt5.

## Exact owner write boundary

Use the shared checkout. Abbreviate the existing Writer candidate root as
`audit/manuscript_nature_health/revision_2026_09_11/consolidated_display_repair_order72k/`.
New recovery evidence may be created only beneath
`s2_accessibility_guard_recovery_001/` there. The sole new capture destination
is `capture_s2_attempt5/`; it must not exist before the capture helper runs.
The two original Order72k candidate roots and their inherited conditional
document-completion bounds remain unchanged. No canonical promotion.

Before editing, preserve the live `helpers/capture_word_tables.mjs` as an exact
new `s2_accessibility_guard_recovery_001/capture_word_tables.preimage.mjs`.
The preimage must be
`7f5cdeb35280de680dfc6135dca462ba5e16cbc2e5085539810805d34f70331a`,
21,562 bytes. Apply only the centrally retained prospective helper:

`audit/report_harmonization/report018_order72k_s2_accessibility_guard_recovery_001/prospective/capture_word_tables.mjs`

Exact postimage:
`ecf7525cb5a89438b43588c8ac2acd9ed8e8bcb568b3fc78676ba349792871ec`,
42,404 bytes. Use those sealed bytes without formatting or tuning. Require
installed-Node syntax PASS and zero-fuzz application of the retained
`prospective/reverse.diff` to a fresh temporary copy, reproducing the preimage.
Do not import or run the full capture helper as an uncounted preliminary test.

Both candidate `project/order72k_layout.css` files stay
`03d9ff7f9cbe1d258d335f262b948f32f6b8e59dba46dbe9cf336c9d6708a4e9`,
2,493 bytes. Widths stay
`[200,80,105,86,86,86,86,86,86,86,86,86,100,231]`, total 1,490 pixels.
Keep viewport 3,000 pixels, device scale 2, all fonts, padding, row partitions
[0,9), [9,17), [17,23), miniplots, all 14 columns and every source cell unchanged.
No extra crop, split, text shortening, accessibility removal or page-size edit.

Historical reconciliation must key on BOTH exact path and expected version.
The stopped335 references to helper7f5c resolve only through the new retained
7f5c preimage after its authorized transition. Older references to helper8dd
retain the existing `s2_width_repair_001/capture_word_tables.preimage.mjs`.
Old CSS44b5 references retain the two existing width-repair preimages;
current CSS03d9 remains live-exact. Never apply a blanket path exemption or
route an older helper version to the new preimage. Preserve all historical
manifests, stopped evidence, prior capture files and receipts byte-for-byte.
Any unclassified version/path transition stops.

## Exact guard contract

The only exemption is text directly belonging to an individually validated
hidden description span. The immutable 17-record contract pins original body
row and column14, parent/span tags, exact description and inline style, sibling
image payload-string SHA-256, absent alt and original aria-hidden attribute.
Require exactly one direct single-text-node span and one associated image in
each expected cell. Missing, extra, moved, nested, restyled or re-associated
nodes fail closed.

Before cloning, source records must match that entire contract. In the existing
browser callbacks, computed hidden semantics must match absolute positioning,
1-pixel dimensions, hidden overflow, zero clip rectangle, inset(50%) clip-path
and nowrap. The generic overflow-clearing loop skips only validated element
identities so their hidden styles remain exact. Revalidate the original-row
subsets after each partition, exactly 7/5/5 descriptions.

All other nonempty text in every thead/tbody/tfoot th/td remains subject to
left/right/top/bottom containment with the unchanged 0.5-pixel tolerance.
Unit, Scaling, visible distribution text, headings and notes are not exempt.
Keep image containment, aspect-ratio and exact body-cell checks before PNG
creation. Do not weaken those gates to achieve a pass. Record the new
`screenReaderDescriptions` proof metadata alongside the unchanged capture
manifest interface. Static synthetic styles are not measured browser styles.

## One new capture and bounded visual route

Harmonizer must read this complete order, reproduce input/release/dispatch
pins, confirm Writer idle and lease003 closed, then create one fresh exclusive
visual lease004. Record delivery and actual owner execution separately. No
other owner wake or hypothesis-matrix edit is authorized by this order.

Rehash the immutable 12-file `preview_attempt1/` set, 19 prior
`capture_attempt1/` files and every stopped S2 attempt. No render or content
restaging is allowed before this S2 capture. Copy the existing restart-only
server helper to the new recovery evidence root; its RECORD-relative receipt
and preflight paths therefore become new without changing served content.
Preserve its GET/HEAD-only, no-directory-listing behavior. Only an occupied
port may require an exact port-literal change and recorded reverse proof.

Run the mandatory complete symlink/private-resource preflight, exact served
membership and hash checks, and verify the task TMPDIR is not a substituted
symlink. Serve only the existing minimal rendered-output set on 127.0.0.1,
never the project or evidence root. Record command, PID, port, root, target URL,
start and teardown in fresh evidence. Do not stop an unrelated listener.

This order grants exactly ONE NEW S2-only capture invocation into attempt5:

```sh
TMPDIR=/private/tmp/order72k_3ok569jd /Users/zauner/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node audit/manuscript_nature_health/revision_2026_09_11/consolidated_display_repair_order72k/helpers/capture_word_tables.mjs http://127.0.0.1:58005/main/ZaunerEtAl2026_NatHealth_phase3_brown.html audit/manuscript_nature_health/revision_2026_09_11/consolidated_display_repair_order72k/capture_s2_attempt5 only-s2
```

Only the recorded port may differ if occupied. Retain the existing installed
Node/Playwright/Chrome and task-only ephemeral profile. Request actual narrow
tool permission with `sandbox_permissions=require_escalated` and no broad
approval prefix. This order is not OS permission. Any permission/security
denial stops; no alternative surface, tunnel, CDP, browser-policy bypass or
uncounted browser trial. No S5/S6/S10 recapture or scientific-image export.

After capture, R 4.6.1 must reconcile all 244 body cells character-for-character
against the immutable rendered source, distinguish the already classified
original-markup whitespace serialization, and verify all 17 exact payloads,
17 metric rows, six groups, 14 columns, three complete headers and final notes.
Independently retain exact 17/17 hidden descriptions and runtime 7/5/5 proofs.
Inspect all three PNGs at original and intended Word display sizes, including
Unit, Scaling, distribution images, visible text and notes. Word geometry
remains A3 landscape with maximum 15.55-inch width and 8.90-inch height,
aspect ratio preserved. The Word helper and page geometry stay frozen.
Record actual dimensions and any limitation. DOM PASS alone is insufficient.

Stop the exact server after bounded capture/inspection, close only task capture
contexts, prove listener/process teardown, and rehash the served set, earlier
captures, scientific sources and historical records. No fresh loop is implied
by a failure. Return one consolidated stop for a genuinely new defect.

## Inherited completion budget, not additional render authority

Only after actual S2 visual PASS may a NEW exact assembly mapping select the
attempt5 capture manifest plus the four preserved S5/S6/S10 PNG parts. Create
the mapping/checks in the new recovery evidence root. Do not rerun or edit the
historical `prepare_correction_manifest.py`, its attempt2/width1400 record or
any older capture/correction manifest. Preserve the key/files/path interface.

The already authorized consolidated correction allowance remains one selection
HTML, one main HTML and one main DOCX, each unused at this release. This order
does not add or restore any of those slots. Original assembly/SVG-embedding and
office-QA limits remain 0 of 2 consumed. Use only the previously approved
isolated targets, exact three-Lua dependency closure and normal-cache tool
permission strategy. No full project render or scientific QMD execution.

Native Word still requires the author to unlock the Mac. Do not unlock it
automatically or claim native acceptance from another viewer. Return
VISUAL_QA_PENDING if required native views remain unavailable. Permitted work
that does not need that surface can continue within the inherited bounds.

The mandatory return is
`REPORT018-ORDER72K-NON-S5-INTEGRATED-PREVIEW-REVIEW`. Preserve the Brown-linkage
pending notice and bedside-environment, not ocular-exposure, qualification.
Brown fitting/prediction/inference/data pipelines, Figure S5 replacement,
optional H11 compatibility, manuscript-language work, canonical promotion,
package/lock/profile edits, commit and upload all remain held.
