# REPORT-018 Order72j: native SVG component exports

Date: 2026-09-11.

Status: released for one dispatch by the Harmonizer after the non-circular
release seal verifies. This is actual candidate-generation authority, not
another planning-only order. It does not release manuscript integration,
Quarto or Word generation, canonical replacement, or scientific computation.

Coordinator acceptance:
`audit/report_harmonization/report018_order72j_component_exports_release/independent_preflight_acceptance.md`.

Control package:
`audit/report_harmonization/report018_order72j_component_exports_release/`.
Rehash `release_manifest.csv` and the recipient's input/preservation CSVs
before dispatch and before generation. The Order72i historical proposal and its
11-member seal remain unchanged. This order supersedes its dispatch prohibition
only for the candidate work expressly named below.

## Dispatch and ownership

The Harmonizer task 019ff52e-48ac-77b3-9a0e-9a87749a3bba verifies current task
identity, checkout and absence of a competing same-owner mutation, then sends
this order exactly once per released owner:

| Owner | Task | Released work | Sole new write root |
|---|---|---|---|
| H07 | 019fbe52-6781-7c32-bdcf-379c88ef1e78 | One S7-B native SVG export | audit/hypotheses/H07/report018_order72j_split_svg_export/ |
| H09 | 019fdc1b-b927-7fb1-ac61-88993c0a818a | Two S15 native SVG exports | audit/hypotheses/H09/report018_order72j_split_svg_export/ |
| H11 | 019fba59-0f3c-74a0-ab3d-58d389365ad1 | Optional S17 SVG-only compatibility trial | audit/hypotheses/H11/report018_order72j_libreoffice_compatibility/ |

All three tasks belong to the existing shared project checkout. A `notLoaded`
state is not a failed dispatch or a reason to create another task. Confirm
that no current turn is running, then use the existing task. Record tool
receipts and the immediate resulting task state. A real unavailable task is
a per-owner hold, not a block on the other released owners.

Their disjoint code/static checks and vector exports may run concurrently.
The Harmonizer records serial visual-QA leases in H07, H09, H11 order, skipping
a task that has not reached the QA safe point. At most one task may control a
browser/office QA session or project loopback listener at a time. Owners stop
after candidate/static completion if no visual lease has been issued; the
Harmonizer may issue that already-authorized lease without another central
scientific decision.

Harmonizer coordination writes are confined to the new directory
`audit/report_harmonization/report018_order72j_component_exports_dispatch/`.
Record owner, order hash, release hash, recipient, dispatch time, receipt,
candidate status and visual lease there. Do not rewrite the 15-row website
integration matrix for these manuscript-format candidates. Do not alter the
immutable release package. Return the dispatch identities promptly, then
coordinate completion rather than reopening the preflight.

## Common execution and protection boundary

Use R 4.6.1 with `RENV_CONFIG_AUTOLOADER_ENABLED=FALSE` and the already installed
project R 4.6 library. Record `.libPaths()`, R/package versions and the exact
command. No installation, lock change, cache reset, profile edit or full builder
is authorized. If startup fails, seal the exact environment stop without a
blind retry or a changed R environment.

Create the owner root only if it is absent. Allowed new subdirectories are
`code/`, `attempts/`, `candidate/`, `qa/` and `evidence/`, plus a handoff and
non-circular manifest in that root. Copy only the relevant display functions
and literal constants into a dedicated new R implementation. Preserve every
existing script, test, source CSV, figure, manifest, QMD and HTML. Do not source
the original broad builders, load any model/inferential RDS, run a model
method, fit/refit, predict, derive a derivative/interval, recompute a p-value
or multiplicity decision, resample, simulate, or regenerate source data.

Allowed in-memory operations are exact selection of the specified existing
display rows, existing factor/label mappings and existing plotting statistics
needed to reproduce the accepted geometry. They do not authorize a different
sample, estimator, aggregation, model line or statistical decision. Preserve
every point, interval, reference line, rug, status, missing/non-estimable state,
annotation, metric/site order, palette, non-colour cue and qualification.

Before any export, parse/check the new implementation and prove its read/write
boundary and complete downstream verification path. Each implementation must
fail closed on an unexpected input, row/key set, missing label or write path.
All new writes remain inside its root or a recorded fresh temporary directory.
No ad hoc test or package operation may mutate author inputs. Record every
attempt; never overwrite stopped evidence.

The 62-row preflight inventory is historical cross-task evidence. The exact
recipient input pin file and recipient preservation inventory are the live
execution pins. Authorized work in another recipient's new root, Brown's
separate scientific root or coordinator evidence is not owner drift. This is
not a general exemption for shared/source changes: every pinned input and
existing protected owner path must remain exact. Stop on any other discrepancy.

## H07: one frozen-source S7-B export

Released matrix row: DR-S7-02. H01's DR-S7-01 requires no owner work or export.

Final candidate filename:
`candidate/H07_revised_smooth_derivative_pairs_near_eye.svg`.

Use only the five pinned H07 plotting CSVs (curves, revised derivatives,
plateau annotations, photoperiod rug rows, paired figure settings), plus the
pinned H05 metric registry for the same nine labels/units. Copy the nine-ID
literal order and plot construction from the pinned H07 core/paired-figure
scripts; do not source them. No chest export is released.

Retain `REVISED_CENTRAL_UNCONDITIONAL_POINTWISE`, the recorded-range selection,
nine metric rows with fitted values left and derivatives right, existing
pointwise uncertainty/plateau qualifications and the 9 by 18 inch canvas.
Preserve accepted text, including internal component titles. This export is
not permission to apply a new language matrix or remove words such as primary.
The accepted PNG f19763fe3c14c723ac38846bf735c0465ba2ee18765777a4f7304c677be71113
is the visual comparator. The H01 S7-A SVG remains byte-exact at
4ddf972fc4c8082594d13a3b446537ae8a525ca35077e0605967f9c4c0ce519e.

Allow one native SVG export trial and at most one correction confined to
SVG-device/font-metric/serialization compatibility. No data, plot semantics,
panel redesign or canonical replacement is included. Retain both attempts and
their checks. Seal one candidate only after the full component gate passes.

## H09: two frozen-source S15 exports

Released matrix rows: DR-S15-01 and DR-S15-02.

Final candidate filenames:
`candidate/H09_primary_effects.svg` and
`candidate/H09_observed_timing_patterns.svg`.

For the primary-effects export, read only
`artifacts/11_source_data/H09/H09_primary_effects_data.csv` and copy the primary
factor mapping, horizontal interval helper and `make_primary_plot()` with
`theme_mode = "repaired"` from the pinned Order56 refresh implementation.
Keep its 10.5 by 6.5 inch canvas with scale 1.5, including the two instrument
panels and the accepted placement encodings. Do not read paired/diagnostic
inputs merely because the original broad builder references them.

For the observed-timing export, read only
`artifacts/11_source_data/H09/H09_observed_timing_patterns_data.csv`; copy
`h09_make_stage3_observed_plot()` and its clock-label helper, not
`h09_prepare_stage3_observed_layers()` or its model/frame readers. Keep the
10.5 by 8.5 inch canvas with scale 1.5. Model lines and bands come directly
from the sealed display rows. Preserve all internal panels, points, site
encodings and boxplots. The existing deterministic boxplot statistic operating
on those frozen overview rows is permitted solely to reproduce its accepted
geometry; do not export a new scientific summary or reconstruct input rows.

The pinned current PNG and PDF files are comparison-only inputs. Allow one
export trial per SVG and at most one renderer-only correction per SVG. Preserve
all attempts. A correction cannot change values, uncertainty, layout design,
labels, model meaning or source CSVs. Never execute the original broad builder
or replace either accepted PNG/PDF.

## H11: optional, strictly reversible S17 trial

Released matrix row: DR-S17-01, candidate only.

Final candidate filename:
`candidate/H11_reader_near_eye_curves_arial_safe_margin.svg`.

Start from accepted SVG
ee82f8f1ef3f360a584a92712ea1e9e1e48341617d5d0e332743c72c956cdbfe.
Do not run its former export script or read scientific data. Permit only exact
Helvetica font-family declarations to become Arial and an extension of the
outer height/viewBox at the bottom, with unchanged width and origin. Record
original and replacement attribute values; cap the extension at 36 SVG points.
All graphical coordinates, text strings, line lengths, marks, scientific
numbers, intervals, styles other than font-family, and notes remain unchanged.
No translation, cropping, text outlining or raster embedding is authorized.

Allow one candidate and one renderer-only correction within those same limits.
The inverse literal/attribute changes must reproduce the original file bytes
and SHA exactly. Failure to improve LibreOffice is a legitimate
`NO_IMPROVEMENT_RETAIN_ACCEPTED` return, not permission for another trial.
The candidate cannot supersede S17 without native Word non-regression and a
demonstrated LibreOffice improvement. H07/H09 do not wait for this optional
compatibility result.

## Component verification and serial visual QA

For each exported SVG, require parseable XML, zero image elements, zero raster
payloads, no script/foreign-object/external-resource dependency, unique IDs and
resolving local references. Require a complete plot-layer-to-frozen-row map,
expected layer/panel/label/order equality, exact numerical and interval inputs,
non-estimable/state equality and no participant identifier in visible text,
alt text or SVG metadata. Logs should use counts and hashes rather than
reproduce identifying records.

Use the installed native SVG device. Do not convert a raster, wrap a PNG in
SVG, trace a bitmap or substitute PDF conversion for the available native plot
route. Temporary rasterizations are comparison evidence only and never final
figure inputs. Preserve intrinsic aspect ratio and explicit comparison width
and height. The known Quick Look square-canvas output is not a valid
like-for-like comparator; do not consume an export trial to repeat that harness.

Inspect each SVG against the accepted PNG/PDF at original and intended reader
sizes, including 170 mm (642 CSS px) and 708 px. Cross-device anti-aliasing need
not be byte-identical. Scientific geometry, content, relative panel structure
and essential-text legibility must agree; record any renderer differences.
Do not label dimension/DOM checks alone as visual acceptance. Native Word
compatibility is still provisional until the later independently reviewed Word
candidate; no Word generation is released here.

When the Harmonizer grants the visual lease, use the supported browser route
without a policy workaround. Prefer direct immutable SVG inspection. If a
served comparison is needed, create a minimal static comparison page containing
only the pinned display copies/candidates in `qa/serve/`; no Quarto execution.
Inventory that narrow root, require no external-resolving symlinks, and serve
read-only GET/HEAD on one unused port bound strictly to 127.0.0.1. Never serve
the project or source-data root. Record exact routes, dimensions, captures,
console result and limitations. A rejected browser route requires a stopped QA
return, not another browser surface or hidden emulation workaround.

Close only task-created tabs and stop the task-created listener immediately
after inspection, including failure. Prove no listener remains and rehash the
candidate, all live pins and preservation inventory. No render, source change
or candidate alteration may occur during the immutable visual review.

## Return, acceptance and downstream holds

Each owner returns implementation SHA, attempt inventory, source-row/layer
checks, SVG structure/numerical-preservation checks, visual observations,
pre/post protections, session/runtime, teardown and a non-circular manifest
excluding itself and future coordinator records. Mark component status
`CANDIDATE_PASS_AWAITING_INDEPENDENT_ACCEPTANCE`, `STOPPED`, or for H11
`NO_IMPROVEMENT_RETAIN_ACCEPTED`. Report all findings in one combined return.
The Harmonizer independently rehashes and reviews each component and returns
its acceptance to the coordinator. No candidate is promoted by this order.

Mandatory gate: REPORT018-ORDER72J-COMPONENT-REVIEW.

The following are planned but NOT released: S5 historical compatibility or
final composition; table recapture and A3 Word layout; QMD caption/alt-text
integration; S7/S15 two-block source edits; any Quarto/Pandoc/Word/LibreOffice
generation; manuscript rewrite; canonical asset promotion; shared manifest,
profile, lockfile or ledger changes. In particular, later Word integration
must inspect 22 SVG inputs/23 appearances only after those actual components
are accepted. Those are prospective counts, not authority to regenerate now.

Brown continues only under its separate scientific order. Final S5 awaits
accepted replacement panels. Its sleep interpretation is already governed by
the pinned placement decision: bedside sleep-environment measurements, not
verified ocular or whole-room exposure. Keep historical captions untouched
until a separate integrated source revision. Table 3 and S12 remain byte-exact,
and H06_daily remains absent from selected displays. No new author approval is
needed merely to dispatch the H07/H09 scopes in this sealed order.
