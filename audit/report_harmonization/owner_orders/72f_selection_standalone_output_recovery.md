# REPORT-018 Order 72f: standalone output-path recovery

Date: 2026-09-11

Status: SEALED FOR ONE HARMONIZER CONTINUATION DISPATCH.

Executor: Harmonizer task 019ff52e-48ac-77b3-9a0e-9a87749a3bba.

This order corrects only the command/output-location boundary of Order 72e,
SHA-256 58eec65aae5da801f994c64eb7f874f267a098837c891e2c096640ce5452509f.
Its source contract, complete candidate acceptance, secure loopback procedure,
one canonical HTML promotion and final holds remain controlling. No source,
caption, SVG, table, manuscript or scientific correction is authorized.

## Frozen state and required preflight

Read the complete Order 72e and this amendment. Rehash the unique non-circular
release_manifest.csv and current_execution_pins.csv in:

    audit/report_harmonization/report018_order72f_standalone_output_recovery/

That package preserves the prior release, exact current owner state, fixture
proof and independent stop disposition. Do not rerun its evidence-creating
coordinator preflight script, which requires a previously absent central root.
Audit its pins with a read-only checker instead.

The selection source remains:

    audit/manuscript_nature_health/manuscript_figure_table_selection.qmd

SHA-256 9acec033d0c24cbb0ee7649c38f55d5cea90052e11b6be7fc5f9e7310890d8f3,
49,865 bytes. No second source promotion, regeneration or edit is authorized.

The existing canonical preview remains:

    audit/manuscript_nature_health/manuscript_figure_table_selection.html

SHA-256 82100e0d3990dec39f61e94970e7a434b02cf94a6d25819ede1be4d24f4130a6,
30,925,051 bytes, until complete candidate acceptance permits the one HTML
promotion already authorized in Order 72e.

Preserve all 14 inventoried owner stop files byte-for-byte, all prior endpoint
preimages, the historical contracts, all 20 accepted SVGs and 19 retained gt
fragments. The current 20/20 source checks and exact forward/reverse proof
remain valid. Reconfirm that no source/asset/renderer context has drifted.

The owner write root remains:

    audit/manuscript_nature_health/figure_table_selection_svg_revision_2026_09_11/

New recovery logs and evidence may be written beneath its order72f_recovery/
subdirectory. Existing failed evidence must not be overwritten.

## One tested command, one explicitly added temporary endpoint

The installed Quarto is 1.9.37. This is a standalone Markdown-only document
with execution disabled and embedded resources, not a website project render.
Neither --output-dir nor a path-valued --output is permitted.

Require this exact new temporary sibling to be absent before invocation:

    audit/manuscript_nature_health/manuscript_figure_table_selection_order72f_candidate.html

It is the only extension outside the prior owner write root. It is not the
canonical preview and must never be mistaken for an accepted endpoint.

Also require the intended destination below to be absent. Inspect any
same-basename support directory before invocation. Do not overwrite, clean
or remove an existing support directory. The successful embedded-resource
fixture left no persistent support sidecar. If the real invocation introduces
an unexpected persistent output or modifies another existing path, inventory
the exact delta and stop before promotion.

From the exact working directory audit/manuscript_nature_health, invoke once:

    quarto render manuscript_figure_table_selection.qmd --to html --output manuscript_figure_table_selection_order72f_candidate.html

Use the current standalone YAML without --profile, --output-dir, alternate
target, source copying or project execution. Retain the existing narrowly
approved Quarto cache access. Do not change HOME, caches, packages, lockfiles,
configuration or environment policy. Separate R checks retain R 4.6.1,
RENV_CONFIG_AUTOLOADER_ENABLED=FALSE and the accepted library
/Users/zauner/Library/R/arm64/4.6/library. No computational engine is released.

Record the complete command, cwd, environment, start/end, exit and created-path
inventory in new owner recovery evidence. The earlier rejection is historical
pre-conversion evidence. It does not authorize multiple attempts here: if this
single invocation fails, stop without another render or source patch.

## Exact move, unchanged verification and promotion

After exit 0, rehash the temporary sibling, frozen QMD, canonical old HTML and
all protected inputs. Require a self-contained HTML at the expected basename.
Record its SHA-256 and bytes before moving it intact to:

    audit/manuscript_nature_health/figure_table_selection_svg_revision_2026_09_11/rendered/manuscript_figure_table_selection.html

The destination must still be absent. Use a recoverable exact move, not a
rewrite or render. Require identical SHA-256/bytes after the move and absence
of the temporary sibling. If the move fails, retain the existing candidate and
stop; do not delete it or rerender. This move keeps the complete existing owner
verifier's candidate path contract unchanged.

Then continue Order 72e's complete candidate path without another source edit:
the frozen task-owned source/HTML verifier, exact decoded bytes for all 20
selected SVGs, exact 19 retained native gt fragments and Table 3 order, 22 total
tables, 11 disclosures, semantic ID/header resolution, alt text, captions,
privacy, local/canonical-relative links, scoped inventories and no stale
selection claims. The seven already accepted embedded SVG components are
historical composite content, not permission for a PNG figure substitution.

Order 72e's conditional candidate-only semantic normalization with exact
reverse/reapply proof remains available if actually required. It does not
authorize another render or a content/layout correction.

Carry out the same full visual QA at 1440, 708 and 390 pixels, all 20 figures
and retained tables, intended final display sizes and complete Table 3 order.
Use only a narrowly staged immutable rendered output, mandatory symlink
containment preflight and one GET/HEAD server bound strictly to 127.0.0.1.
Do not serve the source project or bypass a browser rejection. Record teardown,
close the review tab, prove no listener and rehash. Any genuine new defect
stops with all evidence intact.

Only after the complete candidate PASS may the canonical selection HTML be
replaced once with the accepted candidate bytes. Preserve the old HTML copy
and all historical seals. Create a new non-circular completion manifest and
combined return only in the owner root.

## Unchanged holds

Mandatory stop remains REPORT018-ORDER72E-SELECTION-SVG-PREVIEW-REVIEW for
independent coordinator acceptance. No production website/corpus, scientific
artifact, model, inference, source-data, package/lock, hypothesis/Brown QMD,
manuscript or Word candidate change is authorized. Writer Order 72d remains
separate. Brown scientific reopening remains held. Do not dispatch another
owner or independently notify Writer of acceptance before the review closes.
