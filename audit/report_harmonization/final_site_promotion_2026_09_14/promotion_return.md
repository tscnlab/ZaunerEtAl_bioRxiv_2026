# Order013 completion: exact local website promotion

14 September 2026. Harmonizer task 019ff52e-48ac-77b3-9a0e-9a87749a3bba. Returned to Coordinator task 019faf58-3df3-7383-8034-f715cdfdd154.

## Outcome

The independently accepted Order011 website has been promoted exactly to `_build/nathealth`. All 914 public files match the accepted candidate in path, bytes and SHA-256. The transaction changed exactly 25 website files, comprising four replacements and 21 additions, followed by the exact prospective corpus manifest as operation 26. There was no candidate regeneration or additional content/layout transformation.

The durable transaction began after complete implementation preflight and immediate baseline verification. All 26 operations completed exactly once at 13:34 UTC. The corpus was copied last, not rebuilt. Production browser QA and teardown were complete before the final fixed-input and live inventory recheck at 13:47:36 UTC.

This is static local integration only. It is not remote publication or a fresh scientific/source render. The later Writer Order012 A4 revision is not incorporated and remains a separate candidate.

## Exact closure and postimages

| Item | Final SHA-256 |
| --- | --- |
| Main entry, `_build/nathealth/index.html` | `588bf4f60c7e1ca9cf409e86696632b8a14cc9d79e8aecf2aa105c8190b7cdd2` |
| Supplement entry | `649bbc1796cad3bbd2ff1b372a46599a297cef98e3a338b4985824bb5764c53e` |
| Full Word download | `325c3a8e3a76ea225970f80e177ceed0bbdd592b72e79196d154597f3581250b` |
| Search index | `7469b089dddcedf93b7acd7e414ca1f200180d134a9edffdbf7224db7925c686` |
| Live corpus manifest | `b5b4b009db76e6f869f324a360e6eeafdf63438539382f1b8d85058f277eb93f` |
| Accepted candidate inventory | `0610b27e315cf73fd2d55bb4116d87c6706ad7de92d21e5f5dfd767034dbb862` |
| Preserved Order011 completion manifest | `34c5804faa43c9f71bdd2c9be7cc8bc3e03f85b84227a6d94f10f9ed349e1199` |

The exact path-by-path preimage/postimage transaction is in `transaction_plan.json`, `transaction_journal.jsonl` and `evidence/changed_paths26.csv`. The 889 retained public files and all 35 non-entry reader reports remain byte-exact. All 37 historical source-hash cells in the corpus remain unchanged; only the two accepted HTML-hash cells differ from its original preimage.

## Verification

- Complete pre-mutation and final post-QA closure checks each passed 3,869 assertions, including release and acceptance seals, owner1026, candidate914, fixed1771 input rows, five backups and the appropriate live inventory. The initial live893 baseline and 21 addition absences were verified before writing.
- All ten transaction, serving, validation, postflight and finalization helpers were present and preflighted before the first live write. Python helpers were parsed, the R script was parsed, the new structural validator was executed against the unchanged accepted candidate, staged bytes and backups were verified, and atomic replacement/guarded rollback fixtures passed. The helper hashes are fixed in `evidence/implementation_preflight.json`.
- Production structural checks passed 53,747 assertions: exact 914-file closure, 19 native downloads, final Word, unique changed-page IDs, table-scoped headers, ARIA references, links, 942 search records across 37 routes, the 20-row supplement revision utility and the exact four inherited font-reference exceptions. No new missing resource or fragment was introduced.
- R passed all 75 exact reported-content checks against accepted C HTML. This includes reader text, all 19 manuscript and 16 supplement gt tables, complete table structure and annotations, the 23 main-entry and 20 supplement SVG elements, accepted S2 distribution payloads and all 170 whole mean/SD runs, accepted display/column-layout CSS, and the 20 exact position/old/new revision rows. No estimate was recalculated or reinterpreted. The verifier's only changes are production/evidence routing and identifying comments/labels; the exact diff is retained.
- The R command was `Rscript --vanilla audit/report_harmonization/final_site_promotion_2026_09_14/helpers/verify_content.R`. Runtime: R 4.6.1, xml2 1.6.0, digest 0.6.39 and jsonlite 2.0.0. Input identities, command and complete session information are retained in the evidence directory.
- All 22 download targets returned HTTP 200 for GET and HEAD, with response lengths and SHA-256 identical to accepted files. These were read-only in-memory checks; no browser download-folder writes were made.

## Brief production browser review and teardown

Sixteen native tool captures, P01 through P16, cover the main and supplement routes at 1440, 708 and 390 pixels. The captures are retained in the task's CUA tool transcript and indexed by `evidence/browser_observations.json`; no screenshot files are invented. Exact candidate identity carries the complete prior Order011 visual review, so no full 37-report review was repeated.

The review confirmed loaded SVG figures and distribution images; complete manuscript and 19 editable-table labels; contained Table2 and S2 horizontal panning; wrapping download utilities; desktop and mobile TOC navigation; the mobile navigation submenu; a successful search and result navigation; and the 20-row revision utility with reachable old/new text columns. Document scroll width equalled client width at each prescribed size. S2's accepted secondary typography and all fixed visual payloads were left unchanged.

The temporary GET/HEAD-only server served only the symlink-free production root on `127.0.0.1:55182`, PID 28418. Its normal permission request was used after sandbox denial. It was stopped after QA and its exec session exited 0. An approved `lsof` check returned no listener and the final closed-port probe passed. The viewport override was reset, only the task-created tab 3 was closed, and the browser reported no remaining tabs. This durable safe point was reported promptly to the Coordinator before sealing this return so the separate Writer HTML review could proceed. No further browser use followed.

## Recovery, limitations and preserved scope

No production failure or rollback occurred. Exact preimages for the four replaced website files and corpus remain in both the unchanged Order011 backup package and this promotion's `preimages/` subtree. All 26 promoted payloads remain in `staged/`. The guarded rollback implementation restores only still-matching transaction postimages and moves only exact transaction additions into recovery evidence. Unexpected concurrent target bytes stop recovery rather than being overwritten. A future rollback is not automatically authorized by this completion; the retained procedure is recovery capability.

Two pre-mutation validator assumptions were corrected before first writing: a read-only fixed source in another worktree and the accepted placement of the revision table in the supplement rather than both entries. Browser selector misses were resolved by inspecting the live DOM. These are recorded implementation/interaction corrections, not content changes or production defects.

The exact four inherited missing font references and historical source/render gaps remain recorded; no offline-completeness or fresh-source-render claim is made. The complete Order011 package is unchanged. No Quarto, knitr, Pandoc, Office render, source QMD/CSS/JS/profile edit, scientific artifact/model edit, package/lockfile change, manuscript alias change, commit, push, upload or deployment was performed. Writer Order012 was not contacted, interrupted, pinned as an input or incorporated.

`completion_manifest.csv` seals every member of this new promotion package except itself and `completion_seal.json`. The separate seal records the manifest's exact identity and those exclusions, avoiding a circular receipt.
